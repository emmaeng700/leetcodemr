# Auto-generated — do not hand-edit
# Sources: quick_review_info.json + neetcodereview.json + am600review.json
QUICK_REVIEW = {
    "longest-consecutive-sequence": {
        "key_insights": "Utilize a set (or hash table) for constant time look-ups.\nOnly start counting a sequence from a number that is the beginning (its previous number is not in the set).\nEach number is visited only once, ensuring an O(n) time complexity.\nUpdate the maximum sequence length as you discover longer consecutive sequences.",
        "complexity":   "Time Complexity: O(n)\nSpace Complexity: O(n)",
        "solution":     "We first insert all the numbers into a hash set to enable O(1) membership checks. For each number in the set, we check if it is the start of a sequence by ensuring that the number minus one is not in the set. If it is the beginning, we then count all consecutive numbers following it, updating the maximum sequence length when necessary. This method guarantees that we only count each number once, satisfying the O(n) time requirement.",
    },
    "find-the-celebrity": {
        "key_insights": "Use pairwise comparisons to eliminate non-celebrities.\nIf a candidate knows another person, the candidate cannot be the celebrity.\nVerify the candidate by checking that they do not know anyone and that everyone knows them.\nAchieve the solution in two passes: one for candidate selection and the other for validation.",
        "complexity":   "Time Complexity: O(n) – One pass for candidate selection and one for validation.\nSpace Complexity: O(1) – Only constant extra space is used.",
        "solution":     "The approach consists of two main steps:\nCandidate Selection: Iterate through the people and maintain a candidate. For every person i, if the current candidate knows i, then set candidate = i. This works because if candidate knows i, candidate cannot be the celebrity.\nVerification: Check that the candidate does not know any other person and that every other person knows the candidate. If both conditions hold, candidate is the celebrity; otherwise, return -1.\nThe main data structures used are simple integer variables. The algorithm leverages a two-pass technique to reduce the number of knows API calls while ensuring correctness.",
    },
    "parallel-courses": {
        "key_insights": "Model the problem as a directed graph where nodes are courses and edges represent prerequisites.\nUse topological sorting (Kahn’s algorithm) to process courses with zero prerequisites.\nEach level (or layer) of processing represents one semester.\nDetect cycles by ensuring all courses are eventually processed; if not, a cycle exists.\nThe number of BFS levels gives the minimum number of semesters necessary.",
        "complexity":   "Time Complexity: O(n + m) where n is the number of courses and m is the length of relations (prerequisites).\nSpace Complexity: O(n + m) for storing the graph, in-degree counts, and the processing queue.",
        "solution":     "We solve the problem using a topological sort with Kahn's algorithm. First, construct a graph representation using an adjacency list and compute the in-degree for each course. Initialize a queue with courses that have no prerequisites (in-degree == 0). Then, process courses level by level. Each level processed represents one semester. For every course taken in that semester, decrease the in-degree of all its neighbors. Add any neighbor with an in-degree that becomes zero to the queue for the next semester. If, after processing, the total courses taken is less than n, then a cycle exists and return -1. Otherwise, the number of semesters processed is the answer.",
    },
    "number-of-islands-ii": {
        "key_insights": "Use Union Find (Disjoint Set Union) to efficiently manage connected components (islands).\nMap 2D grid coordinates to a 1D index.\nWhen adding a new land, check its 4 neighbors (up, down, left, right) and perform unions if they are also lands.\nAvoid duplicate processing if the same cell is added multiple times.\nMaintain a counter for islands that is updated during each union operation.",
        "complexity":   "Time Complexity: O(K * α(m\nn)) where K is the number of operations and α is the inverse Ackermann function (almost constant time per union/find).\nSpace Complexity: O(m\nn) for storing the union-find structure.",
        "solution":     "The solution relies on the Union Find data structure to dynamically merge islands. When a new land cell is added:\nIf the cell is already land, record the current island count.\nOtherwise, mark the cell as land and increment the island count.\nCheck the four neighboring cells; if any neighbor is land, attempt to union the current cell with that neighbor.\nIf a union actually happens (i.e., the two cells were in separate islands), decrement the island counter.\nOutput the island count after each add-land operation.\nThe key trick is converting 2D indices to a 1D representation (r * n + c) and using path compression along with union by rank in the Union Find implementation for optimal performance.",
    },
    "string-transforms-into-another-string": {
        "key_insights": "Each character mapping from str1 to str2 must be consistent; one character in str1 always maps to the same character in str2.\nA bijective mapping is not necessary; multiple characters in str1 can map to the same character in str2.\nThe order of conversions matters. When there is a cycle (e.g., mapping 'a'->'b' and 'b'->'a'), an intermediate unused character might be required to break the cycle.\nIf str2 uses all 26 letters, no temporary character is available to break mapping cycles. In this case, transformation is possible only if str1 is already equal to str2.",
        "complexity":   "Time Complexity: O(n), where n is the length of the strings, because we traverse the strings to build and check the mapping.\nSpace Complexity: O(1) or O(26), which is constant space, to store the mapping for the 26 lowercase English letters.",
        "solution":     "The approach is as follows:\nIf str1 equals str2, return true immediately.\nBuild a mapping from each character in str1 to the corresponding character in str2. While creating the mapping, check that each character maps consistently.\nCount the number of distinct characters in str2. If this number is 26 and str1 is not already equal to str2, it is impossible to perform the transformation because there is no spare character available as a temporary placeholder.\nIf the mapping is consistent and there exists at least one character not used in str2 (i.e., distinct characters in str2 is less than 26), the transformation is possible.\nThe important trick is identifying the need for a free character when the target mapping is “full” (i.e., covers all 26 letters). Without a free character, you cannot avoid cycles where a temporary intermediate is necessary.",
    },
    "longest-palindrome": {
        "key_insights": "Count the frequency of each character.\nFor each character, you can use pairs (even count or odd count minus one) to form the palindrome.\nIf any character has an odd frequency, you can add one extra character as the center of the palindrome.",
        "complexity":   "Time Complexity: O(n), where n is the length of the string.\nSpace Complexity: O(1) since the frequency counter will have at most 52 keys (considering both uppercase and lowercase letters).",
        "solution":     "The solution uses a hash table to count the frequency of each character. For every character, the maximum number of characters that can be used in a palindrome is determined by taking the largest even number that is less than or equal to its frequency (which is frequency - (frequency % 2)). If there exists any character with an odd frequency, one of these can be used as the center of the palindrome, adding an extra character to the total length. This greedy approach efficiently computes the length of the largest possible palindrome from the available characters.",
    },
    "gas-station": {
        "key_insights": "If the total amount of gas available is less than the total travel cost, then completing the circuit is impossible.\nA greedy approach can be used by keeping track of the current surplus of gas. If the surplus becomes negative at any station, the journey cannot start from any station before that point.\nRestart the journey from the next station whenever the surplus goes negative.\nThe problem guarantees that if a solution exists, it is unique.",
        "complexity":   "Time Complexity: O(n), where n is the number of gas stations, as we traverse the list only once.\nSpace Complexity: O(1), as no extra space is used other than a few variables.",
        "solution":     "The solution involves iterating over the list of stations while maintaining two variables: one for the current gas surplus and one for the total gas surplus. If at any station the current surplus drops below zero, the current starting station is not viable and we set the starting station to the next index and reset the current surplus. After one pass, if the total gas surplus is non-negative, then the identified station is the valid starting point.",
    },
    "largest-number": {
        "key_insights": "The order in which numbers appear drastically affects the concatenated result.\nA custom sorting strategy is required where two numbers are compared by checking which concatenation (first-second vs second-first) yields a larger number.\nEdge case: When the numbers are all zeros, the result should be \"0\" instead of several leading zeros.",
        "complexity":   "Time Complexity: O(n log n * k), where n is the number of elements in the array and k is the maximum number of digits in a number (due to string comparisons in the custom sort).\nSpace Complexity: O(n * k) for storing and processing the strings.",
        "solution":     "We solve the problem by converting all integers to strings so that we can use string concatenation to compare the results. A custom comparator is defined that, given two number strings, compares the concatenated results in both orders. By sorting the array using this comparator in descending order, we ensure that placing the highest contributing number first yields the largest possible number. After sorting, a check is performed to return \"0\" if the highest number is \"0\". Finally, the sorted strings are concatenated to form the answer.",
    },
    "wiggle-sort": {
        "key_insights": "The alternating \"wiggle\" requirement can be satisfied by ensuring two conditions:\nFor every odd index i, nums[i] should be greater than or equal to nums[i-1].\nFor every even index i (where i > 0), nums[i] should be less than or equal to nums[i-1].\nA single pass through the array can ensure these conditions by swapping adjacent elements when the condition is violated.\nThis greedy approach works because the local adjustments guarantee the overall wiggle pattern without needing to sort the entire array.",
        "complexity":   "Time Complexity: O(n) — Only one pass through the array is needed.\nSpace Complexity: O(1) — The modifications are done in-place without extra data structures.",
        "solution":     "We can achieve a wiggle sort in one pass by iterating through the array and, at each index i (starting from 1), checking if the pair (nums[i-1], nums[i]) satisfies the wiggle condition. If i is odd, nums[i] should be at least nums[i-1]; if not, we swap them. Conversely, if i is even and nums[i] is greater than nums[i-1], then we swap them. These local swaps adjust the order incrementally while ensuring that the overall wiggle pattern is maintained. This greedy approach works because only two adjacent numbers are involved in any violation and fixing them locally does not disturb the rest of the order.",
    },
    "array-of-doubled-pairs": {
        "key_insights": "Sort the array based on the absolute value of the elements to handle negative numbers correctly.\nUse a hash table (or frequency counter) to track the occurrence of each element.\nFor each number in the sorted list, if the number is still available in the frequency map, try to pair it with its double.\nDecrement the appropriate counts and validate that it is possible to find sufficient doubles for all numbers.\nSpecial case: when dealing with zeros, pairing works within the same value.",
        "complexity":   "Time Complexity: O(n log n) due to sorting.\nSpace Complexity: O(n) for storing the frequency map.",
        "solution":     "The solution starts by counting the frequency of each element in the array. Sorting the array by absolute value ensures that when we process an element, any potential pair (i.e., its double) comes later in the sequence. For each number, if there are available occurrences, we try to match it with its double. If there are not enough doubles available, the answer is false. If all elements can be paired appropriately by decrementing their counts in the frequency map, the function returns true.",
    },
    "longest-palindrome-by-concatenating-two-letter-words": {
        "key_insights": "Pair words with their reverse (e.g., \"ab\" with \"ba\") to form a palindrome.\nHandle words that are palindromic by themselves (like \"gg\" or \"aa\") differently; they can be used in pairs and one extra can be placed in the center.\nUse a hash map (or dictionary) to count frequencies of each word.\nFor non-palindromic word pairs, consider each pair only once to avoid double counting.\nFor self-palindromic words, count how many pairs can be used and whether an extra word can be placed in the middle to maximize length.",
        "complexity":   "Time Complexity: O(n), where n is the number of words, since we iterate through the list and perform constant time operations per word.\nSpace Complexity: O(n), to store counts of words in the hash map.",
        "solution":     "The approach is to count the occurrences of each word using a hash map. For each word:\nIf the word is not self-palindromic, check if its reverse exists and form pairs by considering the minimum count between the word and its reverse.\nIf the word is self-palindromic, form as many pairs as possible (using count // 2) and note if there is any word left; you can use one extra self-palindromic word as the center of the palindrome.\nFinally, compute the total length by multiplying the number of words used by 2, since each word contributes 2 characters.",
    },
    "debounce": {
        "key_insights": "Each call to the debounced function resets the timer.\nOnly the most recent function call (that isn’t subsequently cancelled) will eventually execute.\nPassing of parameters is handled by storing the arguments of the latest call.\nThe solution leverages timers (or equivalent scheduling mechanisms) and cancellation techniques.",
        "complexity":   "Time Complexity: O(1) per call, as each invocation only clears and sets a timer.\nSpace Complexity: O(1), since only a single timer reference is maintained.",
        "solution":     "The solution creates a wrapper function that maintains an internal timer reference. On each call to the debounced function, if a timer exists, it is cancelled. A new timer is then set to execute the original function after t milliseconds with the provided arguments. This ensures that only the last invocation runs if multiple calls occur within the delay period.",
    },
    "json-deep-equal": {
        "key_insights": "Use recursion to compare nested structures.\nFor primitive types, a simple equality check (=== in JavaScript, == in Python, etc.) suffices.\nFor arrays, verify both have the same length and compare each element recursively.\nFor objects, ensure both have the same keys (order does not matter) then recursively compare the corresponding values.\nHandle cases where one operand is null or differs by type early to avoid unnecessary recursion.",
        "complexity":   "Time Complexity: O(N) in the worst-case, where N is the total number of elements/values in the JSON structure.\nSpace Complexity: O(N) due to the recursion call stack in the worst-case scenario of deeply nested objects or arrays.",
        "solution":     "We solve this problem using a recursive approach. The algorithm first checks if both values are strictly equal, which handles primitives immediately. For arrays and objects, it verifies that the sizes (length for arrays and number of keys for objects) match. Then it recurses into each element (for arrays) or checks each key-value pair (for objects) to ensure that they are also deeply equal. For objects, the order of keys is irrelevant, so we compare key sets. This method naturally handles nested structures by delegating equality checks to the same function.",
    },
    "memoize": {
        "key_insights": "Cache previously computed results using a data structure keyed by the function arguments.\nEnsure the key uniquely represents the argument order; simple tuple (or equivalent) suffices.\nUse closures (or object state) to maintain both the cache and a call count tracking actual function invocations.\nExpose a method (getCallCount) on the memoized function to report the number of times the original function was actually called.\nThe memoized version works for functions that might take one or more parameters.",
        "complexity":   "Time Complexity: O(1) average for each call assuming efficient hashing of arguments.\nSpace Complexity: O(n) for caching n distinct input combinations.",
        "solution":     "We use a hash-based cache (a dictionary or map) to store results for every unique set of arguments. Upon each call, we check if the key (constructed from the input arguments) exists in the cache. If not, we increment a counter, call the original function, store the result in the cache, and return it. Otherwise, we return the cached result. The memoized function is augmented with a getCallCount method to report the number of actual calls made to the original function.",
    },
    "convert-object-to-json-string": {
        "key_insights": "The solution must support all JSON primitives: strings, numbers, booleans, and null.\nComposite types such as arrays and objects require recursive traversal for proper serializing.\nFor strings, wrap them in double quotes.\nFor arrays, recursively process every element and join them with commas between square brackets.\nFor objects, iterate keys in insertion order and combine key-value pairs (key must be a string in double quotes) with commas between curly braces.\nUse recursion carefully to handle nested structures without adding unnecessary spaces.",
        "complexity":   "Time Complexity: O(n), where n is the total number of elements/characters in the JSON structure.\nSpace Complexity: O(n) due to the recursion stack and the constructed output string.",
        "solution":     "We use a recursive approach that inspects the type of the value and processes it accordingly:\nIf the value is null, output \"null\".\nIf the value is a boolean or number, convert it directly to its string representation.\nFor a string, ensure it is enclosed in double quotes.\nFor an array, iterate over each element, serialize each recursively, join them with commas, and enclose in square brackets.\nFor an object, iterate over its keys in insertion order (or using Object.keys in JS / LinkedHashMap in Java), serialize both key (wrapped in double quotes) and its corresponding value, join with commas, and enclose in curly braces.\nThis approach leverages recursion to handle arbitrarily nested objects and arrays while ensuring there are no extra spaces in the final string.",
    },
    "promise-pool": {
        "key_insights": "Use a concurrency control mechanism to limit the number of actively running promises.\nMaintain an index or pointer to track which function from the input array should be executed next.\nUpon the resolution of any promise, start a new one if available until all functions have been processed.\nThe overall task is complete when there are no functions left and all running promises have resolved.",
        "complexity":   "Time Complexity: O(m), where m is the number of functions to execute.\nSpace Complexity: O(n) for the active pool of concurrent promises.",
        "solution":     "We can solve the problem by maintaining an index that tracks which asynchronous function to execute next. We define a helper routine (or executor) that:\nChecks if there is a function left to run.\nIf yes, increments the index and awaits the result of running that function.\nOnce the function completes, the routine recursively calls itself to pick up the next function.\nWe start n such routines (to respect the pool limit) and use Promise.all (or the equivalent in other languages) to wait until all concurrent routines have finished executing. This structure ensures that we never run more than n promises concurrently and that a new promise starts as soon as one finishes.",
    },
    "convert-an-array-of-objects-to-matrix": {
        "key_insights": "- Flatten each object or array into dot-separated paths such as `a.b` or `a.0.c`.\n- Use DFS so nested objects and nested arrays are handled uniformly.\n- For each row, store a map from flattened path to value.\n- Collect every path seen across all rows into a set of column names.\n- Sort the column names lexicographically to build the header row.\n- For each flattened row map, output values in header order and use an empty string when a column is missing.",
        "complexity":   "",
        "solution":     "We solve the problem by flattening every input row first. A DFS walks through objects and arrays, extending the current path with either the property name or the array index. When the DFS reaches a primitive value or `null`, it stores that value under the built path. While flattening each row, we also collect every path into a global set of column names. After all rows are processed, we sort the columns lexicographically, place them in the first row of the matrix, and then build each remaining row by reading values from that row's flattened map, using `\"\"` for missing paths.",
    },
    "differences-between-two-objects": {
        "key_insights": "Use recursion to traverse nested objects and arrays.\nCompare only keys that exist in both objects.\nWhen encountering two values of different types or unequal primitives, record the difference as [value from obj1, value from obj2].\nWhen both values are objects or arrays, recursively compute their differences and include them only if the resulting nested diff is non-empty.\nArrays are treated like objects with indices as keys.\nThe solution requires careful type checking and handling of recursive structures.",
        "complexity":   "Time Complexity: O(n) in average, where n is the number of keys/elements in the common parts of the structures.\nSpace Complexity: O(n) due to recursion and storage needed for the output differences.",
        "solution":     "We solve the problem using a recursive function that compares keys present in both input objects. For each common key:\nIf both values are objects (or both are arrays), recursively compute their differences.\nIf the recursive call returns a non-empty object, include that key in the result.\nIf the two values are of different types or are primitives that differ, add the key with a difference array [value from obj1, value from obj2].\nIgnore keys that exist only in one object.\nThis recursive approach naturally handles the deeply nested structure while ensuring only common keys with different values are captured.",
    },
    "ransom-note": {
        "key_insights": "Use a hash table (or dictionary) to store the frequency of each character present in magazine.\nIterate over every character in ransomNote and check if the corresponding count in the hash table is available and sufficient.\nDecrease the character count for every match; if any character count falls below zero, return false.\nIf all characters in ransomNote can be accounted for, return true.",
        "complexity":   "Time Complexity: O(n + m), where n is the length of magazine and m is the length of ransomNote.\nSpace Complexity: O(1), since the extra space required for the hash table is limited to at most 26 entries for lowercase English letters.",
        "solution":     "We construct a frequency map for the magazine string using a hash table. This frequency map records how many times each letter appears in magazine. Then we iterate through the ransomNote string, and for every letter, we check the corresponding frequency in the map. If a letter is not found or its frequency is zero, we immediately return false as it means the letter cannot be used to build the note. Otherwise, we decrement the frequency count by one. If we successfully process all characters in ransomNote, then it can be constructed from magazine, and we return true.",
    },
    "sentence-similarity": {
        "key_insights": "First, check if both sentences have the same length.\nA word is always similar to itself.\nSimilarity is defined only by the given pairs; it is not transitive.\nUse a hash table (dictionary) to store bi-directional similar pairs for quick look-ups.",
        "complexity":   "Time Complexity: O(n + m), where n is the number of words in the sentences and m is the number of similar pairs.\nSpace Complexity: O(m), for storing the similar pairs in a hash table.",
        "solution":     "Check if the two sentences have the same length; if not, return false.\nBuild a hash table where each word maps to a set of words it is similar to. For each pair [x, y] in similarPairs, add y to the set for x and x to the set for y.\nIterate over the words of both sentences simultaneously. For each corresponding pair, if the words are identical, continue; otherwise, check if one word appears in the similar set of the other.\nIf all pairs meet the similarity condition, return true; otherwise, return false.",
    },
    "single-row-keyboard": {
        "key_insights": "Create a mapping (hash table/dictionary) from each character to its index on the keyboard for O(1) lookups.\nThe cost to type each character is the absolute difference between the current position and the target character's position on the keyboard.\nStart from index 0 and sum up the distances moved for each successive character in the word.",
        "complexity":   "Time Complexity: O(n), where n is the length of the word.\nSpace Complexity: O(1) (constant space for a mapping of 26 characters).",
        "solution":     "We solve the problem by first building a dictionary that maps each character in the keyboard layout to its corresponding index. We then initialize a variable to track the current finger position (starting at 0). For each character in the word, we look up its index, calculate the absolute difference from the current position, add that distance to the total time, and update the current position. This approach uses a single pass over the word with constant time lookups, ensuring an efficient solution.",
    },
    "string-to-integer-atoi": {
        "key_insights": "Skip leading spaces in the string.\nDetect and handle the optional sign indicator ('+' or '-').\nRead and convert digit characters until a non-digit is encountered.\nHandle cases with no digits or leading zeros.\nClamp the result to fit within the 32-bit signed integer range.",
        "complexity":   "Time Complexity: O(n) where n is the length of the string.\nSpace Complexity: O(1) as only a constant amount of extra space is used.",
        "solution":     "We iterate through the string to first eliminate any leading whitespace. Next, we check for a sign character to determine if the resulting integer should be negative. We then convert each consecutive digit into an integer, all the while updating the result and checking for potential overflow. If an overflow is detected, we immediately return the appropriate maximum or minimum 32-bit signed integer boundary value. This approach ensures that we strictly follow the conversion rules and efficiently handle edge cases.",
    },
    "group-shifted-strings": {
        "key_insights": "All strings in the same group have the same relative differences between adjacent characters.\nNormalize each string’s pattern by converting it to a key representation based on the differences between consecutive letters.\nA single character string forms its own group as there are no differences to compute.",
        "complexity":   "Time Complexity: O(n * m) where n is the number of strings and m is the average length of the strings (to compute each string's key).\nSpace Complexity: O(n * m) to store the keys and grouping in the hash table.",
        "solution":     "The solution involves iterating over each string and generating a unique key that represents the relative differences between adjacent characters. This key is calculated by computing the difference (mod 26) between every consecutive pair in the string. Strings that share the same key can be grouped together since they can be shifted into one another. A hash table (or dictionary) is used to store and group the strings by their computed key. This approach handles edge cases like single-character strings separately.",
    },
    "encode-and-decode-strings": {
        "key_insights": "Use length-prefix encoding for each string to avoid ambiguity.\nAppend a delimiter (e.g., \"#\") after the length to separate it from the string content.\nThis method ensures that even if the original strings contain special characters, including the delimiter, the decoding remains unambiguous.\nThe approach handles edge cases, such as empty strings.",
        "complexity":   "Time Complexity: O(n), where n is the total number of characters across all strings.\nSpace Complexity: O(n), required for the encoded string and the resulting decoded list.",
        "solution":     "We encode by iterating over each string and building a new string that contains the length of the string, a delimiter (e.g., \"#\"), and then the string itself. This way, during decoding, we can safely determine how many characters to extract by first reading until the delimiter to get the length, and then reading that number of characters. This length-prefix approach avoids conflicts with potential characters in the strings.",
    },
    "design-log-storage-system": {
        "key_insights": "Store logs in a simple data structure such as a list or array.\nConvert the timestamps into strings; due to fixed-length and zero padding, lexicographical string comparisons are valid.\nUse a mapping to determine the slice length for each granularity, e.g. \"Year\" corresponds to the first 4 characters, \"Month\" corresponds to the first 7 characters, etc.\nDuring retrieval, compare the relevant substring of each stored timestamp against the start and end query timestamp slices.\nThe inclusive nature of the query simplifies checking conditions.",
        "complexity":   "Time Complexity: O(N) per retrieval query, where N is the number of logs stored (since each log is checked). In the worst case, all log entries are examined.\nSpace Complexity: O(N) where N is the number of logs stored.",
        "solution":     "We implement a LogSystem class that supports putting log entries and retrieving them using a specified granularity.\nData Structures:\nUse a list/array to store log entries as tuples (or objects) containing the timestamp and log ID.\nA dictionary/map is used to store the mapping from granularity string to the substring index (e.g., \"Year\" => 4, \"Month\" => 7, etc.).\nFor the put() method, simply append the new log entry into the list.\nFor the retrieve() method, compute the substring length based on the provided granularity. Then iterate over all stored logs and compare the sliced timestamp against the sliced start and end query values. If it falls within the bounds, include the log ID in the result.\nSince the timestamps have fixed width with leading zeros, slicing and lexicographical comparisons are both valid and simple.",
    },
    "alphabet-board-path": {
        "key_insights": "Determine the row and column for each letter (using integer division and modulus).\nBecause \"z\" is in a special row that only contains one column, the move order is critical to avoid invalid positions.\nFor target letter \"z\", perform horizontal moves before vertical moves; for all other letters, do vertical moves before horizontal moves.\nBuild the answer step-by-step by moving from your current position to each target letter.",
        "complexity":   "Time Complexity: O(N) where N is the length of target (each move is processed in constant time)\nSpace Complexity: O(N) for the resulting string of moves",
        "solution":     "The approach calculates the destination coordinates for each character. For each letter in the target:\nConvert the letter to its board coordinates (row and col). For example, for letter c, row = (ord(c) - ord('a')) // 5 and col = (ord(c) - ord('a')) % 5. Note that \"z\" (the 26th letter) always maps to (5, 0).\nTo move from the current position (curRow, curCol) to the target's (targetRow, targetCol), decide on the order of moves:\nIf the target letter is \"z\", first move horizontally (left or right) then vertically (up or down) to avoid stepping into invalid regions from the extra row.\nFor any other letter, move vertically (up or down) first, then horizontally (left or right).\nAppend \"!\" to the moves after reaching each letter.\nUpdate the current position to the letter's coordinates.\nThis method ensures you only traverse valid cells on the board and minimizes moves by taking the optimal coordinate difference each step.",
    },
    "longest-substring-without-repeating-characters": {
        "key_insights": "Use the sliding window technique to maintain a window of unique characters.\nUse a hash table (or dictionary) to quickly check for duplicate characters and update window boundaries.\nThe window is expanded by moving a pointer (right) and contracted by moving another pointer (left) when duplicates are encountered.\nTime complexity can be maintained at O(n) by ensuring each character is processed a limited number of times.",
        "complexity":   "Time Complexity: O(n), where n is the length of the input string.\nSpace Complexity: O(min(n, m)), where m is the size of the character set (constant for fixed character sets).",
        "solution":     "The solution uses a sliding window technique with two pointers, left and right, to keep track of the current substring without repeating characters. A hash table (dictionary or map) is used to store the most recent index of each character encountered. As the right pointer moves forward, if a duplicate character is found within the current window, the left pointer is advanced to the position right after where the duplicate was last seen. This ensures that the window always contains unique characters. The maximum length is updated throughout the process.",
    },
    "longest-substring-with-at-most-two-distinct-characters": {
        "key_insights": "Use a sliding window to maintain a contiguous substring.\nKeep track of character frequencies within the current window using a hash map or dictionary.\nWhen the window contains more than two distinct characters, shrink the window from the left until only two remain.\nUpdate the maximum length each time the window satisfies the condition.",
        "complexity":   "Time Complexity: O(n) where n is the length of the string, since each character is processed at most twice.\nSpace Complexity: O(1) because the hash map holds at most 3 entries (usually up to 2 distinct characters).",
        "solution":     "We use the sliding window technique with two pointers (left and right) to represent the current window. A hash table/dictionary records the count of each character inside the window. As we extend the right pointer, we update the count. If the hash table contains more than two distinct characters, we start moving the left pointer to reduce the number of distinct characters until we have at most two distinct keys. The length of the current valid window is then used to update our maximum length answer.",
    },
    "longest-substring-with-at-most-k-distinct-characters": {
        "key_insights": "Use the sliding window technique to efficiently scan through the string.\nMaintain a hash table (or dictionary) that keeps track of the frequency of characters in the current window.\nWhen the number of distinct characters exceeds k, increment the left pointer to shrink the window until the condition is met.\nUpdate the maximum length of valid substrings throughout the process.",
        "complexity":   "Time Complexity: O(n), where n is the length of the string, as each character is processed at most twice (once when expanding the window and once when contracting it).\nSpace Complexity: O(min(m, k)), where m is the size of the charset (or number of unique characters in s), since at most k+1 keys are stored.",
        "solution":     "This problem is solved using the sliding window technique. The algorithm keeps track of a window defined by two indices (left and right pointers). A hash map is used to count the frequency of each character in the current window. As the right pointer extends the window, the count of the character is incremented. When the count of distinct keys in the map exceeds k, the left pointer is moved forward while decrementing the count of the leftmost character, until the condition is restored. The maximum window length encountered is recorded and returned as the result.",
    },
    "longest-repeating-character-replacement": {
        "key_insights": "Use a Sliding Window algorithm to keep track of a contiguous substring.\nMaintain a frequency count of letters in the current window.\nKeep track of the count of the most frequent character in the window.\nIf the current window size minus the maximum frequency is greater than k, shrink the window.\nThe maximum window size seen during the process is the answer.",
        "complexity":   "Time Complexity: O(n) where n is the length of the string, since the window traverses the string in linear time.\nSpace Complexity: O(1) because the frequency count for letters (A-Z) has a constant size.",
        "solution":     "The problem is solved using the sliding window technique. We initialize two pointers representing the left and right bounds of the current window. As we expand the window by moving the right pointer, we update the frequency count of the letter at the right pointer and track the letter that appears most frequently. The key observation is that the number of characters that need to be replaced in the current window is the window's size minus the count of the most frequently occurring letter. If this number exceeds k, we increment the left pointer to shrink the window until the condition is satisfied again. The maximum size of the window during the process gives the answer.",
    },
    "find-all-anagrams-in-a-string": {
        "key_insights": "Use a sliding window of length equal to p over s.\nMaintain frequency counts of characters in p and within the current window in s.\nCompare frequency counts to determine if the current window is an anagram.\nUpdate the window efficiently by adding one character and removing the character that goes out of the window.",
        "complexity":   "Time Complexity: O(n) where n is the length of s.\nSpace Complexity: O(1) since we use fixed-size frequency arrays or hash maps.",
        "solution":     "We use the sliding window technique to iterate over s with a window size equal to the length of p. First, count the frequencies of characters in p. Then, iterate over s and update a frequency count for the current window. When the window size equals the length of p, compare the two frequency counts. If they match, record the start index. To slide the window efficiently, increment the count for the incoming character and decrement the count for the outgoing character. This way, we achieve an O(n) solution that is optimal for the problem constraints.",
    },
    "max-consecutive-ones-ii": {
        "key_insights": "Utilize a sliding window to maintain the current segment.\nThe window can contain at most one 0.\nExpand the window with the right pointer and contract it with the left pointer when the constraint is violated.\nThe maximum window size observed during this process is the desired result.",
        "complexity":   "Time Complexity: O(n) where n is the number of elements in the array.\nSpace Complexity: O(1).",
        "solution":     "We use a sliding window approach to efficiently find the longest contiguous subarray that contains at most one 0. Two pointers (left and right) define the window. As we iterate with the right pointer, counting zeros in the window, we adjust the left pointer when the count of zeros exceeds one. The length of the valid window gives us the number of consecutive 1's (considering one flipped 0), and we update the maximum length accordingly.",
    },
    "find-k-length-substrings-with-no-repeated-characters": {
        "key_insights": "Use a sliding window of fixed size k.\nTrack character counts within the current window using a hash table (or frequency array).\nWhen a duplicate is found in the window, slide the window from the left to remove duplicates.\nCount a valid window when its size equals k and all characters are unique.\nOptimize by adjusting the window immediately after counting a valid substring.",
        "complexity":   "Time Complexity: O(n) where n is the length of s, as each character is visited at most twice.\nSpace Complexity: O(1) since the frequency tracking is over a fixed set (26 lowercase letters).",
        "solution":     "We use a sliding window approach with two pointers: left and right. A hash table (or frequency array) tracks the occurrences of characters in the window. As we move the right pointer, we update the frequency and check for duplicates. If a duplicate is encountered, we increment the left pointer until the duplicate is removed. Once the window size exactly equals k, we have a valid substring, so we increment our count and then slide the window forward. This ensures that every possible k-length substring is examined once.",
    },
    "count-number-of-nice-subarrays": {
        "key_insights": "Convert the problem to counting subarrays with a specific \"prefix sum\" value where the sum represents the count of odd numbers.\nUse a hash table (or dictionary) to store the frequency of prefix sums encountered.\nThe number of nice subarrays ending at the current index equals the frequency of (current prefix sum - k) seen so far.\nThis approach allows you to find the answer in one pass over the array.",
        "complexity":   "Time Complexity: O(n), where n is the number of elements in nums, because we traverse the array once.\nSpace Complexity: O(n), in the worst case where each prefix sum is unique and stored in the hash table.",
        "solution":     "We solve the problem using a prefix sum approach. First, we iterate through the array and convert it into a prefix sum representing the cumulative count of odd numbers encountered. As we compute the prefix sums, we use a hash table to record how many times each prefix sum appears. For every current prefix sum value, we check how many times we have seen a prefix sum equal to (current prefix sum - k). The idea is that if prefix[j] - prefix[i] equals k, then between indices i+1 and j, there are exactly k odd numbers. This gives us the count of valid subarrays ending at the current index.\nThis method works efficiently as it requires only a single pass through the array and maintains a running frequency count using the hash table.",
    },
    "minimum-window-substring": {
        "key_insights": "Use a sliding window approach to dynamically adjust the window boundaries.\nUtilize a hash table (or dictionary) to keep track of the frequency of characters required (from t) and the frequency within the current window.\nExpand the window until it satisfies the requirements, then contract to minimize the window.\nEfficiently update counts and check valid window with two pointers (left and right).",
        "complexity":   "Time Complexity: O(m + n), where m is the length of s and n is the length of t.\nSpace Complexity: O(m + n) in the worst case due to the storage for the hash maps.",
        "solution":     "We use a sliding window technique with two pointers representing the window's boundaries. First, create a frequency map of characters from t. Then, expand the right pointer to include characters in the window until the window contains all required characters (meeting or exceeding the counts in t). Once the requirement is met, try contracting the window by moving the left pointer to reduce its size while still maintaining a valid window. Throughout the process, if a valid window is found that is smaller than the previously recorded minimum window, update the minimum. The solution leans heavily on hash tables to track counts and compares them against the required counts from t.",
    },
    "majority-element": {
        "key_insights": "The majority element appears more than half the length of the array.\nA simple hash table approach can count occurrences, but a more efficient solution exists.\nThe Boyer-Moore Voting Algorithm finds the majority in O(n) time and O(1) space.\nThe algorithm maintains a candidate and a counter, adjusting the candidate when the count drops to zero.",
        "complexity":   "Time Complexity: O(n)\nSpace Complexity: O(1)",
        "solution":     "We use the Boyer-Moore Voting Algorithm to identify the majority element. The algorithm iterates through the array using two variables: one for the candidate and one for the count. Initially, we set the candidate to an arbitrary value and count to 0. For each element, if the count is 0, we update the candidate to the current element. Then, if the current element is the same as the candidate, we increment the count; otherwise, we decrement it. Since the majority element appears more than n/2 times, it will remain as the candidate by the end of the iteration.",
    },
    "contains-duplicate": {
        "key_insights": "A hash set can be used to keep track of seen numbers.\nIterating once through the array provides an efficient solution.\nIf a number is already in the set, a duplicate is found.\nAlternative approaches like sorting have a higher time complexity (O(n log n)).",
        "complexity":   "Time Complexity: O(n)\nSpace Complexity: O(n)",
        "solution":     "The solution uses a hash set to track the numbers seen while iterating through the array. For each number in the array:\nCheck if it exists in the hash set.\nIf it does, return true immediately as a duplicate is found.\nOtherwise, add the number to the hash set.\nIf the iteration completes without finding any duplicates, return false. This approach efficiently determines if any duplicates exist in a single pass.",
    },
    "valid-anagram": {
        "key_insights": "If s and t have different lengths, they cannot be anagrams.\nAn anagram requires exactly the same character frequency for both strings.\nUsing a hash table (or frequency array when limited to 26 lowercase letters) provides an efficient solution.\nAn alternative approach is to sort both strings and compare them.\nFor Unicode inputs, a hash table is more adaptable than a fixed-size frequency array.",
        "complexity":   "Time Complexity: O(n), where n is the length of the strings (using a hash table for counting).\nSpace Complexity: O(1) if we use a fixed-size array (for 26 letters), otherwise O(n) for a hash table with a larger character set.",
        "solution":     "We use a hash table (dictionary) to count the frequency of each character in the first string s. Then, we iterate over string t and decrement the count for each character. If at any point a character count goes negative or a character is missing in the hash table, t cannot be an anagram of s. Finally, if all counts return to zero, the strings are anagrams. This approach is efficient and handles the problem in linear time.",
    },
    "meeting-rooms": {
        "key_insights": "Sort the intervals based on their starting times.\nCompare each interval's end time with the next interval's start time.\nDetect overlap if the end time of one meeting exceeds the start time of the following meeting.\nIf no overlaps are detected, all meetings can be attended.",
        "complexity":   "Time Complexity: O(n log n) due to sorting the intervals.\nSpace Complexity: O(1) if sorting is done in-place; otherwise O(n).",
        "solution":     "The solution starts by sorting the intervals in ascending order by their start times. After sorting, iterate through the intervals and for each consecutive pair, check if the previous meeting's end time exceeds the next meeting's start time. An overlap indicates that the person cannot attend all meetings. The primary data structure used is an array (or list) for holding the intervals. Sorting makes it easier to compare adjacent intervals for potential overlaps.",
    },
    "largest-unique-number": {
        "key_insights": "Use a hash table (or dictionary) to count the frequency of each number.\nIterate over the hash table to find the numbers that appear exactly once.\nIdentify the maximum number among the unique numbers.\nReturn -1 if no unique number exists.",
        "complexity":   "Time Complexity: O(n), where n is the number of elements in nums.\nSpace Complexity: O(n), for the hash table storing frequency counts.",
        "solution":     "The approach starts by initializing a hash table to keep track of the frequency of each element in the array. Once the frequency count is complete, we traverse the hash table to collect numbers that appear exactly once. Finally, we determine the maximum number among these unique elements. If there is no unique element, we return -1. This method is efficient because the frequency count and the search for the maximum unique element both run in linear time relative to the input size.",
    },
    "relative-sort-array": {
        "key_insights": "Use a hash table (or array for counting given the value constraints) to count the occurrences of each element in arr1.\nIterate through arr2 and for each element, append it to the result based on its count from the hash table.\nRemove the used elements from the hash table.\nSort the remaining elements (those not in arr2) in ascending order and append them to the result.",
        "complexity":   "Time Complexity: O(n + m log m) where n is the length of arr1 and m is the number of leftover elements not in arr2. Given that element values are bounded (0 to 1000), a counting sort can make this nearly O(n).\nSpace Complexity: O(n) for storing the frequency counts and the result array.",
        "solution":     "The solution leverages a frequency counter (hash table) to count how many times each element appears in arr1. We then process arr2 to add each number in its given order according to its frequency. Finally, we sort the remaining numbers that are not in arr2 and append them to the result array. This approach ensures that the relative order specified by arr2 is maintained and that the other elements are sorted in ascending order.",
    },
    "group-anagrams": {
        "key_insights": "Anagrams have the same characters when sorted alphabetically.\nSorting each string provides a unique key for grouping.\nUse a hash table (dictionary) to map each sorted key to a list of its original strings.\nFinally, gather all the lists from the dictionary to form the result.",
        "complexity":   "Time Complexity: O(n * k log k), where n is the number of strings and k is the maximum length of a string (due to sorting each string).\nSpace Complexity: O(n * k) for storing the grouped strings in the hash table.",
        "solution":     "The primary idea is to iterate through each string, sort it to obtain a key that represents its character composition, and then group the original string under this key in a hash table. After processing all strings, the values of the hash table will be the groups of anagrams. This approach efficiently groups anagrams by leveraging sorting and a dictionary for constant-time lookups.",
    },
    "merge-intervals": {
        "key_insights": "Sort the intervals based on their start values.\nUse a pointer or iteration to compare the current interval with the last merged interval.\nIf the current interval overlaps with the previous one, merge them by updating the end time.\nOtherwise, add the current interval to the results as a new merged interval.",
        "complexity":   "Time Complexity: O(n log n) due to sorting the intervals.\nSpace Complexity: O(n) to store the merged intervals.",
        "solution":     "First, sort the array of intervals based on the starting time. Then, iterate through the sorted list and check if the current interval overlaps with the last interval in the merged list. If an overlap is detected (i.e., the start of the current interval is less than or equal to the end of the last merged interval), update the last merged interval's end to be the maximum of both ends. If there is no overlap, simply add the current interval to the merged list. This approach efficiently merges overlapping intervals and ensures that all intervals are processed in a single pass after sorting.",
    },
    "analyze-user-website-visit-pattern": {
        "key_insights": "Combine the inputs into a single list of events and sort them by timestamp.\nGroup the visits by each user, preserving the sorted order.\nGenerate all unique combinations of 3-sequences for each user (order matters and non-contiguous visits are allowed).\nUse a dictionary (or hash map) to map each 3-sequence pattern to the set of users who visited that pattern.\nThe problem constraints are small, so generating combinations per user (even using triple nested loops) is acceptable.\nWhen multiple patterns have the same frequency, choose the lexicographically smallest one.",
        "complexity":   "Time Complexity: O(U * L^3) where U is the number of users and L is the maximum length of visits per user (since we generate combinations of 3 visits per user). Sorting all events costs O(N log N) for N total events.\nSpace Complexity: O(P) where P is the number of unique 3-sequence patterns stored in the dictionary, plus O(N) for grouping the events.",
        "solution":     "Aggregate the events by user while sorting them based on timestamp.\nFor each user, generate all sets of 3-website patterns (to avoid duplicate patterns per user).\nUse a hash map to count the number of unique users for each pattern.\nTraverse the map to find the pattern with the highest count. In case of a tie, compare patterns lexicographically.\nReturn the pattern that meets the criteria.",
    },
    "letter-combinations-of-a-phone-number": {
        "key_insights": "Use a mapping from digits to the respective letters as defined by a telephone keypad.\nBacktracking is an ideal approach to generate all possible combinations.\nEach digit adds a level in the recursive tree where you append one letter at a time.\nHandling edge cases such as an empty input string is crucial.",
        "complexity":   "Time Complexity: O(4^n * n) where n is the length of the input string (each digit may map to up to 4 letters and adding a combination takes O(n) time).\nSpace Complexity: O(n) for the recursion call stack, not counting the space required for storing the output.",
        "solution":     "We can solve the problem using a backtracking algorithm. First, we define a dictionary mapping each digit to its corresponding letters. Then we initiate a recursive function called backtrack that takes the current combination and the index of the next digit to process. At each call, if the combination is complete (length equal to the input string), we add it to the result list. Otherwise, we iterate through the letters corresponding to the current digit, append a letter to the combination, and recursively call backtrack with the next index. This approach ensures that all possible letter combinations are generated.",
    },
    "generate-parentheses": {
        "key_insights": "Use a backtracking approach to build the valid combinations step by step.\nAt any recursion step, you can add an opening parenthesis if you still have one available.\nYou can add a closing parenthesis only if it would not result in an invalid sequence (i.e., the number of closing parentheses is less than the number of opening ones).\nThe solution leverages recursion to explore all potential valid sequences while pruning invalid paths early.",
        "complexity":   "Time Complexity: O(4^n / (n^(3/2)))\nSpace Complexity: O(n) for the recursion stack, plus space for storing all valid combinations.",
        "solution":     "The solution uses a recursive backtracking technique to generate the combinations. The algorithm maintains counters for the number of '(' and ')' added to the current string. It recursively adds a '(' if the count is less than n and adds a ')' if doing so does not make the string invalid (i.e., if the count of ')' is less than the count of '('). When a valid combination reaches a length of 2*n, it is added to the result list.",
    },
    "combination-sum": {
        "key_insights": "Backtracking is a natural fit to explore all potential combinations.\nSorting candidates simplifies the process by allowing early termination if the current candidate exceeds the remaining target.\nRecursion is used to build combinations and backtrack once the current path exceeds or meets the target.\nThe same candidate can be reused in the combination, so the recursive call does not advance the index.",
        "complexity":   "Time Complexity: O(2^(target/min(candidates))) in the worst case where many combinations are possible.\nSpace Complexity: O(target/min(candidates)) for the recursion stack and temporary space used to store combinations.",
        "solution":     "The solution uses a backtracking approach. First, sort the candidates to enable pruning of recursive calls once the candidate exceeds the remaining target. A recursive helper function builds up a combination, subtracting the candidate value from the target until it reaches zero (a valid combination) or becomes negative (an invalid path). The recursion allows the same candidate to be chosen again. Each valid combination is then added to the result list.",
    },
    "permutations": {
        "key_insights": "Use backtracking to generate all possible permutations.\nBuild each permutation by selecting numbers one at a time.\nWhen the current permutation reaches the length of nums, add it to the result.\nBacktracking allows you to undo choices and explore different orderings.",
        "complexity":   "Time Complexity: O(n * n!) - There are n! permutations, and constructing each permutation takes O(n) time.\nSpace Complexity: O(n) - Recursion depth is limited to n, plus additional space for the current permutation.",
        "solution":     "The solution employs a backtracking algorithm. A helper function is used to build permutations recursively. At each step, the algorithm iterates through the available numbers, adds one to the current permutation if it has not been used yet, and recurses to handle the remaining numbers. Once the current permutation matches the length of the input array, it is added to the result list. Then, by \"backtracking\" (removing the last number added), the algorithm explores alternative possibilities. This systematic exploration ensures that all possible permutations are generated.",
    },
    "word-search": {
        "key_insights": "Use Depth-First Search (DFS) to explore each cell in the board as a potential starting point.\nEmploy backtracking to mark cells as visited when exploring a path and revert them back when backtracking.\nCheck boundary conditions and ensure the current cell’s character matches the corresponding character in the word.\nGiven the small grid and word sizes, a recursive solution is both feasible and straightforward.\nSearch pruning can be applied by stopping a DFS branch as soon as the current path does not match the prefix of the target word.",
        "complexity":   "Time Complexity: O(m * n * 3^L), where L is the length of the word (each DFS may branch to at most 3 further cells, excluding the coming cell).\nSpace Complexity: O(L) due to recursion call stack (where L is the word length).",
        "solution":     "The solution uses DFS with backtracking to traverse the grid. For each cell, we:\nCheck if the cell matches the first character of the word.\nRecursively explore the adjacent cells (up, down, left, right) while marking the current cell as visited.\nIf the path does not lead to a complete match, backtrack by resetting the visited cell.\nThe DFS stops if all characters in the word have been found in sequence.\nThe algorithm leverages in-place modifications (or an auxiliary visited array) to avoid revisiting cells while exploring a candidate path.",
    },
    "path-sum-ii": {
        "key_insights": "Use Depth-First Search (DFS) to explore all paths from the root to the leaves.\nBacktracking is essential: add the current node value to the path before the recursive calls and remove it (backtrack) after exploring both subtrees.\nAt each leaf, check if the accumulated sum equals targetSum.\nMaintain a running sum or subtract the current node's value from the target sum as you traverse.",
        "complexity":   "Time Complexity: O(N^2) in the worst-case scenario (where N is the number of nodes) when most nodes are leaf nodes, since each potential path copy operation can be O(N).\nSpace Complexity: O(N) for the recursion stack and the path storage, where N is the height of the tree.",
        "solution":     "We solve the problem using a recursive DFS approach combined with backtracking. The idea is to traverse the tree while maintaining the current path and the remaining sum needed to reach targetSum. When a leaf node is reached (node with no children), we check if the remaining sum equals the leaf's value – if yes, we record a copy of the current path.\nData structures and algorithmic approaches used:\nRecursion for DFS: to traverse to each leaf.\nList (or Array) for storing the current path.\nBacktracking: to remove the last added element when stepping back up the recursion stack.\nCopying the path when a valid leaf is reached to store it in the final result.\nA common pitfall is to forget to backtrack, which would result in incorrect paths being recorded.",
    },
    "sudoku-solver": {
        "key_insights": "Use backtracking to explore digit placements for each empty cell.\nValidate placements by ensuring that the chosen digit is not already present in the corresponding row, column, or 3x3 sub-box.\nEmploy recursion to fill the board incrementally and backtrack upon encountering invalid configurations.\nAlthough the worst-case time complexity is exponential, the fixed board size (9x9) keeps the problem computationally manageable.",
        "complexity":   "Time Complexity: In the worst-case scenario, the algorithm can explore up to O(9^(n)) possibilities where n is the number of empty cells. However, since n is at most 81 and many branches are pruned early, the practical runtime is far less.\nSpace Complexity: O(n) due to the recursion stack, where n is the number of empty cells; given the board’s fixed size, this space is constant in practice.",
        "solution":     "The solution uses a backtracking approach. For each empty cell on the board, we attempt to place a digit from '1' to '9'. Before placing a digit, we check if the placement is valid by ensuring that:\nThe digit is not already present in the same row.\nThe digit is not already present in the same column.\nThe digit is not already present in the corresponding 3x3 sub-box.\nIf a valid digit is found, we place it on the board and recursively attempt to solve the rest of the board. If no valid digit can be placed, the algorithm backtracks by reverting the last move and trying a different digit. This recursive search continues until the board is completely filled with a valid Sudoku configuration.",
    },
    "n-queens": {
        "key_insights": "Use backtracking to explore potential positions row by row.\nUse sets to track which columns and diagonals are under attack.\nDiagonals can be tracked by (row - col) and (row + col), ensuring that queens do not conflict along any diagonal.\nRecursively build each valid board configuration and backtrack when a conflict arises.",
        "complexity":   "Time Complexity: O(n!) in the worst-case scenario, as the solution explores permutations for queen placements.\nSpace Complexity: O(n^2) for storing board configurations and O(n) for recursion stack and conflict-tracking sets.",
        "solution":     "We use a backtracking algorithm that places queens row by row. For each row, we try placing a queen in each column. Before placing, we check if the column, main diagonal (row - col), and anti-diagonal (row + col) are free using dedicated sets. If a queen can be safely placed, we mark the column and diagonals as occupied and recurse to the next row. Once all rows are processed, the board configuration is a valid solution and is added to the result. After that, we backtrack by unmarking the sets and removing the queen from the board. This systematic approach ensures all possible configurations are considered.",
    },
    "word-ladder-ii": {
        "key_insights": "Use Breadth-First Search (BFS) to traverse level-by-level and capture only the shortest paths.\nBuild a parent mapping during BFS to record which words lead to each newly discovered word.\nOnce the shortest path is found via BFS, use backtracking (or DFS) from endWord to reconstruct all transformation sequences.\nRemove words as they are visited within a level to avoid unnecessary revisits and cycles.\nThe solution must handle multiple shortest paths, so maintaining a list of predecessors for each word is crucial.",
        "complexity":   "Time Complexity: O(N * L * 26) where N is the number of words in the wordList and L is the length of each word. This accounts for checking all possible one-letter transformations.\nSpace Complexity: O(N * L) for storing the parent mapping and maintaining the BFS queue and other auxiliary data structures.",
        "solution":     "We solve the problem in two major phases:\nUse BFS to explore from beginWord to endWord. During BFS, record all parents for each word encountered. This ensures we capture all possible predecessors that yield a shortest transformation.\nOnce the BFS completes (once endWord is reached), use backtracking (DFS) beginning from the endWord and moving backwards via the parent mapping to reconstruct all transformation sequences in reverse. Finally, reverse each sequence to present it from beginWord to endWord.\nThis two-phase approach guarantees that only the shortest paths are reconstructed, as BFS ensures level-by-level exploration.",
    },
    "number-of-squareful-arrays": {
        "key_insights": "The problem requires ensuring adjacent pairs sum up to a perfect square.\nSorting the array helps in efficiently handling duplicates; when iterating in the backtracking, skip duplicate elements to avoid overcounting.\nFor each valid candidate, check that the sum with the previous number (if any) is a perfect square.\nUse backtracking (DFS) to generate all valid permutations while maintaining a visited flag for each index.\nSince the array may contain duplicates, extra care is taken to only use each occurrence in a controlled order (i.e., if two identical numbers exist, only the first unvisited instance can be chosen at a given recursive call).",
        "complexity":   "Time Complexity: O(n^2 * 2^n) in the worst-case scenario due to exploring all bitmask states with n elements and checking pairwise sums.\nSpace Complexity: O(n) for recursion and O(n) for the visited flag, plus potentially O(2^n * n) for memoization if implemented.",
        "solution":     "The solution uses a backtracking algorithm to explore all permutations. The array is first sorted to handle duplicates. A helper function checks if a number is a perfect square and is used to validate that the sum of every adjacent pair is a perfect square. During DFS, a visited array is maintained to track elements included so far. Skipping duplicate elements (unless the earlier duplicate was used) is essential to avoid overcounting. This approach ensures that every valid permutation is considered exactly once.",
    },
    "walls-and-gates": {
        "key_insights": "Use a multi-source Breadth-First Search (BFS) starting from all the gates, as this allows spreading the shortest distance to each empty room.\nInstead of starting from every empty room separately, starting from all gates simultaneously helps update all distances in one pass.\nOnly update a room if the new calculated distance is smaller than its current value to prevent unnecessary processing.\nThe problem is essentially a multi-source shortest path problem on a grid.",
        "complexity":   "Time Complexity: O(m * n) where m and n are the dimensions of the grid, as each cell is processed at most once.\nSpace Complexity: O(m * n) in the worst-case scenario used by the BFS queue.",
        "solution":     "The solution uses a multi-source BFS algorithm by initially enqueuing all the gate positions (cells with value 0). Then, for each cell obtained from the queue, check its four possible neighbors (up, down, left, right). If a neighbor is an empty room (INF), update its distance to be the current cell's distance plus one, and then enqueue the neighbor to process further. This ensures that each empty room gets the minimum distance from any gate.",
    },
    "coin-change": {
        "key_insights": "Use a dynamic programming approach to build up a solution from smaller subproblems.\nDefine dp[x] as the minimum number of coins required for amount x.\nThe recurrence relation is: dp[x] = min(dp[x], dp[x - coin] + 1) for each coin.\nInitialize dp[0] = 0 and set other values to a number larger than the maximum possible coins (amount+1 works as an infinity substitute).\nAn alternative approach is through BFS, treating each amount as a node and each coin as an edge, but the DP method is more intuitive here.",
        "complexity":   "Time Complexity: O(amount * n) where n is the number of coins.\nSpace Complexity: O(amount) for the dp array.",
        "solution":     "We use a bottom-up dynamic programming approach. The idea is to use a one-dimensional dp array where each element at index x represents the minimum number of coins required to form the amount x. Starting from dp[0] which is 0, we iterate through all amounts up to the target amount and update dp[x] based on each coin denomination. If after filling the dp array the value dp[amount] remains unchanged (i.e., greater than amount), it means the amount cannot be formed and we return -1.",
    },
    "01-matrix": {
        "key_insights": "Treat all 0s as starting points for a multi-source BFS.\nUpdate each neighboring cell's distance if a shorter distance is found.\nUtilize a queue to process cells in order of increasing distance.\nEnsure that each cell is processed only once by marking visited cells.",
        "complexity":   "Time Complexity: O(m\nn) since each cell is processed once.\nSpace Complexity: O(m\nn) for the queue and the answer matrix.",
        "solution":     "We use a multi-source Breadth-First Search (BFS) where every 0 in the matrix is initially added to the BFS queue. Each neighboring cell that is a 1 will have its distance updated to be one plus the distance of the current cell if it hasn’t been visited yet (or if a smaller distance is found). This ensures that the first time a 1 is reached, it is reached by the shortest path. Data structures used include a queue (or deque) for BFS and the matrix itself for storing distances.",
    },
    "rotting-oranges": {
        "key_insights": "Use Breadth-First Search (BFS) starting from all initially rotten oranges.\nProcess the grid level by level, where each level represents one minute.\nKeep a count of fresh oranges and decrement it as they become rotten.\nIf after the BFS there are still fresh oranges, then it is impossible to rot every orange.",
        "complexity":   "Time Complexity: O(m * n) as every cell is processed at most once.\nSpace Complexity: O(m * n) in the worst case for the queue and auxiliary storage.",
        "solution":     "We begin by scanning the grid to locate all rotten oranges and count the fresh ones. These rotten oranges are added to a queue for a BFS. For each minute (BFS level), we process all the current rotten oranges and try to infect their adjacent fresh oranges. If an adjacent cell contains a fresh orange, we mark it rotten, add it to the queue, and decrement our fresh count. We continue this process level by level (minute by minute) until there are no fresh oranges left. If, after processing, there are still fresh oranges remaining, we return -1, indicating that not all oranges can become rotten.",
    },
    "minimum-knight-moves": {
        "key_insights": "Leverage the symmetry of the chessboard by converting target coordinates to their absolute values.\nUse a Breadth-First Search (BFS) because all moves have equal weight, ensuring the first encounter with the target is the minimal path.\nPrune the search space by only considering positions that are likely beneficial (e.g., positions having coordinates not far less than –1).\nTrack visited states to avoid redundant processing.\nOptimize by bounding the search region relative to the target distance.",
        "complexity":   "Time Complexity: O(n) where n is the number of positions processed during the BFS\nSpace Complexity: O(n) due to the storage requirements for the BFS queue and the visited set",
        "solution":     "We solve the problem using a BFS starting from the origin. First, normalize the target by taking the absolute value of x and y. BFS is then applied by exploring all eight possible knight moves at each step. Each move generates a new position that is enqueued if it hasn't been visited and falls within a reasonable bound (e.g., coordinates >= -1). The search terminates as soon as the target position is reached.",
    },
    "shortest-path-to-get-food": {
        "key_insights": "Use Breadth-First Search (BFS) to explore the grid level by level.\nThe BFS guarantees that when a food cell ('#') is reached, the current distance is the shortest.\nMaintain a visited structure to avoid re-processing the same cell.\nConsider boundary and obstacle ('X') conditions while traversing.",
        "complexity":   "Time Complexity: O(m * n), where m is the number of rows and n is the number of columns since each cell is processed at most once.\nSpace Complexity: O(m * n) in the worst case due to the queue and visited matrix.",
        "solution":     "We use Breadth-First Search (BFS) starting from the cell marked with '*'. The BFS uses a queue to explore neighbors in the four allowed directions (up, down, left, right). Each level of BFS represents one step in the path. As soon as a cell containing food ('#') is reached, the current step count is returned. We also maintain a visited matrix to ensure that cells are not revisited, which could otherwise result in cycles or redundant processing. This approach ensures that the first time we encounter food, we have found the shortest possible path.",
    },
    "word-ladder": {
        "key_insights": "Use Breadth-First Search (BFS) to explore transformations level by level to guarantee the shortest path.\nPreprocess words by generating intermediate representations (e.g., replacing each letter with a wildcard) to quickly find neighbors.\nUse a visited set to avoid cycles.\nEach transformation counts as a level in the BFS, so the answer is the number of levels traversed.",
        "complexity":   "Time Complexity: O(N * M^2) where N is the number of words and M is the length of each word.\nSpace Complexity: O(N * M) for the storage of intermediate mappings and the BFS queue.",
        "solution":     "We solve the problem using a BFS approach. First, we preprocess the wordList by creating a mapping of all possible intermediate states. For example, for the word \"dog\" and wildcard position, we generate \"\nog\", \"d\ng\", and \"do*\". This helps us find all adjacent words (neighbors) that are only one letter different in constant time. We then initialize a queue with the beginWord and initiate the BFS. For each word dequeued, we explore all valid transformations using the precomputed intermediate mapping. If the endWord is reached during this process, we return the current level (transformation sequence length). Otherwise, we continue the BFS until all possibilities are exhausted. If no valid transformation sequence is found, we return 0.",
    },
    "shortest-distance-from-all-buildings": {
        "key_insights": "We need to compute the Manhattan distance from each building (cell value 1) to all empty lands (cell value 0) by performing a BFS.\nMaintain two matrices: one for cumulative total distances and another for the count of buildings that reached a particular empty cell.\nEnsure that only those empty cells reachable by every building are considered.\nThe ideal cell will have a reach count equal to the total number of buildings.\nIf multiple buildings cannot all reach any specific empty cell, then return -1.",
        "complexity":   "Time Complexity: O(k * m * n) where k is the number of buildings and m,n are the grid dimensions.\nSpace Complexity: O(m * n) due to the auxiliary matrices and visited state tracking used in BFS.",
        "solution":     "We first iterate through the grid to locate all buildings while counting them. For each building, we perform a BFS to compute the shortest distance to each reachable empty cell. During the BFS, we update the cumulative distance for that cell and record that it was reached by one additional building. After processing all the buildings, we scan the grid to find the empty cell that is reachable from all buildings and has the minimum cumulative distance. If no such cell exists, we return -1.",
    },
    "sliding-puzzle": {
        "key_insights": "Represent the board as a string (e.g. \"123450\") to simplify state comparison and manipulation.\nUse Breadth First Search (BFS) to explore all possible moves since the puzzle has a small fixed state space (6! = 720 possible states).\nPrecompute the valid neighbor indices for each board position to efficiently generate new states.\nTrack visited states to avoid loops and redundant work.",
        "complexity":   "Time Complexity: O(1) in practice, since there are at most 720 states (6! permutations).\nSpace Complexity: O(1) for the same reason, as the state space is fixed and limited.",
        "solution":     "We solve the problem using a Breadth First Search (BFS) approach. The board configuration is flattened into a string to represent states. A mapping from each index to its possible neighbors (i.e., positions to which 0 can move) is precomputed. BFS is then used to explore every valid move from the initial state while marking states as visited to avoid cycles. When the goal state (\"123450\") is reached, we return the number of moves taken. If BFS completes without reaching the goal, the puzzle is unsolvable and we return -1.",
    },
    "bus-routes": {
        "key_insights": "Model the problem as a graph where nodes are bus routes.\nTwo bus routes are connected if they share at least one common bus stop.\nUse a hash map to quickly look up bus routes available at any bus stop.\nEmploy Breadth-First Search (BFS) on the bus routes graph to find the minimum number of bus transfers.\nMark visited bus routes and bus stops to avoid redundant work.",
        "complexity":   "Time Complexity: O(N * L) in the worst-case scenario, where N is the number of bus routes and L is the average number of stops per route.\nSpace Complexity: O(N * L) due to storage of the stop-to-buses mapping and BFS auxiliary data structures.",
        "solution":     "We begin by constructing a mapping from each bus stop to the list of buses passing through it. This allows quick retrieval of buses available at a given stop. Starting at the source stop, initialize the BFS queue with all buses that can be boarded at that stop. Then, for each bus route in the queue, inspect each stop along that route. If a stop is the target, return the number of buses taken so far. Otherwise, for each unvisited stop, add all adjacent bus routes (buses that pass through this stop) to the BFS queue with an incremented bus count. Continue until the queue is empty, which means the target cannot be reached.",
    },
    "add-binary": {
        "key_insights": "Simulate binary addition just like you add numbers digit by digit.\nProcess the strings from right to left, handling the carry at each step.\nUse modulo and division operations to extract the current digit and update the carry.\nAppend any remaining carry at the end.",
        "complexity":   "Time Complexity: O(max(n, m)), where n and m are the lengths of the two binary strings.\nSpace Complexity: O(max(n, m)) for storing the resulting binary string.",
        "solution":     "The strategy is to iterate over the input strings from the end to the beginning. At each step, calculate the sum of corresponding bits and the existing carry. Use the modulo operation (% 2) to determine the current digit and integer division (// 2) to update the carry. Once all digits have been processed, reverse the result to obtain the final binary string.",
    },
    "single-number": {
        "key_insights": "Using bitwise XOR: XOR-ing a number with itself cancels out to 0.\nXOR is both commutative and associative, so the order of operations does not matter.\nA single traversal (linear time) and constant extra space is sufficient for solving the problem.",
        "complexity":   "Time Complexity: O(n) – We only traverse the array once.\nSpace Complexity: O(1) – Only a single extra variable is used regardless of input size.",
        "solution":     "We can solve this problem by initializing a variable to 0 and then traversing the array while XOR-ing each element with the variable. Due to the properties of XOR, pairs of identical numbers will cancel each other out (x XOR x = 0) and the unique number will be left as the final result. This approach uses constant extra space and performs in linear time.",
    },
    "reverse-bits": {
        "key_insights": "The input is a binary string of fixed length (32 bits).\nReversing the bits is equivalent to reversing the string.\nThe reversed binary string can be converted back into its integer representation.\nSince the number of bits is constant (32), the solution can be achieved in constant time and constant space.\nFor performance optimization when this function is called many times, caching may be applied for common intermediate results.",
        "complexity":   "Time Complexity: O(1) – We always process 32 bits regardless of the input.\nSpace Complexity: O(1) – Only constant extra space is used.",
        "solution":     "We solve the problem by reversing the 32-character binary string representing the unsigned integer. Once reversed, the new binary string is converted into its integer value. An alternative solution involves using bitwise operations where you iterate over each bit, shift the result to the left, and add the current bit. This approach does the reversal one bit at a time. The chosen approach leverages string reversal which is straightforward to implement and understand.",
    },
    "number-of-1-bits": {
        "key_insights": "The problem is essentially counting the number of 1s in the binary representation of a number.\nA common efficient approach uses bit manipulation: repeatedly clear the lowest set bit until n becomes zero.\nThe expression n & (n - 1) clears the lowest set bit of n.\nThis method only iterates as many times as there are set bits, making it efficient.",
        "complexity":   "Time Complexity: O(k), where k is the number of set bits, worst-case O(log n) (or O(1) considering 32-bit integers).\nSpace Complexity: O(1).",
        "solution":     "To solve the problem, we use the bit manipulation trick n & (n - 1) which removes the lowest set bit from n. The algorithm is as follows:\nInitialize a counter to 0.\nWhile n is not zero, update n by n = n & (n - 1), and increment the counter.\nThe counter will hold the number of set bits by the end of the loop.\nThis approach leverages efficient bit-level operations and works well even when the function is called frequently.",
    },
    "missing-number": {
        "key_insights": "The range of numbers is continuous from 0 to n, but one number is missing.\nThe sum of numbers from 0 to n can be computed using the arithmetic sum formula.\nSubtracting the sum of the given array from the expected sum gives the missing number.\nThere is a follow-up challenge to solve the problem in O(n) time and O(1) extra space.",
        "complexity":   "Time Complexity: O(n)\nSpace Complexity: O(1)",
        "solution":     "We compute the expected sum of all numbers from 0 to n using the formula sum = n*(n+1)/2. Then, we compute the actual sum of the numbers in the array. The missing number is obtained by subtracting the actual sum from the expected sum.\nThis approach uses arithmetic operations (O(1) extra space) and loops over the array once (O(n) time).",
    },
    "counting-bits": {
        "key_insights": "We can use dynamic programming to build the solution by utilizing the count of bits for smaller numbers.\nThe relationship dp[i] = dp[i >> 1] + (i & 1) holds, where i >> 1 gives the count for i divided by 2 (dropping the least significant bit) and (i & 1) checks if the least significant bit is 1.\nThis approach computes the result for each number in constant time, resulting in a linear time solution.",
        "complexity":   "Time Complexity: O(n)\nSpace Complexity: O(n)",
        "solution":     "We use a dynamic programming approach where an array (or list) dp is initialized with size n+1. The key idea is to iterate from 1 to n and for each number, use the formula dp[i] = dp[i >> 1] + (i & 1).\nThe expression (i >> 1) effectively divides i by 2, giving the count of 1's for that half number.\nThe (i & 1) operation checks if i is odd, adding 1 if true.\nThis method ensures each dp[i] is computed in constant time, leading to an overall O(n) time complexity.",
    },
    "subsets": {
        "key_insights": "The problem is equivalent to generating the power set of the given set.\nA recursive backtracking approach can be used to explore both including and excluding each element.\nIterative (bit manipulation) solutions are also possible since there are 2^n subsets in total.\nThe maximum array length is small (up to 10), making exponential time solutions feasible.",
        "complexity":   "Time Complexity: O(2^n * n) - There are 2^n subsets and creating each subset may take up to O(n) time.\nSpace Complexity: O(2^n * n) - Space for storing all subsets. The recursion stack has a maximum depth of O(n).",
        "solution":     "We solve the problem using backtracking, where we decide for each element whether to include it in the current subset or not. We recursively build all subsets starting at an empty list and at each step appending the current subset to our result list. We can also use iterative bit manipulation but here we focus on the backtracking approach. This algorithm leverages recursion and a list to maintain the current state, leading to a clear and concise implementation.",
    },
    "subsets-ii": {
        "key_insights": "Sort the array first so that duplicate elements are adjacent.\nUse backtracking to generate all possible subsets.\nSkip duplicate elements in the recursion to avoid forming duplicate subsets.\nThe use of sorted order and conditionally skipping elements is the trick to handle duplicates.",
        "complexity":   "Time Complexity: O(2^n * n) since each subset creation may take O(n) in the worst case.\nSpace Complexity: O(2^n * n) for storing the subsets, plus O(n) for recursion stack.",
        "solution":     "The solution uses a backtracking technique that builds subsets incrementally. The input array is first sorted to bring duplicates together. During recursion, if the current element is the same as the previous one and it has not been used in the current recursion path, it is skipped to prevent duplicate subsets. This ensures that each subset is unique. The algorithm stores each valid subset by copying the current subset state into the result list.",
    },
    "find-the-duplicate-number": {
        "key_insights": "Since there are n+1 elements with values only from 1 to n, by the pigeonhole principle at least one number must be repeated.\nThe problem can be approached by mapping the array values to indices, forming a cycle structure.\nFloyd’s Tortoise and Hare algorithm (cycle detection) can be applied to find the duplicate number in linear time and constant space.\nThere is no need for extra data structures like hash sets or alterations to the original array, satisfying the space and array-modification constraints.",
        "complexity":   "Time Complexity: O(n)\nSpace Complexity: O(1)",
        "solution":     "The solution leverages Floyd's Tortoise and Hare cycle detection algorithm. Think of each array element as a pointer to another index, forming a linked list with a cycle (the duplicate number creates the cycle). The algorithm works in two phases:\nDetect a meeting point within the cycle by having two pointers (tortoise and hare) move at different speeds.\nFind the cycle entrance by resetting one pointer to the start and moving both pointers at the same speed. The meeting point reveals the duplicate number.\nThis approach provides an elegant solution that satisfies both linear runtime and constant space requirements.",
    },
    "find-root-of-n-ary-tree": {
        "key_insights": "Every node except the root appears as a child of another node.\nBy summing all node values and then subtracting the sum of all children node values, the remaining value corresponds to the root.\nThis subtraction trick allows us to identify the root in a single pass of the data structure without extra memory for tracking child nodes.",
        "complexity":   "Time Complexity: O(n) where n is the number of nodes, since we iterate through all nodes and their children.\nSpace Complexity: O(1) extra space using arithmetic accumulators.",
        "solution":     "The solution calculates two sums: one for all nodes' values and one for all children nodes' values. The difference between these gives the value of the root node since every node's value (except the root's) is added once as a child. Finally, we iterate over the nodes to find the node with the computed root value and return it. This approach meets the requirement of constant space complexity and linear time.",
    },
    "palindrome-number": {
        "key_insights": "Negative numbers are not palindromic.\nA number ending in 0 (except 0 itself) cannot be a palindrome.\nInstead of reversing the entire number (risking overflow), reverse only half of the number and compare with the other half.\nFor numbers with an odd number of digits, the middle digit can be disregarded.",
        "complexity":   "Time Complexity: O(log10(n)) - Each iteration reduces the number by a factor of 10.\nSpace Complexity: O(1) - Only constant extra space is used.",
        "solution":     "The solution checks for obvious non-palindrome cases first (negative numbers and numbers ending with 0 that are not 0). Then, it reverses half of the digits of the number while the original number is larger than the reversed half. Finally, it compares the remaining part of the original number with the reversed half. In the case of an odd number of digits, the middle digit is removed by dividing the reversed half by 10 before the final comparison.",
    },
    "roman-to-integer": {
        "key_insights": "Map each Roman numeral character to its integer value using a hash table or dictionary.\nLoop through the string and check if the current numeral is less than the next numeral; if so, subtract its value.\nOtherwise, add its value to the total.\nThis approach takes advantage of the subtractive notation used in Roman numerals.",
        "complexity":   "Time Complexity: O(n), where n is the length of the input string.\nSpace Complexity: O(1), since the mapping and additional variables use constant space.",
        "solution":     "We use a dictionary (or hash map) to store the values of each Roman numeral. As we iterate over the string, we compare the current numeral's value with the next numeral's value. When a numeral of smaller value appears before a numeral of larger value, we subtract the current numeral's value; otherwise, we add it. This technique efficiently handles both the standard and subtractive cases in one pass through the string.",
    },
    "base-7": {
        "key_insights": "Handle the edge case where num is 0.\nUse division and modulo operations to extract base 7 digits.\nManage negative numbers by converting to positive and reattaching the minus sign.\nBuild the base 7 digits in reverse order during iteration.",
        "complexity":   "Time Complexity: O(log_7(n)) where n is the absolute value of the number.\nSpace Complexity: O(log_7(n)) to store the digits of the number.",
        "solution":     "The algorithm follows these steps:\nCheck if num equals 0 and return \"0\".\nDetermine if the number is negative. If so, work with its absolute value but remember the negative flag.\nIteratively compute the remainder when divided by 7 (using modulo) and update the number by integer division by 7.\nAppend each remainder (converted to character) to a list representing the digits.\nThe digits are computed in reverse order; reverse the list for the correct order.\nIf the original number was negative, append the \"-\" sign to the front.\nJoin the digits to form the resulting base 7 string.\nThis approach uses basic arithmetic (division and modulo) and a list (or equivalent structure) to accumulate the computed digits.",
    },
    "confusing-number": {
        "key_insights": "Use a mapping dictionary for valid rotations: {0:0, 1:1, 6:9, 8:8, 9:6}.\nProcess the digits of n from right to left to form the rotated number.\nIf any digit is not present in the mapping, the number is instantly not confusing.\nCompare the rotated number with the original number to check if they are different.",
        "complexity":   "Time Complexity: O(d), where d is the number of digits in n (approximately O(log n)).\nSpace Complexity: O(d), needed to store the rotated number string.",
        "solution":     "The approach is to convert the number to a string and iterate through it in reverse. For each digit, check if it is valid using the predefined mapping. If it is, append its rotated counterpart to a new string. After processing, convert the rotated string to an integer (ignoring any leading zeros). Finally, compare the rotated number to the original number. If they are different, the number is confusing.",
    },
    "missing-number-in-arithmetic-progression": {
        "key_insights": "The complete arithmetic progression has equal differences between consecutive numbers.\nThe missing element can be found by calculating the expected common difference using the first and last elements.\nSince exactly one number is missing, iterate through the provided array to detect where the difference deviates from the expected common difference.",
        "complexity":   "Time Complexity: O(n)\nSpace Complexity: O(1)",
        "solution":     "We first compute the expected common difference (diff) for the original arithmetic progression. Since the actual progression had one more element than the current array, the formula used is:\ndiff = (last element - first element) / (n)\nwhere n is the length of the current array.\nNext, iterate over the array and check the difference between each pair of consecutive elements. When the actual difference deviates from diff, it indicates that the missing number is the previous element plus diff. This approach uses a simple loop and constant extra space.",
    },
    "perform-string-shifts": {
        "key_insights": "Consolidate all shifts by calculating a net shift: subtract for left shifts and add for right shifts.\nUse modulo arithmetic with the string length to avoid unnecessary full rotations.\nSlice the string based on the effective shift to construct the final string.",
        "complexity":   "Time Complexity: O(n + m), where n is the length of the string and m is the number of shift operations.\nSpace Complexity: O(n) for the output string.",
        "solution":     "The solution involves computing the net shift value by iterating through the shift operations. Left shifts subtract from the net value, and right shifts add to the net value. After obtaining the cumulative shift, use modulo with the string length to normalize the shift, ensuring it lies within bounds. The final string is constructed by slicing the string into two parts and concatenating them appropriately. This approach leverages string slicing as the primary operation for shifting.",
    },
    "reverse-integer": {
        "key_insights": "The problem requires reversing the digits while preserving the sign.\nWatch out for integer overflow due to the reversed number exceeding the 32-bit signed integer limits.\nThe approach involves repeatedly extracting the last digit from the number and appending it to a new reversed integer.\nOverflow checks are done during each iteration before actually appending the digit.",
        "complexity":   "Time Complexity: O(n), where n is the number of digits in the integer.\nSpace Complexity: O(1), only constant extra space is used.",
        "solution":     "The solution uses a while loop to extract the last digit from the original number using modulo operation (x % 10) and then removes this digit using integer division (x //= 10). A reversed number is constructed by multiplying the current reversed number by 10 and adding the extracted digit. Before appending each digit, the algorithm checks if the new reversed number will overflow a 32-bit signed integer by comparing it with the thresholds derived from INT_MAX and INT_MIN. The algorithm handles negative values by working with x directly since the modulo and division operations work properly with negatives in most languages.",
    },
    "powx-n": {
        "key_insights": "Use exponentiation by squaring to reduce the number of multiplications.\nFor a negative exponent, compute the power for |n| and then take the reciprocal.\nHandle the edge case when n is 0 (return 1).\nThe algorithm works in O(log n) time complexity.",
        "complexity":   "Time Complexity: O(log |n|)\nSpace Complexity: O(1) for the iterative solution, or O(log |n|) if implemented recursively",
        "solution":     "We use exponentiation by squaring, an algorithm that reduces the number of multiplications by repeatedly squaring the base and halving the exponent. Here's how it works:\nIf n is negative, switch x to 1/x and n to -n.\nInitialize the result as 1.\nLoop while n is greater than 0:\nIf the current n is odd, multiply the result by x.\nSquare the base x.\nDivide n by 2 (using integer division).\nThis iterative approach efficiently computes the power in logarithmic steps. The same approach can be implemented recursively.",
    },
    "insert-delete-getrandom-o1": {
        "key_insights": "Use a dynamic array (or list) to store the elements to allow O(1) access by index.\nUse a hash table (or dictionary/map) to store the mapping from value to its index in the array.\nFor removal, swap the element to be removed with the last element in the array, then delete the last element. This avoids expensive shifting of elements.\ngetRandom can be implemented by generating a random index into the array.",
        "complexity":   "Time Complexity: O(1) on average for insert, remove, and getRandom operations.\nSpace Complexity: O(n) where n is the number of elements stored in the data structure.",
        "solution":     "The solution involves maintaining a list to store the values and a hash map to quickly check for the presence of a value and to know its index in the list. When an element is removed, swap it with the last element of the list and update the map accordingly; then, remove the last element in O(1) time. This approach ensures that insert, remove, and getRandom can all be achieved in constant time on average.",
    },
    "convert-to-base-2": {
        "key_insights": "Use division and remainder operations to simulate converting a number to a different base.\nWhen working with a negative base (-2), the remainder can become negative; adjust the remainder (by adding the base's absolute value) and update the quotient accordingly.\nFor n = 0, simply return \"0\" since there is nothing to convert.\nBuild the answer by collecting digits (remainders) and then reversing the order since the conversion provides digits from least significant to most significant.",
        "complexity":   "Time Complexity: O(log|n|) – the loop runs for about the number of digits in the base -2 representation.\nSpace Complexity: O(log|n|) – extra space for storing the computed digits.",
        "solution":     "To solve the problem, we simulate the conversion process similar to how you would convert an integer to a positive base. However, since the base here is -2, special handling of negative remainders is needed. In each iteration, we:\nDivide the current number by -2.\nCalculate the remainder. If the remainder is negative, adjust it by adding 2, and increment the quotient to compensate.\nAppend the computed remainder to a list (which represents the binary digit).\nContinue until the number reduces to zero.\nFinally, reverse the list to form the correct string representation from the most significant to the least significant digit.",
    },
    "find-the-number-of-distinct-colors-among-the-balls": {
        "key_insights": "Use a dictionary (or hash map) to track the current color for each ball.\nUse another dictionary (or hash map) to count the frequency of each color that is actively used.\nWhen updating the color of a ball, decrement the frequency of the old color, and if its count reaches zero, remove it.\nIncrement the frequency of the new color and record the current number of distinct colors.",
        "complexity":   "Time Complexity: O(n) on average, where n is the number of queries, since each query performs O(1) operations.\nSpace Complexity: O(n) in the worst case for storing the ball-to-color mapping and the color frequency counts.",
        "solution":     "The solution primarily uses two hash maps (dictionaries): one to store the current color of each ball and another to store the frequency of each color that has been applied. For every query:\nCheck if the ball already has a color. If yes, decrement the count for that color and remove it if the count becomes zero.\nUpdate the ball’s color with the new color and increment the frequency for the new color.\nAfter processing the query, the number of keys in the frequency map gives the number of distinct colors currently in use.\nThis approach ensures that each query is processed in O(1) average time.",
    },
    "text-justification": {
        "key_insights": "Use a greedy approach to fill each line with as many words as possible without exceeding maxWidth.\nCalculate total space to distribute by subtracting the combined words length from maxWidth.\nIf the line has more than one word, distribute extra spaces evenly; if not, pad the line with spaces at the end.\nHandle the last line separately by left-justifying the text (i.e., adding a single space between words and padding the end).",
        "complexity":   "Time Complexity: O(n) where n is the number of words, since each word is processed once.\nSpace Complexity: O(n) for storing the result and temporary buffers.",
        "solution":     "The solution iterates over the list of words and groups them into lines such that the combined length of the words and minimum required spaces does not exceed maxWidth. For each completed group (line):\nIf it’s not the last line, calculate extra spaces required.\nIf the line has multiple words, distribute the spaces evenly, adding any extra spaces from left to right.\nIf the line contains a single word, append spaces to the right.\nFor the last line, join words with a single space and pad the remaining width with spaces.\nData structures used include lists (or arrays) for current words accumulation and the final result. The logic is implemented using a simulation approach to mimic text formatting.",
    },
    "longest-common-prefix": {
        "key_insights": "Compare characters of the strings one column at a time (vertical scanning).\nThe longest possible prefix is limited by the length of the shortest string in the array.\nAs soon as a mismatch is found, the prefix up to that point is the answer.\nHandle edge cases such as an empty input array or strings with no common prefix.",
        "complexity":   "Time Complexity: O(N*M) where N is the number of strings and M is the length of the shortest string.\nSpace Complexity: O(1) additional space (ignoring output space).",
        "solution":     "We use the vertical scanning approach:\nIf the input array is empty, return an empty string.\nIterate character by character over the first string.\nFor each character, compare it with the corresponding character in each of the other strings.\nIf a mismatch or end of any string is encountered, return the substring from the start up to the current index.\nIf no mismatch is found, the entire first string is the common prefix.\nThis solution ensures that we only traverse the necessary part of the strings and stops early if a discrepancy is discovered.",
    },
    "word-break": {
        "key_insights": "Use Dynamic Programming (DP) to break the problem into subproblems.\nCreate a DP array where dp[i] is true if the substring s[0:i] can be segmented using words from the dictionary.\nLeverage a hash set for fast look-up of dictionary words.\nFor every index i, check all possible previous break points j such that dp[j] is true and s[j:i] is in the wordDict.\nThe problem can also be approached with recursion and memoization, but DP is more straightforward for iterative implementation.",
        "complexity":   "Time Complexity: O(n^2) in the worst-case scenario, where n is the length of s (each substring check is O(1) if using a hash set, and there could be O(n^2) checks overall).\nSpace Complexity: O(n) for the dp array and O(k) for the hash set where k is the number of words in the dictionary.",
        "solution":     "The solution uses a Dynamic Programming (DP) approach:\nConvert wordDict to a hash set for constant time lookup.\nCreate a boolean dp array of size n+1 (n is the length of s), where dp[i] indicates if s[0:i] can be segmented.\nSet dp[0] to true because an empty string is always segmentable.\nFor each index i from 1 to n, iterate through all indices j from 0 to i:\nIf dp[j] is true and the substring s[j:i] is in the dictionary, then set dp[i] to true.\nBreak early from the inner loop when a valid break is found.\nReturn dp[n] as the result.\nThis method efficiently builds up the solution by utilizing previous results to determine if the entire string can be segmented.",
    },
    "implement-trie-prefix-tree": {
        "key_insights": "Use a tree-like structure where each node represents a character.\nEach node stores its children in a dictionary (or hash map) for O(1) average time access.\nMark the end of a word with a boolean flag.\nInsertion is done character by character, creating new nodes as needed.\nSearching and checking prefixes follow traversals down the tree.",
        "complexity":   "Time Complexity:\nInsert: O(m) where m is the length of the word.\nSearch: O(m)\nstartsWith: O(m)\nSpace Complexity:\nO(N * M) in the worst-case scenario, where N is the number of words and M is the average length of the words, due to storing each character in the Trie.",
        "solution":     "We implement the Trie using a node-based approach. Each TrieNode contains a hash map (or dictionary) of its children and a boolean indicating if the node marks the end of a word. The Trie class uses these nodes to build out the words character by character. The insert method traverses and builds the necessary nodes; the search method verifies that all characters exist in order and that the final node marks the end of a word; the startsWith method checks if the prefix exists in the Trie. No tricky edge cases exist beyond handling null or empty inputs, as constraints guarantee valid lowercase strings.",
    },
    "design-add-and-search-words-data-structure": {
        "key_insights": "Utilize a Trie (prefix tree) to efficiently store words.\nEach node in the Trie contains a dictionary for its children and a flag indicating the end of a word.\nThe search operation is implemented with a depth-first search (DFS) approach.\nWhen the search query contains a dot, explore all possible child nodes.",
        "complexity":   "Time Complexity:\naddWord: O(w), where w is the length of the word.\nsearch: Worst-case O(26^w) for words with multiple wildcards, but practical performance is better given constraints.\nSpace Complexity: O(n * w), where n is the number of words and w is the average word length.",
        "solution":     "The WordDictionary is implemented using a Trie. For adding, we insert each character of the word into the Trie. For searching, we use DFS. When encountering a wildcard '.', we recursively search all children nodes. This structure combined with DFS efficiently addresses the wildcard matching and ensures good performance for multiple operations.",
    },
    "add-bold-tag-in-string": {
        "key_insights": "We need to identify every occurrence of each word in s.\nOverlapping or consecutive intervals should be merged to form one continuous bold region.\nUse an auxiliary data structure (e.g., a boolean array) to mark positions that should be bolded.\nOnce the positions are marked, build the final string by inserting bold tags at the appropriate boundaries.",
        "complexity":   "Time Complexity: O(n * m * k) where n is the length of s, m is the number of words, and k is the average length of the words (due to substring matching).\nSpace Complexity: O(n) for the auxiliary boolean array used to mark bold positions.",
        "solution":     "The solution involves two main steps:\nIdentify and mark the indices in the string s that are part of any matching word. We do this by iterating over each word and marking every occurrence’s start and end in a boolean array.\nConstruct the final string by traversing the boolean array. When encountering a segment of consecutive true values, wrap that segment with  and  tags.\nThe key idea is to merge intervals by marking every index that should be bold and then using a single pass to insert tags only when transitioning from non-bold to bold and vice versa.",
    },
    "top-k-frequent-words": {
        "key_insights": "Count the frequency of each unique word using a hash table or dictionary.\nUse a min-heap (priority queue) to keep track of the top k frequent words. In the heap, the ordering is based on frequency; if frequencies are equal, order by lexicographical order (inverted condition for min-heap).\nAlternatively, you can sort the unique words based on frequency and lexicographical order and then pick the top k.\nFor an efficient solution, use a min-heap that maintains size k achieving O(n log k) time complexity.",
        "complexity":   "Time Complexity: O(n log k) when using a heap, where n is the number of unique words.\nSpace Complexity: O(n) for storing the frequency counts and the heap.",
        "solution":     "We first calculate the frequency of each word using a hash table (or dictionary). Then, we use a min-heap that stores pairs of (frequency, word). The comparator for the heap is defined such that the root of the heap is the word with the smallest frequency; if two words have the same frequency, the word with the lexicographically larger value is considered “smaller” in our ordering so that it gets popped when the heap size exceeds k. After processing all words, the heap contains the top k frequent words. Finally, the heap elements are extracted and reversed (if needed) to produce the final list sorted from highest to lowest frequency with lexicographic tie-breaking.",
    },
    "longest-word-in-dictionary": {
        "key_insights": "The word must be built character by character; hence every prefix (removing one character from the end at a time) must exist in the dictionary.\nSorting the input list can help in ensuring that smaller words (prefixes) are processed first.\nUsing a hash set to store valid words enables rapid prefix checks.\nLexicographical order plays a role when words have the same length.",
        "complexity":   "Time Complexity: O(n * L + n log n) where n is the number of words and L is the maximum length of a word (n * L for prefix checking, and n log n for sorting).\nSpace Complexity: O(n * L) for storing words in the set.",
        "solution":     "The approach begins by sorting the list of words. Sorting is done primarily by word length and secondarily by lexicographical order so that when we iterate through the words, all possible prefixes for a word have already been considered. Initialize a set to keep track of valid words that can be built one character at a time. For each word in the sorted list, check if all its prefixes (word without the last character progressively) exist in the set. If a word qualifies, add it to the set and check if it should be the new result based on length and lexicographical order. The set lookup ensures that checking each prefix is efficient.",
    },
    "design-file-system": {
        "key_insights": "Use a data structure (like a hash map) to store paths and associated values.\nTo create a new path, check that the path does not already exist.\nEnsure the immediate parent path exists before creation.\nWhen retrieving a value, simply look it up in the storage structure.\nSplitting the path by \"/\" allows easy identification of the parent.",
        "complexity":   "Time Complexity:\ncreatePath: O(n) per call where n is the number of segments in the path (to process and validate parent path).\nget: O(1) per call if using a hash map.\nSpace Complexity: O(m * L) where m is the number of paths stored and L is the average length of a path.",
        "solution":     "The solution uses a hash map (dictionary) to store file paths along with their associated values. For the createPath method, the algorithm:\nChecks if the path already exists.\nExtracts the parent path by removing the last segment.\nValidates the existence of the parent path.\nInserts the new path with its corresponding value if validation passes.\nFor the get method, the algorithm simply returns the stored value if the path exists; otherwise, it returns -1. This approach ensures efficient lookup and validation using string manipulation and hash map operations.",
    },
    "word-search-ii": {
        "key_insights": "Use Depth-First Search (DFS) to explore possible words starting from each board cell.\nBuild a Trie (prefix tree) to store the words for O(1) prefix checks and efficient pruning.\nMark visited cells and restore them after DFS to avoid revisiting in the current search path.\nPrune search paths early if the current prefix is not part of any word in the Trie.",
        "complexity":   "Time Complexity: O(m * n * 4^L) in the worst case, where L is the maximum word length; however, using a Trie prunes unnecessary paths.\nSpace Complexity: O(K) for the Trie storage, where K is the total number of letters in all words, plus O(m * n) auxiliary space for recursion and board marking.",
        "solution":     "We first insert all words into a Trie data structure to allow quick lookups of prefixes. Then, for each cell in the board, we perform DFS. During DFS, if the current path matches a Trie node that represents a complete word, we add that word to the result list and mark it as found to avoid duplicates. We continue exploring adjacent cells (up, down, left, right) while marking the cell as visited. After exploring, we restore the original letter for further DFS calls. This combination of Trie and DFS with backtracking efficiently finds valid words on the board.",
    },
    "palindrome-pairs": {
        "key_insights": "A palindrome is a string that reads the same forwards and backwards.\nFor any given word, if you split it into two parts, one part (either prefix or suffix) might be a palindrome.\nIf the other part’s reverse exists elsewhere in the array, then that reverse can be concatenated to yield a palindrome.\nCarefully handle edge cases like empty strings, since an empty string is a palindrome by default.",
        "complexity":   "Time Complexity: O(n * k^2) in the worst case where n is the number of words and k is the maximum word length, but with the palindrome checking optimized this can be considered O(sum of word lengths): Each word is processed once and each split is checked in O(k) time.\nSpace Complexity: O(n * k) due to the hashmap storing all words and their indices.",
        "solution":     "We use a hashmap (dictionary) to store each word and its corresponding index. For every word, we iterate through every possible split position to divide the word into a prefix and a suffix. For each split:\nIf the prefix is a palindrome, we look for the reverse of the suffix in the hashmap. If found (and not the same word), then combining the reverse with the current word forms a palindrome.\nSimilarly, if the suffix is a palindrome, we look for the reverse of the prefix.\nWe must be cautious with edge cases, especially when either part is empty, as the empty string is considered a palindrome. This method avoids checking every possible pair explicitly and leverages string reversal and palindrome checks to achieve efficiency.",
    },
    "design-in-memory-file-system": {
        "key_insights": "Use a tree (trie) to represent directories and files.\nEach node can be either a directory or a file; directories maintain a mapping of names to child nodes.\nFor ls, if the given path is a file, return only its name. If it's a directory, return the sorted list of names of its children.\nmkdir must create all intermediate directories if they do not already exist.\nFile nodes store content and support content appending.",
        "complexity":   "Time Complexity:\nls: O(k log k) where k is the number of entries in the directory (due to sorting), or O(n) if traversing a long path.\nmkdir, addContentToFile, readContentFromFile: O(d) where d is the depth of the file path.\nSpace Complexity:\nO(T) where T is the total number of characters stored in directory names and file contents.",
        "solution":     "The solution uses a tree-based approach where each node represents either a directory or a file. Each directory node stores its children in a hash table (or dictionary) mapping names to node objects. File nodes store their content as a string and carry a flag indicating they are files.\nFor the ls operation, check if the node is a file—if so, return its name; otherwise, list and sort the names of all children in that directory. The mkdir operation traverses (or creates) nodes for each segment of the provided path. The addContentToFile method accesses or creates the file node and appends the content. Finally, readContentFromFile retrieves the stored content from the file node.",
    },
    "design-search-autocomplete-system": {
        "key_insights": "Use a Trie to efficiently store and retrieve sentences based on their prefixes.\nMaintain a mapping at each Trie node to record sentences (or their frequencies) that pass through that node.\nUse sorting or a min-heap to select the top three sentences based on their frequency (hot degree) and lexicographical order if frequencies tie.\nOn receiving '#', update the Trie with the current sentence and reset the current search state.",
        "complexity":   "Time Complexity: O(L * log k) per character input query, where L is the length of the current prefix and k is 3 (constant factor), thus ensuring efficient autocompletion.\nSpace Complexity: O(N * L), where N is the number of unique sentences and L is the average sentence length, for maintaining the Trie.",
        "solution":     "The solution revolves around building a Trie where each node contains:\nA mapping to its child nodes.\nA hash map that records all sentences passing through the node along with their occurrence counts.\nFor each character entered (except '#'), the system:\nTraverses the Trie to find the current prefix node.\nRetrieves stored sentences at that node and sorts them based on the following criteria: descending by frequency and ascending lexicographically if frequencies are equal.\nReturns the top three sentences from the sorted result.\nWhen the user inputs '#', the current sentence (tracked as user input so far) is added or its frequency updated in the Trie, and the input state resets.",
    },
    "two-sum": {
        "key_insights": "Use a hash table to store the elements and their indices for fast lookup.\nFor each element, calculate its complement: target minus the current element.\nCheck if the complement exists in the hash table.\nIf found, return the indices of the current element and its complement.\nThis approach only requires a single pass through the array.",
        "complexity":   "Time Complexity: O(n) - We traverse the list only once.\nSpace Complexity: O(n) - In the worst-case, we store all elements in the hash table.",
        "solution":     "We use a hash table (dictionary) to map each array element to its index as we iterate. For each number, we compute the complement (target - number) and check if it already exists in our hash table. If the complement is found, we return the indices. This one-pass solution ensures linear time complexity and avoids the need for nested loops.",
    },
    "missing-ranges": {
        "key_insights": "Use a pointer (or iterate index) to traverse the nums array while keeping track of the previous number considered.\nCheck for missing numbers before the first element, between consecutive elements, and after the last element.\nWhen a gap is found, convert it to a range string that is either a single number (if the gap is exactly one number) or an interval.\nHandle edge cases when the nums array is empty.",
        "complexity":   "Time Complexity: O(n), where n is the length of the nums array, since we iterate through the array once.\nSpace Complexity: O(1) additional space (ignoring the output list), as we only use a few extra variables.",
        "solution":     "The solution leverages a single pass through the input array and calculates the missing ranges by comparing the expected number with the current number in nums. First, initialize a variable \"prev\" to one less than the lower bound so that missing numbers from the very start of the range can be handled uniformly. For each number in the array, if there is a gap between prev and the current number (i.e., current number is at least 2 more than prev), then add the missing range (prev+1 to current number-1) to the output list. Update \"prev\" to the current number and finally handle any gap between the last number and the upper bound. The primary data structures used are the list for the output; the approach is iterative.",
    },
    "moving-average-from-data-stream": {
        "key_insights": "Use a queue (or circular buffer) to keep track of the current window elements.\nMaintain a running sum to quickly update the window's total when a new element comes in.\nWhen the window is full, subtract the value of the element that falls out as a new one is added.\nEach call to next() is executed in constant time, O(1).",
        "complexity":   "Time Complexity: O(1) per call to next()\nSpace Complexity: O(window size)",
        "solution":     "The solution leverages a queue data structure to store the elements of the sliding window. A running sum variable is maintained for efficient calculation of the moving average. When a new integer is added:\nIf the size of the queue is equal to the maximum window size, remove the oldest element and subtract its value from the running sum.\nAdd the new element to both the queue and the running sum.\nCompute and return the moving average as the running sum divided by the number of elements in the queue.\nThis approach ensures that each update is done in constant time without having to sum all elements in the window repeatedly.",
    },
    "find-anagram-mappings": {
        "key_insights": "Since nums2 is an anagram of nums1, every element in nums1 has a corresponding position in nums2.\nA dictionary (or hash table) can be used to map each number in nums2 to its indices.\nWhen iterating through nums1, we can assign the next available index from the dictionary for that number.",
        "complexity":   "Time Complexity: O(n) where n is the number of elements in the arrays (single pass to build the dictionary and another pass to form the mapping).\nSpace Complexity: O(n) for storing the dictionary that holds indices of elements in nums2.",
        "solution":     "The approach consists of two primary steps:\nBuild a dictionary to record all indices where each number appears in nums2. This dictionary maps a number to a list of indices.\nIterate over nums1 and for each element, remove (or pop) one index from the corresponding list in the dictionary, and add that index to the final mapping array.\nThis guarantees that each number from nums1 is correctly mapped to one of its positions in nums2 while efficiently handling duplicates.",
    },
    "counting-elements": {
        "key_insights": "Use a hash table or set to store unique elements from arr for constant time lookups.\nIterate through each element in arr and check if x + 1 exists in the set.\nCount each valid occurrence, including duplicates.\nThis approach efficiently handles the problem without needing nested loops.",
        "complexity":   "Time Complexity: O(n), where n is the number of elements in arr (set lookups are O(1) on average).\nSpace Complexity: O(n), due to the use of an auxiliary set to store unique elements.",
        "solution":     "The solution leverages a set to achieve constant time membership checks. First, we convert arr to a set of unique numbers. Then, iterate through arr, and for each element, check if (element + 1) exists in the set. If it does, increment the count. This approach correctly handles duplicates by counting each occurrence during iteration.",
    },
    "count-good-triplets": {
        "key_insights": "Brute force approach using three nested loops is acceptable since the maximum array length is 100.\nCheck all possible triplets and count only those meeting the specified conditions.\nAbsolute difference comparisons are the key condition validations.",
        "complexity":   "Time Complexity: O(n^3) where n is the length of the array.\nSpace Complexity: O(1) extra space (ignoring input space).",
        "solution":     "We use a brute force algorithm with three nested loops, iterating over all combinations of triplets (i, j, k) ensuring i < j < k. For each triplet, we calculate the absolute differences:\n|arr[i] - arr[j]|\n|arr[j] - arr[k]|\n|arr[i] - arr[k]|\nThe triplet is counted as good if all absolute differences satisfy the conditions (<= a, <= b, and <= c respectively). This simple approach leverages the small input size, eliminating the need for more complex data structures or optimizations.",
    },
    "insert-interval": {
        "key_insights": "The intervals array is sorted by start times.\nThree main steps are used:\nAdd intervals that come completely before the new interval.\nMerge overlapping intervals with the new interval.\nAppend intervals that start after the new (merged) interval.\nIterating through the list once allows an O(n) solution.",
        "complexity":   "Time Complexity: O(n) since we scan the list once.\nSpace Complexity: O(n) to store the resulting list of intervals.",
        "solution":     "Traverse the intervals list and perform the following:\nAppend all intervals that end before the new interval starts.\nFor intervals that overlap with newInterval, update newInterval to merge them (using min for start and max for end).\nAppend the new merged interval.\nAppend all remaining intervals.\nThis ensures that all intervals remain non-overlapping and sorted with a single pass.",
    },
    "product-of-array-except-self": {
        "key_insights": "The product for each index can be computed by multiplying the product of all numbers to the left and the product of all numbers to the right of that index.\nDivision is not allowed, so we must compute the cumulative products explicitly.\nCreate two passes: one forward pass for prefix products and one backward pass for suffix products.\nUse the output array to store the cumulative product values, achieving O(1) extra space by not using additional arrays.",
        "complexity":   "Time Complexity: O(n) because we iterate through the array twice.\nSpace Complexity: O(1) extra space (excluding the output array).",
        "solution":     "The approach uses two passes over the input array:\nInitialize the answer array and fill it with prefix products. For each index i, answer[i] contains the product of all elements before i.\nTraverse the array from the end using a running suffix product. Update the answer array by multiplying the current suffix product with the prefix product already stored.\nThis method leverages an output array to store intermediate results and uses a temporary variable for the suffix product, ensuring constant extra space.",
    },
    "zigzag-iterator": {
        "key_insights": "Use a FIFO (first-in-first-out) structure to maintain the order of vectors which still have remaining elements.\nFor each call to next(), select the next element from the current vector and then move that vector to the end of the queue if it still contains elements.\nThe approach ensures proper zigzag (or cyclic) ordering among the vectors.\nWhen extending to k vectors, the same approach works: initialize the queue with each non-empty vector.",
        "complexity":   "Time Complexity: O(1) per next() call on average, with an overall O(n) for n total elements.\nSpace Complexity: O(k) for the queue structure (where k is the number of vectors), plus the storage for the input vectors.",
        "solution":     "We can solve the Zigzag Iterator problem by using a queue (or deque) to hold pairs of the vector’s current index and the vector itself. For each call to next(), we remove the front element from the queue, return the current item from its associated vector, and if the vector has more elements, reinsert the pair (with an updated index) into the back of the queue. This cyclic process naturally interleaves the elements from the provided vectors.",
    },
    "range-sum-query-mutable": {
        "key_insights": "To efficiently support both point updates and range sum queries, a specialized data structure like a Binary Indexed Tree (Fenwick Tree) or a Segment Tree is ideal.\nBoth data structures provide update and sum query operations in O(log n) time.\nThe challenge is to maintain a mutable array while enabling fast sum computation over any specified range.",
        "complexity":   "Time Complexity: O(log n) for both update and sumRange operations\nSpace Complexity: O(n) to store the additional tree structure",
        "solution":     "We can use a Binary Indexed Tree (Fenwick Tree) to solve this problem. The Fenwick Tree allows for efficient prefix sum calculations and point updates, both in O(log n) time. The approach involves:\nBuilding the Fenwick Tree using the initial array.\nOn an update, computing the difference between the new value and the current value, then propagating the change appropriately in the tree.\nFor the sum range query, computing the prefix sum up to right and subtracting the prefix sum up to left-1.",
    },
    "4sum-ii": {
        "key_insights": "Use a hash table to store the frequency of all possible sums from nums1 and nums2.\nFor each pair from nums3 and nums4, compute the complement (negative sum) and look it up in the hash table.\nThis reduces the time complexity from O(n^4) to O(n^2).",
        "complexity":   "Time Complexity: O(n^2) where n is the length of each array.\nSpace Complexity: O(n^2) due to the storage of pair sums in the hash table.",
        "solution":     "The solution employs a hash table to precompute and store the frequency of sums of all pairs from the first two arrays. Then, by iterating over all pairs from the third and fourth arrays, we calculate the required complement that would result in a total sum of zero. This complement is checked in the hash table, and the frequency (number of valid pairs) is added to the result. This method efficiently finds the number of valid quadruplets without resorting to a brute-force O(n^4) approach.",
    },
    "contiguous-array": {
        "key_insights": "Convert 0s to -1 so that a balanced subarray (equal number of 0s and 1s) sums to zero.\nUse a prefix sum to track the cumulative sum as you iterate through the array.\nUse a hash table (or dictionary) to store the first occurrence of each prefix sum.\nWhen the same prefix sum is encountered again, it indicates that the subarray between these indices is balanced.",
        "complexity":   "Time Complexity: O(n) - We iterate through the array once.\nSpace Complexity: O(n) - In the worst case, we store an entry for each prefix sum.",
        "solution":     "We use a technique where we convert each 0 in the array to -1. Then, we compute a running prefix sum as we iterate through the array. A prefix sum of 0 at any point indicates that the subarray from the beginning to the current index is balanced. For each prefix sum, if we have seen it before, the subarray between the previous index (where the prefix sum was first seen) and the current index sums to 0. We update the maximum length accordingly. We use a hash table to store the first index where each prefix sum occurs.",
    },
    "subarray-sum-equals-k": {
        "key_insights": "Utilize the prefix sum technique to simplify sum calculation for subarrays.\nUse a hash table (or dictionary/map) to store the frequency of prefix sums encountered.\nFor each element, check if (current prefix sum - k) exists in the hash table; if it does, it indicates a valid subarray.\nInitialize the hash table with {0: 1} to manage cases where a subarray’s sum is exactly k.",
        "complexity":   "Time Complexity: O(n)\nSpace Complexity: O(n)",
        "solution":     "We solve the problem by maintaining a running sum (prefix sum) and using a hash table to record the number of times each sum has occurred. As we iterate through the array, we update the running sum. For each new sum, we check if (current sum - k) exists in our hash table. If it does, the value associated with (current sum - k) gives the number of subarrays ending at the current index that sum to k. This method efficiently computes the result in a single pass through the array.",
    },
    "pairs-of-songs-with-total-durations-divisible-by-60": {
        "key_insights": "Each song duration's effect is determined by its remainder when divided by 60.\nFor any given module value r (where 0 ≤ r < 60), the complementary module needed to complete 60 is (60 - r) % 60.\nSpecial handling is needed when r is 0 (or 30, since 30 + 30 = 60) to avoid double-counting.\nA frequency counter for remainders can be efficiently used to count valid pairs in one pass through the list.",
        "complexity":   "Time Complexity: O(n), where n is the number of songs.\nSpace Complexity: O(60) → O(1), since we only need to keep track of 60 possible remainders.",
        "solution":     "We iterate over the list of song durations and calculate the remainder of each duration modulo 60. A frequency array (or hash table) is used to record how many times each remainder has been seen so far. For each song, we look up the frequency of its complementary remainder (i.e., (60 - current_remainder) % 60) already encountered. This frequency indicates how many valid pairs can be formed with the current song. After counting pairs with the current song, we update the frequency of its remainder for future pairings.",
    },
    "remove-interval": {
        "key_insights": "Iterate over each interval and check if it overlaps with the toBeRemoved interval.\nIf the current interval is completely outside of toBeRemoved, add it to the result as is.\nFor overlapping intervals, compute the non-overlapping portions:\nIf the left side of the interval lies before toBeRemoved, include [start, min(end, toBeRemoved.start)].\nIf the right side of the interval lies after toBeRemoved, include [max(start, toBeRemoved.end), end].",
        "complexity":   "Time Complexity: O(n), where n is the number of intervals.\nSpace Complexity: O(n), for storing the resulting intervals.",
        "solution":     "The approach involves iterating through the list of sorted disjoint intervals and checking for overlap with the toBeRemoved interval. For each interval:\nIf it is completely before or after the removal interval, add it directly to the result.\nIf it overlaps with toBeRemoved, determine the remaining portion(s):\nThe left side of the interval that does not intersect (if any).\nThe right side of the interval that does not intersect (if any).\nUse simple conditional checks and list manipulation to form the final set of intervals.",
    },
    "first-unique-number": {
        "key_insights": "Use a queue to maintain the order of potential unique numbers.\nUse a hash table (or map) to count the occurrences of each number.\nWhen showing the first unique number, remove numbers from the front of the queue if their frequency is more than one.\nAll operations (add and showFirstUnique) can be done in O(1) amortized time.",
        "complexity":   "Time Complexity: O(1) amortized for each add and showFirstUnique call.\nSpace Complexity: O(n), where n is the number of distinct integers encountered.",
        "solution":     "We design the FirstUnique class using two main data structures: a queue and a hash map (or dictionary). The queue preserves the order of insertion for unique candidates, while the hash map holds the frequency count for each integer.\nWhen a new number is added:\nIncrement its count in the hash map.\nIf the count is one, append it to the queue.\nTo retrieve the first unique number:\nWhile the queue is not empty and the count for the element at the front is greater than one, remove it from the queue.\nReturn the front element of the queue if it exists, otherwise return -1.\nThis approach ensures that we are always up-to-date with the current first unique number.",
    },
    "find-occurrences-of-an-element-in-an-array": {
        "key_insights": "Pre-process the nums array to record the positions (indices) where the element x appears.\nFor each query, check if the occurrence number requested is within the bounds of the precomputed list.\nReturn the corresponding index if available; otherwise, return -1.",
        "complexity":   "Time Complexity: O(n + q) where n is the length of nums (to precompute positions) and q is the length of queries (to answer each query in constant time).\nSpace Complexity: O(n) in the worst case where all elements in nums equal x and are stored in the positions array.",
        "solution":     "The solution involves a two-step approach:\nIterate through the nums array once to store the index of each occurrence of x in a list called positions.\nFor each query in queries, check if there is at least as many occurrences as the query specifies by verifying if queries[i] is less than or equal to the length of positions. If yes, return the (queries[i]-1)th element from positions (since it is zero-indexed). Otherwise, return -1.\nThis approach efficiently handles multiple queries by utilizing precomputed data and simple indexing.",
    },
    "first-missing-positive": {
        "key_insights": "The missing positive number must be in the range [1, n+1] where n is the length of the array.\nUse the array indices to place numbers in their correct positions (i.e., number i should be at index i-1) with in-place swapping.\nAfter rearrangement, scan the array: the first index where the number is not equal to index+1 indicates the missing positive integer.",
        "complexity":   "Time Complexity: O(n) as each number is swapped at most once.\nSpace Complexity: O(1) since the rearrangement occurs in-place.",
        "solution":     "The approach involves reordering the array so that each positive integer in the range [1, n] is placed at the index corresponding to its value minus one. This means for any valid number i, nums[i-1] should be i. We iterate through the array and perform in-place swaps to achieve this order. After this process, another pass through the array reveals the first position where the number does not match the expected value (i+1), which is the smallest missing positive. If every position is correct, then the missing value is n+1.",
    },
    "merge-sorted-array": {
        "key_insights": "Both arrays are already sorted in non-decreasing order.\nThe merge should be done in-place into nums1.\nStart merging from the end to avoid overwriting elements in nums1.\nUse two pointers, one for nums1 and one for nums2, and a tail pointer for placement.",
        "complexity":   "Time Complexity: O(m + n)\nSpace Complexity: O(1)",
        "solution":     "We use a two-pointer approach starting from the end of both arrays. Initialize three pointers:\np1 pointing to the last valid element in nums1.\np2 pointing to the last element in nums2.\ntail pointing to the last index of nums1.\nWhile both pointers are valid, compare the elements pointed by p1 and p2, placing the larger element at the tail position. Decrement the corresponding pointer and the tail pointer. Finally, if any elements remain in nums2 (i.e., p2 >= 0), copy them over into nums1. This solution leverages the extra space at the end of nums1 and ensures the merge is done in-place in linear time.",
    },
    "valid-palindrome": {
        "key_insights": "Use two pointers (one starting at the beginning and one at the end) to compare characters.\nSkip characters that are not alphanumeric.\nCompare characters in a case-insensitive manner.\nContinue until the pointers meet or cross.",
        "complexity":   "Time Complexity: O(n), where n is the length of the string, as each character is processed at most once.\nSpace Complexity: O(1), using only a few extra variables for pointers and temporary comparisons.",
        "solution":     "The solution employs a two-pointer approach:\nInitialize two pointers: one at the start (left) and one at the end (right) of the string.\nSkip any non-alphanumeric characters using these pointers.\nConvert the remaining characters at each pointer to lowercase and compare them.\nIf any mismatch is found, return false; otherwise, continue until the pointers cross.\nReturn true if all corresponding characters match.\nThis method efficiently checks for a palindrome without needing additional space for a filtered string.",
    },
    "move-zeroes": {
        "key_insights": "Use a two-pointer technique: one pointer to iterate through the array and another pointer to track the position for the next non-zero element.\nFor each non-zero element encountered, swap it with the element at the tracking pointer.\nThis strategy minimizes operations by ensuring each non-zero element is moved at most once.\nAfter repositioning all non-zero elements, zeros will naturally occupy the remaining positions.\nThe in-place requirement ensures space complexity remains O(1).",
        "complexity":   "Time Complexity: O(n), where n is the number of elements in the array, since only one pass (or at most two simple passes) is required.\nSpace Complexity: O(1) extra space, as the reordering is accomplished in-place.",
        "solution":     "The solution leverages a two-pointer approach. The left pointer (j) keeps track of the index where the next non-zero element should be placed, while the right pointer (i) iterates through the array. Whenever a non-zero element is encountered, it is swapped with the element at index j, and j is then incremented. This effectively gathers all non-zero elements at the beginning while moving zeros to the end. A key optimization is to perform the swap only when necessary (i.e., when i and j are not the same) to reduce the number of write operations.",
    },
    "valid-word-abbreviation": {
        "key_insights": "Use two pointers: one for looping through the word and one for the abbreviation.\nWhen encountering a digit in abbr, accumulate the number and skip that many characters in the word.\nCheck for invalid cases such as numbers with leading zeros or consecutive digit segments that might imply adjacent skipped substrings.\nCompare letters directly when they are not digits.",
        "complexity":   "Time Complexity: O(n + m) where n is the length of the word and m is the length of the abbreviation.\nSpace Complexity: O(1) (constant extra space)",
        "solution":     "The approach uses two pointers to iterate over the word and abbreviation. For each character in the abbreviation:\nIf it is a letter, compare it with the current character in the word.\nIf it is a digit, first ensure that it does not start with '0'. Then, convert the sequence of digits into a number which represents how many characters in the word to skip.\nAfter processing, both pointers should have reached the end of their respective strings for the abbreviation to be valid. This approach is efficient due to its linear pass with constant extra space.",
    },
    "squares-of-a-sorted-array": {
        "key_insights": "Squaring numbers can disrupt the original order since negative numbers become positive.\nA two-pointer approach can efficiently retrieve the largest square from either end of the array.\nBy comparing the absolute values at the two pointers, the largest square is inserted from the back of the result array.\nThis method achieves an O(n) time complexity without needing to sort the squared values afterward.",
        "complexity":   "Time Complexity: O(n) - We traverse the array only once.\nSpace Complexity: O(n) - We use an extra array to store the output.",
        "solution":     "We use a two-pointer technique to traverse the array from both ends. Initialize pointers (left and right) at the beginning and end of the array, respectively, and an index pointer starting at the end of a new results array. At each step, compare the absolute values of the elements at the left and right pointers. The larger absolute value, when squared, is placed in the result array at the current index, and the corresponding pointer is adjusted. This continues until the left pointer exceeds the right pointer. Filling the result array from the back ensures that the array remains sorted in non-decreasing order.",
    },
    "container-with-most-water": {
        "key_insights": "The container's area is determined by the distance between two lines multiplied by the shorter line's height.\nA brute-force approach checking all possible pairs would result in O(n^2) time complexity, which is inefficient for large inputs.\nA two-pointer approach allows us to solve the problem in O(n) time by initializing pointers at both ends and gradually moving them inward.\nBy always moving the pointer corresponding to the shorter line, we are more likely to find a taller line that could potentially form a container with a larger capacity.",
        "complexity":   "Time Complexity: O(n) - The algorithm makes a single pass through the array with two pointers.\nSpace Complexity: O(1) - Only a few extra variables are used, regardless of the input size.",
        "solution":     "The optimal approach uses a two-pointer strategy:\nStart with two pointers at each end of the array.\nCalculate the area formed between the two lines at these pointers.\nUpdate the maximum area if the calculated value is larger.\nMove the pointer pointing to the shorter line inward, as this is the only possible way to potentially increase the height of the shorter line and thus the area.\nContinue until the two pointers meet.\nThis method effectively reduces the number of comparisons and ensures that every possible pair that can yield a larger area is considered.",
    },
    "3sum": {
        "key_insights": "Sort the array to efficiently skip duplicates and facilitate the two pointers approach.\nUse a fixed pointer for the first element, and then use two additional pointers (left and right) to find pairs that sum to the negation of the fixed element.\nSkip duplicate elements to ensure unique triplets.",
        "complexity":   "Time Complexity: O(n²) where n is the number of elements in the array.\nSpace Complexity: O(1) extra space (ignoring the space required for the output).",
        "solution":     "We first sort the array to bring duplicates together and allow the two-pointer technique to work efficiently. Then, iterate over the array and for each element, use two pointers (one at the beginning right after the current element and one at the end of the array) to find a pair that sums with the current element to zero. Skip duplicates for the current element and within the inner loop to avoid duplicate triplets. This ensures that each valid triplet is unique and the overall runtime remains efficient.",
    },
    "3sum-closest": {
        "key_insights": "Sort the array to make it easier to use the two pointers technique.\nFix one element and then use two pointers (left and right) to find the best pair that, along with the fixed element, produces a sum closest to the target.\nUpdate the best sum whenever a closer sum is found.\nThe sorted order helps eliminate unnecessary iterations and allows efficient movement of pointers.",
        "complexity":   "Time Complexity: O(n^2)\nSpace Complexity: O(log n) to O(n) depending on the sorting algorithm used (typically in-place sort, so extra space might be minimal)",
        "solution":     "The solution starts by sorting the array. For each index in the array (considered as the first element of the triple), we use two pointers: one starting just after the fixed element and the other at the end of the array. We calculate the sum of the fixed element and the two pointers. If this sum is closer to the target than the current best, we update our best sum. Depending on whether the current sum is less than or greater than the target, we adjust the left or right pointer to try and get closer to the target. This two-pointer method leverages the sorted order of the array for efficient summing.",
    },
    "next-permutation": {
        "key_insights": "The problem is solved by identifying the first pair from the right where the left element is less than the right element.\nOnce this pivot is found, find the rightmost element that is greater than this pivot.\nSwap these two elements.\nReverse the subarray that comes after the pivot to ensure the next permutation is the next lexicographically larger sequence.\nIf no such pivot exists, the array is in descending order and should be reversed to get the smallest permutation.",
        "complexity":   "Time Complexity: O(n)\nSpace Complexity: O(1)",
        "solution":     "We start by scanning the array from the right to find the first index (i) such that nums[i] is less than nums[i+1]. This index 'i' represents the pivot point where the next permutation can be formed. If we find such an index, we then scan from the right again to locate the first number that is greater than nums[i] (let this index be j), and swap these two numbers. Finally, we reverse the subarray starting from index i+1 to the end of the array, which gives us the next permutation in lexicographical order. This approach leverages in-place modifications using basic operations like swapping and reversing, thus meeting the constant space requirement.",
    },
    "sort-colors": {
        "key_insights": "Use the Dutch National Flag algorithm to partition the array into three sections.\nMaintain three pointers: one for the next position for 0 (low), one for the next position for 2 (high), and one to traverse the array (mid).\nSwap elements appropriately to ensure that all 0's are at the beginning, all 2's are at the end, and 1's remain in the middle.",
        "complexity":   "Time Complexity: O(n)\nSpace Complexity: O(1)",
        "solution":     "The solution uses a three-pointer approach (Dutch National Flag algorithm). Initialize three pointers: low, mid, and high. As you iterate through the array:\nIf the element is 0, swap it with the element at the low pointer and increment both low and mid.\nIf the element is 1, simply move mid forward.\nIf the element is 2, swap it with the element at the high pointer and decrement high.\nThis single pass method ensures that the array is sorted in-place with constant extra space. Key details include correctly handling swaps without losing any data and ensuring that the pointers are updated in the proper order.",
    },
    "one-edit-distance": {
        "key_insights": "The difference in length between s and t must be at most 1.\nWhen lengths are equal, the only possibility is that exactly one character is different (replacement).\nWhen one string is longer by one character, the difference can be fixed by one insertion (or deletion in the other direction).\nUse two pointers to compare corresponding characters and carefully handle the first difference.",
        "complexity":   "Time Complexity: O(n), where n is the length of the shorter string, as we traverse both strings at most once.\nSpace Complexity: O(1), using only constant extra space.",
        "solution":     "The solution uses a two-pointer approach. First, we check if the length difference is greater than 1, in which case they cannot be one edit apart. For the two primary cases:\nWhen strings are of equal length, iterate through both strings and count the number of differing characters. There should be exactly one difference.\nWhen lengths differ by one, use pointers for both strings. Upon encountering a difference, increment the pointer for the longer string only and ensure that no previous difference has been encountered. If a second discrepancy is found, return false.\nThe key is to handle the edge case where the difference is at the end of the string.",
    },
    "two-sum-ii-input-array-is-sorted": {
        "key_insights": "The array is sorted in non-decreasing order, which makes the two pointers technique very effective.\nUsing two pointers (one at the start and one at the end) allows checking pairs and adjusting the pointers based on the sum.\nSince the problem guarantees exactly one solution and requires constant space, using two pointers satisfies both constraints.",
        "complexity":   "Time Complexity: O(n), where n is the number of elements in the array, because in the worst-case scenario each element is visited at most once.\nSpace Complexity: O(1), as no additional data structure is used that scales with the input size.",
        "solution":     "We use the two pointers approach:\nInitialize two pointers, left at the beginning (index 0) and right at the end (last index) of the array.\nWhile left is less than right, compute the sum of the elements at the two pointers.\nIf the sum equals the target, return the indices as [left+1, right+1] (to adjust for the 1-indexed array).\nIf the sum is less than the target, move the left pointer to the right to increase the sum.\nIf the sum is greater than the target, move the right pointer to the left to decrease the sum.\nThis approach leverages the sorted property and uses constant extra space.",
    },
    "reverse-words-in-a-string-ii": {
        "key_insights": "Reverse the entire array first to obtain the reversed order of characters.\nThen, reverse each individual word to restore the correct letter order.\nDoing so leverages the two-pointer technique for in-place reversals.\nThis method accomplishes the goal in O(n) time while using O(1) extra space.",
        "complexity":   "Time Complexity: O(n)\nSpace Complexity: O(1)",
        "solution":     "The solution employs a two-step reversal process using the two-pointer technique. First, reverse the entire character array to reverse the overall order of characters. Although this reverses both the words and the letters in each word, it sets up the scenario where each word's letters are in reverse order. The next step is to iterate through the array and locate each word (using the space character as a delimiter) and then reverse each word individually to correct its letter order. This process is done in-place without any additional data structures, conserving space.",
    },
    "rotate-array": {
        "key_insights": "The rotation amount k might be greater than the length of the array, so use k % n (where n is the array length) to compute the effective rotation.\nA smart in-place solution involves reversing sections of the array:\nReverse the entire array.\nReverse the first k elements.\nReverse the remaining n-k elements.\nAlternative approaches include using extra space or performing k single-step rotations (which is less optimal).",
        "complexity":   "Time Complexity: O(n) where n is the number of elements in the array.\nSpace Complexity: O(1) if done in-place.",
        "solution":     "We solve the problem using the \"reverse\" technique to perform the rotation in-place. First, we adjust k by taking k modulo the length of the array to handle cases where k exceeds the array length. Then, we reverse the entire array. Next, we reverse the first k elements to restore their correct order. Finally, we reverse the remaining n-k elements. This sequence yields the array rotated to the right by k positions while strictly using O(1) extra space.",
    },
    "k-diff-pairs-in-an-array": {
        "key_insights": "For k < 0, the answer is always 0 because the absolute difference cannot be negative.\nWhen k is 0, you are looking for numbers that appear at least twice.\nFor k > 0, it is efficient to use a hash set or a hash map to check if each number's complement (number + k) exists.\nUniqueness is ensured by processing each number only once from the set of unique numbers.",
        "complexity":   "Time Complexity: O(n), where n is the length of the array since we iterate over the array and then over the unique numbers.\nSpace Complexity: O(n), for storing the frequency count or the unique elements in a hash map or hash set.",
        "solution":     "The solution first checks if k is negative, returning 0 immediately if true. For k equals 0, we count numbers that appear more than once using a frequency map. For k greater than 0, a set of unique numbers is created and for each number, we check if (number + k) is also present in the set. Each valid case contributes to the result count, ensuring that only unique pairs are considered.",
    },
    "3sum-with-multiplicity": {
        "key_insights": "Use a frequency counter for elements since arr[i] is in the range [0, 100].\nIterate through all possible combinations of numbers x, y, and z with x <= y <= z.\nFor each valid triplet (x, y, z) that sums to target, count the number of ways using combinatorial formulas:\nAll distinct: count[x] * count[y] * count[z].\nTwo numbers the same: use nC2 for the repeated element.\nAll three the same: use nC3.\nUse modulo arithmetic (MOD = 10^9 + 7) at every step to avoid overflow.",
        "complexity":   "Time Complexity: O(M^2), where M = 101 (the range of possible numbers), which is efficient given the constraints.\nSpace Complexity: O(M) for the frequency array.",
        "solution":     "The solution first counts the frequency of each number in the array. Then it iterates through all valid triplets (x, y, z) (with x <= y <= z) and checks if they sum up to the target. Depending on whether x, y, and z are distinct or have duplicates, the combination count is computed accordingly:\nIf x, y, and z are all the same, compute the combination as nC3.\nIf only two are the same, compute as nC2 multiplied by the count of the third number.\nIf all are distinct, use the direct product of the counts.\nThis covers all cases while applying the modulo operation to the result.",
    },
    "interval-list-intersections": {
        "key_insights": "Use two pointers to traverse both lists simultaneously.\nDetermine the overlapping interval by taking the maximum of the start points and the minimum of the end points.\nIf the overlapping condition (start <= end) is met, add the intersection to the result.\nMove the pointer that has the smaller end value to explore further potential intersections efficiently.\nAchieve an overall time complexity of O(m+n), where m and n are the lengths of the two interval lists.",
        "complexity":   "Time Complexity: O(m + n) where m and n are the lengths of firstList and secondList respectively.\nSpace Complexity: O(1) extra space (excluding the space used for the output).",
        "solution":     "The solution uses a two pointers approach. Initialize two indices, one for each list, and iterate through both lists. For each pair of intervals, the potential intersection is computed by:\nCalculating the maximum of the two start values.\nCalculating the minimum of the two end values.\nIf the maximum start is less than or equal to the minimum end, there is an intersection, and it is added to the result list. The pointer corresponding to the interval with the smaller end value is then incremented since that interval cannot intersect with any further intervals from the other list. This process repeats until one of the lists is fully traversed.",
    },
    "shortest-way-to-form-string": {
        "key_insights": "Check if every character in target exists in source; if not, the answer is -1.\nUse a greedy two-pointer strategy: iterate over target while scanning source for matching characters.\nDuring each pass over source, match as many characters as possible from target.\nCount the number of passes (subsequences) required until the target is completely matched.\nAn optimized approach may precompute indices for binary search; however, the basic greedy simulation is efficient for the given constraints.",
        "complexity":   "Time Complexity: O(n * m) in the worst-case, where n = length of source and m = length of target.\nSpace Complexity: O(1) for the greedy simulation (O(n) if additional data structures for binary search are used).",
        "solution":     "The solution uses a greedy two-pointer approach. We simulate the process of reading the target by repeatedly scanning through the source string. Each scan through source is treated as one subsequence concatenated to form the target. A pointer in the target moves forward whenever a matching character is found in source. If a complete pass through source does not advance the target pointer, then it is impossible to form the target and we return -1. The algorithm counts the number of passes required until the target is fully matched.",
    },
    "count-the-number-of-fair-pairs": {
        "key_insights": "Sorting the array simplifies searching for pairs with required sum limits.\nFor each element, binary search is used to locate:\nThe first index where the complement makes the sum >= lower.\nThe last index where the complement makes the sum <= upper.\nThis avoids the need for a nested loop through the entire array, yielding a more efficient solution.",
        "complexity":   "Time Complexity: O(n log n), primarily due to sorting and (for each element) binary search operations.\nSpace Complexity: O(n) if sorting creates a copy; O(1) additional space if sorting in-place.",
        "solution":     "The solution starts by sorting the input array. For each element at index i in the sorted array, we search for indices j (where j > i) such that the sum of nums[i] and nums[j] falls between lower and upper.\nTwo binary search operations are performed:\nTo find the smallest index j where nums[i] + nums[j] >= lower.\nTo find the largest index j where nums[i] + nums[j] <= upper.\nSubtracting the two positions (and summing the counts for each i) gives the total number of fair pairs.\nEdge cases (like no valid pair) are automatically handled by the binary search approach.",
    },
    "kth-largest-element-in-an-array": {
        "key_insights": "kth largest element can be converted to finding the element at index n-k if the array were sorted.\nA min-heap of size k is a practical method to maintain the k largest elements seen so far.\nThe Quickselect algorithm can find the kth largest element in average O(n) time with in-place partitioning.\nChoose an appropriate method based on performance needs and worst-case scenarios.",
        "complexity":   "Time Complexity:\nMin-Heap: O(n log k) by maintaining a heap of size k.\nQuickselect: Average O(n) but worst-case O(n^2) with poor pivot selection.\nSpace Complexity:\nMin-Heap: O(k)\nQuickselect: O(1) additional space with in-place partitioning.",
        "solution":     "We can solve the problem using either a min-heap or the Quickselect algorithm.\nMin-Heap Approach:\nInitialize a min-heap with the first k elements.\nIterate through the remaining elements; if an element is greater than the heap’s root (the smallest in the heap), replace the root.\nAfter processing all elements, the root of the min-heap is the kth largest element.\nQuickselect Approach:\nChoose a pivot and partition the array so that elements larger than the pivot come before it.\nDetermine the position of the pivot; if it matches the index corresponding to the kth largest element, return it.\nRecurse into the appropriate part of the array.\nKey points to consider include maintaining heap size for the first approach and proper pivot selection for the Quickselect approach.",
    },
    "meeting-rooms-ii": {
        "key_insights": "Sort the intervals based on start times to process meetings in order.\nUse a min-heap (priority queue) to keep track of meeting end times.\nWhen processing a new meeting, check if its start time is greater than or equal to the smallest end time from the heap.\nIf so, it means a meeting room has been freed (the meeting with the smallest end time has ended) and can be reused.\nIf not, allocate a new room by pushing the current meeting's end time into the heap.\nThe size of the heap at any moment represents the number of rooms currently in use.",
        "complexity":   "Time Complexity: O(n log n) due to sorting and each heap operation (push/pop) taking O(log n).\nSpace Complexity: O(n) in the worst-case scenario when all meetings overlap, requiring all end times in the heap.",
        "solution":     "The solution involves first sorting the meeting intervals by their start times. Then, we utilize a min-heap (priority queue) to keep track of the end times of ongoing meetings. For each meeting, we compare its start time with the smallest end time in the heap:\nIf the meeting can start after the meeting at the top of the heap has ended (i.e., its start time is greater than or equal to the smallest end), we remove that end time from the heap (freeing up a room).\nThen, we add the current meeting's end time to the heap.\nBy the end of processing all meetings, the size of the heap represents the minimum number of conference rooms required.",
    },
    "top-k-frequent-elements": {
        "key_insights": "Count the frequency of each element using a hash table.\nUse bucket sort to achieve linear time complexity by grouping numbers by their frequency.\nTraverse the frequency buckets from highest to lowest to collect the k most frequent elements.\nAlternative approaches include using heaps or Quickselect, but bucket sort meets the follow-up requirement of better than O(n log n) time complexity.",
        "complexity":   "Time Complexity: O(n) where n is the length of the array (bucket sort and frequency counting are linear).\nSpace Complexity: O(n) for storing the counts and buckets.",
        "solution":     "The solution starts by counting the frequency of each element with a hash table (or dictionary). Then, we create an array of buckets where the index represents the frequency. Each bucket holds the numbers that appear with that frequency. Finally, we iterate over the buckets in reverse order (from high frequency to low) and collect elements until we have k of them. This bucket sort approach avoids the higher complexity of sorting a list of frequencies and guarantees linear time performance.",
    },
    "the-maze-ii": {
        "key_insights": "The ball rolls until it hits a wall, so each move is not one step but a continuous travel in a direction.\nThe problem can be modeled as finding the shortest path in a weighted graph where nodes are positions in the maze and edge weights are the distance traveled in that roll.\nA priority queue (min-heap) based algorithm, such as Dijkstra’s algorithm, is ideal since each roll covers variable distances.\nAlways check if the new computed distance for a cell is less than a previously recorded one before pushing it to the heap.",
        "complexity":   "Time Complexity: O(m * n * log(m * n)), where m and n are the maze dimensions (each cell may get processed and queued with a logarithmic cost).\nSpace Complexity: O(m * n) for maintaining the distance matrix and the priority queue.",
        "solution":     "We solve the problem using a modified version of Dijkstra’s algorithm. The maze grid is treated as a graph where each cell is a node and an edge exists between the current cell and the cell where the ball stops after rolling in one direction. For every cell popped from the priority queue, we roll the ball in all four directions until it hits a wall, counting the number of steps taken. If the distance calculated from this roll is shorter than a previously stored distance for the stopping cell, we update the distance and add it to the priority queue. This process continues until we either reach the destination (and return the distance) or exhaust all possibilities (returning -1).",
    },
    "task-scheduler": {
        "key_insights": "Count the frequency of each task.\nThe task with the highest frequency determines the base structure.\nIdentify how many tasks share the maximum frequency.\nCalculate the total time based on the formula: max(total tasks, (max frequency - 1) * (n + 1) + count of tasks with maximum frequency).\nThis approach avoids simulating the entire scheduling by using a greedy strategy.",
        "complexity":   "Time Complexity: O(N) where N is the number of tasks (counting frequency, then iterating over constant 26 letters).\nSpace Complexity: O(1) since the frequency count uses an array or hash map with fixed size (26 letters).",
        "solution":     "We first count the frequency of each task. The task(s) with the maximum frequency establish the minimum structure required. Compute the ideal idle slots based on the count of these maximum tasks and the cooling period n. Finally, compare this computed length with the total number of tasks to get the final answer. This greedy strategy avoids explicit simulation and leverages mathematical formulation.",
    },
    "find-k-closest-elements": {
        "key_insights": "The input array is already sorted.\nA binary search can be used to efficiently narrow down the potential starting position.\nA two-pointers or sliding window approach is effective for selecting k consecutive elements.\nThe comparison of absolute differences (with a tie-breaker on values) is crucial.\nMaintaining ascending order simplifies the final output.",
        "complexity":   "Time Complexity: O(log n + k), where O(log n) for binary search and O(k) for collecting results.\nSpace Complexity: O(k), for storing the output.",
        "solution":     "We can solve this problem by first using binary search to identify the left-most window where the k closest elements might start. The binary search operates over the possible starting indices for windows of size k (from 0 to n-k). For each middle index mid, we compare the distances between x and arr[mid] and arr[mid + k] to decide whether to move the window to the right or left. Once the correct starting position is found, return the k elements starting from that index. This approach leverages the sorted property of the array to efficiently converge on the optimal window.",
    },
    "cheapest-flights-within-k-stops": {
        "key_insights": "Use a variant of BFS combined with a min-heap (priority queue) to always expand the cheapest route.\nEach state in the queue includes the current cost, current city, and the number of stops used so far.\nTrack the best-known cost to reach a city with a certain number of stops to prune non-optimal routes.\nEnsure that you do not exceed k stops during exploration.",
        "complexity":   "Time Complexity: O(E log(E)) in the worst-case scenario, where E is the number of flights, due to the heap operations.\nSpace Complexity: O(n + E) for storing the graph and auxiliary data structures like the heap and best-cost map.",
        "solution":     "We solve the problem using a modified Dijkstra's algorithm. The approach employs a min-heap to ensure that we always process the state with the lowest cumulative cost next. Each state tracks the total cost so far, the current city, and the number of stops made. When a state reaches the destination city, we return its cost. To avoid unnecessary work, we record the lowest cost at which each city has been reached with a given number of stops. This state-tracking helps prune routes that are more expensive or exceed the allowed number of stops.",
    },
    "k-closest-points-to-origin": {
        "key_insights": "Calculate the squared Euclidean distance (x² + y²) to avoid the overhead of square root computation.\nSorting the points based on their distance gives an O(n log n) solution.\nAlternatively, using a max-heap to keep track of the k closest points results in O(n log k) time, which is effective if k is much smaller than n.\nUsing Quickselect can achieve an average time complexity of O(n), though the worst-case is O(n²).\nThe problem does not require the output to be sorted in any specific order.",
        "complexity":   "Time Complexity: O(n log n) with a sorting approach, O(n log k) with a heap approach, or average O(n) with a Quickselect approach.\nSpace Complexity: O(n) if extra space is used for sorting, or O(k) for the heap approach.",
        "solution":     "We can solve this problem by first computing the squared Euclidean distance for each point to avoid unnecessary square root operations. Then, we can sort the points based on their computed distance and select the first k points.\nAlternatively, a max-heap or Quickselect algorithm can be used:\nMax-Heap: Maintain a heap of size k while iterating through the points, ensuring that only the k points with the smallest distances are kept.\nQuickselect: Partition the array like in QuickSort to position the kth closest point in its correct position. Once partitioned, all points to the left of that position will be the k closest.\nThe sorting solution is straightforward and efficient enough given the constraints.",
    },
    "campus-bikes": {
        "key_insights": "Compute the Manhattan distance between every worker and bike pair.\nTo achieve the tie-breaking rules, sort all pairs by distance, then by worker index and bike index.\nOnce a worker is assigned a bike, skip any other pairs involving that worker or the already assigned bike.\nThe problem can also be approached using bucket sort because the Manhattan distance range is limited, but sorting is intuitive given the constraints.",
        "complexity":   "Time Complexity: O(n * m * log(n * m)) where n is the number of workers and m is the number of bikes.\nSpace Complexity: O(n * m) for storing all worker-bike pairs.",
        "solution":     "Create all possible (worker, bike) pairs along with their Manhattan distances. Sort this list of pairs by:\nManhattan distance,\nWorker index (if distances are equal),\nBike index (if both distance and worker index are equal).\nThen, iterate over the sorted list and assign bikes to workers if neither the worker nor the bike has already been assigned. This guarantees that at each step, the optimal available pairing (according to the problem’s requirements) is made. This method naturally enforces the tie-breaking rules specified in the problem.",
    },
    "minimum-cost-to-connect-sticks": {
        "key_insights": "Always connect the two smallest sticks available to minimize the incremental cost.\nA min-heap (or priority queue) is an ideal data structure to efficiently retrieve the smallest sticks.\nThe process continues until only one stick remains.",
        "complexity":   "Time Complexity: O(n log n), where n is the number of sticks (due to heap insertion and removal operations).\nSpace Complexity: O(n), for storing the sticks in the heap.",
        "solution":     "The solution uses a greedy algorithm with a min-heap:\nInsert all stick lengths into a min-heap.\nWhile there are at least two sticks in the heap:\nExtract the two smallest sticks.\nCompute the cost of connecting them (sum the two lengths).\nAdd this cost to the total result.\nPush the combined stick (with length equal to their sum) back into the heap.\nOnce the heap is reduced to one element, return the total cost.\nThis approach ensures that at every connection, the smallest possible sticks are merged, which leads to the minimum overall cost.",
    },
    "diagonal-traverse-ii": {
        "key_insights": "The diagonal key for each element can be defined as (row_index + column_index).\nElements in the same diagonal (with equal diagonal key) must be output in an order such that the element from the lower row comes before the one from a higher row.\nBecause the input grid may be jagged (rows with varying lengths), you must carefully iterate only over valid indices.\nA hash table (or map) can be used to collect elements grouped by their diagonal key.\nInserting each element at the beginning of the list for that diagonal builds the list in the desired order without needing an extra reverse step later.\nFinally, traverse the keys in increasing order (from 0 to maximum key) to produce the final result.",
        "complexity":   "Time Complexity: O(N), where N is the total number of elements (each element is processed once).\nSpace Complexity: O(N), for storing the elements in the map and the output array.",
        "solution":     "The solution iterates over every element in nums while computing its diagonal key as (i + j). For each element, the value is inserted at the beginning of the list corresponding to that diagonal key. This ensures that when the diagonal groups are later concatenated in order of increasing key, the internal ordering within each diagonal is correct (i.e., elements appear from the bottom of the diagonal to the top). The final result is built by iterating over the keys in sorted order and appending each list of diagonal elements.",
    },
    "merge-k-sorted-lists": {
        "key_insights": "Use a min-heap (priority queue) to efficiently extract the smallest value among the current heads of the lists.\nBy pushing the head of each non-empty list into the heap, we can pull the smallest node, then push that node’s next element into the heap.\nAlternatively, a divide and conquer approach can be used by recursively merging pairs of lists.\nCareful management of pointers (or references) is key to constructing the new linked list.",
        "complexity":   "Time Complexity: O(N log k) where N is the total number of nodes across all lists and k is the number of lists.\nSpace Complexity: O(k) due to the heap containing at most one node from each list.",
        "solution":     "We use a min-heap to always retrieve the smallest node among the current nodes in the linked lists. First, initialize the heap with the head of each list. Then, continuously extract the smallest node from the heap, attach it to the merged list, and if the extracted node has a next node, insert that into the heap. The process repeats until the heap is empty.",
    },
    "the-skyline-problem": {
        "key_insights": "Use a line sweep approach to process building start and end events in sorted order.\nDifferentiate between start events (which add to the skyline) and end events (which remove from the skyline).\nEmploy a max heap (or a priority queue) to efficiently track the current highest building at each sweep line position.\nWhen the current maximum height changes, record the corresponding x-coordinate and new height as a key point.\nTake care to remove expired building heights from the heap as the sweep line moves past the building's end.",
        "complexity":   "Time Complexity: O(n log n) due to sorting events and heap operations for each building event.\nSpace Complexity: O(n) for the event list and the heap used for processing active buildings.",
        "solution":     "The solution uses the line sweep algorithm with a max heap (or priority queue) to maintain the heights of active buildings. Each building contributes two events: one when the building starts (adding its height) and one when it ends (removing its height). These events are sorted primarily by their x-coordinate. For start events, we use negative height values to ensure they are processed before end events at the same x-coordinate. During the sweep, the heap is updated by adding new building heights and removing buildings that have ended. Whenever there is a change in the top of the heap (i.e., the current maximum height), a key point is added to the result. This key point signifies a vertical change in the skyline.",
    },
    "closest-binary-search-tree-value-ii": {
        "key_insights": "The BST property allows efficient narrowing down of candidate nodes.\nUsing two stacks to track predecessors (values <= target) and successors (values > target) can efficiently yield the closest values.\nA modified in-order traversal helps in retrieving the next closest candidate from either side.\nMerging two sorted lists of candidate values based on absolute difference with the target is the key step.",
        "complexity":   "Time Complexity: O(k + log n) – O(log n) to initialize the two stacks and O(k) to collect k elements.\nSpace Complexity: O(log n) – due to the stack space in a balanced BST.",
        "solution":     "We can solve the problem by splitting it into two parts:\nBuild two stacks:\nThe predecessor stack for nodes with values less than or equal to target.\nThe successor stack for nodes with values greater than target.\nTo build these stacks, traverse the BST starting from the root and push nodes along the path: if the node's value is less than or equal to target, push it to the predecessor stack and go right; if greater, push it to the successor stack and go left.\nMerge the two stacks:\nAt each step, compare the top element of the predecessor stack (if available) and the successor stack (if available) based on their distance from the target.\nPop from the stack with the closer candidate and add that value to the result list.\nFor the popped node, update the given stack by moving to the next appropriate node in the in-order traversal (for predecessor, go to left child then rightmost descendant; for successor, go to right child then leftmost descendant).\nRepeat until k values have been collected.\nKey Data Structures and Techniques:\nTwo stacks to simulate reverse and forward in-order traversals.\nMerge technique to choose the closest candidate at each iteration.",
    },
    "find-median-from-data-stream": {
        "key_insights": "Use two heaps: a max-heap for the lower half and a min-heap for the upper half of numbers.\nBalancing the heaps ensures the median can be computed in O(1) time once numbers are added.\nRebalance the heaps after each insertion to ensure size properties hold (difference in sizes is at most 1).",
        "complexity":   "Time Complexity:\naddNum: O(log n) per insertion (heap operations).\nfindMedian: O(1) (accessing the top elements).\nSpace Complexity:\nO(n), where n is the number of elements added in the data stream, as they are stored in the two heaps.",
        "solution":     "We maintain two heaps:\nA max-heap (lowerHalf) for the smaller half of the numbers.\nA min-heap (upperHalf) for the larger half of the numbers.\nOn inserting a new number, decide the heap membership based on the current median. After each insertion, ensure that the heaps have balanced sizes (the size difference is at most one).\nWhen computing the median:\nIf both heaps have the same number of elements, the median is the average of the max of lowerHalf and the min of upperHalf.\nIf one heap has more elements, the median is the top element of that heap.\nThis method makes it efficient to dynamically update and retrieve the median as new numbers are inserted.",
    },
    "rearrange-string-k-distance-apart": {
        "key_insights": "The frequency of each character dictates the feasibility and placement within the rearrangement.\nA greedy approach picks the most frequent available character to maximize the chance of successful placement.\nA max-heap (priority queue) efficiently selects the highest frequency character each time.\nA waiting queue ensures that once a character is used, it can't be reused until k positions later.\nSpecial case: When k is 0, no rearrangement is required because all orders satisfy the condition.",
        "complexity":   "Time Complexity: O(n log C) where n is the length of s and C is the number of unique characters.\nSpace Complexity: O(n) due to the storage used for the heap and wait queue.",
        "solution":     "We use a greedy algorithm with a max-heap to always select the character with the highest remaining frequency that is currently available. The algorithm first counts the frequency of each character and pushes these into a max-heap (using negative frequencies for languages like Python where heapq is a min-heap). Each time a character is selected, it is appended to the result string and its frequency is decreased. This character is then placed in a waiting queue to ensure it isn’t reused until k positions later. Once the waiting period completes, if the character still has remaining occurrences, it is reinserted into the heap. If at any point the heap is empty and the waiting queue still contains characters that haven't been reinserted while the result string is incomplete, we conclude that a valid rearrangement is impossible.",
    },
    "the-maze-iii": {
        "key_insights": "The ball rolls continuously until hitting a wall or falling into the hole.\nA shortest path search is needed based on rolling distance (each step counts when passing an empty cell).\nDijkstra’s algorithm (or a modified BFS with a priority queue) is appropriate since we need to consider distance and lexicographic order.\nWhen expanding moves, simulate the roll until the ball stops — if a hole is passed en route, use that stopping point.\nTrack the best (distance, path) for each cell to avoid redundant work.",
        "complexity":   "Time Complexity: O(mn log(mn)) where m and n are the maze dimensions\nSpace Complexity: O(mn)",
        "solution":     "We use a Dijkstra-like approach with a priority queue containing tuples of (distance, path, row, col). The priority queue is ordered by distance first and then by the instruction string lexicographically. For each cell, simulate rolling in each possible direction (up, down, left, right; iterated in alphabetical order so that the lexicographical property is maintained) until the ball stops. If the ball encounters the hole during rolling, treat that as a valid stop immediately. For every new stopping cell, if a shorter distance or lexicographically smaller path is found, update the record and add the new state to the priority queue. If you reach the hole, return the corresponding path string. If no solution is found when the queue is empty, return \"impossible\".",
    },
    "smallest-range-covering-elements-from-k-lists": {
        "key_insights": "Use a min heap to maintain the smallest current element among the selected elements from each list.\nTrack the current maximum among the chosen elements to easily compute the range.\nIteratively replace the minimum element with the next element from the same list and update the current maximum.\nStop when one of the lists is exhausted, as the range must include at least one element from each list.",
        "complexity":   "Time Complexity: O(N log k), where N is the total number of elements across all lists.\nSpace Complexity: O(k) for the heap, plus additional space for storing the range.",
        "solution":     "The solution uses a min heap (priority queue) that stores a tuple containing the current element, the index of the list it comes from, and its index in that list. Initially, the first element from each list is inserted into the heap and the current maximum among them is tracked. In each iteration, the smallest element is removed (heap root) which provides the current minimum, and the range [current_min, current_max] is assessed. If the list from which the minimum element was extracted has a next element, that element is inserted into the heap and the current maximum is updated if necessary. This process continues until one of the lists is exhausted.",
    },
    "employee-free-time": {
        "key_insights": "Flatten the schedule to combine all employees’ intervals.\nSort the flattened intervals by start time.\nMerge overlapping intervals to form a consolidated view of busy times.\nIdentify gaps between merged intervals as the common free time.\nEnsure free intervals have strictly positive length.",
        "complexity":   "Time Complexity: O(N log N), where N is the total number of intervals (due to sorting).\nSpace Complexity: O(N), used for the flattened list and merged intervals.",
        "solution":     "The solution leverages interval merging. First, all intervals from every employee are collected and sorted by start time. By iterating through the sorted intervals, overlapping intervals are merged into one consolidated busy period. Finally, the gaps between consecutive merged intervals are identified as free periods available to all employees. This approach efficiently handles the intervals with a simple sort and a linear scan.",
    },
    "constrained-subsequence-sum": {
        "key_insights": "Use dynamic programming where dp[i] represents the maximum sum of a valid subsequence ending at index i.\nFor each index i, consider taking nums[i] alone or appending it to a valid subsequence ending at an index j (where i - j <= k) that maximizes the sum.\nTo efficiently obtain the maximum dp[j] in a sliding window of the last k indices, use a monotonic (decreasing) deque.\nThe idea is to maintain the window maximum and update it as we proceed through the array.",
        "complexity":   "Time Complexity: O(n)\nSpace Complexity: O(n) (O(k) extra space for the deque, with dp array using O(n))",
        "solution":     "We solve the problem by defining a dp array where dp[i] = nums[i] + max(0, maximum dp value in the window [i-k, i-1]). The trick is to include max(0, ...) to allow starting a new subsequence when previous sums are negative.\nTo access the maximum dp in the window in constant time, a monotonic deque is maintained that stores indices with their dp values in decreasing order.\nAt each index, we:\nRemove elements from the front of the deque if they are out of the current window (i - index > k).\nUse the front of the deque (if non-empty) as the maximum dp value from the previous k indices.\nCompute dp[i] and update the global maximum answer.\nRemove from the back of the deque all indices with dp values less than the current dp[i] since they are not useful.\nInsert the current index into the deque.",
    },
    "valid-word-square": {
        "key_insights": "The k-th row should be equal to the k-th column.\nNot every word is the same length; checks must be performed within bounds.\nIterating over each character and cross-verifying with the corresponding column character is sufficient.",
        "complexity":   "Time Complexity: O(N * L) where N is the number of words and L is the average length of the words.\nSpace Complexity: O(1) additional space is used for the checking procedure.",
        "solution":     "We iterate through each word (row) and each character (column) in the word. For every character at position (i, j), we confirm that there is a corresponding word at index j and that its length is greater than i. Then we check that the character at position (j, i) in that word is the same as the current character. If any of these checks fail, we return false. If all checks pass, the words form a valid word square.",
    },
    "reshape-the-matrix": {
        "key_insights": "Check if the reshape operation is possible by comparing the total elements (m * n with r * c).\nFlatten the original matrix while preserving the row-traversing order.\nUse the flattened list to build the new matrix row by row.\nIf the total number of elements does not match, simply return the original matrix.",
        "complexity":   "Time Complexity: O(m\nn) as we iterate through all elements of the matrix once.\nSpace Complexity: O(m\nn) for the additional list used to store flattened elements (or the new matrix).",
        "solution":     "The solution involves first checking whether the reshape is possible by ensuring that the product of the rows and columns of the original matrix equals that of the desired matrix (r * c). If not, the original matrix is returned. Otherwise, the matrix is flattened into a one-dimensional list, preserving the row-order traversal. Finally, the flattened list is segmented by the new column size c to form the reshaped matrix. The primary data structures used include a list (or array) for flattening the matrix and then another list (or vector/array) to hold the final reshaped matrix.",
    },
    "toeplitz-matrix": {
        "key_insights": "Each element (except those in the first row and first column) must be equal to its top-left neighbor.\nYou can validate the matrix by iterating from the second row/column and comparing each element with matrix[i-1][j-1].\nThis simple check allows you to determine if every diagonal has the same elements without extra space.\nFor streaming data (loading one row at a time), maintain the previous row for comparison.",
        "complexity":   "Time Complexity: O(m * n)\nSpace Complexity: O(1)",
        "solution":     "The problem is solved by iterating through the matrix starting from the element at position (1, 1). For every element at matrix[i][j], compare it with the element at matrix[i-1][j-1]. If any two elements in this pair do not match, the matrix is not Toeplitz. This check ensures that every diagonal from the top-left to bottom-right has identical elements. In terms of auxiliary space, only constant extra space is used, which makes this approach efficient both in time and space.",
    },
    "transpose-matrix": {
        "key_insights": "The value at position (i, j) in the original matrix moves to (j, i) in the transposed matrix.\nThe transposed matrix dimensions are the reverse of the original: if the original is m x n, then the transposed is n x m.\nA simple double loop is sufficient to swap the indices, ensuring O(m*n) time complexity.\nThis problem handles both square and rectangular matrices.",
        "complexity":   "Time Complexity: O(m\nn)\nSpace Complexity: O(m\nn) for the new matrix holding the transposed values",
        "solution":     "The solution involves iterating over each element of the original matrix and assigning it to the corresponding position in the new matrix where the indexes are swapped. We create the transposed matrix with dimensions based on the number of columns and rows of the original matrix, respectively. The approach leverages basic array manipulation with nested loops to fill in the new matrix.",
    },
    "largest-local-values-in-a-matrix": {
        "key_insights": "The size of the output matrix is (n - 2) x (n - 2).\nFor each valid index (i, j) in the output, examine the 3 x 3 block in grid starting at grid[i][j] to grid[i+2][j+2].\nSince each 3 x 3 block contains exactly 9 elements, finding the maximum in a block takes constant time.\nUse nested loops to slide over the grid and efficiently compute each local maximum.",
        "complexity":   "Time Complexity: O(n^2) – We iterate over (n-2) x (n-2) positions, and for each position, we look at 9 elements.\nSpace Complexity: O(n^2) – The additional space is required for the output matrix.",
        "solution":     "The solution involves iterating over each possible 3 x 3 submatrix in grid. For each (i, j) starting index of the submatrix:\nExamine the nine elements from grid[i][j] to grid[i+2][j+2].\nCompute the maximum value from these elements.\nStore the maximum in the corresponding position of the result matrix.\nData structures used include the result matrix for storing the maximum values. The algorithmic approach involves two nested loops (one for rows and one for columns) to traverse each possible starting position of a 3 x 3 window. The simplicity of the solution comes from the fixed 3x3 block size, which enables constant-time maximum computation for each block.",
    },
    "valid-sudoku": {
        "key_insights": "Validate each row by checking for duplicate numbers while ignoring empty cells.\nValidate each column in the same way.\nDivide the board into nine 3x3 sub-boxes and check each for duplicate numbers.\nUse hash sets (or equivalent data structures) to keep track of seen digits for rows, columns, and boxes.\nThe board size is fixed (9x9), so the overall time complexity is constant with respect to input size.",
        "complexity":   "Time Complexity: O(1) — Since the board size is fixed at 9x9.\nSpace Complexity: O(1) — The extra space used by the sets is bounded by the fixed board size.",
        "solution":     "The solution uses three main data structures: an array of sets for rows, an array of sets for columns, and an array of sets for 3x3 sub-boxes. For each cell, if it is not empty, check if the number is already present in the corresponding row, column, or box. If found, return false immediately. Otherwise, add the number to the respective sets. Compute the index for the 3x3 sub-box using the formula: (row_index // 3) * 3 + (col_index // 3). If no duplicates are found after traversing the entire board, the board is considered valid.",
    },
    "rotate-image": {
        "key_insights": "The rotation can be achieved in two steps: first transpose the matrix and then reverse each row.\nTransposing the matrix swaps matrix[i][j] with matrix[j][i], converting rows into columns.\nReversing each row post-transposition yields the desired clockwise rotation.\nThis approach works in-place with O(1) extra space.",
        "complexity":   "Time Complexity: O(n^2) since every element is processed during the transpose and row reversal.\nSpace Complexity: O(1) as the operations are performed in-place without extra data structures.",
        "solution":     "The solution leverages a two-step in-place transformation:\nTranspose the matrix: Iterate through the matrix and swap each element (i, j) with element (j, i) for j >= i.\nReverse each row: After the transpose, each row is reversed to rotate the matrix 90 degrees clockwise.\nThe key trick is to realize that a transpose followed by a reverse of each row achieves the rotation without needing extra space.",
    },
    "spiral-matrix": {
        "key_insights": "Use four boundaries: top, bottom, left, and right to track the current layer.\nTraverse right across the top row, then down the right column, then left on the bottom row, and finally up the left column.\nAfter each direction, adjust the corresponding boundary to move inward.\nContinue the traversal until the boundaries cross.",
        "complexity":   "Time Complexity: O(m * n) since each element is visited exactly once.\nSpace Complexity: O(1) extra space (excluding the space required for the output list).",
        "solution":     "The solution uses a simulation approach with boundary pointers (top, bottom, left, right). At each step, the boundaries indicate the current submatrix to traverse. You iterate as follows:\nTraverse from left to right along the top boundary.\nTraverse downward along the right boundary.\nIf the top boundary has not passed the bottom, traverse from right to left along the bottom boundary.\nIf the left boundary has not passed the right, traverse upward along the left boundary.\nAfter each traversal, the boundaries are adjusted inward. This continues until all elements have been visited.",
    },
    "spiral-matrix-ii": {
        "key_insights": "Use four pointers (top, bottom, left, right) to keep track of the matrix boundaries.\nTraverse the matrix in a spiral: move right along the top row, then down the right column, left along the bottom row, and finally up the left column.\nUpdate the boundary pointers after each complete traversal of one side.\nContinue until all numbers from 1 to n² are filled into the matrix.",
        "complexity":   "Time Complexity: O(n²)\nSpace Complexity: O(n²) (required for the output matrix)",
        "solution":     "We simulate the spiral movement by initializing four pointers: top, bottom, left, and right, which denote the current boundaries of the matrix that have not yet been filled. For each iteration, we:\nTraverse from left to right along the top boundary, then increment the top pointer.\nTraverse from top to bottom along the right boundary, then decrement the right pointer.\nIf there are rows remaining, traverse from right to left along the bottom boundary, then decrement the bottom pointer.\nIf there are columns remaining, traverse from bottom to top along the left boundary, then increment the left pointer.\nThis algorithm ensures that the matrix is filled in a spiral order until all numbers are placed.",
    },
    "minimum-path-sum": {
        "key_insights": "The problem can be solved using Dynamic Programming.\nThe optimal solution for each cell depends on the minimum path sum of the cell above it and the cell to its left.\nSince you can only move right or down, only two adjacent cells affect the current cell's result.\nEdge cases include the first row and first column, which have only one direction of movement.",
        "complexity":   "Time Complexity: O(m * n), where m and n are the number of rows and columns.\nSpace Complexity: O(m * n) for the DP table (this can be optimized to O(n) with in-place computations).",
        "solution":     "We use a Dynamic Programming (DP) approach where we create a DP table (or use the grid itself) that stores the minimum path sum to each cell. For each cell (i, j):\nIf i = 0 (first row), the only possible path is from the left.\nIf j = 0 (first column), the only possible path is from above.\nFor other cells, the minimum path sum is the cell value plus the minimum of the two possible previous cells (above and left).\nThis ensures that by the time we reach the bottom-right cell, we have accumulated the minimum sum required to reach that cell.",
    },
    "set-matrix-zeroes": {
        "key_insights": "The challenge is to mark rows and columns that need to be zeroed without using extra space.\nA common trick is to use the first row and first column as markers.\nSpecial care is needed for the first row and first column, as they are also used as markers.\nThe algorithm involves two passes: one for marking and one for updating the matrix based on the markers.",
        "complexity":   "Time Complexity: O(m * n)\nSpace Complexity: O(1) (in-place, using only constant extra space)",
        "solution":     "We can solve this problem by using the first row and first column of the matrix as markers. First, determine if the first row or first column needs to be zeroed, then traverse the rest of the matrix and mark the corresponding first row and column positions if a zero is encountered. Finally, update the matrix backward using the markers, and finally update the first row and column if needed.\nThe algorithm works in three main steps:\nCheck if the first row or first column should be zeroed (store this in two variables).\nIterate through the rest of the matrix; for any element that is zero, mark its row and column by setting the corresponding element in the first row and first column to zero.\nIterate through the matrix again (excluding the first row and column) and set elements to zero if their row or column marker is zero.\nFinally, zero out the first row and/or first column if needed.\nThis approach avoids extra space usage apart from the two boolean flags while ensuring that the matrix is updated correctly.",
    },
    "search-a-2d-matrix": {
        "key_insights": "The matrix is essentially a flattened sorted array due to its properties.\nUse binary search to efficiently determine if the target exists.\nMap the 1D binary search index to the corresponding 2D matrix coordinates using division and modulus operations.\nAchieve O(log(m*n)) time complexity by navigating the matrix as if it were a single sorted list.",
        "complexity":   "Time Complexity: O(log(m * n))\nSpace Complexity: O(1)",
        "solution":     "We can treat the matrix as a flattened sorted list without physically copying the elements. Given the total number of elements = m * n, perform binary search on indices ranging from 0 to (m*n - 1). For any index mid, calculate its corresponding row as mid // n and column as mid % n. Compare the element at matrix[row][col] with the target. If it equals target, return true; if it is less than target, search the right half; otherwise, search the left half.",
    },
    "maximal-square": {
        "key_insights": "Use dynamic programming to build a solution where each cell in the DP table represents the side length of the largest square ending at that cell.\nIf the current cell is '1', its DP value is the minimum of the top, left, and top-left neighbors plus one.\nUpdate a global maximum side length during the process to determine the area.\nThe square's area is the square of its maximum side length.",
        "complexity":   "Time Complexity: O(m * n), where m is the number of rows and n is the number of columns.\nSpace Complexity: O(m * n) for the DP table (this can be optimized to O(n) with space reduction techniques).",
        "solution":     "We use a two-dimensional dynamic programming approach. We define a DP matrix with one extra row and column to simplify boundary conditions. For each cell (i, j) in the input matrix, if the value is '1', then we calculate the DP value as the minimum of its top, left, and top-left neighboring DP values plus one. This value represents the side length of the square ending at (i, j). We also keep track of the maximum side length found and finally compute the area of the maximal square by squaring the maximum side length.",
    },
    "sparse-matrix-multiplication": {
        "key_insights": "Exploit sparsity by iterating only over non-zero elements.\nFor each non-zero element in mat1, multiply it with corresponding non-zero elements in mat2.\nAccumulate the product into the result matrix.\nThis approach reduces unnecessary multiplication operations when many elements are zero.",
        "complexity":   "Time Complexity: O(m * k * n) in the worst-case, but significantly lower when many elements are zero.\nSpace Complexity: O(m * n) for the resulting matrix.",
        "solution":     "We loop through each row of mat1 and for every non-zero element, we traverse the corresponding row in mat2. By checking if an element in both matrices is non-zero before multiplying, we avoid redundant calculations. This optimization leverages the sparsity of the matrices. We use standard nested loops along with conditional checks to implement this efficient multiplication.",
    },
    "diagonal-traverse": {
        "key_insights": "Traverse the matrix diagonally with alternating directions: upward (top-right) and downward (bottom-left).\nWhen the traversal reaches a boundary (top, bottom, left, or right), adjust the position and change the direction.\nUse simulation to process each element exactly once while managing the indices based on the current direction.",
        "complexity":   "Time Complexity: O(m * n) where m is the number of rows and n is the number of columns, as every element is processed once.\nSpace Complexity: O(m * n) for the output array (excluding the input matrix), while additional space usage remains constant.",
        "solution":     "The solution simulates the diagonal traversal using a direction flag (1 for upward and -1 for downward).\nBegin at the matrix's top-left corner.\nAppend the current element to the result.\nDepending on the current direction:\nIf moving upward, decrease the row and increase the column.\nIf moving downward, increase the row and decrease the column.\nAdjust for boundary hits:\nWhen moving upward:\nIf at the last column, increment the row and switch direction.\nIf at the first row, increment the column and switch direction.\nWhen moving downward:\nIf at the last row, increment the column and switch direction.\nIf at the first column, increment the row and switch direction.\nThis process continues until all elements are added to the result array.",
    },
    "lonely-pixel-i": {
        "key_insights": "Count the number of 'B' pixels in each row.\nCount the number of 'B' pixels in each column.\nA pixel is considered lonely if the count in its corresponding row and column is exactly 1.\nTwo passes through the matrix can efficiently solve the problem: one for counting and one for checking conditions.",
        "complexity":   "Time Complexity: O(m * n), where m and n are the dimensions of the picture.\nSpace Complexity: O(m + n), used to store the row and column counts.",
        "solution":     "The solution works by first scanning the entire matrix to compute the number of black pixels in each row and each column using two arrays/lists. Once these counts are available, a second pass through the matrix determines for each black pixel if it is lonely by checking if its corresponding row and column count equals 1. The approach uses simple array data structures for tallying counts and ensures that each element is processed only a constant number of times.",
    },
    "candy-crush": {
        "key_insights": "Use a simulation approach: repeatedly detect crushable candies and simulate their removal.\nTraverse horizontally and vertically to mark candies that are in groups of three or more.\nInstead of removing candies immediately, mark them for removal and update the board once for the entire pass.\nAfter crushing, simulate gravity by shifting non-zero candies downward in each column.\nContinue the process until no further candy crushes can be detected.",
        "complexity":   "Time Complexity: O((m\nn)^2) in the worst case since each iteration includes scanning the m x n grid and then applying gravity.\nSpace Complexity: O(m\nn) for maintaining additional markers or flags (or using the board itself with in-place modifications).",
        "solution":     "The approach involves iteratively scanning the board to identify candy groups that should be crushed using two passes:\nHorizontal Check – For each row, check segments of 3 consecutive candies.\nVertical Check – For each column, check segments of 3 consecutive candies.\nIf any candy qualifies, mark its position for crushing.\nAfter marking, update the board by turning all marked positions into 0.\nThen, for each column, drop down non-zero candies such that all empty cells (zeros) are at the top.\nRepeat this process until no group of 3 or more adjacent candies is found.\nKey data structures include the board array and a temporary marker (can be implicit by using signs or a separate boolean matrix) for tracking candies to crush. The algorithm ensures a stable board before returning it.",
    },
    "image-overlap": {
        "key_insights": "You only need to consider the positions of 1’s in each matrix.\nThe overlap count for a given translation can be computed by comparing the relative differences between the positions of 1’s in the two images.\nUsing a hash map (or dictionary) to count how many times each translation vector appears lets you compute the maximum overlap efficiently.\nThe translation operation is equivalent to shifting the positions, so the best possible translation is the one that aligns the most pairs of 1’s.",
        "complexity":   "Time Complexity: O(n^4) in the worst-case when using a nested iteration over positions for both matrices, though the average situation is typically better since not all cells are 1.\nSpace Complexity: O(n^2) for storing the positions of 1’s and the counts for translation vectors.",
        "solution":     "The solution involves the following steps:\nTraverse both images to record the coordinates of all 1’s.\nFor every pair of 1’s (one from the first image, one from the second), compute the translation vector (offset) that aligns them.\nUse a hash map to count how many times each offset appears.\nThe maximum count recorded in the hash map is the largest overlap achievable by any translation.\nThis approach avoids simulating every possible slide over the matrix and focuses only on relative alignments of 1’s.",
    },
    "find-smallest-common-element-in-all-rows": {
        "key_insights": "Since every row is strictly increasing, binary search can be used efficiently to check for the presence of an element in a row.\nIterating over the first row’s elements and verifying their presence in all other rows is an effective approach.\nEarly termination is possible if an element is not found in any one row.\nTime complexity is manageable given the constraints, even using binary search for each candidate across rows.",
        "complexity":   "Time Complexity: O(m * n * log(n)) where m is the number of rows and n is the number of columns (binary search in each row for each candidate from the first row).\nSpace Complexity: O(1) (only a few auxiliary variables are used).",
        "solution":     "The solution iterates over each element (candidate) in the first row of the matrix. For each candidate, it checks if the candidate is present in every other row using binary search, taking advantage of the sorted order. If a candidate is found in every row, return it immediately as it will be the smallest such common element because the first row is processed in order. If no candidate is common across all rows, return -1.",
    },
    "number-of-submatrices-that-sum-to-target": {
        "key_insights": "Transform the 2D submatrix sum problem into multiple 1D subarray sum problems.\nPrecompute cumulative sums by fixing two row boundaries and then summing columns between these rows.\nUse a hash table (or dictionary) to count the number of subarrays with a given sum, reducing the problem to the classic \"subarray sum equals k\" question.\nIterate over all possible row pairs and for each, treat the column sums as an array where the target sum is sought.",
        "complexity":   "Time Complexity: O(n² * m), where n is the number of rows and m is the number of columns.\nSpace Complexity: O(m), used by the hash table for storing cumulative sums along the columns.",
        "solution":     "The idea is to iterate over all pairs of rows (top and bottom) and, for each pair, compute the cumulative column sums between these rows. This converts the problem into finding the number of subarrays in a one-dimensional array (the column sums) that add up to the target. For each such 1D problem, use a hash table to track the frequency of cumulative sums seen so far and count how many times the difference (current sum - target) has been seen. This count gives the number of subarrays ending at the current index which sum to target. Combining counts from all possible row pairs yields the final result.",
    },
    "island-perimeter": {
        "key_insights": "Each land cell contributes 4 edges if isolated.\nFor every adjacent pair of land cells (neighbors horizontally or vertically), the shared edge should be subtracted from the total perimeter.\nInstead of DFS/BFS, we can iterate the grid and check the four neighbors for each land cell.\nAlternatively, one may use DFS/BFS to visit all connected cells if needed, but an iterative solution is straightforward with O(rows*cols) time complexity.",
        "complexity":   "Time Complexity: O(n * m), where n is the number of rows and m is the number of columns.\nSpace Complexity: O(1) for the iterative solution (excluding the input storage).",
        "solution":     "We iterate through every cell in the grid. For each land cell, we initially add 4 to the perimeter. Then we check its four neighbors (up, down, left, right). For every neighbor that is also land, we subtract 1 from the current cell’s contribution since that side is not exposed. This simple approach ensures that shared borders between adjacent land cells are not over-counted.",
    },
    "flood-fill": {
        "key_insights": "The flood fill operation can be executed using either DFS (Depth First Search) or BFS (Breadth First Search).\nA check is needed at the beginning to determine if the new color is the same as the original; if so, no changes are needed.\nThe algorithm recursively or iteratively visits each pixel that is directly adjacent and matches the original color, then updates its color.\nThe problem has a worst-case scenario where all pixels are the same color, leading to a full traversal of the grid.",
        "complexity":   "Time Complexity: O(m * n), where m and n are the dimensions of the image grid, because in the worst-case all cells are visited.\nSpace Complexity: O(m * n) in the worst-case due to the recursion stack (or BFS queue) if the entire image needs to be filled.",
        "solution":     "We use a DFS approach to perform the flood fill. The algorithm starts at the given starting pixel and uses recursion to fill connected pixels with the same original color. The following steps are key:\nCheck if the starting pixel's color is already the target color; if so, return the image immediately.\nDefine a recursive helper function that:\nChecks boundary conditions.\nUpdates the current pixel's color.\nRecursively calls itself on the four adjacent directions (up, down, left, right).\nReturn the modified image after the recursion completes.\nData structures used:\nRecursion call stack for the DFS implementation.\nVariables to store the image dimensions and original color.",
    },
    "surrounded-regions": {
        "key_insights": "Regions connected to the border are not captured.\nUse DFS/BFS from border 'O's to mark safe cells.\nAfter marking, flip all remaining 'O's to 'X' and then revert the marked cells back to 'O'.",
        "complexity":   "Time Complexity: O(m * n)\nSpace Complexity: O(m * n) in the worst case due to recursion stack or queue storage for DFS/BFS",
        "solution":     "The solution involves two main steps. First, traverse the board's borders and run a DFS or BFS from any encountered 'O', marking it (e.g., using '\n') to indicate that it should not be flipped. Next, go through each cell in the board: flip any remaining 'O' (i.e., not marked safe) to 'X' and change the temporary marker '\n' back to 'O'. This approach ensures only the regions completely surrounded by 'X' are captured.",
    },
    "clone-graph": {
        "key_insights": "The graph is undirected and connected.\nEach node must be cloned exactly once to prevent cycles and repetitions.\nUse a mapping/dictionary to keep track of cloned nodes.\nA depth-first search (DFS) or breadth-first search (BFS) can be used to traverse and clone the graph.\nHandle edge cases where the graph might be empty.",
        "complexity":   "Time Complexity: O(N + E), where N is the number of nodes and E is the number of edges, because each node and each edge are traversed once.\nSpace Complexity: O(N), to store the mapping from original nodes to their clones, and for the recursion/queue used in traversal.",
        "solution":     "We employ a depth-first search (DFS) approach to clone the graph. The main idea is to create a mapping (hash table) that links each original node to its corresponding cloned node. During the DFS traversal, if a node is encountered for the first time, a new node is created and stored in the mapping; then, the algorithm recursively clones all of its neighbors. For an already visited node, the cloned node is directly returned from the mapping. This ensures that every node is cloned only once and correctly handles cycles in the graph.",
    },
    "number-of-islands": {
        "key_insights": "Use graph traversal (DFS/BFS) to explore connected components of land.\nIterate over the matrix and start a traversal (e.g., DFS) when encountering an unvisited \"1\".\nDuring traversal, mark visited cells to avoid duplicate counting.\nEach DFS/BFS call completely explores one island.\nAlternative approach can use Union Find to group connected lands.",
        "complexity":   "Time Complexity: O(m * n), as each cell is visited at most once.\nSpace Complexity: O(m * n) in the worst-case scenario due to recursion stack (DFS) or auxiliary data structures.",
        "solution":     "We solve the problem using a DFS-based approach. The algorithm iterates over each cell in the grid; when a \"1\" (land) is encountered, the DFS is initiated to mark the entire island (all connected \"1\"s) as visited (by setting them to \"0\"). This prevents recounting the same island. The island count is incremented each time a new island is discovered and fully explored.",
    },
    "course-schedule": {
        "key_insights": "This problem can be modeled as a directed graph where courses are nodes and prerequisites are edges.\nIf the graph has a cycle, then it is not possible to complete all courses.\nWe can detect cycles using either Topological Sort (BFS/Kahn’s algorithm) or DFS-based cycle detection.\nThe time complexity is O(V + E), where V is the number of courses and E is the number of prerequisite pairs.",
        "complexity":   "Time Complexity: O(V + E)\nSpace Complexity: O(V + E)",
        "solution":     "We solve the problem using the Topological Sort algorithm (BFS/Kahn’s algorithm). First, we build a graph (using an adjacency list) and track the indegree (number of prerequisites) for each course. We then find all courses with an indegree of zero (courses with no prerequisites) and add them to a processing queue. While the queue is not empty, we remove a course from the queue, reduce the indegree for its neighboring courses, and if any neighbor’s indegree becomes zero, we add it to the queue as well. If we successfully process all courses, then it means that the graph was acyclic and a valid schedule exists, returning true. If not, a cycle exists, and we return false.",
    },
    "course-schedule-ii": {
        "key_insights": "This is a topological sort problem on a directed graph.\nThe courses are nodes, and prerequisites form directed edges.\nWe can solve the problem by either:\nUsing DFS with cycle detection and a stack for topological ordering.\nUsing BFS (Kahn's Algorithm) to process nodes with zero in-degrees.\nIf at any point no node with a zero in-degree is available, a cycle exists and no valid order can be produced.",
        "complexity":   "Time Complexity: O(V + E) where V is the number of courses and E is the number of prerequisite pairs.\nSpace Complexity: O(V + E) for storing the graph and in-degree array.",
        "solution":     "We treat the problem as a topological sort on a directed acyclic graph (DAG). For a BFS approach using Kahn’s algorithm:\nBuild an adjacency list for the graph and an in-degree array to record the number of prerequisites (incoming edges) for each course.\nInitialize a queue with all courses that have an in-degree of zero (i.e., courses that can be taken without prerequisites).\nDequeue nodes from the queue, add them to the ordering, and reduce the in-degree of their neighbors.\nIf a neighbor’s in-degree becomes zero, add it to the queue.\nIf the final ordering includes all courses, return the ordering; otherwise, return an empty array indicating a cycle.\nThe chosen data structures are:\nA list (or array) for the in-degree counts.\nA dictionary (or array of lists) for the adjacency list.\nA queue to process nodes with zero in-degree.",
    },
    "graph-valid-tree": {
        "key_insights": "A tree must be acyclic and connected.\nFor n nodes, a valid tree must have exactly n - 1 edges.\nA DFS/BFS traversal can check connectivity and simultaneously detect cycles.\nThe Union-Find (disjoint set) approach provides an alternative for cycle detection and connectivity verification.",
        "complexity":   "Time Complexity: O(n + e), where e is the number of edges\nSpace Complexity: O(n + e) for storing the graph and traversal/union-find structures",
        "solution":     "We can solve the problem using either a DFS/BFS approach or a Union-Find algorithm. For the DFS/BFS method, we first check if the number of edges is exactly n - 1. If not, it cannot be a tree. Then, we build an adjacency list for the graph and perform a DFS starting from node 0 while keeping track of visited nodes and parent information to avoid false-positive cycle detections. If during DFS we encounter a node that has been visited (and is not the parent), then a cycle exists. Finally, if all nodes are reached from node 0 and no cycle is found, the graph is a valid tree.\nUsing Union-Find, we iterate over each edge and try to union the two nodes. If they already share the same parent (i.e., are in the same set), a cycle exists. After processing all edges, we confirm that exactly one connected component exists.",
    },
    "minimum-height-trees": {
        "key_insights": "A tree can have at most two centers that will serve as the roots for its minimum height trees.\nInstead of trying all possible roots, we can remove leaves layer by layer until we are left with the central node(s).\nThe process is similar to a topological sort on the tree.\nWhen there are 2 or fewer nodes left, these nodes are the centers (roots) of MHTs.",
        "complexity":   "Time Complexity: O(n) where n is the number of nodes, since we process each node and edge once.\nSpace Complexity: O(n) for storing the graph and additional data structures like the leaves list.",
        "solution":     "The solution builds an undirected graph from the input, then identifies and removes node-leaves iteratively. A leaf is defined as a node connected to only one other node. By repeatedly removing the leaves and updating the graph (decreasing neighbor degrees), the algorithm peels away the outer layers of the tree. When only one or two nodes remain, these nodes are the centers of the tree and form the roots of the minimum height trees.",
    },
    "number-of-connected-components-in-an-undirected-graph": {
        "key_insights": "Use an adjacency list to represent the graph.\nA DFS (or BFS) traversal from an unvisited node will traverse an entire connected component.\nEach new DFS/BFS call from an unvisited node signals a separate connected component.\nAlternatively, the Union-Find data structure can be used to union connected nodes and count distinct sets.",
        "complexity":   "Time Complexity: O(n + e), where n is the number of nodes and e is the number of edges.\nSpace Complexity: O(n + e) due to the storage of the graph and tracking of visited nodes.",
        "solution":     "We solve the problem by iterating over each node and performing a DFS for any node that hasn't been visited. The DFS uses an iterative approach with a stack to traverse all nodes in a connected component. Each DFS initiation corresponds to one connected component. This method efficiently explores the graph using an adjacency list and marks nodes as visited to prevent redundant traversals.",
    },
    "pacific-atlantic-water-flow": {
        "key_insights": "Reverse the typical DFS/BFS perspective: start from the oceans and work inward.\nMaintain two matrices (or sets) to keep track of cells reachable from the Pacific and Atlantic edges.\nA cell can be added to the result if it is reached in both traversals.\nThe height constraint ensures water flows only in non-increasing order.",
        "complexity":   "Time Complexity: O(m * n) where m is the number of rows and n is the number of columns. Each cell is processed up to twice (once from each ocean).\nSpace Complexity: O(m * n) for the visited matrices and the recursion/queue used in DFS/BFS.",
        "solution":     "The solution uses depth-first search (DFS) from the ocean boundaries. We initialize two boolean matrices, one for each ocean. For the Pacific, we start DFS from the leftmost column and top row; for the Atlantic, we start from the rightmost column and bottom row. In each DFS, if the next cell has a height equal to or greater than the current cell and hasn’t been visited, we continue the search. Finally, we scan through the grid to collect cells that are reachable from both oceans. This reverse search simplifies the path validations and leverages the allowed water flow conditions.",
    },
    "the-maze": {
        "key_insights": "The ball continues moving in one direction until it hits a wall (or the boundary, which is considered a wall).\nYou must simulate the ball's rolling rather than taking a single step.\nEven if the ball passes through the destination, it is only valid if it can stop exactly there.\nA breadth-first search (BFS) or depth-first search (DFS) strategy is ideal because we need to explore all stopping positions.\nUse a visited structure to avoid revisiting positions and thus prevent infinite loops.",
        "complexity":   "Time Complexity: O(m * n) where m and n are the dimensions of the maze.\nSpace Complexity: O(m * n) due to the storage used by the visited matrix and the BFS/DFS queue or recursion stack.",
        "solution":     "We simulate the ball's movement using a breadth-first search (BFS) approach. Each time we pick a stop position, we try to roll in all four directions until the ball hits a wall. At that point, if the new stopping position has not been visited, we mark it as visited and add it to our queue. If we ever reach the destination exactly, we return true. The key is simulating the \"roll\" by iteratively moving in a direction until no further move is possible.\nWe store the coordinates in a queue (or stack for DFS) along with a 2D visited array to monitor which positions have been processed. The directions are maintained in a list/array, and for each direction, we keep moving until the next cell would be out of bounds or a wall.",
    },
    "number-of-distinct-islands": {
        "key_insights": "Use DFS/BFS to traverse and mark each island.\nFor each island, record its shape relative to a starting coordinate to allow translation invariance.\nRepresent the island's shape as a unique signature (e.g., tuple or string of relative positions).\nUse a set (or hash table) to store unique shapes.",
        "complexity":   "Time Complexity: O(m * n) as each cell is visited once.\nSpace Complexity: O(m * n) in the worst-case due to recursion stack and storing island shapes.",
        "solution":     "Use depth-first search (DFS) to explore the grid. When an unvisited land cell (1) is encountered, initiate a DFS to traverse the entire island. Record each land cell's coordinates relative to the starting position of the island. This relative representation is invariant under translation, making it possible to compare island shapes. Store the canonical shape signature (for example, as a tuple or string) in a set to ensure uniqueness. Finally, return the size of the set as the number of distinct islands.",
    },
    "max-area-of-island": {
        "key_insights": "Traverse each cell in the grid.\nUse Depth-First Search (DFS) or Breadth-First Search (BFS) to explore connected 1’s.\nMark visited cells to prevent double counting (e.g., by setting them to 0).\nMaintain and update a maximum area count during traversal.\nThe grid's boundaries are naturally water, so no extra edge processing is needed.",
        "complexity":   "Time Complexity: O(m * n) where m and n are the dimensions of the grid since every cell is visited at most once.\nSpace Complexity: O(m * n) in the worst-case scenario for the recursion stack (or auxiliary data structures) when the grid is entirely land.",
        "solution":     "The solution leverages a DFS approach to iterate over each cell in the given grid. When a cell with a value of 1 is encountered, a DFS is initiated from that cell to calculate the area of the island connected to it. Cells are marked as visited (by changing them from 1 to 0) during the DFS to avoid revisiting. The DFS explores all four directions (up, down, left, right) and accumulates the count of connected cells. The maximum area found among all islands is returned at the end. This approach efficiently computes the largest island area with a simple traversal mechanism.",
    },
    "accounts-merge": {
        "key_insights": "Two accounts are merged if they share at least one common email.\nTreat each email as a node in a graph; accounts represent connected components.\nBoth DFS/BFS and Union-Find can be used to find connected components.\nKeep a mapping from email to name because all emails in a connected component belong to the same person.\nAfter grouping, sort the emails and prepend the name to form the final account list.",
        "complexity":   "Time Complexity: O(N * M * log(M)) where N is the number of accounts and M is the number of emails per account (owing to sorting and union operations).\nSpace Complexity: O(N * M), used for storing email mappings and union-find structures.",
        "solution":     "The solution involves using a Union-Find (Disjoint Set Union) data structure to merge emails that are connected by being on the same account. For each account, we union the first email with every other email in that account. We also maintain a mapping from email to owner name. After processing all accounts, we group the emails by their root parent derived from the union-find structure. For each group, we sort the emails and prepend the owner's name. Finally, we return the merged list of accounts.",
    },
    "is-graph-bipartite": {
        "key_insights": "A graph is bipartite if you can assign two colors to each node such that no two adjacent nodes share the same color.\nThe challenge may require exploring disconnected components.\nBFS or DFS can be used to attempt the two-coloring, and a conflict during coloring indicates the graph is not bipartite.\nUnion Find is an alternative approach, but BFS/DFS is more intuitive for graph coloring problems.",
        "complexity":   "Time Complexity: O(V + E) where V is the number of nodes and E is the number of edges, since every node and edge is traversed at most once.\nSpace Complexity: O(V), for the color array and the queue (or recursion stack) used in BFS (or DFS).",
        "solution":     "We use a BFS/DFS approach to assign colors (0 and 1) to nodes. A color array, initially set to -1 for all nodes, is maintained. For each unvisited node (color -1), we start a BFS (or DFS) and assign it an initial color (e.g., 0). For every visited neighbor, if it has not been colored, assign it the opposite color; if it is already colored with the same color as the current node, a conflict has occurred, and the graph is not bipartite. This method gracefully handles disconnected components by iterating over all nodes.",
    },
    "web-crawler": {
        "key_insights": "Treat the set of webpages as a graph where each URL is a node and an edge exists from one URL to another if it is returned by HtmlParser.getUrls(url).\nUse either breadth-first search (BFS) or depth-first search (DFS) to traverse the webpages.\nExtract the hostname from a URL to ensure that only URLs on the same domain are crawled.\nMaintain a visited set (or equivalent) to avoid processing the same URL multiple times.",
        "complexity":   "Time Complexity: O(N + E) where N is the number of nodes (URLs) and E is the number of edges (links between URLs) encountered during the crawl.\nSpace Complexity: O(N) for storing the visited URLs and the queue/stack used for traversal.",
        "solution":     "The solution uses a BFS (or DFS) approach to traverse the web graph:\nExtract the hostname from startUrl (for instance, by splitting on \"://\" and then the first \"/\" that follows).\nInitialize a visited set to keep track of URLs that have been crawled.\nUse a queue to implement the BFS. Enqueue the startUrl.\nWhile the queue is not empty, dequeue a URL and retrieve its outgoing links using HtmlParser.getUrls(url).\nFor each URL obtained:\nExtract its hostname and compare it with the hostname of startUrl.\nIf it matches and it has not been visited before, add it to the visited set and enqueue it.\nFinally, return the set of visited URLs.\nThis approach guarantees that each URL is processed only once and that only URLs from the same hostname are crawled.",
    },
    "web-crawler-multithreaded": {
        "key_insights": "Use multiple threads to perform concurrent crawling, which speeds up the process over a single-threaded approach.\nMaintain a thread-safe set (visited) to ensure no URL is processed twice.\nUtilize a work queue to manage URLs pending processing.\nFilter URLs based on the hostname extracted from startUrl.\nApply synchronization (locks or concurrent data structures) to avoid race conditions when multiple threads access shared data.",
        "complexity":   "Time Complexity: O(N) where N is the number of URLs processed, with additional constant factors due to threading overhead.\nSpace Complexity: O(N) for storing the visited URLs and the work queue.",
        "solution":     "The solution initializes a shared work queue with the startUrl and a thread-safe visited set. Each worker thread dequeues a URL, retrieves its linked URLs via the blocking HtmlParser.getUrls() call, and filters the resulting URLs by comparing their hostnames with the start URL's hostname. New URLs that have not yet been visited are added to both the visited set and the work queue. The use of a thread pool or multiple worker threads along with synchronization ensures that the crawler processes URLs concurrently while avoiding race conditions. The approach follows a BFS-like pattern spread across multiple threads.",
    },
    "alien-dictionary": {
        "key_insights": "Build a graph where each unique character is a node.\nCompare adjacent words to determine the first different character, which gives the ordering (directed edge).\nEnsure all characters, even those without edges, are represented.\nUse a topological sort (using DFS or BFS) to determine a valid character ordering.\nHandle edge cases, such as words where a longer word appears before its prefix which is invalid.",
        "complexity":   "Time Complexity: O(N * L + V + E) where N is the number of words, L is the average length of each word, V is the number of unique characters, and E is the number of edges derived from adjacent word comparisons.\nSpace Complexity: O(V + E) for storing the graph and additional data structures for topological sorting.",
        "solution":     "We use a graph-based approach to solve the problem. First, we initialize a graph (adjacency list) and in-degree dictionary for all unique characters. We then iterate through each pair of consecutive words, comparing them character by character to find the first pair that differs. This difference implies an order: the first character should come before the second character. We add a directed edge accordingly and update the in-degree for the target character.\nTo get the final ordering, we perform a topological sort using Kahn’s algorithm:\nEnqueue characters with in-degree zero.\nRemove characters from the queue and append them to our result.\nDecrement the in-degree of neighboring nodes.\nIf a neighbor's in-degree becomes zero, enqueue it.\nIf the result’s length equals the number of unique nodes, we have a valid ordering; otherwise, a cycle exists, and we return an empty string.",
    },
    "longest-increasing-path-in-a-matrix": {
        "key_insights": "The problem can be modeled as a graph where each cell is a node connected to its neighbors if the neighbor's value is greater.\nA Depth First Search (DFS) with memoization is effective to avoid recalculations.\nDynamic Programming (DP) is used to cache the longest path found from each cell.\nSince each cell can be a start point, compute DFS for every cell and update the global maximum.",
        "complexity":   "Time Complexity: O(m * n) – each cell is computed once due to memoization.\nSpace Complexity: O(m * n) – space is used for the memoization cache and recursion stack.",
        "solution":     "We solve the problem using a DFS approach combined with memoization to keep track of already computed longest paths from each cell. For each cell in the matrix, a DFS is initiated to explore all four valid directions (up, down, left, right). If a neighboring cell has a larger value, the DFS recurses from that neighbor. The result for each cell is cached in a 2D memoization table, ensuring that each cell is processed only once. After processing all cells, the maximum value in the memo table represents the longest increasing path in the entire matrix.",
    },
    "redundant-connection-ii": {
        "key_insights": "The extra edge introduces one of two issues:\nA node has two parents.\nA cycle exists in the graph.\nFirst, scan through edges to detect if any node gets two parents. If found, there are two candidate edges.\nUse the Union-Find data structure (Disjoint Set Union) to detect cycles.\nDepending on whether a conflict (two parents) exists and/or a cycle is detected, decide which edge to remove:\nIf a node has two parents, test by ignoring the second candidate edge during union-find. If no cycle forms, the second candidate is removable; otherwise, remove the first candidate.\nIf no conflict exists, simply remove the edge that causes a cycle.",
        "complexity":   "Time Complexity: O(n)\nSpace Complexity: O(n)",
        "solution":     "The solution consists of two main steps:\nTraverse the list of edges to detect whether any node has two parents. If such a node is found, mark the two candidate edges (candidate1: the earlier edge, candidate2: the later edge).\nUse the Union-Find algorithm to iterate over the edges:\nIf there is a conflict (i.e., a node with two parents), skip candidate2 initially.\nWhile processing edges with Union-Find, check if adding an edge would create a cycle.\nFinally, based on the presence of a cycle and the two-parent conflict, decide which candidate edge to return.\nKey Data Structures:\nAn array (or hash map) to track the parent of each node.\nThe Union-Find (Disjoint Set Union) structure to union sets and detect cycles.",
    },
    "escape-a-large-maze": {
        "key_insights": "The maximum number of blocked cells is 200, which limits the size of a potential completely enclosed region.\nEven if the target is unreachable by direct search, if the BFS (or DFS) explores more than a calculated threshold (based on the blocked cells count), then the region is not fully enclosed.\nPerform two BFS explorations: one starting from the source and one from the target. If neither search is trapped within a small region, then an escape/path exists.\nEarly exit in BFS if the target is reached or if the number of visited nodes exceeds the maximum possible enclosed area.",
        "complexity":   "Time Complexity: O(B^2) in the worst case, where B is the number of blocked cells (with B ≤ 200).\nSpace Complexity: O(B^2) due to the storage of visited cells in the worst-case scenario.",
        "solution":     "The solution uses the Breadth-First Search (BFS) algorithm. A key observation here is that the blocked cells can only enclose a limited area. We compute a threshold (maxSteps) based on the number of blocked cells (n*(n-1)/2) to determine if the search is confined. The BFS from the source (and similarly from the target) stops in two cases: if the target is reached or if the visited area exceeds the threshold, indicating that the region is not fully enclosed by blocked cells. Running BFS from both the source and target ensures that neither is trapped inside a small banned region.",
    },
    "critical-connections-in-a-network": {
        "key_insights": "The problem is about identifying bridges in an undirected graph.\nUse Depth-First Search (DFS) to explore the graph.\nDuring DFS, track discovery times and low-link values for each node.\nFor an edge (u, v), if low[v] > disc[u] then (u, v) is a critical connection.\nTarjan's algorithm is well-suited for solving this problem in O(n + m) time.",
        "complexity":   "Time Complexity: O(n + m) where n is the number of nodes and m is the number of connections.\nSpace Complexity: O(n + m) for the graph representation and DFS recursion stack.",
        "solution":     "We solve this problem using a DFS-based approach (Tarjan's algorithm). First, we transform the list of connections into an adjacency list for efficient graph traversal. We then start a DFS from an arbitrary node (node 0, since the graph is connected) while maintaining two arrays: one for discovery times (disc) and one for low-link values (low). The discovery time is the time when the node is first visited, and the low-link value is the smallest discovery time that can be reached from that node (including via back edges). For each edge (u, v), after a DFS call on v, if low[v] > disc[u] then the edge (u, v) is a bridge because v (and its descendants) cannot reach u or any of its ancestors without this edge. This edge is then recorded as a critical connection.",
    },
    "merge-two-sorted-lists": {
        "key_insights": "Leverage the fact that both lists are sorted.\nUse two pointers (one for each list) to compare nodes sequentially.\nCreate a dummy node to simplify edge cases and build the resulting list.\nIterate until one list is exhausted, then append the remaining nodes from the other list.\nThere is also a recursive approach that can simplify the logic but may use more stack space.",
        "complexity":   "Time Complexity: O(n + m), where n and m are the lengths of the two lists.\nSpace Complexity: O(1) for the iterative solution (O(n + m) stack space for the recursive solution).",
        "solution":     "We use an iterative approach with a dummy node to merge two sorted linked lists. Two pointers traverse list1 and list2; at each step, the node with the smaller value is appended to our merged list. This process repeats until one of the lists is exhausted, after which we append the remaining nodes from the other list. This approach works in one pass over both lists, ensuring linear time complexity and constant extra space usage.",
    },
    "linked-list-cycle": {
        "key_insights": "Use two pointers (slow and fast) to traverse the list.\nThe fast pointer moves two steps at a time and the slow pointer moves one step.\nIf the fast pointer ever meets the slow pointer, a cycle exists.\nIf the fast pointer reaches the end of the list (null), the list has no cycle.",
        "complexity":   "Time Complexity: O(n) where n is the number of nodes in the linked list.\nSpace Complexity: O(1) as only two pointers are used.",
        "solution":     "We use the Floyd's Cycle Detection algorithm (Tortoise and Hare approach). Two pointers, slow and fast, are initialized at the head of the list. The slow pointer moves one step at a time while the fast pointer moves two steps. If the linked list has a cycle, the fast pointer will eventually meet the slow pointer. If the fast pointer reaches the end (i.e., a null pointer), the linked list does not have a cycle.",
    },
    "reverse-linked-list": {
        "key_insights": "The problem can be solved using either an iterative or recursive approach.\nIterative method uses constant space by updating pointers in-place.\nRecursive method leverages the call stack to reverse the list, which could use O(n) space in the worst case.\nBoth approaches traverse each node exactly once, yielding linear time complexity.",
        "complexity":   "Time Complexity: O(n) for both iterative and recursive approaches, where n is the number of nodes.\nSpace Complexity: O(1) for the iterative approach and O(n) for the recursive approach because of the recursion call stack.",
        "solution":     "We can solve the problem by iterating through the list while reversing the next pointers of each node. We maintain two pointers (previous and current) and update them as we traverse the list, so that by the end, the previous pointer is at the new head of the reversed list.\nAlternatively, a recursive approach reverses the rest of the list first and then adjusts the pointers so that the original head becomes the tail.",
    },
    "palindrome-linked-list": {
        "key_insights": "Use two pointers (slow and fast) to reach the middle of the list.\nReverse the second half of the list.\nCompare the nodes from the start of the list to the reversed half.\nThis approach meets the O(n) time and O(1) space follow-up requirement.",
        "complexity":   "Time Complexity: O(n) – each node is visited a constant number of times.\nSpace Complexity: O(1) – only constant extra space is used (in-place reversal).",
        "solution":     "We first use the two-pointer technique to locate the midpoint of the linked list. The slow pointer moves one step at a time while the fast pointer moves two steps. Once the middle is found, we reverse the second half of the list in-place. Finally, we traverse both halves concurrently to compare the node values. If all corresponding values match, the linked list is a palindrome. A potential gotcha is handling the odd number of nodes; in that case, we ignore the middle element when comparing.",
    },
    "design-hashmap": {
        "key_insights": "Use a fixed-size array of buckets where each bucket uses a linked list to handle collisions.\nChoose a simple hash function (e.g., modulo operation) to map a key to its corresponding bucket.\nFor each bucket, traverse the linked list to update an existing key or append a new node if the key does not exist.\nRemoval requires updating pointers in the linked list to bypass the removed node.",
        "complexity":   "Time Complexity: Average O(1) for put, get, and remove; worst-case O(n) when many keys hash to the same bucket.\nSpace Complexity: O(n), where n is the number of key-value pairs stored.",
        "solution":     "We implement the HashMap using an array of fixed size (e.g., 1000). Each array element is the head of a linked list that manages collisions using chaining. The key is hashed using a modulo operation.\nFor the put operation, if a key already exists in the chain, its value is updated; otherwise, a new node is appended.\nThe get operation traverses the chain at the computed bucket index to find and return the corresponding value, or returns -1 if not found.\nThe remove operation also traverses the list, carefully reconnecting the links to remove the target node while handling edge cases such as removal at the head.",
    },
    "middle-of-the-linked-list": {
        "key_insights": "Use two pointers (slow and fast) to traverse the list.\nThe fast pointer moves two steps at a time while the slow pointer moves one step.\nWhen the fast pointer reaches the end, the slow pointer will be at the middle node.\nThis approach naturally handles both odd and even lengths of the list.",
        "complexity":   "Time Complexity: O(n), where n is the number of nodes in the list.\nSpace Complexity: O(1), constant extra space.",
        "solution":     "The solution uses the two-pointer technique:\nInitialize two pointers, slow and fast, at the head.\nTraverse the list, moving slow by one step and fast by two steps during each iteration.\nWhen fast reaches the end or has no next node, slow will be at the middle.\nReturn the slow pointer (the middle node).\nThis strategy ensures linear time complexity and constant space, efficiently finding the middle node even for lists with an even number of elements.",
    },
    "delete-n-nodes-after-m-nodes-of-a-linked-list": {
        "key_insights": "Traverse the linked list while keeping count.\nSkip m nodes (i.e., keep them) and then proceed to delete the next n nodes.\nUpdate pointers to bypass the removed nodes.\nThis is an in-place modification problem, so no additional list structure is needed.\nBe cautious about edge cases when the list has fewer than m or n nodes remaining.",
        "complexity":   "Time Complexity: O(N), where N is the number of nodes in the list.\nSpace Complexity: O(1) since the modification is done in-place.",
        "solution":     "The solution involves iterating through the linked list with a pointer. For each cycle:\nTraverse m nodes and keep them.\nThen, from the (m+1)th node, skip and remove the next n nodes by adjusting the 'next' pointer of the m-th node to point to the node after these n nodes.\nContinue until you reach the end of the list.\nThis in-place approach avoids using extra space and maintains O(N) time complexity.",
    },
    "add-two-numbers": {
        "key_insights": "The digits are stored in reverse order, meaning the first node is the least significant digit.\nA carry may need to propagate through subsequent nodes.\nThe two linked lists might be of different lengths.\nContinue processing until both lists are fully traversed and any remaining carry is handled.",
        "complexity":   "Time Complexity: O(max(n, m)), where n and m are the lengths of the two linked lists.\nSpace Complexity: O(max(n, m) + 1) for the result list in the worst case.",
        "solution":     "The solution iterates through both linked lists node by node. For each pair of corresponding nodes, their values and any carry from the previous operation are summed. The result is decomposed into a current digit (modulo 10) and an updated carry (total divided by 10). A new node is created for the current digit and appended to the resulting linked list. This process continues until all nodes are processed and any leftover carry is added as a final node.",
    },
    "remove-nth-node-from-end-of-list": {
        "key_insights": "Use a dummy node at the start to simplify edge cases, especially when the head is removed.\nUtilize two pointers (fast and slow) and maintain a gap of n+1 nodes between them.\nBy moving both pointers simultaneously, the slow pointer will point to the node immediately before the target node when the fast pointer reaches the end.",
        "complexity":   "Time Complexity: O(L), where L is the length of the list\nSpace Complexity: O(1)",
        "solution":     "We use a two-pointer approach to achieve a one-pass solution. A dummy node is created and linked to the head to handle edge cases gracefully (such as needing to remove the head). The fast pointer is advanced n+1 steps ahead of the slow pointer to maintain the required gap. Then, both pointers are moved simultaneously until the fast pointer reaches the end of the list. At that point, the slow pointer is immediately before the node to be deleted. We then adjust the pointers to remove the target node from the list.",
    },
    "swap-nodes-in-pairs": {
        "key_insights": "Use a dummy node to simplify edge cases, such as when the list is empty or has only one node.\nProcess nodes in pairs and update the pointers accordingly.\nEnsure that the swapping does not lose the connection between the swapped pair and the rest of the list.\nThe solution can be implemented iteratively or recursively.",
        "complexity":   "Time Complexity: O(n) - Each node is processed once.\nSpace Complexity: O(1) - Only constant extra space is used.",
        "solution":     "We use an iterative approach with a dummy node. The dummy node points to the head of the list. We maintain a pointer that traverses the list in pairs. For each pair, we adjust the next pointers to swap the two nodes. By the end, the dummy's next pointer will be the new head of the modified list. The main idea is to keep track of the previous node (prev) before the pair, and then adjust pointers accordingly using temporary variables to hold references to the nodes being swapped.",
    },
    "rotate-list": {
        "key_insights": "Calculate the length of the list and locate the tail.\nUse k modulo length to handle rotations greater than the list size.\nFind the new tail of the list at position (length - k - 1) and update pointers accordingly.\nLink the original tail to the original head to form a circular list, then break the circle at the new tail.",
        "complexity":   "Time Complexity: O(n), where n is the number of nodes in the list, since we traverse the list a few times.\nSpace Complexity: O(1), as the rotation is done in place without extra data structures.",
        "solution":     "We first traverse the list to determine its length and identify the tail node. The effective rotation is calculated by taking k modulo the length of the list. If the result is zero, the list remains unchanged. Otherwise, we locate the new tail node by moving (length - k - 1) steps from the head. The node after the new tail becomes the new head. Finally, we break the link after the new tail and connect the old tail to the original head to finalize the rotated list.",
    },
    "lru-cache": {
        "key_insights": "Use a Hash Table (dictionary) to allow O(1) access to nodes by key.\nUse a Doubly Linked List to record the usage order. The head represents the most recently used element while the tail represents the least recently used.\nWhen a key is accessed or updated, move the corresponding node to the front (head) of the list.\nWhen the cache exceeds capacity, remove the node at the tail end which is the least recently used entry.",
        "complexity":   "Time Complexity: O(1) per get and put operation.\nSpace Complexity: O(capacity), storing key-node pairs and linked list nodes.",
        "solution":     "The solution combines a hash table (or dictionary) and a doubly linked list:\nThe hash table stores keys and corresponding node addresses.\nThe doubly linked list maintains the order of usage with the most recently used items near the head.\nFor get(key), check the dictionary. If found, move the node to the head and return its value; if not, return -1.\nFor put(key, value), if the key exists, update its value and move it to the head. If not, create a new node and add it right after the head. If the capacity is reached, remove the tail node (least recently used) and update the dictionary accordingly.\nDummy head and tail nodes are used to simplify node addition and removal procedures without worrying about edge cases.",
    },
    "sort-list": {
        "key_insights": "Merge sort is well-suited for linked lists; it naturally relies on splitting the list and merging sorted sublists.\nThe slow and fast pointer technique is used to find the middle point of the list.\nA recursive merge sort implementation may use O(log n) space for recursion stack. To achieve O(1) extra space, an iterative bottom-up merge sort approach would be preferred.\nMerging two sorted lists is efficient with linked lists because of their sequential access.",
        "complexity":   "Time Complexity: O(n log n)\nSpace Complexity: O(1) extra space for an iterative bottom-up approach (O(log n) for recursive calls if using recursion)",
        "solution":     "The solution employs the merge sort algorithm tailored for linked lists. The overall steps are:\nUse the slow and fast pointers to find the middle of the list and split it into two halves.\nRecursively sort the two halves.\nMerge the two sorted halves by comparing node values.\nEnsure that during merging, pointers are adjusted properly to build the sorted list.\nFor an O(1) space solution, an iterative bottom-up merge sort can be implemented. However, the recursive merge sort is easier to understand and implement, though it uses O(log n) space on the call stack.",
    },
    "odd-even-linked-list": {
        "key_insights": "Use two pointers: one for odd-indexed nodes and one for even-indexed nodes.\nThe first node is considered odd, and the second node is even.\nTraverse the list once, reassigning the next pointers to segregate odd and even nodes.\nFinally, attach the even list at the end of the odd list.\nThe solution must use O(1) extra space and O(n) time complexity.",
        "complexity":   "Time Complexity: O(n) since the list is traversed once.\nSpace Complexity: O(1) as only a few pointers are used.",
        "solution":     "Maintain two pointers, odd and even, starting from the head and head.next, respectively. As you iterate, update odd.next to point to the next odd element (skipping an even node) and even.next to point to the next even element. Once the end is reached, attach the even list to the end of the odd list. This in-place rearrangement preserves the initial ordering within odd and even groups while meeting the space and time constraints.",
    },
    "plus-one-linked-list": {
        "key_insights": "The digits are stored such that the most significant digit comes first, but addition is easier starting from the least significant digit.\nReversing the list allows simpler management of the carry during addition.\nAfter processing the addition, reversing the list back restores the original order.\nConsider special cases where all digits are 9, requiring a new head node to accommodate an additional digit.",
        "complexity":   "Time Complexity: O(n), where n is the number of nodes in the list\nSpace Complexity: O(1) extra space when using an iterative reversal method. (O(n) if recursion is used)",
        "solution":     "We solve the problem by first reversing the linked list, which converts it to a format where the least significant digit is processed first. We then iterate over the reversed list, adding one and managing any carry. If we encounter a carry at the end of the list, a new node is appended to handle the overflow. Finally, we reverse the list again to restore the original order. This method efficiently handles the addition with constant extra space during processing.",
    },
    "flatten-a-multilevel-doubly-linked-list": {
        "key_insights": "Use a depth-first traversal to process child lists before proceeding to the next pointer.\nWhen a node with a child is found, recursively flatten the child list.\nSplice the flattened child list in between the current node and the next node.\nMaintain previous and next pointers correctly to preserve the doubly linked list properties.\nEnsure that all child pointers are set to null after flattening.",
        "complexity":   "Time Complexity: O(n), where n is the total number of nodes, since each node is processed exactly once.\nSpace Complexity: O(n) in the worst-case due to recursion stack in case of a highly nested list.",
        "solution":     "We can solve this problem using a recursive depth-first search (DFS) approach. The main idea is to traverse the list and, whenever a node with a child pointer is encountered, recursively flatten its child list. Once the child list is flattened, it can be spliced into the main list between the current node and the node originally following it. We then continue processing the next pointer. This ensures that the entire list is flattened in depth-first order. The recursive helper function returns the tail of the flattened segment, which is used to connect the subsequent parts of the list correctly.",
    },
    "design-circular-queue": {
        "key_insights": "Leverage a fixed-size array to store the queue elements.\nUse two pointers: one for the front and one for the rear.\nMaintain a count variable (or use pointer arithmetic) to differentiate between an empty and a full queue.\nUse modulo arithmetic to wrap pointers when they reach the end of the array.\nEnsure all operations (enQueue, deQueue, Front, Rear, isEmpty, isFull) have O(1) time complexity.",
        "complexity":   "Time Complexity: O(1) per operation for enQueue, deQueue, Front, Rear, isEmpty, and isFull.\nSpace Complexity: O(k), where k is the capacity of the circular queue.",
        "solution":     "We implement the circular queue using a fixed-size array alongside two pointers, front and rear, and a counter to keep track of the number of elements in the queue. The front pointer indicates the index of the first element, and the rear pointer indicates the index where the next element will be enqueued. When enqueuing an element, we insert the element at the rear index and then increment the rear pointer modulo the size of the array. Similarly, when dequeuing, we remove the element at the front index and increment the front pointer modulo the size. The counter helps in quickly checking if the queue is empty or full (count equals 0 or count equals capacity, respectively). This design ensures all operations remain O(1) and correctly wraps around, forming a circular structure.",
    },
    "design-linked-list": {
        "key_insights": "Use a dummy head node to simplify insertion and deletion at the head.\nMaintain a size counter to quickly validate index-based operations.\nFor every operation, traverse the list up to the required point since random access is not available.\nEnsure robustness by handling edge cases like negative indices and indices larger than the current length.",
        "complexity":   "Time Complexity:\nget, addAtIndex, and deleteAtIndex operations require O(n) time due to the need to traverse the list.\naddAtHead and addAtTail (if tail pointer is not maintained) are O(n) in worst case; however, these can be optimized further if a tail pointer is added.\nSpace Complexity:\nO(n) space for storing n nodes.",
        "solution":     "The solution employs a singly linked list with a dummy head node to ease edge-case handling. A size counter is maintained for quick index validations. Each node holds a value and a pointer to the next node. Operations operate by traversing the list to the correct insertion or deletion point. This design makes it simple to add the functionality of getting values, inserting at specified indices, and removing nodes.",
    },
    "insert-into-a-sorted-circular-linked-list": {
        "key_insights": "Handle the empty list edge-case by creating a new node that points to itself.\nTraverse the list until the proper insertion point is found.\nConsider the \"turning point\" where the maximum value is followed by the minimum value.\nIf no proper place is found in one full rotation, insert the new node arbitrarily (e.g., after the head).",
        "complexity":   "Time Complexity: O(n) in the worst-case when a full loop is required.\nSpace Complexity: O(1) since we only use a few extra pointers.",
        "solution":     "We traverse the circular linked list starting from the arbitrary given node. For each pair of consecutive nodes (curr and next), we check if the new value can be inserted between them by confirming that:\nThe new value lies between curr and next (curr.val ≤ insertVal ≤ next.val).\nOr if we are at the pivot point where the maximum value meets the minimum value (i.e., curr.val > next.val) and then either the new value is larger than or equal to curr.val (should be appended after the maximum) or less than or equal to next.val (should be inserted before the minimum).\nIf no proper spot is found after one complete loop, we insert anywhere (all values in the list are the same). A pointer reference is updated accordingly to maintain the circular structure.",
    },
    "remove-zero-sum-consecutive-nodes-from-linked-list": {
        "key_insights": "Use a dummy node to handle edge cases such as the removal of nodes at the head.\nCompute a running (prefix) sum while traversing the list.\nUse a hash table (or hashmap) to map each prefix sum to the most recent node where that sum occurred.\nIf the same prefix sum is encountered again, the nodes in between sum to 0 and can be removed by adjusting pointers.\nPerform two passes: the first to record prefix sums and their corresponding nodes, and the second to remove zero-sum sequences.",
        "complexity":   "Time Complexity: O(n) - Two passes over the list.\nSpace Complexity: O(n) - Hash table storing at most n prefix sums.",
        "solution":     "The solution uses a two-pass algorithm with a hash table. In the first pass, compute the prefix sum for each node and store the mapping from prefix sum to the node (overwriting with the latest occurrence). This ensures that for any repeated prefix sum, the nodes in between sum to 0. In the second pass, traverse the list again and use the hash table to skip the zero-sum subsequences by redirecting the next pointer of a node to the next pointer of the stored node for that prefix sum. Key data structures are the hash table and a dummy node to simplify pointer adjustments.",
    },
    "reverse-nodes-in-k-group": {
        "key_insights": "Use a dummy node to simplify edge cases.\nIdentify groups of k nodes using a helper function.\nReverse the nodes in each group using pointer manipulation.\nIf a remaining group has fewer than k nodes, leave it unchanged.\nThe reversal is done in-place with O(1) extra space.",
        "complexity":   "Time Complexity: O(n) where n is the number of nodes (each node is processed a constant number of times)\nSpace Complexity: O(1) (only constant extra space is used)",
        "solution":     "The solution involves iterating through the linked list by identifying groups of k consecutive nodes. For each group:\nFind the kth node from the current starting point. If it does not exist, the remainder of the list is not reversed.\nReverse the nodes in the identified group in-place by adjusting the next pointers.\nReconnect the reversed group with the previous part of the list and update pointers to move to the next group.\nThe approach leverages a dummy node for easier handling of head modifications and uses a helper function to fetch the kth node. The in-place reversal ensures that the extra memory usage is constant.",
    },
    "all-oone-data-structure": {
        "key_insights": "Use a Doubly-Linked List (DLL) where each node holds a unique count value and a set of keys with that count.\nMaintain a Hash Map (keyToNode) to quickly locate the node corresponding to each key.\nInsert new nodes in the DLL when a key’s count is incremented or decremented and the adjacent node does not have the target count.\nRemove nodes from the DLL when no keys remain in that count.\nThe head of the DLL holds the minimum count and the tail holds the maximum count.",
        "complexity":   "Time Complexity: O(1) average for inc, dec, getMaxKey, and getMinKey.\nSpace Complexity: O(n) where n is the number of unique keys in the data structure.",
        "solution":     "The solution uses a combination of a doubly-linked list and hash maps. Each node in the DLL represents a frequency and maintains a set of keys that have that frequency. A hash map maps keys to their corresponding node. When inc(key) or dec(key) is called, we update the key’s frequency and move the key to the appropriate node in the DLL. If the adjacent node with the required frequency doesn’t exist, we create a new node and insert it in the correct position. Removal of a key from a node and deletion of empty nodes ensures that getMinKey() and getMaxKey() can be returned directly from the head and tail of the DLL.",
    },
    "lfu-cache": {
        "key_insights": "Maintain a mapping from key to its value and frequency.\nUse an additional mapping from frequency to keys (maintaining the order of insertion or recent use) for quick eviction.\nKeep a global minFreq variable to track the lowest frequency present in the cache.\nWhen a key is accessed or updated, increase its frequency and relocate it in the frequency mapping.\nEvictions occur by removing the oldest key from the lowest frequency bucket.",
        "complexity":   "Time Complexity: O(1) average for both get and put.\nSpace Complexity: O(capacity), where capacity is the maximum number of keys stored.",
        "solution":     "The solution utilizes two main data structures:\nA hash map (keyToValFreq) that maps keys to a pair (value, frequency).\nA second hash map (freqToKeys) mapping frequencies to an ordered list/set of keys. This data structure preserves the order in which keys were added to allow removal of the least recently used key when multiple keys share the same frequency.\nA global minFreq variable is maintained to quickly identify the bucket with the least frequency. When a key is accessed or updated, the key is removed from its current frequency list and placed in the next higher frequency list. If the frequency list for the minimal frequency becomes empty, minFreq is updated.\nThis design guarantees that both get and put operations execute in constant time on average.",
    },
    "search-insert-position": {
        "key_insights": "The array is sorted, enabling the use of binary search.\nBinary search achieves a time complexity of O(log n) which meets the problem constraints.\nWhen the target is not found during the search, the final low pointer represents the correct insertion index.",
        "complexity":   "Time Complexity: O(log n) due to binary search.\nSpace Complexity: O(1) as no extra space is used.",
        "solution":     "We use binary search to find the target in the sorted array. Initialize two pointers, low and high. At each iteration, compute the mid index. If the value at mid matches the target, return mid. If the target is less than the mid value, adjust the high pointer; if greater, adjust the low pointer. When the iterations finish without finding the target, return the low pointer, which represents the correct insertion index.",
    },
    "sqrtx": {
        "key_insights": "The problem can be solved efficiently using binary search.\nInstead of iterating from 0 to x, binary search narrows down the candidate square roots quickly.\nThe algorithm should handle edge cases like x = 0 and x = 1.\nBy checking mid * mid against x, we can determine if we need to search in the lower or upper half.",
        "complexity":   "Time Complexity: O(log x)\nSpace Complexity: O(1)",
        "solution":     "We use a binary search approach to find the integer square root. Initialize two pointers, low and high, where low is set to 0 and high is set to x. For each iteration, calculate the mid value. If mid squared equals x, we have found the square root. If mid squared is less than x, record mid as a potential answer and move the low pointer to mid + 1. Otherwise, move the high pointer to mid - 1. Continue the search until low exceeds high, then return the last recorded candidate.",
    },
    "first-bad-version": {
        "key_insights": "The API returns true for bad versions and all versions after a bad version.\nThe problem is essentially about finding a transition point in a sorted sequence which calls for a binary search approach.\nThe aim is to minimize the number of calls to isBadVersion by leveraging the ordered nature of the versions.",
        "complexity":   "Time Complexity: O(log n)\nSpace Complexity: O(1)",
        "solution":     "We use binary search to efficiently locate the first bad version. Initialize two pointers, left and right, at 1 and n respectively. At each step, compute the mid point and call isBadVersion(mid). If mid is bad, it indicates that the first bad version may be mid or to its left, so adjust right to mid. Otherwise, adjust left to mid + 1. Continue the process until the pointers converge. The converged pointer will be the first bad version. Key structures include simple integer variables for left, right, and mid, with binary search being the fundamental algorithm to reduce the search space logarithmically.",
    },
    "guess-number-higher-or-lower": {
        "key_insights": "The problem can be effectively solved using binary search since the search space is sorted by nature (1 to n).\nEach API call helps to halve the search space, leading to an efficient solution.\nBe cautious with potential overflow when calculating the mid-point by using low + (high - low) // 2.",
        "complexity":   "Time Complexity: O(log n) since we binary search the range.\nSpace Complexity: O(1) as we use a constant amount of extra space.",
        "solution":     "We use a binary search algorithm to efficiently locate the target number. We initialize two pointers: low (1) and high (n). In each iteration, we compute the mid-point, then call the guess API. Based on the response:\nIf guess(mid) returns 0, we have found the number.\nIf it returns -1, it indicates that our guess is higher than the pick, so we update the high pointer to mid - 1.\nIf it returns 1, it indicates that our guess is lower than the pick, so we update the low pointer to mid + 1.\nThis approach guarantees a logarithmic time solution.",
    },
    "binary-search": {
        "key_insights": "Utilize binary search to achieve O(log n) runtime on a sorted array.\nUse two pointers to maintain the current search region.\nNarrow down the search range by comparing the middle element with the target.\nReturn the index when the target is found; otherwise, return -1 if not found.",
        "complexity":   "Time Complexity: O(log n)\nSpace Complexity: O(1)",
        "solution":     "We use an iterative binary search algorithm. We initialize two pointers, left and right, to represent the current segment of the array that could contain the target. At each iteration, we compute the middle index and compare the element there with the target. If they match, we return the index. Otherwise, based on whether the target is smaller or larger than the middle element, we adjust our pointers to narrow the search to either the left or right half of the array. This process continues until the target is found or until the pointers cross, indicating that the target is not present. No extra data structures are used, making the space complexity O(1).",
    },
    "search-in-rotated-sorted-array": {
        "key_insights": "The array is originally sorted and then rotated at an unknown pivot.\nUse a modified binary search to accommodate the rotation.\nAt every step, determine which half of the array is properly sorted.\nNarrow down the search based on the sorted half and the target’s value.",
        "complexity":   "Time Complexity: O(log n)\nSpace Complexity: O(1)",
        "solution":     "We perform a binary search while accounting for the possibility that the array is rotated. At each iteration, we check if the left-to-mid portion is sorted. If it is, and the target falls within that range, we search the left half; otherwise, we search the right half. If the left half is not sorted, then the right half must be sorted, and similarly, we check if the target is in that sorted half. This approach ensures logarithmic time complexity while handling the rotation correctly.",
    },
    "find-first-and-last-position-of-element-in-sorted-array": {
        "key_insights": "The array is sorted, allowing the use of binary search.\nTwo binary searches can efficiently find the leftmost (first) and rightmost (last) indices of the target.\nHandling edge cases where the target is not present by checking bounds.",
        "complexity":   "Time Complexity: O(log n)\nSpace Complexity: O(1)",
        "solution":     "The approach is to use binary search twice:\nThe first binary search finds the leftmost occurrence of the target by continuing to search in the left half even after finding the target.\nThe second binary search finds the rightmost occurrence by searching in the right half.\nThe algorithm maintains pointers for the current search bounds and narrows the search space using comparisons. If the target is not found during the first binary search, the solution immediately returns [-1, -1]. This method leverages the sorted property of the array, ensuring O(log n) time complexity with constant extra space.",
    },
    "find-minimum-in-rotated-sorted-array": {
        "key_insights": "The rotated sorted array consists of two sorted subarrays.\nA modified binary search can be used to locate the pivot point where the rotation occurs.\nBy comparing the middle element to the rightmost element, we can decide which half of the array contains the minimum.",
        "complexity":   "Time Complexity: O(log n)\nSpace Complexity: O(1)",
        "solution":     "Use a binary search approach. Initialize two pointers, left and right, at the start and end of the array. While left is less than right, calculate the middle index. If the middle element is greater than the right element, then the minimum is in the right half, so move the left pointer to mid + 1; otherwise, the minimum must be in the left half including mid, so adjust the right pointer to mid. When the loop terminates, left equals right and points to the minimum element. This approach leverages the properties of the rotated sorted array to achieve logarithmic time complexity.",
    },
    "longest-increasing-subsequence": {
        "key_insights": "A dynamic programming solution with O(n^2) time complexity exists, but we focus on an optimized approach.\nUsing binary search and a dp array, we can achieve O(n log n) time.\nThe dp array maintains the smallest possible tail for increasing subsequences of various lengths.\nFor each number, perform binary search to decide if it can extend or replace a value in dp.",
        "complexity":   "Time Complexity: O(n log n) where n is the length of nums\nSpace Complexity: O(n) for storing the dp array",
        "solution":     "We use a dynamic programming approach combined with binary search. The idea is to maintain a dp array where dp[i] holds the smallest tail value for any increasing subsequence with length i+1. For each number in the array, we perform a binary search on dp to find its correct insertion position:\nIf the number is larger than all elements, we append it, effectively extending the current longest subsequence.\nOtherwise, we replace the first element in dp that is greater than or equal to the current number to keep the tail as small as possible.\nThis process ensures that dp remains sorted and its length gives the length of the longest increasing subsequence.",
    },
    "design-hit-counter": {
        "key_insights": "Use a sliding window of 300 seconds to count hits.\nSince timestamps are monotonically increasing, outdated hits can be removed from the front.\nA queue (or circular array) is an efficient data structure to maintain and remove expired hit records.\nGrouping consecutive hits at the same timestamp can optimize space.",
        "complexity":   "Time Complexity: O(1) per hit and getHits operation on average, since each hit is enqueued and later dequeued only once.\nSpace Complexity: O(W) where W is the window size (300 seconds) in the worst case.",
        "solution":     "The solution uses a queue (or deque) data structure where each element stores a (timestamp, count) pair. When a hit is recorded, if the last element of the queue shares the same timestamp, then its count is incremented; otherwise, a new element is added. When getHits(timestamp) is called, the algorithm removes elements from the front of the queue whose timestamp is out of the 300-second window relative to the current timestamp, and then sums the counts of the remaining elements to return the result. This ensures that only relevant hits within the past 5 minutes are counted. The design scales well when hit frequency is high because hits at the same timestamp are grouped together instead of inserting individual records.",
    },
    "random-pick-with-weight": {
        "key_insights": "Build a prefix sum array from the weights.\nCalculate the total sum of the weights.\nGenerate a random number in the range [1, total sum] and use binary search to find the corresponding index in the prefix sum array.\nUsing binary search ensures efficient selection in O(log n) time for each call of pickIndex().",
        "complexity":   "Time Complexity: O(n) for constructing the prefix sum array, and O(log n) per pickIndex() call.\nSpace Complexity: O(n) for storing the prefix sum array.",
        "solution":     "The solution leverages a prefix sum array where each entry at index i represents the cumulative sum of weights up to i. After computing the total weight, a random integer is generated between 1 and the total sum (inclusive). A binary search is then used to find the first index where the prefix sum is greater than or equal to the random number. This approach ensures that the index is selected with a probability proportional to its weight.",
    },
    "peak-index-in-a-mountain-array": {
        "key_insights": "The array is strictly increasing up to a peak and then strictly decreasing.\nA binary search technique is applicable because the array exhibits a single transition point (peak) between increasing and decreasing order.\nBy comparing mid and mid+1, we can decide which half of the array to continue the search in.",
        "complexity":   "Time Complexity: O(log(n)) - The binary search approach reduces the search interval by half at each step.\nSpace Complexity: O(1) - Only constant extra space is used.",
        "solution":     "We use a binary search strategy:\nInitialize two pointers, left and right, at the beginning and end of the array.\nIn each iteration, calculate mid. If arr[mid] is less than arr[mid + 1], then the peak is in the right half; otherwise, it is in the left half.\nWhen left equals right, we have found the peak index.\nThis approach leverages the mountain array properties and ensures an O(log(n)) solution.",
    },
    "time-based-key-value-store": {
        "key_insights": "Use a dictionary (hash table) to map each key to a list of (timestamp, value) pairs.\nTimestamps are strictly increasing when setting the values, which allows efficient appending.\nFor getting the value, perform a binary search on the list of timestamps to find the largest timestamp that is <= the queried timestamp.\nIf no valid timestamp exists, return an empty string.",
        "complexity":   "Time Complexity:\nset operation: O(1) per call (amortized).\nget operation: O(log N) per call, where N is the number of timestamps stored for the key.\nSpace Complexity: O(total number of set calls), to store all (timestamp, value) pairs.",
        "solution":     "We use a hash table to store keys mapping to a list of their timestamps and corresponding values. Since the timestamps are inserted in strictly increasing order, the list remains sorted. For the get operation, a binary search is applied to quickly locate the appropriate value for a given timestamp. This approach minimizes the lookup time and efficiently handles the problem constraints.",
    },
    "capacity-to-ship-packages-within-d-days": {
        "key_insights": "The minimal possible capacity is the maximum value in weights; no ship can carry a package heavier than its capacity.\nThe maximum possible capacity is the sum of all weights; this allows shipping all packages in one day.\nThe problem is monotonic: if a capacity C works, any capacity greater than C will also work.\nUse binary search on the capacity range and a greedy approach to simulate shipping for each candidate capacity.",
        "complexity":   "Time Complexity: O(n * log(sum(weights))) where n is the number of packages.\nSpace Complexity: O(1) additional space.",
        "solution":     "The solution employs binary search to find the minimum ship capacity. Start by setting the search bounds: the lower bound is the maximum weight (since each package must be shipped individually if needed) and the upper bound is the sum of all weights (shipping everything in one go). For each midpoint capacity in the binary search, use a greedy method to simulate the shipping process—accumulating weights until adding another package would exceed the candidate ship capacity, at which point you increment the day count. If the number of days required exceeds D, the capacity is insufficient and the lower bound is increased; otherwise, adjust the upper bound. Continue until the bounds converge.",
    },
    "missing-element-in-sorted-array": {
        "key_insights": "The array is sorted and every element is unique.\nFor any index i, the number of missing numbers up to that point can be computed as nums[i] - nums[0] - i.\nWhen missing(i) is less than k, the kth missing element is further to the right.\nUse binary search to find the smallest index where the count of missing numbers is greater than or equal to k.\nIf all missing numbers before the last array element are counted and the kth missing still hasn't been reached, calculate the result by continuing past the last element.",
        "complexity":   "Time Complexity: O(log(n))\nSpace Complexity: O(1)",
        "solution":     "We compute the number of missing elements until an index i with the formula: missing(i) = nums[i] - nums[0] - i. We then use binary search over the indices to find the smallest index where missing(i) >= k. If the binary search index equals the length of the array, it means the kth missing number lies beyond the last element. Otherwise, the kth missing number is derived as: nums[index - 1] + (k - missing(index - 1)). This approach uses binary search for logarithmic time and does not require extra auxiliary data structures.",
    },
    "longest-repeating-substring": {
        "key_insights": "Use binary search to efficiently check for the existence of a repeating substring of a given length.\nApply a rolling hash technique to compute hash values for substrings in O(1) time after initial processing.\nUse a hash set to detect duplicate hash values (indicating a repeating substring).\nAdjust binary search bounds based on whether a repeating substring of the current length exists.\nAlternative approaches include dynamic programming and suffix arrays, but the binary search with rolling hash is efficient and conceptually clean.",
        "complexity":   "Time Complexity: O(n log n) on average, where n is the length of the string. (Each binary search step takes O(n) time for computing rolling hashes.)\nSpace Complexity: O(n) for storing hash values in the hash set.",
        "solution":     "The solution uses binary search to decide on the candidate length L of the repeating substring. For each candidate length L, we use a rolling hash to compute hash values for all substrings of length L and store them in a hash set. If any hash is repeated, it confirms the presence of a repeating substring of length L, and we try a longer length. Otherwise, we decrease the candidate length. The rolling hash is computed using a base (26, for lowercase letters) and a modulus (2^32 to limit integer overflow) which allows efficient hash recomputation for sliding windows. This approach efficiently narrows down the maximum repeating substring length.",
    },
    "find-the-index-of-the-large-integer": {
        "key_insights": "Use binary search to efficiently narrow down the position of the unique large value.\nAt each step, divide the candidate range into two halves (or almost two halves when the range size is odd) and use compareSub to determine which half contains the large integer.\nComparing two subarrays of equal length will reveal the region that contains the extra value via the sum difference.\nHandle odd-length ranges carefully by isolating the middle element if needed.",
        "complexity":   "Time Complexity: O(log n) – because binary search is performed over the range.\nSpace Complexity: O(1) – only a few pointers are maintained, no additional space is required.",
        "solution":     "The solution applies a binary search approach over the array indices. At every iteration:\nCalculate the current segment length.\nIf the length is even, split the segment evenly and compare the left half versus the right half using compareSub.\nIf the length is odd, compare two halves of equal size while ignoring the middle element. If the sums are equal, then the middle index holds the large value.\nUpdate the search bounds based on the result of compareSub.\nContinue until the search space is reduced to one index, which is the answer.\nThis method ensures that you locate the large integer with O(log n) calls to compareSub, well within the limit of 20 calls.",
    },
    "median-of-two-sorted-arrays": {
        "key_insights": "The median divides the sorted array into two equal halves.\nWe can perform binary search on the smaller array to partition both arrays correctly.\nWe need to ensure that every element in the left partition is less than or equal to every element in the right partition.\nEdge cases must be handled when partitions are at the boundaries of either array.",
        "complexity":   "Time Complexity: O(log(min(m, n)))\nSpace Complexity: O(1)",
        "solution":     "The solution uses a binary search technique on the smaller array. We define a partition index i for nums1 and compute the corresponding index j for nums2 such that i + j equals half of the total number of elements. The algorithm checks if the largest element on the left side of nums1 (if any) is less than or equal to the smallest element on the right side of nums2, and vice versa for nums2. If the condition holds, we have found the correct partition and compute the median. If not, adjust the binary search range until the correct partition is found. This partitioning strategy allows us to find the median in logarithmic time.",
    },
    "count-of-smaller-numbers-after-self": {
        "key_insights": "The brute force solution takes O(n²) time, which is too slow for large input arrays.\nAn efficient approach leverages the concept of Merge Sort to both sort the array and count the number of smaller elements by tracking the number of elements that \"jump over\" during the merge step.\nOther efficient methods include Binary Indexed Trees (Fenwick Tree) or Balanced Binary Search Trees.",
        "complexity":   "Time Complexity: O(n log n) on average due to the merge sort based approach.\nSpace Complexity: O(n) for auxiliary arrays used during sorting and counting.",
        "solution":     "We use a modified Merge Sort to sort the array while counting the number of smaller elements to the right. We keep track of the original indices by pairing each element with its index. During the merge process, when an element from the left subarray is placed into the temporary array, we add the count of elements from the right subarray that have already been placed (since these represent smaller elements that were to its right). This efficient divide and conquer strategy guarantees an overall O(n log n) time complexity. Care must be taken to update the counts based on indices and during the merge step.",
    },
    "split-array-largest-sum": {
        "key_insights": "Use binary search over the answer: the candidate value is the maximum subarray sum.\nThe lower bound of the search is max(nums) and the upper bound is sum(nums).\nA greedy approach is used to check if a candidate maximum sum allows splitting nums into at most k subarrays.\nAdjust the binary search bounds based on the feasibility check.",
        "complexity":   "Time Complexity: O(n * log(sum(nums))) – n elements are processed for each binary search iteration.\nSpace Complexity: O(1) – Constant extra space is used.",
        "solution":     "The solution employs binary search to determine the smallest maximum subarray sum possible. We set the initial lower bound to the maximum element and the upper bound to the total sum of the array. For each candidate value (mid), we check if the array can be split into k or fewer subarrays without any subarray sum exceeding mid using a greedy approach. If possible, we try to lower the candidate; if not, we increase the candidate. This continues until the optimal solution is found.",
    },
    "kth-smallest-number-in-multiplication-table": {
        "key_insights": "The table elements are not fully sorted row-wise or column-wise globally, but there is a pattern that can be exploited.\nFor any candidate number x, we can count how many numbers in the table are less than or equal to x by summing over rows: for row i, there are min(x // i, n) numbers.\nBinary search can be used over the range [1, m*n] to find the smallest x such that the count is at least k.\nThe counting function is the key to connecting the candidate value with the order statistic.",
        "complexity":   "Time Complexity: O(m * log(m*n))\nSpace Complexity: O(1)",
        "solution":     "We use binary search over the possible values in the multiplication table, which range from 1 to m*n. For each midpoint x in the binary search, we compute the number of elements in the table that are less than or equal to x by iterating over each row and summing up min(x // i, n). This count tells us whether the kth smallest element is to the left or right of x. We then adjust the binary search boundaries accordingly. The approach leverages the multiplicative structure of the table and the monotonicity of the counting function.",
    },
    "random-pick-with-blacklist": {
        "key_insights": "The valid output range has a total count of available numbers W = n - len(blacklist).\nInstead of repeatedly generating random numbers and checking against a set, we can transform the range into a contiguous block [0, W - 1] and map any blacklist number within that block to a valid number in the upper range.\nMapping is precomputed: for any blacklisted number x in [0, W-1], assign it a valid number from [W, n-1] that is not blacklisted.\nThis approach ensures O(1) pick time after an O(b) precomputation where b = length of blacklist.",
        "complexity":   "Time Complexity: constructor - O(b) for initializing data structures, pick() - O(1) per call.\nSpace Complexity: O(b) for storing the blacklist set and mapping.",
        "solution":     "We compute the count of valid numbers, W = n - len(blacklist). For numbers in the blacklist that are less than W, we need to map them to a valid number from the range [W, n-1]. To do this, we first store all blacklist numbers in the higher range [W, n-1] in a set for quick lookup. Then for each blacklisted value in [0, W-1], we iterate from W upwards to find a number that is not blacklisted and assign it in a mapping. During the pick() function, we generate a random integer r in [0, W-1]. If r is in our mapping, we return its corresponding mapped value; otherwise, we return r as it is already a valid number.",
    },
    "minimize-max-distance-to-gas-station": {
        "key_insights": "The optimal \"penalty\" (i.e., the maximum distance between adjacent stations after placement) can be found using binary search over a continuous distance range.\nFor each candidate distance (mid), simulate the number of gas stations required by iterating over each adjacent station gap and calculating:\nstations_required = ceil(gap / mid) - 1\nIf the total number of required additional stations is less than or equal to k, then mid is a feasible candidate; otherwise, it is too small.\nContinue the binary search until the gap between low and high is within the desired tolerance (1e-6).",
        "complexity":   "Time Complexity: O(n * log(max_gap/precision)), where n is the number of stations.\nSpace Complexity: O(1) (ignoring input storage)",
        "solution":     "We use binary search over the possible maximum distances. The algorithm sets low = 0 and high = the maximum gap between adjacent stations. For a candidate distance mid, we count how many new stations are required for all gaps by computing ceil(gap / mid) - 1. If the total required is within k, we try a smaller candidate; otherwise, we increase the candidate distance. This approach efficiently zooms into the minimum possible maximum distance (penalty).",
    },
    "maximum-profit-in-job-scheduling": {
        "key_insights": "Sort jobs based on their end times to simplify finding non-conflicting jobs.\nUse dynamic programming where the state represents the maximum profit until a given point.\nFor each job, perform a binary search to find the last job that ends before the current job’s start time.\nThe recurrence relation is: dp[i] = max(dp[i-1], profit[i] + dp[index]) where dp[index] is from the latest non-conflicting job.",
        "complexity":   "Time Complexity: O(n log n) due to sorting and binary search for each job.\nSpace Complexity: O(n) for the dp arrays.",
        "solution":     "The solution begins by sorting the jobs by their end times. For each job, a binary search is used to quickly locate the last job that does not conflict with the current job (i.e., whose end time is less than or equal to the current job’s start time). Then, using dynamic programming, the maximum profit is updated by choosing whether to include the current job or skip it. Two parallel arrays (or lists) are maintained: one for the end times of the jobs considered so far, and another to store the corresponding maximum profit at those times. This approach efficiently computes the answer while keeping time complexity within acceptable bounds.",
    },
    "climbing-stairs": {
        "key_insights": "The problem can be solved using dynamic programming because the solution for a given step depends on the solutions of the previous two steps.\nThe recurrence relation is: ways[i] = ways[i-1] + ways[i-2].\nBase cases: when there is 1 step, there is 1 way; when there are 2 steps, there are 2 ways.\nThis is analogous to the Fibonacci sequence, shifting indices accordingly.",
        "complexity":   "Time Complexity: O(n)\nSpace Complexity: O(n)  (can be optimized to O(1) using constant space)",
        "solution":     "We use a dynamic programming approach to build a solution gradually. The idea is that to reach step i, you could have come from step i-1 (taking one step) or from step i-2 (taking two steps). Therefore, the total number of ways to reach step i is the sum of ways to reach step i-1 and i-2. We initialize dp[1] = 1 and dp[2] = 2, and iterate from 3 to n to fill dp[]. Each code solution below uses this approach with comments to explain the logic.",
    },
    "best-time-to-buy-and-sell-stock": {
        "key_insights": "Traverse the list of prices once while keeping track of the minimum price encountered so far.\nAt each day, calculate the profit if you sold the stock on that day.\nUpdate the maximum profit when the current profit exceeds the previous maximum.\nThe optimal solution makes a single pass through the array with constant space usage.",
        "complexity":   "Time Complexity: O(n) - requires one pass through the prices array.\nSpace Complexity: O(1) - only a few extra variables are used.",
        "solution":     "We solve the problem using a simple linear scan. We maintain two key variables: one for the minimum price seen so far and another for the maximum profit calculated so far. As we iterate, we continuously update the minimum price if a lower price is found, and then compute the potential profit at the current price. If this profit exceeds the current maximum profit, we update the maximum profit. The final maximum profit is returned after iterating through all prices.",
    },
    "longest-palindromic-substring": {
        "key_insights": "Every palindrome is centered around a character (for odd-length) or a pair of characters (for even-length).\nExpand from each center until the substring is no longer a palindrome.\nUpdate the longest palindrome when a longer one is found during the expansion.\nThe solution employs a two-pointer technique without extra storage for dynamic programming tables.",
        "complexity":   "Time Complexity: O(n^2) in the worst case as we expand around each center.\nSpace Complexity: O(1), excluding the space required for the output substring.",
        "solution":     "The approach is based on expanding around potential centers of palindromes. For each character in the string, consider it as the center for an odd-length palindrome and the gap between it and the next character for an even-length palindrome. Using two pointers, expand outward until the characters differ. Track the longest palindrome found during these expansions. This method is efficient given the input constraints, and no extra data structures are needed besides variables to store indices or substrings.",
    },
    "maximum-subarray": {
        "key_insights": "Use Kadane's Algorithm to solve the problem in O(n) time.\nKeep track of a running sum (currentSum) and update it with the maximum between the current element and the sum including the current element.\nThe maximum sum encountered during the iteration (maxSum) is the answer.\nA divide and conquer approach also exists, splitting the array and merging solutions, but Kadane's method is optimal for this problem.",
        "complexity":   "Time Complexity: O(n)\nSpace Complexity: O(1)",
        "solution":     "The solution uses Kadane's Algorithm. Iterate over the array while maintaining two variables: currentSum, which tracks the maximum subarray sum ending at the current position, and maxSum, which stores the best sum seen so far. For each element, update currentSum to be either the current element or the sum of currentSum and the current element, whichever is higher. Update maxSum accordingly. This approach efficiently computes the maximum contiguous subarray sum in a single pass.",
    },
    "jump-game": {
        "key_insights": "The greedy approach is optimal for this problem.\nTrack the furthest index reachable as you iterate through the array.\nIf the current index exceeds the maximum reachable index, it is impossible to proceed further.\nThe solution only requires constant extra space.",
        "complexity":   "Time Complexity: O(n) where n is the length of the array.\nSpace Complexity: O(1)",
        "solution":     "The solution uses a greedy algorithm. As you iterate through each array element, you maintain a variable (maxReach) that represents the furthest index you can reach so far. For each index i, if i is greater than maxReach then it is not possible to reach index i and hence return false immediately. Otherwise, update maxReach to the maximum of its current value and i + nums[i]. If you can iterate through the array without i exceeding maxReach, then you can reach the end and return true.",
    },
    "unique-paths": {
        "key_insights": "The robot is constrained to only move right or down.\nAny valid path consists of exactly (m-1) down moves and (n-1) right moves.\nThe problem can be solved using Dynamic Programming where each cell is the sum of the ways to reach the cell from the top and left.\nAlternatively, a combinatorial solution is possible by calculating the number of unique sequences of moves, equivalent to choosing (m-1) moves from (m+n-2) moves.",
        "complexity":   "Time Complexity: O(m\nn)\nSpace Complexity: O(m\nn) – can be optimized to O(n) using a 1D DP array.",
        "solution":     "We can solve the problem using a dynamic programming approach. The idea is to create a 2D DP table where each cell (i, j) represents the number of ways to reach that cell. Since the robot can only come from above or from the left, we update the DP table as follows:\nInitialize dp[0][j] = 1 for all j (only one way to move right in the first row).\nInitialize dp[i][0] = 1 for all i (only one way to move down in the first column).\nFor each other cell, compute dp[i][j] = dp[i-1][j] + dp[i][j-1].\nThe final answer will be at dp[m-1][n-1].\nThe combinatorial alternative computes C(m+n-2, m-1) or C(m+n-2, n-1) using basic factorial arithmetic, which is efficient for the given constraints.",
    },
    "edit-distance": {
        "key_insights": "This is a classic dynamic programming problem.\nUse a 2D DP table where dp[i][j] represents the minimum edit distance between word1[0…i-1] and word2[0…j-1].\nBase cases: converting an empty string to a string of length j requires j insertions, and vice versa.\nTransition: If characters match then dp[i][j] = dp[i-1][j-1], otherwise take minimum among insertion, deletion, and replacement operations and add 1.",
        "complexity":   "Time Complexity: O(m\nn), where m and n are the lengths of word1 and word2 respectively.\nSpace Complexity: O(m\nn) due to the DP table used for storing computed distances.",
        "solution":     "We use a dynamic programming approach by constructing a 2D table dp of size (m+1) x (n+1), where m and n are the lengths of word1 and word2. Each dp[i][j] holds the minimum number of operations needed to convert the first i characters of word1 to the first j characters of word2.\nThe algorithm includes the following steps:\nInitialize the base cases:\ndp[0][j] = j (converting an empty word1 to j characters of word2 requires j insertions)\ndp[i][0] = i (converting i characters of word1 to an empty word2 requires i deletions)\nIterate over each character in word1 and word2:\nIf the current characters are equal, no additional operation is needed and dp[i][j] = dp[i-1][j-1].\nOtherwise, choose the best operation among insertion (dp[i][j-1]), deletion (dp[i-1][j]), or substitution (dp[i-1][j-1]) and add 1.\nThe answer is found in dp[m][n].\nThis method reliably computes the edit distance using a systematic DP approach, ensuring that all subproblems are solved and reused efficiently.",
    },
    "decode-ways": {
        "key_insights": "A digit '0' cannot be mapped on its own; it must be part of \"10\" or \"20\".\nUse dynamic programming where dp[i] represents the number of ways to decode the substring s[0...i-1].\nFor each index i, if the digit at position i-1 is non-zero, add dp[i-1] to dp[i].\nIf the two-digit number formed by s[i-2] and s[i-1] is between 10 and 26, add dp[i-2] to dp[i].\nInitialize dp[0] as 1 (an empty string has one way to be decoded).",
        "complexity":   "Time Complexity: O(n), where n is the length of the string.\nSpace Complexity: O(n), due to the dp array (which can be optimized to O(1)).",
        "solution":     "We solve the problem using a dynamic programming approach. We initialize a dp array where dp[i] indicates the number of ways to decode the substring s[0...i-1]. We set dp[0] = 1 since an empty string is trivially decodable. We loop through the string, and at each step:\nIf s[i-1] is not '0', we add dp[i-1] to dp[i] because s[i-1] can be mapped to a valid letter.\nIf the substring s[i-2:i] (for i >= 2) forms a valid two-digit number between 10 and 26, we add dp[i-2] to dp[i].\nThis cumulative approach yields the total ways to decode the string, and if the string starts with '0' or contains invalid sequences, dp[n] will eventually be 0.",
    },
    "maximum-product-subarray": {
        "key_insights": "Maintain both the maximum and minimum product at the current index because a negative number can swap the max and min.\nA subarray must be contiguous.\nUse dynamic programming to update both the maximum product and the minimum product ending at each index.\nResetting the product in case of a zero is crucial since the product becomes 0.",
        "complexity":   "Time Complexity: O(n) – The solution iterates through the array once.\nSpace Complexity: O(1) – Constant space is used for tracking products.",
        "solution":     "The solution uses an iterative dynamic programming approach. At each index, compute the maximum product (maxEndingHere) and minimum product (minEndingHere) ending at that index. If the current element is negative, swap these two values, because multiplying a negative with a minimum product (possibly negative) might yield a larger product. After processing each element, update the global maximum. This method handles edge cases like zeros and negatives efficiently with constant additional space.",
    },
    "house-robber": {
        "key_insights": "The problem can be solved using Dynamic Programming.\nFor each house, decide whether to rob it or not.\nRecurrence relation: dp[i] = max(dp[i-1], dp[i-2] + currentHouseMoney)\nUse iterative computation to keep track of the optimal solutions without needing extra space for the entire dp array.",
        "complexity":   "Time Complexity: O(n), where n is the number of houses.\nSpace Complexity: O(1), using only two variables to track the previous decisions.",
        "solution":     "We use a dynamic programming approach by iterating through each house in the array. At each step, we consider two scenarios: either we rob the current house (and add its money to the total from two houses ago) or we skip it (keeping the optimal solution up to the previous house). By using two variables to store these intermediate results, we achieve an optimized space complexity of O(1).",
    },
    "paint-house": {
        "key_insights": "The choice for painting each house depends on the previous house's color choices.\nUse dynamic programming to accumulate the cost while ensuring adjacent houses do not have the same color.\nThe state of each house can be represented by the minimum cost when the house is painted a specific color, computed using the minimal costs of the previous house from the other two colors.\nThe problem only requires updating the cost matrix in-place for optimal space usage.",
        "complexity":   "Time Complexity: O(n), where n is the number of houses.\nSpace Complexity: O(1) extra space if the input matrix is modified in place.",
        "solution":     "We use a dynamic programming approach with iterative in-place updating of the cost matrix. For each house starting from the second one, we update the cost for each color by adding the minimal cost of painting the previous house with either of the other two colors. This guarantees we never use the same color for adjacent houses while accumulating the minimal total cost. The final answer is the minimum value for the last house.",
    },
    "best-time-to-buy-and-sell-stock-with-cooldown": {
        "key_insights": "Use Dynamic Programming to solve the problem.\nMaintain three states to represent:\ns0: the state where you are ready to buy (not holding any stock).\ns1: the state where you are holding a stock.\ns2: the cooldown state encountered right after selling a stock.\nTransition between states:\nTransition into s0 can come from staying in s0 or coming out from the cooldown state s2.\nTo enter s1, you either continue holding your stock, or buy a stock using the profit available from s0.\nThe only way to reach s2 is by selling the stock from s1.\nThe final answer is determined by the maximum profit when you are not holding a stock (i.e., max(s0, s2)).",
        "complexity":   "Time Complexity: O(n), where n is the number of days.\nSpace Complexity: O(1), since only a few variables are maintained for the states.",
        "solution":     "The solution involves updating three state variables for each day:\ns0 (ready to buy) is updated as the maximum of the previous s0 and the previous cooldown s2.\ns1 (holding a stock) is updated as the maximum of the previous s1 or buying today with the profit from s0 minus the current price.\ns2 (cooldown) is updated by selling the stock held in s1 (i.e., previous s1 plus the current price).\nAt the end of the iteration, the maximum profit will be in state s0 (ready to buy) or s2 (cooldown), since remaining in s1 means you are holding a stock, which does not constitute a realized profit.",
    },
    "combination-sum-iv": {
        "key_insights": "Use dynamic programming to count the number of valid combinations.\nThe order in which numbers are picked matters, making this akin to permutation counting.\nInitialize a DP table where dp[i] represents the number of ways to form the sum i.\ndp[0] is initialized to 1 since there is one way to form a sum of 0 (by choosing nothing).\nFor each sum i from 1 to target, iterate over nums and update dp[i] based on dp[i - num] when i >= num.\nFor negative numbers, the problem becomes unbounded due to potential infinite combinations; hence a constraint on the combination length is required to make it well-defined.",
        "complexity":   "Time Complexity: O(target * n), where n is the length of nums.\nSpace Complexity: O(target), for the dp array of size target + 1.",
        "solution":     "The problem is solved using a bottom-up dynamic programming approach. We create a dp array where each dp[i] represents the number of possible combinations to reach the sum i. We iterate from 1 up to target, and for each sum, we check every number in nums. If the number can contribute to the current sum (i - num >= 0), we add the number of ways to form the remainder (i - num) to dp[i]. This ensures that every possible permutation is counted. When negative numbers are allowed in the input, the potential for an infinite number of combinations arises unless we impose additional constraints, such as limiting the length of the combination.",
    },
    "partition-equal-subset-sum": {
        "key_insights": "The total sum of the array must be even, otherwise, partitioning into two equal subsets is impossible.\nThe problem can be transformed into a \"subset sum\" problem where we need to check if there exists a subset of numbers that sums to half of the total sum.\nA dynamic programming approach can efficiently decide if such a subset exists.",
        "complexity":   "Time Complexity: O(n * target), where target is half of the total sum.\nSpace Complexity: O(target), using a one-dimensional DP array.",
        "solution":     "First, compute the total sum of the array. If the total is odd, return false since an odd number cannot be equally partitioned. Next, set the target sum to half of the total. Use a dynamic programming array (dp) where dp[i] represents whether a subset with sum i is achievable. Initialize dp[0] as true because a subset sum of 0 is always possible. Then, for each number in the array, iterate through the dp array backwards from target to the number’s value and update dp[i] to true if dp[i - num] is true. Ultimately, if dp[target] is true, the array can be partitioned into two subsets with equal sum.",
    },
    "non-overlapping-intervals": {
        "key_insights": "Sorting is the key: By sorting intervals by their end times, we can greedily choose the optimal set of non-overlapping intervals.\nGreedy approach: Always select the interval that ends the earliest, as it leaves maximum room for subsequent intervals.\nCounting removals: The answer is the total number of intervals minus the count of intervals selected by this greedy approach.\nEdge observation: Intervals that touch at the boundary are not overlapping, so the check uses “>=” instead of “>”.",
        "complexity":   "Time Complexity: O(n log n) due to sorting the intervals.\nSpace Complexity: O(n) if considering the storage for the sorted list (or O(1) if sorting in-place).",
        "solution":     "The solution involves first sorting the intervals by their end times. Initialize a variable to track the end of the last chosen interval. Iterate over the sorted intervals: if the current interval’s start is greater than or equal to the last chosen interval’s end, update the last end and include this interval in the non-overlapping set. Otherwise, count the interval as one that must be removed. The final answer is the removal count, which is computed as the total number of intervals minus the count of non-overlapping intervals selected.",
    },
    "longest-palindromic-subsequence": {
        "key_insights": "The problem can be solved efficiently using dynamic programming.\nA 2D DP table is used where dp[i][j] represents the length of the longest palindromic subsequence in the substring s[i...j].\nIf the characters at the two ends of the substring match (s[i] == s[j]), they contribute to the palindrome length.\nOtherwise, the solution is the maximum sequence length by either excluding the left or the right character.\nThe approach builds the solution from smaller substrings (length 1) to the entire string.",
        "complexity":   "Time Complexity: O(n^2) where n is the length of the string.\nSpace Complexity: O(n^2) due to the usage of a 2D DP table.",
        "solution":     "We solve this problem using a bottom-up dynamic programming approach. We create a 2D table dp[][] such that dp[i][j] stores the longest palindromic subsequence length for the substring s[i...j]. For any single character, dp[i][i] is 1. For substrings of length greater than 1, if the characters at positions i and j (s[i] and s[j]) match, then dp[i][j] = dp[i+1][j-1] + 2. If they do not match, we take the maximum of dp[i+1][j] and dp[i][j-1]. Finally, the dp[0][n-1] holds the result for the entire string.",
    },
    "out-of-boundary-paths": {
        "key_insights": "Use Dynamic Programming (DP) as the state is defined by (current row, current column, moves remaining).\nTransitions are made from a cell to its four adjacent cells.\nIf the ball moves out of bounds, it contributes to one valid path.\nStates can be memoized to avoid recomputation and optimize the recursive solution.",
        "complexity":   "Time Complexity: O(maxMove * m * n), since we compute each state at most once.\nSpace Complexity: O(maxMove * m * n) for the memoization table.",
        "solution":     "We use a recursive dynamic programming (or memoization) approach. Define a function dp(i, j, movesLeft) that returns the number of ways to move out of bounds starting from cell (i, j) with movesLeft moves remaining. For each move from the current cell, check:\nIf the cell is out of bounds, return 1 (base case).\nIf no moves are left (movesLeft == 0) and we are still inside, return 0.\nOtherwise, use recursion to explore all four directions and sum the paths.\nStore results in a memoization table to avoid recomputation, then return the computed value modulo 10^9 + 7.",
    },
    "longest-common-subsequence": {
        "key_insights": "The problem is well-suited for a dynamic programming approach.\nUse a 2D table where dp[i][j] stores the length of the longest common subsequence for text1[0...i-1] and text2[0...j-1].\nIf the characters match at the current position, extend the subsequence by 1 from dp[i-1][j-1]; otherwise, take the maximum from dp[i-1][j] or dp[i][j-1].\nFilling the table in a bottom-up manner ensures that all subproblems are solved optimally.",
        "complexity":   "Time Complexity: O(m * n), where m and n are the lengths of text1 and text2.\nSpace Complexity: O(m * n) for maintaining the 2D DP table. Space can be optimized to O(min(m, n)) if needed.",
        "solution":     "This solution uses a dynamic programming approach with a 2D array (dp) to record the lengths of common subsequences at each substring pair. We initialize a table of dimensions (m+1) x (n+1) with zeros, where m and n are the lengths of text1 and text2. We iterate over both strings, comparing characters; if they match, we set dp[i][j] = dp[i-1][j-1] + 1, otherwise we choose the maximum from dp[i-1][j] and dp[i][j-1]. The final answer is found at dp[m][n].",
    },
    "regular-expression-matching": {
        "key_insights": "Use recursion or dynamic programming to simulate matching as the '*' operator can affect the current and following characters.\nThe '.' operator is a wildcard that matches any single character.\nWhen encountering a '*' in the pattern, consider both skipping the preceding element or consuming one character from the string if it matches.\nUsing memoization helps to save intermediate results and efficiently process overlapping subproblems.",
        "complexity":   "Time Complexity: O(m\nn) where m = length(s) and n = length(p), due to exploring each subproblem combination.\nSpace Complexity: O(m\nn) for storing the memoization table in the dynamic programming or recursive approach.",
        "solution":     "The solution uses a top-down recursive strategy with memoization to efficiently match the string against the pattern. For each character position in s and p, we check if they match directly or if the pattern character is '.' to allow any match. When encountering '\n' in the pattern, two possibilities are explored: either the '\n' stands for zero occurrences of the preceding element, or if there is a valid match, it accounts for one occurrence and we proceed further in the string while maintaining the same pattern index. The memoization aspect prevents repeated computations of the same (i, j) state in the recursion, resulting in an improved overall runtime.",
    },
    "distinct-subsequences": {
        "key_insights": "Use dynamic programming to build the solution by comparing characters of s and t.\nDefine a 2D dp table where dp[i][j] represents the number of ways to form the first j characters of t using the first i characters of s.\nThe recurrence relation:\nIf s[i-1] equals t[j-1], then include both the cases of matching this character as well as skipping it: dp[i][j] = dp[i-1][j-1] + dp[i-1][j].\nIf they do not match, then simply skip the current character of s: dp[i][j] = dp[i-1][j].\nInitialize dp[i][0] = 1 for all i since an empty t can always be formed.\nThe final answer is dp[s.length][t.length].",
        "complexity":   "Time Complexity: O(n * m), where n is the length of s and m is the length of t.\nSpace Complexity: O(n * m), which can be optimized to O(m) with careful row reuse.",
        "solution":     "We use a dynamic programming approach with a 2D table. The table has dimensions (len(s) + 1) x (len(t) + 1). Each cell dp[i][j] stores the number of distinct subsequences from the first i characters of s that match the first j characters of t.\nSteps:\nInitialize dp[i][0] = 1 for every i, because an empty t is a subsequence of any prefix of s.\nIterate over each character of s (outer loop) and for each, iterate over t (inner loop).\nFor every character in s, if it equals the current character in t, update dp[i][j] as the sum of the ways by using or skipping this character. Otherwise, carry over the previous count.\nReturn dp[len(s)][len(t)] as the result.",
    },
    "best-time-to-buy-and-sell-stock-iii": {
        "key_insights": "The problem requires the optimal selection of at most two buy-sell transactions.\nOne effective strategy is to split the problem into two parts:\nFor each day i, calculate the maximum profit obtainable from day 0 to i with one transaction.\nFor each day i, calculate the maximum profit obtainable from day i to the end with one transaction.\nFinally, for each day, the sum of these two profits (left and right) gives a candidate answer.\nAlternatively, a state-based dynamic programming approach can track 4 states corresponding to the two transactions.",
        "complexity":   "Time Complexity: O(n) where n is the number of days\nSpace Complexity: O(n) due to extra arrays used for forward and backward computations (this can be optimized to O(1) with a state variable approach)",
        "solution":     "We use a two-pass dynamic programming approach. In the first pass (forward), we compute an array \"leftProfits\" where leftProfits[i] is the maximum profit from day 0 to day i with one transaction. We do this by keeping track of the minimum price observed so far and calculating the profit if sold on day i. In the second pass (backward), we compute an array \"rightProfits\" where rightProfits[i] is the maximum profit obtainable from day i to the last day with one transaction by tracking the maximum price seen in reverse order. Finally, for every index i, the sum leftProfits[i] + rightProfits[i] represents the maximum profit achievable by completing the first transaction on or before day i and the second transaction starting from day i. The answer is the maximum sum over all i.",
    },
    "palindrome-partitioning-ii": {
        "key_insights": "Precompute a 2D table (isPal) where isPal[i][j] indicates if the substring s[i...j] is a palindrome.\nUse dynamic programming: dp[i] represents the minimum cuts needed for the substring s[0...i].\nFor every index i, iterate over all possible starting indices j. If s[j...i] is a palindrome, update dp[i] using dp[j-1] + 1 (or 0 if j is 0).\nTime complexity is dominated by the O(n^2) palindrome checking and state transitions.",
        "complexity":   "Time Complexity: O(n^2)\nSpace Complexity: O(n^2) (due to the 2D table for palindrome checks, plus O(n) for the dp array)",
        "solution":     "The solution relies on dynamic programming with a two-step approach. First, precompute a 2D table (isPal) where each cell indicates if a substring is a palindrome. This is done in O(n^2) time by leveraging the fact that a substring s[j...i] is a palindrome if the characters at the ends match and the inner substring s[j+1...i-1] is also a palindrome (or if the length is less than 3).\nThen, use a dp array where dp[i] holds the minimum number of cuts needed for the substring s[0...i]. For each i, check every index j (0 ≤ j ≤ i) and if s[j...i] is a palindrome, update dp[i] to be the minimum of its current value and dp[j-1] + 1 (with a special case when j is 0, meaning no cut is needed).\nThis two-phase approach efficiently breaks down the problem and computes the answer while avoiding redundant palindrome checks.",
    },
    "burst-balloons": {
        "key_insights": "Transform the problem by adding virtual balloons with value 1 at both ends to simplify edge handling.\nUse interval dynamic programming where dp[i][j] represents the maximum coins that can be obtained by bursting all balloons between indices i and j (exclusive).\nRealize that the order of bursting balloons matters; choose the last balloon to burst in each interval to break dependencies into independent subproblems.\nThe solution involves iterating over all possible intervals and determining the optimal burst order.",
        "complexity":   "Time Complexity: O(n^3) – Three nested loops: one for the interval length, one for the starting index, and one for the final balloon choice.\nSpace Complexity: O(n^2) – A 2D dynamic programming table is used to store the maximum coins for each interval.",
        "solution":     "The problem is solved using interval dynamic programming. First, extend the input array by adding 1 at both ends to handle edge cases seamlessly. Define a dp table where dp[i][j] represents the maximum coins that can be obtained by bursting all the balloons between i and j (not including i and j, as these are the virtual boundaries). For every interval [i, j], iterate through the possible choices for the last balloon k to burst in that interval. The value for bursting balloon k will be determined by the coins from bursting it last (i.e., nums[i] * nums[k] * nums[j]) plus the maximum coins from the two resulting subintervals: (i, k) and (k, j). By filling the dp table in increasing interval lengths, you build up to the solution for the overall interval that covers the entire (extended) array.",
    },
    "minimum-cost-to-merge-stones": {
        "key_insights": "You can only merge stones into one final pile if (n - 1) is divisible by (k - 1). If not, return -1.\nA dynamic programming approach is used where dp[i][j] represents the minimum cost to merge the subarray stones[i...j] into as few piles as possible.\nA prefix sum array is precomputed to quickly calculate the sum of stones in any subarray.\nWhen a segment can be merged into one pile (i.e. when (length - 1) % (k - 1) == 0), add the sum of stones in that segment to the dp value.",
        "complexity":   "Time Complexity: O(n^3) due to the triple nested loop over segments and splits.\nSpace Complexity: O(n^2) from the dp table and O(n) for the prefix sum array.",
        "solution":     "The problem is solved using a range DP approach. We first verify that merging all piles into one is feasible by checking if (n - 1) % (k - 1) == 0. Next, a prefix sum array is computed to optimize range sum calculations. A 2D dp table dp[i][j] is then used to store the minimum cost to merge stones[i...j]. For each interval, we consider valid split points, updating dp[i][j] by combining sub-problems. If the current segment can form a single merged pile (determined by the condition on the interval length), the cost of merging that segment (the sum of stones) is added to dp[i][j].",
    },
    "valid-parentheses": {
        "key_insights": "The problem can be solved using a stack data structure.\nWhen encountering an opening bracket, push it onto the stack.\nWhen encountering a closing bracket, check if the top of the stack is its matching opening bracket.\nIf the stack is empty before finding a match or if there is a mismatch, the string is invalid.\nAt the end, the string is valid only if the stack is empty, meaning all brackets were properly matched.",
        "complexity":   "Time Complexity: O(n), where n is the length of the string, since we process each character once.\nSpace Complexity: O(n) in the worst case for the stack when all characters are opening brackets.",
        "solution":     "We use a stack to check for valid parentheses. The algorithm iterates through the string, pushing opening brackets onto the stack and popping them when a corresponding closing bracket is encountered. A mapping of closing to opening brackets helps in validating the matching pairs. By ensuring that the stack is empty at the end, we confirm that every opening bracket was properly closed.",
    },
    "implement-queue-using-stacks": {
        "key_insights": "Use two stacks:\nOne (input stack) for enqueue operations.\nThe other (output stack) for dequeue operations.\nWhen performing pop or peek, if the output stack is empty, transfer all elements from the input stack to the output stack. This reversal makes the oldest element accessible.\nEach element is moved at most once between the stacks, ensuring an amortized O(1) cost per operation.\nThe space complexity is O(n) where n is the number of elements in the queue.",
        "complexity":   "Time Complexity: Amortized O(1) per operation\nSpace Complexity: O(n)",
        "solution":     "We maintain two stacks: an input stack for pushing new elements and an output stack for popping or peeking the front element of the queue. When we need to access the front of the queue (either during a pop or peek), we check if the output stack is empty. If it is, we transfer all elements from the input stack to the output stack. This reversal of order allows us to simulate the FIFO nature of the queue. When performing a push, the element is simply added to the input stack.",
    },
    "backspace-string-compare": {
        "key_insights": "'#' represents a backspace operation that deletes the previous character.\nA direct simulation using a stack can reconstruct the final string.\nA two-pointer approach allows comparison from the end of the strings while skipping backspaced characters.\nThe two-pointer technique can achieve O(n) time and O(1) space by processing the strings in reverse.",
        "complexity":   "Time Complexity: O(n) where n is the length of the longer string.\nSpace Complexity: O(1) using the two-pointer strategy (O(n) if using stacks).",
        "solution":     "The solution uses two pointers starting from the end of each string. For each string, maintain a skip counter to account for backspace characters. As you iterate backwards:\nIf a '#' is encountered, increment the skip counter.\nIf a normal character is encountered while the skip counter is positive, skip that character by decrementing the counter.\nOnce a valid character is found, compare the characters from both strings.\nIf all corresponding characters match and both pointers finish processing at the same time, the strings are considered equal.",
    },
    "reorder-list": {
        "key_insights": "Find the middle of the linked list using the fast and slow pointer technique.\nReverse the second half of the list.\nMerge the two halves, alternating nodes from the first half and the reversed second half.\nThe challenge is done in-place, ensuring a linear time solution with constant extra space.",
        "complexity":   "Time Complexity: O(n) – Each of the three main steps (finding the middle, reversing, merging) takes O(n) time.\nSpace Complexity: O(1) – The algorithm is done in-place with only a few pointers.",
        "solution":     "The solution involves three key steps:\nUse two pointers (slow and fast) to identify the middle of the list.\nReverse the second half of the list. This can be done iteratively by adjusting the next pointers.\nMerge the two lists: One starting from the head and the other from the head of the reversed second half. Interleave the nodes by alternating between the two lists until complete.\nThis approach uses in-place pointer manipulation, thus ensuring no extra space is used aside from a few temporary variables.",
    },
    "evaluate-reverse-polish-notation": {
        "key_insights": "Use a stack to process the tokens.\nPush numbers onto the stack.\nWhen an operator is encountered, pop the top two numbers, perform the operation, and push the result back.\nHandle division carefully to ensure it truncates toward zero.\nThe final result remains as the only value in the stack.",
        "complexity":   "Time Complexity: O(n), where n is the number of tokens (each token is processed once).\nSpace Complexity: O(n), in the worst-case scenario where all tokens are numbers and are stored in the stack.",
        "solution":     "We use a stack data structure to evaluate the RPN expression. Iterate through each token:\nIf the token is a number, convert and push it onto the stack.\nIf the token is an operator, pop the top two numbers (be careful about order), perform the corresponding arithmetic operation, and push the result back onto the stack.\nAt the end of processing, the stack contains the result of the expression.\nSpecial attention is needed for division to ensure it truncates toward zero (using language-specific functions when necessary).",
    },
    "min-stack": {
        "key_insights": "Use an auxiliary data structure to track the current minimum element.\nA common approach is to use two stacks: one regular stack for all elements and a second stack to keep track of the minimum values.\nWhen pushing a new element, push it onto the main stack and also update the min stack if the element is less than or equal to the current minimum.\nWhen popping, if the popped element is equal to the top of the min stack, pop from the min stack as well.\nThis design guarantees that all operations (push, pop, top, getMin) run in constant time, O(1).",
        "complexity":   "Time Complexity: O(1) per operation (push, pop, top, getMin)\nSpace Complexity: O(n), where n is the number of elements in the stack, since an additional stack is used to keep track of minima.",
        "solution":     "The solution involves using two stacks:\nMain Stack: Stores all the elements.\nMin Stack: Stores the minimum elements encountered so far.\nWhen an element is pushed, we compare it with the top of the min stack. If it is smaller than or equal to the current minimum, we also push it onto the min stack. For the pop operation, if the element being popped is the same as the top of the min stack, we pop from the min stack as well. This approach ensures that the getMin operation always returns the top element of the min stack, which is the current minimum in constant time.",
    },
    "binary-search-tree-iterator": {
        "key_insights": "Use an iterative in-order traversal strategy with a stack.\nIn-order traversal for BST yields sorted order.\nPre-load the stack with all left descendants from the root.\nAfter returning a node, process its right subtree by pushing its left branch.\nThis method maintains an average time complexity of O(1) per operation and uses O(h) memory, where h is the tree height.",
        "complexity":   "Time Complexity: O(1) on average per operation (amortized)\nSpace Complexity: O(h), where h is the height of the tree",
        "solution":     "We use a stack to simulate an in-order traversal. The process involves initially pushing all left-child nodes from the root into the stack. For each call to next(), we pop the top element (the current smallest node) and then push all the left descendents of the node's right child. This ensures that the next smallest element is always at the top of the stack. The hasNext() function then simply checks if the stack is non-empty.",
    },
    "basic-calculator-ii": {
        "key_insights": "The expression can be processed in one pass while using a stack to handle operator precedence.\nWhen encountering '+' or '-' operators, the current number is directly added or subtracted (as a positive or negative value).\nFor '*' and '/' operators, compute immediately with the last number from the stack and update it.\nHandle spaces and multi-digit numbers by building the number while iterating over the string.\nDivision truncation toward zero must be carefully applied, which aligns with typical integer division in most languages.",
        "complexity":   "Time Complexity: O(n) where n is the length of the expression, since the algorithm processes each character once.\nSpace Complexity: O(n) in the worst-case scenario when all numbers are stored in the stack.",
        "solution":     "We use a stack-based approach. Iterate over each character of the string while maintaining a variable for the current number and a previous operator (initialized to '+'). When an operator or the end of the string is reached, perform an action based on the previous operator:\nFor '+' add the current number to the stack.\nFor '-' add the negative of the current number.\nFor '*' and '/' pop the last number from the stack, compute the result by performing multiplication or division, and then push the result back to the stack.\nAfter processing the entire string, sum the elements in the stack to get the final result. This approach ensures that multiplication and division are handled before addition and subtraction.",
    },
    "verify-preorder-sequence-in-binary-search-tree": {
        "key_insights": "A BST has the property that every node's left subtree contains values less than the node, and every right subtree contains values greater.\nPreorder traversal processes nodes as: root, left subtree, then right subtree.\nUsing a monotonic stack, we can simulate constructing the BST while tracking a lower bound for allowable node values.\nWhen moving to a right child, update the lower bound to ensure subsequent nodes are valid.",
        "complexity":   "Time Complexity: O(n) - Each element is processed once.\nSpace Complexity: O(n) - In the worst-case an explicit stack is used; can be optimized to O(1) by reusing the input array.",
        "solution":     "We use a monotonic stack to simulate the BST construction from the preorder sequence. The process is:\nInitialize a variable lower_bound to -∞.\nIterate through each value in the preorder array:\nIf a value is less than lower_bound, it violates BST properties and the sequence is invalid.\nWhile the stack is not empty and the current value is greater than the element at the top of the stack, pop from the stack and update lower_bound to the popped value. This simulates moving to the right subtree.\nPush the current value onto the stack.\nIf the sequence is processed without violations, return true.\nThis approach efficiently ensures that each value adheres to BST requirements during preorder traversal.",
    },
    "decode-string": {
        "key_insights": "Use a stack or recursion to manage nested encoded patterns.\nProcess the string character by character, accumulating digits for the repeat count.\nWhen encountering a '[', start a new context (recursively or via a new stack frame) for the encoded substring.\nWhen encountering a ']', finish the current context and repeat the accumulated substring as needed.\nCareful handling of multi-digit numbers is essential.",
        "complexity":   "Time Complexity: O(n), where n is the length of the string.\nSpace Complexity: O(n), for the recursion stack or the auxiliary stack used.",
        "solution":     "We can solve this problem using a recursive approach. When we see a digit, we determine the full repeat count. Upon encountering a '[', we recursively read the encoded substring until the corresponding ']'. Each recursive call returns the decoded substring until it hits a ']', which is then repeated based on the previously computed count.\nAlternatively, an iterative solution using a stack is also feasible. As we parse the string, push the current string and number onto the stack when encountering '[', then pop and combine when ']' is reached. This approach correctly handles nested patterns.",
    },
    "ternary-expression-parser": {
        "key_insights": "The ternary operator groups right-to-left. This property can be exploited by processing the string from the end to the beginning.\nUsing a stack allows us to \"collapse\" nested ternary expressions as soon as their three components (condition, true branch, false branch) are available.\nAn iterative approach is efficient and avoids building an explicit syntax tree for the expression.\nThe condition is not stored on the stack; instead, it is encountered immediately preceding the '?' operator when traversing backwards.",
        "complexity":   "Time Complexity: O(n), where n is the length of the expression, as we process each character exactly once.\nSpace Complexity: O(n), due to the use of a stack for storing characters during evaluation.",
        "solution":     "The solution uses an iterative approach with a stack. We traverse the expression string from right to left. For every character:\nIf it is a digit, 'T', or 'F' (and not a ':' or part of a pending operator), we push it onto the stack.\nWhen we encounter a '?' character, we know that the next available items on the stack represent the true and false results of the ternary expression. We then skip the ':' separator that comes between them.\nWe then use the condition (the character immediately preceding the '?') to choose which result to push back on the stack.\nThis method “collapses” each ternary expression into a single result, and by the time we finish traversing the string, the stack contains the final evaluated result.",
    },
    "find-permutation": {
        "key_insights": "The answer is a permutation of numbers from 1 to n, where n = len(s) + 1.\nA common strategy is to use a stack to reverse the segments when a decrease ('D') is encountered.\nWhen an 'I' is encountered (or at the end), pop all numbers from the stack to output them in reverse order, ensuring the smallest lexicographical order.\nThis greedy approach guarantees that as soon as a rising pattern is detected, we “flush” the pending decreases correctly.",
        "complexity":   "Time Complexity: O(n) – Each element is pushed and popped exactly once.\nSpace Complexity: O(n) – For the stack and the output array.",
        "solution":     "We use a greedy approach with a stack. Iterate from 0 to n (where n = len(s) + 1):\nPush the current number (i+1) onto the stack.\nIf the current character is 'I' or we have reached the end of the string, pop all elements from the stack and append them to the result.\nBy doing so, segments corresponding to series of 'D's are reversed, while 'I's trigger the flush, ensuring that the final permutation is the lexicographically smallest.",
    },
    "asteroid-collision": {
        "key_insights": "Use a stack to keep track of the surviving asteroids.\nOnly a collision happens when a right-moving asteroid (positive) is on the stack and a left-moving asteroid (negative) is encountered.\nCompare the absolute values to decide if one or both asteroids explode.\nContinue processing collisions until no further collisions are possible with the current asteroid.",
        "complexity":   "Time Complexity: O(n) where n is the number of asteroids, since each asteroid is pushed and popped at most once.\nSpace Complexity: O(n) in the worst case, when no collisions occur and all asteroids remain on the stack.",
        "solution":     "The solution uses a stack to simulate the asteroid collision process. For each asteroid in the input:\nIf it is moving to the right or the stack is empty, simply add it to the stack.\nIf it is moving to the left, check the asteroid on top of the stack:\nIf the top asteroid is moving to the right, they might collide.\nCompare sizes:\nIf the left-moving asteroid is larger (in absolute terms), pop the stack and continue checking.\nIf both are equal, pop the stack and do not add the current asteroid.\nIf the right-moving asteroid is larger, the current asteroid explodes.\nIf no collision occurs for the current asteroid, push it onto the stack.\nFinally, the stack represents the surviving asteroids order.",
    },
    "daily-temperatures": {
        "key_insights": "Use a monotonic stack to keep track of indices of days with unresolved warmer temperatures.\nAs you iterate through the temperatures, resolve previous days that have found a warmer temperature.\nThe index differences yield the number of days waited until a warmer temperature.",
        "complexity":   "Time Complexity: O(n), where n is the number of temperatures, because each index is pushed and popped from the stack at most once.\nSpace Complexity: O(n) for storing the stack and the answer array.",
        "solution":     "The solution uses a monotonic stack to track the indices whose next warmer temperature has not been found yet. As you iterate through the temperature list, compare the current temperature with the temperature at the index stored at the top of the stack. If the current temperature is higher, it means you've found a warmer day for the day represented by the index from the stack. Pop that index, calculate the difference between the current index and the popped index, and store the result. Push the current index onto the stack for future comparisons. This approach efficiently finds the answer without nested iterations.",
    },
    "maximum-width-ramp": {
        "key_insights": "We need to identify pairs (i, j) where i < j and nums[i] <= nums[j].\nA monotonic decreasing stack can be constructed to maintain candidate starting indices.\nBy iterating from left to right, we push indices onto the stack only when they have a smaller value than the last.\nThen, a backward pass from the end of the array allows us to efficiently match each element with the smallest possible candidate index from the stack.",
        "complexity":   "Time Complexity: O(n)\nSpace Complexity: O(n)",
        "solution":     "The solution is built in two phases:\nBuild a decreasing stack of indices by iterating through the array (only push an index if its value is less than the value at the current top of the stack).\nIterate through the array backwards. For each index j, check and pop elements from the stack while nums[j] is greater than or equal to the nums value at the popped index. For each valid ramp found, compute the width (j - popped index) and update the maximum width accordingly.\nThis approach guarantees we examine each element a constant number of times ensuring an efficient solution.",
    },
    "two-sum-bsts": {
        "key_insights": "Traverse the first tree to collect all node values.\nTraverse the second tree to check for each node if the complement (target - current node value) exists in the first tree’s values.\nUsing a hash set for the first tree values allows efficient O(1) lookups.\nThe BST property is not essential for the hash set solution, but can be exploited if using iterator techniques for a two-pointer approach.",
        "complexity":   "Time Complexity: O(n + m) where n and m are the number of nodes in the first and second trees respectively.\nSpace Complexity: O(n) for storing the node values from the first tree (in worst-case scenario).",
        "solution":     "We perform a DFS (or any tree traversal) on the first BST to collect all its values in a hash set. Then, we traverse the second BST and for each node, we calculate the complement (target - node value) and check whether this complement exists in the hash set. If we find such a pair, we return true. If no such pair exists after traversing the second tree completely, we return false.\nThis approach is straightforward and leverages extra space (the set) for constant time lookups, yielding an overall linear time complexity relative to the number of nodes.",
    },
    "buildings-with-an-ocean-view": {
        "key_insights": "Processing the buildings from right to left simplifies the problem because the rightmost building always has an ocean view.\nKeep track of the highest building encountered while iterating from right to left.\nA building has an ocean view if its height is greater than the maximum height seen so far.\nAppend indices meeting the criteria, then reverse the order of indices for the solution since they were collected in reverse order.",
        "complexity":   "Time Complexity: O(n) where n is the number of buildings.\nSpace Complexity: O(n) for storing the result in the worst-case scenario.",
        "solution":     "Iterate over the \"heights\" array from right to left while maintaining a variable to store the maximum height encountered so far. For each building, if its height is greater than this maximum height, add its index to the results list and update the maximum height. Since indices are collected in reverse order, reverse the list before returning it. This approach ensures that each building is visited only once.",
    },
    "longest-valid-parentheses": {
        "key_insights": "Use a stack to keep track of indices where potential valid substrings begin.\nStart by pushing a dummy index (-1) to handle edge cases.\nFor every '(', push its index onto the stack.\nFor every ')', pop from the stack. If the stack becomes empty, push the current index as a new base. Otherwise, compute the length of the valid substring using the current index minus the top index of the stack.\nAn alternative is the dynamic programming approach where dp[i] represents the length of the longest valid substring ending at index i.",
        "complexity":   "Time Complexity: O(n) where n is the length of the string.\nSpace Complexity: O(n) for the stack data structure.",
        "solution":     "The stack-based solution works by maintaining indices for unmatched parentheses. Initially, a dummy index (-1) is pushed onto the stack to serve as the base for calculating subsequent valid substring lengths. As we iterate through the string:\nIf the character is '(', we push its index onto the stack.\nIf the character is ')', we pop from the stack. If the stack is empty afterwards, that means there is no valid starting position, so we push the current index to reset the base. Otherwise, we calculate the length of the current valid substring by subtracting the current index with the new top index of the stack and update the maximum length accordingly.",
    },
    "trapping-rain-water": {
        "key_insights": "The water trapped at any index is determined by the minimum of the maximum height on its left and right minus the height at that index.\nA two-pointers approach can efficiently solve the problem in one pass by maintaining left and right boundaries.\nAlternative approaches include dynamic programming with pre-computed left max and right max arrays, or using a monotonic stack to determine boundaries.",
        "complexity":   "Time Complexity: O(n)\nSpace Complexity: O(1) when using the two-pointer approach",
        "solution":     "We use a two-pointers strategy by initializing pointers at the beginning and the end of the array. Keep track of the maximum height seen so far from the left (left_max) and the right (right_max). At each step, move the pointer with the lesser height because the water trapped is determined by the shorter boundary. Compute the water at that position as the difference between the boundary (left_max or right_max) and the current height, accumulating it into the total. This approach ensures linear time scanning with constant extra space.",
    },
    "largest-rectangle-in-histogram": {
        "key_insights": "Each bar can potentially extend left and right as long as the neighboring bars are not shorter.\nA monotonic (increasing) stack can be used to efficiently track indices of bars.\nWhen encountering a bar lower than the one on the top of the stack, pop the stack and calculate the area of the rectangle formed with the popped bar height.\nA dummy bar (of height 0) is appended at the end to ensure all bars are processed.",
        "complexity":   "Time Complexity: O(n)\nSpace Complexity: O(n)",
        "solution":     "The solution employs a monotonic stack to traverse the histogram once. For each bar, while the current bar's height is less than the height at the top index of the stack, it pops from the stack and calculates the area using the popped height as the smallest bar. The width for the rectangle is determined by the difference between the current index and the index from the stack (or the current index if the stack is empty). This effectively finds the maximum rectangular area possible. A sentinel value (0) is added at the end of the histogram to flush any remaining bars from the stack.",
    },
    "basic-calculator": {
        "key_insights": "Use a stack to save previous results and signs when encountering parentheses.\nProcess the string left-to-right by accumulating multi-digit numbers and applying the current sign.\nWhen encountering a '(', push the current result and sign onto the stack and reset them.\nWhen encountering a ')', complete the current sub-expression then pop the previous state to incorporate the computed value.\nHandle whitespaces by simply skipping them.",
        "complexity":   "Time Complexity: O(n) where n is the length of the input string, since we process each character once.\nSpace Complexity: O(n) in the worst-case scenario due to the use of a stack for nested parentheses.",
        "solution":     "The solution uses an iterative approach with a stack:\nInitialize variables: result to keep the running total, sign to handle addition/subtraction, and index to iterate over the string.\nAs you iterate, build multi-digit numbers by checking for consecutive numerical characters.\nAdjust the total by adding the product of the current number and its sign.\nWhen encountering a '(', push the current result and sign onto the stack, then reset results and sign for the new sub-expression.\nWhen encountering a ')', complete the sub-expression by popping the previous result and sign, then combine.\nSkip over spaces.\nThe final result is the computed value after the end of the string.\nThis approach effectively simulates recursion and correctly handles nested expressions and negation.",
    },
    "sliding-window-maximum": {
        "key_insights": "Use a deque (double-ended queue) to maintain the indices of potential maximum elements in a decreasing order.\nEnsure that the deque always has the current window's indices by removing elements that fall outside the current window.\nOnly add elements that are greater than the ones at the back of the deque, thus preserving a monotonic decreasing order.\nThe maximum for the current window is at the front of the deque.",
        "complexity":   "Time Complexity: O(n) where n is the number of elements in nums, since each element is processed at most twice.\nSpace Complexity: O(k), as the deque stores indices for at most k elements at any time.",
        "solution":     "The solution uses a monotonic deque to maintain indices of potential maximum elements in the current window. As the window slides, we:\nRemove indices from the front if they are out of the window.\nRemove indices from the back if the current element is larger, because they cannot be the maximum if the current element is in the window.\nAppend the current index.\nAfter the first k elements, the element at the front of the deque is the maximum for that window.\nThis approach ensures that each element is pushed and popped from the deque only once, achieving an overall O(n) time complexity.",
    },
    "max-stack": {
        "key_insights": "Use a doubly-linked list to support fast insertion and removal from the stack.\nMaintain a balanced tree (or ordered dictionary/multiset) to quickly locate the maximum element in O(log n) time.\nMap each value to its corresponding node(s) in the doubly-linked list, so that when popMax is called, you can immediately remove the corresponding node from the list.\nThe double-linking allows removal of arbitrary nodes in O(1), once you have the reference.",
        "complexity":   "Time Complexity:\npush: O(log n) due to insertion in the ordered structure.\npop: O(1) for removal from the doubly-linked list, plus O(log n) for updating the tree.\ntop: O(1)\npeekMax: O(1) or O(log n) depending on the tree implementation.\npopMax: O(log n) for looking up and removal from the tree, O(1) for doubly-linked list deletion.\nSpace Complexity:\nO(n) for storing all nodes in the doubly-linked list and the tree structure mapping values to nodes.",
        "solution":     "We use two data structures:\nA doubly-linked list to store the elements in stack order. This supports fast push and pop operations and allows O(1) removal when given a reference.\nAn ordered structure (like a balanced BST, TreeMap in Java, or SortedList in Python) that maps each value to a list of nodes that hold that value in the linked list. This allows us to quickly determine the maximum element. When there are duplicates, we remove the one closest to the top by storing nodes in order.\nThe trick is to keep these two structures synchronized. When an element is pushed, add its node to both the doubly-linked list and the ordered structure. When an element is removed (either via pop or popMax), remove its node from both structures.",
    },
    "basic-calculator-iii": {
        "key_insights": "Use recursion to handle nested expressions defined by parentheses.\nA stack can be used to correctly evaluate the expression and respect operator precedence.\nMultiplication and division have higher precedence than addition and subtraction.\nWhen encountering an open parenthesis, recursively compute its value until a closing parenthesis is found.\nUse immediate calculation for multiplication and division by updating the top of the stack before moving to the next operator.",
        "complexity":   "Time Complexity: O(n) where n is the length of the string, as every character is processed.\nSpace Complexity: O(n) due to the recursion stack or auxiliary stack used for intermediate results.",
        "solution":     "We use a recursive approach combined with a stack to solve the problem. As we iterate through the string:\nConvert digits into numbers.\nWhen encountering an operator or parenthesis, process the previously accumulated number based on the preceding operator.\nFor multiplication or division, pop the last number from the stack, apply the operation, and push the result back.\nWhen a '(' is encountered, recursively evaluate the substring until the corresponding ')'.\nAfter processing all tokens, sum the values in the stack to get the final result.\nThis method correctly handles both operator precedence and nested expressions.",
    },
    "maximum-frequency-stack": {
        "key_insights": "Use a hash map to count the frequency of each element.\nMaintain another hash map that maps frequencies to stacks (lists) of elements.\nTrack the current maximum frequency to enable O(1) retrieval of the most frequent element.\nWhen pushing an element, update its frequency and add it to the corresponding frequency stack.\nWhen popping an element, remove it from the stack corresponding to the maximum frequency and update the frequency count; if that stack becomes empty, decrease the current maximum frequency.",
        "complexity":   "Time Complexity: O(1) per push and pop.\nSpace Complexity: O(n), where n is the number of elements pushed onto the stack.",
        "solution":     "The solution involves using two main data structures:\nA hash map (freq) to store the frequency count for each value.\nA hash map (group) that maps a frequency to a stack (list) of values that have that frequency.\nDuring a push:\nIncrement the frequency count of the value.\nPush the value onto the stack corresponding to its updated frequency.\nUpdate the maximum frequency if necessary.\nDuring a pop:\nPop the top element from the stack corresponding to the current maximum frequency.\nDecrement its frequency count in the freq map.\nIf the frequency stack becomes empty after popping, decrement the current maximum frequency.\nThis design enables the retrieval of the most frequent element in constant time while dealing with frequency ties by returning the element closest to the top.",
    },
    "same-tree": {
        "key_insights": "Use recursion to compare nodes in both trees.\nIf both nodes being compared are null, they are the same.\nIf one node is null while the other is not, the trees differ.\nCheck if current nodes’ values match, then recursively verify the left and right subtrees.",
        "complexity":   "Time Complexity: O(n), where n is the number of nodes in the trees, since every node is visited once.\nSpace Complexity: O(n) in the worst-case (unbalanced tree), due to recursion call stack usage. In a balanced tree, the space would be O(log n).",
        "solution":     "The problem can be solved using a recursive approach (Depth-First Search) where we compare the current nodes from both trees. If the current nodes are both null, they match; if one is null, then the trees are not identical. For non-null nodes, check if the values are equal. Then, recursively apply the same logic on the left and right children of the nodes. This approach ensures that both the structure and the node values are identical across the two trees.",
    },
    "symmetric-tree": {
        "key_insights": "Use recursion (or iteration) to compare pairs of nodes.\nFor two trees (or two nodes) to be mirror images:\nTheir root values must be the same.\nThe left subtree of one must match the right subtree of the other.\nThe right subtree of one must match the left subtree of the other.\nAlternatively, iterative approaches using a queue can be applied to compare the nodes in pairs.",
        "complexity":   "Time Complexity: O(n), where n is the number of nodes in the tree, since we traverse each node once.\nSpace Complexity: O(h) for recursive call stack (worst-case O(n) for a skewed tree) or O(n) for the iterative approach using a queue.",
        "solution":     "We solve the problem using recursion by defining a helper function that takes two nodes and checks if they are mirrors of each other. The helper function:\nReturns true if both nodes are null.\nReturns false if one node is null or if their values differ.\nRecursively compares the left child of the first node with the right child of the second node, and vice-versa.\nThis method ensures each comparison verifies symmetry at every level. A similar logic can be applied iteratively by using a queue to compare pairs of nodes.",
    },
    "maximum-depth-of-binary-tree": {
        "key_insights": "Use a recursive strategy (DFS) to traverse the binary tree.\nAt each node, compute the maximum depth of its left and right subtree.\nThe maximum depth at the current node is 1 (current node) plus the maximum of the left and right subtree depths.\nAlternatively, an iterative approach using BFS (level-order traversal) can be used.",
        "complexity":   "Time Complexity: O(n), where n is the number of nodes, since every node is visited once.\nSpace Complexity: O(h) for recursion stack in DFS, where h is the height of the tree (worst-case O(n) for a skewed tree); O(n) for BFS in the worst-case scenario if the tree is balanced.",
        "solution":     "The problem is approached using Depth-First Search (DFS) with recursion. The idea is to visit every node and calculate the maximum depth of the left and right subtrees, then add 1 for the current node. This method naturally handles the base case when a null node (empty tree) is reached by returning 0. Alternatively, a Breadth-First Search (BFS) approach can determine the tree level by level, with the total number of levels equaling the maximum depth. Key data structures include recursion call stack for DFS or a queue for BFS.",
    },
    "convert-sorted-array-to-binary-search-tree": {
        "key_insights": "The array is sorted in ascending order, which allows the use of a divide and conquer strategy.\nChoosing the middle element of the array as the root ensures that the left and right subtrees are roughly equal in size, yielding a balanced BST.\nA recursive approach is natural for building the tree: the base case is when the subarray is empty.\nThe algorithm splits the array into two halves to recursively build the left and right subtrees.",
        "complexity":   "Time Complexity: O(n) - Every element is processed exactly once.\nSpace Complexity: O(n) - O(n) space is used to store the BST, and additional O(log n) space may be used for the recursion stack.",
        "solution":     "We use a divide and conquer approach with recursion. The main idea is to select the middle element of the current subarray as the root node so that the left subarray elements form the left subtree and the right subarray elements form the right subtree, ensuring the tree is balanced. The recursion continues until the subarray is empty, which serves as the base case. This strategy naturally maintains the BST property since all elements in the left subarray are smaller than the root, and all in the right subarray are larger.",
    },
    "balanced-binary-tree": {
        "key_insights": "Use a bottom-up DFS (Depth-First Search) traversal to compute the height of each subtree.\nIf any subtree is found unbalanced, propagate an error indicator (e.g., -1) upward, allowing early termination.\nAn empty tree is considered balanced.\nThe key check is to ensure that for each node, the absolute difference between the heights of its left and right subtrees is no more than 1.",
        "complexity":   "Time Complexity: O(n) where n is the number of nodes in the tree, since we visit each node once.\nSpace Complexity: O(h) where h is the height of the tree, which corresponds to the recursion stack (O(n) in the worst-case of a skewed tree).",
        "solution":     "The solution uses a recursive DFS approach. A helper function recursively computes the height of a subtree. If a subtree is found to be unbalanced (the height difference between its left and right subtrees exceeds 1), the helper returns a special value (e.g., -1) to indicate that the subtree is unbalanced. This value is then propagated up the recursion to terminate further unnecessary computations. The main function checks the result of the helper function and returns true if the tree is balanced (i.e., the helper did not return -1) and false otherwise.",
    },
    "path-sum": {
        "key_insights": "Use a depth-first search (DFS) strategy to traverse the tree from the root to the leaves.\nAt each node, subtract the node’s value from the targetSum.\nCheck if the node is a leaf; if yes and the remaining targetSum equals the node's value then a valid path is found.\nIf the tree is empty, return false as no path exists.",
        "complexity":   "Time Complexity: O(N) where N is the number of nodes, since every node may need to be visited.\nSpace Complexity: O(H) where H is the height of the tree, accounting for the recursion stack (worst-case O(N) for a skewed tree).",
        "solution":     "The approach uses a recursive depth-first search. The algorithm subtracts the current node’s value from targetSum as it moves down the tree. When it reaches a leaf, it checks if the modified targetSum is equal to the leaf's value. If yes, it returns true. The recursion continues until either a valid path is found or all paths are exhausted. The primary data structure used here is the call stack for recursion.",
    },
    "invert-binary-tree": {
        "key_insights": "The problem can be solved using recursion (depth-first search) by swapping the children of every node.\nAlternatively, an iterative approach (using a queue for breadth-first search) can be used.\nThe recursion terminates when a null node is encountered.\nThe operation is performed in-place, modifying the original tree structure.",
        "complexity":   "Time Complexity: O(n) where n is the number of nodes, since each node is visited once.\nSpace Complexity: O(h) where h is the height of the tree, which is the recursion stack space in the recursive approach. In worst-case (skewed tree), this could be O(n).",
        "solution":     "We solve the problem using recursion (depth-first search). The approach is to:\nCheck the base case: if the node is null, simply return null.\nRecursively invert the left subtree and the right subtree.\nSwap the left and right children of the current node.\nReturn the current node after the swap.\nThe recursive approach elegantly handles all nodes and ensures the inversion process covers the complete tree.",
    },
    "closest-binary-search-tree-value": {
        "key_insights": "BST property allows us to traverse the tree efficiently by comparing the target with node values.\nWe can iteratively traverse the tree, moving left if the target is smaller than the current node’s value and moving right if greater.\nAt each node, calculate the absolute difference between node's value and the target, and update the answer if the difference is smaller—or equal with a smaller candidate value.\nThis approach minimizes the search area by leveraging the inherent ordering in BSTs.",
        "complexity":   "Time Complexity: O(h) on average, where h is the height of the tree (O(log n) for a balanced tree, and O(n) in the worst-case scenario for a skewed tree).\nSpace Complexity: O(1) for an iterative solution (or O(h) if using recursion due to the call stack).",
        "solution":     "We solve the problem by performing an iterative traversal of the BST. Starting at the root, we maintain a variable to store the value closest to the target. For every node encountered, we:\nCompute the absolute difference between the node’s value and the target.\nUpdate the stored closest value if the computed difference is less than the current smallest difference; if the difference is the same, choose the smaller node value.\nDepending on whether the target is less than or greater than the current node's value, move to the left or right child accordingly.\nThis approach efficiently defines a search path that is consistent with the properties of a BST.",
    },
    "find-mode-in-binary-search-tree": {
        "key_insights": "The BST in-order traversal yields a sorted order, which helps in counting consecutive duplicates.\nA two-pass in-order traversal can be used: one pass to determine the maximum frequency (maxCount) and a second pass to collect all values that match this frequency.\nThe solution can be implemented without extra space (apart from recursion stack) by leveraging constant space counters and state variables.",
        "complexity":   "Time Complexity: O(n), where n is the number of nodes, because each node is processed twice (once in each traversal).\nSpace Complexity: O(h) where h is the height of the tree due to the recursion stack. O(1) extra space is used aside from this.",
        "solution":     "We perform an in-order traversal of the BST twice. The first traversal determines the maximum frequency of any value in the BST by comparing the current node's value with the previously visited node. Since the in-order traversal processes nodes in sorted order, duplicate values are consecutive, making the counting simple. After calculating maxCount, we reset our state and perform a second in-order traversal to collect all node values with frequency equal to maxCount. The key trick is to leverage the BST property (sorted in-order traversal) to count frequencies in one pass without using additional data structures like hash maps.",
    },
    "diameter-of-binary-tree": {
        "key_insights": "Use depth-first search (DFS) to explore the tree.\nThe diameter at a node is the sum of the depths of its left and right subtrees.\nTrack the maximum diameter found during the DFS traversal.\nRecursively calculating the depth of subtrees provides an efficient solution.",
        "complexity":   "Time Complexity: O(n), where n is the number of nodes in the tree.\nSpace Complexity: O(n), in the worst-case scenario due to the recursion stack (skewed tree).",
        "solution":     "The solution employs a DFS traversal of the binary tree. At every node in the tree, compute the depth of the left and right subtrees. The sum of these depths gives the potential diameter (longest path through that node). Update a global maximum diameter variable if this sum exceeds the current maximum. Finally, return the maximum diameter after completing the DFS.",
    },
    "subtree-of-another-tree": {
        "key_insights": "Use Depth-First Search (DFS) to visit each node in the main tree.\nAt each node, check if the subtree starting from that node is identical to subRoot.\nA helper function is used to compare the structure and node values of two trees.\nConsider recursion as the primary approach to traverse and compare trees.\nEarly exit when a mismatch is found can optimize average-case performance.",
        "complexity":   "Time Complexity: O(m * n) in the worst case, where m is the number of nodes in the main tree and n is the number of nodes in the subRoot tree.\nSpace Complexity: O(max(depth of main tree, depth of subRoot)) due to recursive call stack.",
        "solution":     "We solve the problem by performing a DFS traversal on the main tree. For each visited node, a helper function checks whether the subtree starting at that node is identical to subRoot by comparing node values and recursively checking both left and right subtrees. This method leverages recursion effectively and provides early termination if mismatches occur, allowing for efficient traversal and comparison.",
    },
    "validate-binary-search-tree": {
        "key_insights": "Every node must adhere to a range: all nodes in the left subtree should be less than the current node, and all nodes in the right subtree should be greater.\nA recursive depth-first search approach works well by passing down the valid range (lower and upper bounds) for each node.\nUsing the limits (negative and positive infinity) simplifies handling edge cases at the tree root.\nAn in-order traversal would alternatively yield a sorted order for node values, but updating ranges directly is more intuitive for recursive implementation.",
        "complexity":   "Time Complexity: O(N), where N is the number of nodes, as each node is visited once.\nSpace Complexity: O(H), where H is the height of the tree, due to the recursion stack.",
        "solution":     "We use a recursive approach to validate the BST property. We maintain two variables, low and high, representing the valid range for the current node’s value. For the left child, the valid range becomes (low, node.val) and for the right child, it becomes (node.val, high). By checking if each node’s value falls within its respective range, we can ensure that the tree satisfies the BST properties.",
    },
    "binary-tree-level-order-traversal": {
        "key_insights": "Use Breadth-First Search (BFS) to traverse the tree.\nA queue data structure is ideal for maintaining the order of nodes at each level.\nFor every level, process all nodes currently in the queue and add their children for the next level.",
        "complexity":   "Time Complexity: O(n), where n is the number of nodes, because each node is processed once.\nSpace Complexity: O(n), which accounts for the space used by the queue in the worst-case scenario.",
        "solution":     "This solution leverages a BFS approach using a queue. The algorithm starts by enqueuing the root node. Then, it enters a loop that continues until the queue is empty. For each iteration—representing one level of the tree—it computes the number of nodes at that level (levelSize), processes these nodes by dequeuing them, and collects their values. Their non-null children are then enqueued. The list of values for each level is added to the final result list. Key data structures include the queue (for managing nodes) and a list (to store level values).",
    },
    "binary-tree-zigzag-level-order-traversal": {
        "key_insights": "Use a breadth-first search (BFS) approach to traverse the tree level by level.\nAlternate the direction of traversal on each level to achieve the zigzag pattern.\nA flag or counter can be used to determine the order of nodes for the current level.\nUsing a double-ended data structure or simply reversing the list at every alternate level is an effective approach.",
        "complexity":   "Time Complexity: O(n), where n is the number of nodes, because each node is visited exactly once.\nSpace Complexity: O(n), as in the worst-case scenario (e.g., a complete binary tree) the queue used for BFS may hold O(n) nodes at once.",
        "solution":     "We can solve this problem using a breadth-first search (BFS). Start by initializing a queue to hold nodes for each level. For each level:\nDetermine the number of nodes at the current level.\nIterate over these nodes, appending their children for the next level.\nDepending on a flag (or level count), either append node values in the natural order (left to right) or reverse the order (right to left) before adding to the result.\nKey data structures:\nA queue for BFS traversal.\nA list (or similar container) to store current level values.\nThe main trick is toggling the order of insertion or reversing the list at alternate levels to achieve the \"zigzag\" pattern.",
    },
    "construct-binary-tree-from-preorder-and-inorder-traversal": {
        "key_insights": "Preorder traversal always starts with the root node.\nInorder traversal splits the tree into left subtree and right subtree based on the root.\nA hash table (map/dictionary) can be used to quickly locate the index of the root in the inorder array.\nUse recursion to construct left and right subtrees.",
        "complexity":   "Time Complexity: O(n) since we process every node exactly once.\nSpace Complexity: O(n) due to the recursion stack and the hash map used for inorder indices.",
        "solution":     "We solve the problem using a recursive divide-and-conquer approach. First, create a hash map to store each value's index in the inorder array for quick lookups. The preorder array always provides the current root. Use the inorder array to divide the tree into left and right subtrees. Recursively construct the subtrees by adjusting the preorder index and the boundaries of the inorder array. The algorithm leverages recursion and a hash map to achieve O(n) time complexity.",
    },
    "binary-tree-right-side-view": {
        "key_insights": "The problem is essentially about capturing the last node at each level (or the rightmost node) in a binary tree.\nA Breadth-First Search (BFS) approach naturally works by processing nodes level by level.\nAlternatively, Depth-First Search (DFS) can be adapted by prioritizing the right subtree first, ensuring that the first node encountered at each depth is the visible one.\nThe number of nodes is capped at 100, so both approaches are efficient.",
        "complexity":   "Time Complexity: O(n) - we visit every node in the tree.\nSpace Complexity: O(n) - in the worst case, the queue (or call stack for DFS) may hold all nodes of the tree.",
        "solution":     "We can solve this problem using BFS (level-order traversal). The idea is to traverse the tree level by level using a queue. For each level, the last element added to the level represents the rightmost view from that level. We then append that element’s value to our result list.\nAlternatively, a DFS approach can be used where we visit the right subtree first. By keeping track of the current depth and checking if it’s the first time we’ve encountered this depth, we can add the first node visited at that depth (which will be the rightmost one) to the result.\nIn both approaches, appropriate data structures (a queue for BFS or recursion with a list for DFS) are used to store nodes and track the traversal state.",
    },
    "kth-smallest-element-in-a-bst": {
        "key_insights": "An in-order traversal of a BST visits nodes in increasing order.\nUse a counter to keep track of the number of nodes visited.\nAs soon as the counter reaches k, the current node’s value is the kth smallest.\nFor frequent modifications (insertions/deletions) and queries, consider augmenting the BST with subtree counts.",
        "complexity":   "Time Complexity: O(n) in the worst-case scenario (when k ≈ n), but on average the traversal may stop early.\nSpace Complexity: O(h) where h is the height of the tree (O(n) in the worst-case for a skewed tree).",
        "solution":     "We perform an in-order traversal of the BST which visits nodes in ascending order. During the traversal, we maintain a counter (or decrement k) to check when we have encountered the kth smallest element. When the counter reaches 0 (or k becomes 0), we capture the current node’s value as our answer and return it. This approach is both simple and efficient for the given constraints.",
    },
    "lowest-common-ancestor-of-a-binary-search-tree": {
        "key_insights": "Utilize the BST property: for any node, values in the left subtree are less and values in the right subtree are greater.\nIf both p and q have values less than the current node, the LCA must lie in the left subtree.\nIf both p and q have values greater than the current node, the LCA must lie in the right subtree.\nWhen one node value is less than and the other is greater than (or equal to) the current node, the current node is the lowest common ancestor.\nAn iterative approach is efficient and uses constant extra space.",
        "complexity":   "Time Complexity: O(h), where h is the height of the tree (O(log n) for balanced trees and O(n) in the worst-case skewed tree).\nSpace Complexity: O(1) with an iterative approach (O(h) if using recursion due to call stack).",
        "solution":     "The solution leverages the BST properties.\nAlgorithm:\nStart at the root node.\nWhile the current node exists:\nIf both p and q have values less than the current node’s value, move to the left child.\nIf both p and q have values greater than the current node’s value, move to the right child.\nOtherwise, the current node is the split point where p and q diverge, so it is the lowest common ancestor.\nReturn the current node.\nThis approach efficiently finds the LCA using a single traversal from the root.",
    },
    "lowest-common-ancestor-of-a-binary-tree": {
        "key_insights": "Use a depth-first search (DFS) traversal to explore the tree.\nIf the current node is either p or q, it could be the LCA.\nRecursively find p and q in the left and right subtrees.\nIf both left and right recursive calls return a non-null value, then the current node is the LCA.\nIf only one side returns a non-null value, propagate that value up the recursion.",
        "complexity":   "Time Complexity: O(n), where n is the number of nodes in the tree since each node is visited once.\nSpace Complexity: O(h), where h is the height of the tree due to the recursion stack (O(n) in the worst case of a skewed tree).",
        "solution":     "We solve the problem using recursion with depth-first search. The algorithm checks if the current node matches p or q. It then recursively searches the left and right subtrees. If both subtrees contain one of p or q, the current node is the LCA. Otherwise, the algorithm forwards the non-null result up the recursion. This approach naturally handles cases where one node is an ancestor of the other.",
    },
    "count-univalue-subtrees": {
        "key_insights": "Perform a postorder traversal (left, right, root) to determine whether each subtree is univalue.\nTreat null nodes as univalue to ease the recursion.\nA node's subtree is univalue if both left and right subtrees are univalue and, if they exist, their values equal the current node’s value.\nUse a global or external counter that increments every time a univalue subtree is confirmed.",
        "complexity":   "Time Complexity: O(n), where n is the number of nodes, since each node is visited once.\nSpace Complexity: O(h), where h is the height of the tree, due to the recursion stack.",
        "solution":     "We use a recursive DFS approach (postorder traversal) to solve the problem. For each node, we recursively check its left and right subtrees to decide if they are univalue. By default, if a child is null, it is considered univalue. Then, we verify the current node’s value against its non-null children. If both conditions hold (subtrees are univalue and matching values), we count the current node's subtree as univalue. This DFS checks in a bottom-up manner, ensuring that the properties required for a subtree to be univalue are maintained. The key trick is to return a boolean indicating if the subtree at each node is univalue and, while backtracking, increment the counter appropriately.",
    },
    "inorder-successor-in-bst": {
        "key_insights": "In a BST, an in-order traversal yields nodes in ascending order.\nIf the node p has a right subtree, the in-order successor is the left-most node in that right subtree.\nIf p has no right subtree, the successor is one of its ancestors; traverse from the root towards p while keeping track of the last node that is greater than p.\nAn iterative approach is efficient and avoids recursion stack overhead.",
        "complexity":   "Time Complexity: O(h), where h is the height of the tree. In the worst-case (skewed tree), it can be O(n).\nSpace Complexity: O(1) as only a constant amount of extra space is used.",
        "solution":     "We solve the problem using two cases:\nIf node p has a right subtree, we simply find the left-most node in that right subtree.\nIf node p does not have a right subtree, initiate a search from the BST root. As we traverse:\nIf the current node's value is greater than p.val, the current node is a candidate for the successor; move to the left subtree to look for a smaller candidate.\nOtherwise, move to the right subtree.\nBy the end of the traversal, the candidate (if exists) is the in-order successor. This approach benefits from the BST property, limiting traversal to O(h) time.",
    },
    "binary-tree-longest-consecutive-sequence": {
        "key_insights": "Use a Depth-First Search (DFS) to traverse the binary tree.\nPass the current node’s value and the length of the current consecutive sequence in the recursive call.\nWhen a node’s value is exactly one greater than its parent’s value, increment the sequence length; otherwise, reset it to 1.\nKeep track of the maximum sequence length found during the traversal.",
        "complexity":   "Time Complexity: O(N), where N is the number of nodes in the tree, since each node is visited once.\nSpace Complexity: O(H), where H is the height of the tree. In the worst-case of a skewed tree, the recursion stack can be O(N).",
        "solution":     "The solution involves using DFS to explore every node in the binary tree while keeping track of the length of the consecutive sequence. For each node, if the node's value is one greater than its parent’s value, we increment the sequence length; otherwise, we reset the sequence length to 1. During the traversal, a global variable is updated if the current sequence length exceeds the previously recorded maximum. This approach ensures that every possible consecutive path is considered.",
    },
    "binary-tree-vertical-order-traversal": {
        "key_insights": "Use Breadth-First Search (BFS) to traverse the tree level by level.\nTrack the column index for each node where root is column 0, left child is column-1, and right child is column+1.\nGroup nodes by their column indices using a hash table (or dictionary).\nBFS traversal naturally handles left-to-right ordering within the same level.\nFinally, sort the keys (column indices) to generate results from leftmost column to rightmost column.",
        "complexity":   "Time Complexity: O(n log n) in the worst-case due to sorting the column indices (n nodes in the worst-case if each node is on a unique column).\nSpace Complexity: O(n) for storing the node information and the resultant structure.",
        "solution":     "The solution uses BFS with a queue that stores nodes along with their corresponding column index. For each visited node, add its value to a dictionary keyed by its column index. When processing children, update the column index appropriately (left child: col-1, right child: col+1). After the traversal, sort the keys of the dictionary and compile the node values in order from smallest to largest column index. This approach guarantees that nodes at the same level will be processed in left-to-right order, matching the required output.",
    },
    "largest-bst-subtree": {
        "key_insights": "Use a bottom-up DFS (post-order traversal) to process each node.\nFor each node, determine whether its left and right subtrees are BSTs.\nMaintain additional data per subtree: size (number of nodes), minimum value, and maximum value.\nA subtree rooted at the current node is a BST if:\nIts left subtree is a BST.\nIts right subtree is a BST.\nThe maximum value in the left subtree is less than the current node’s value.\nThe current node’s value is less than the minimum value in the right subtree.\nTrack the size of the largest BST identified during the traversal.",
        "complexity":   "Time Complexity: O(n) (each node is visited once)\nSpace Complexity: O(h), where h is the height of the tree (due to recursion stack)",
        "solution":     "Perform a post-order traversal of the binary tree. For each node, return a tuple (or object in some languages) that provides:\nWhether the subtree rooted at this node is a BST.\nThe size of the subtree if it is a BST (or the size of the largest BST found within it).\nThe minimum value in the subtree.\nThe maximum value in the subtree.\nAt each node, if both left and right children form valid BSTs and the current node’s value fits the BST condition (greater than left’s max and less than right’s min), then the subtree rooted at the node is also a BST. Calculate its size and update the current global maximum if necessary. If the condition fails, return the maximum BST size found in its subtrees.",
    },
    "find-leaves-of-binary-tree": {
        "key_insights": "Use a postorder traversal (bottom-up approach) to process the tree.\nDetermine the \"height\" of each node (distance from the deepest leaf) where leaves have a height of 1.\nGroup nodes by their computed height to simulate the layer-wise removal of leaves.\nThe same node may become a leaf after its children are removed.",
        "complexity":   "Time Complexity: O(n) — Each node is visited exactly once.\nSpace Complexity: O(n) — Space used for the recursion stack and the result list.",
        "solution":     "The solution uses a recursive helper function that performs a postorder traversal of the tree. For each node, it computes its height as max(height of left, height of right) + 1. Nodes with the same height are collected together in a list. This idea mirrors the process of removing leaves level by level, where leaves (height 1) are removed first, then their parents become new leaves, and so on.\nData Structures used:\nAn array or list to maintain the groups of node values by their height.\nAlgorithmic Approach:\nUse Depth-First Search (DFS) via recursion.\nPostorder traversal ensures that leaf nodes (base cases) are processed before their parents.\nAppend the node's value to the correct list based on the computed height.",
    },
    "path-sum-iii": {
        "key_insights": "A path can start and end at any nodes, as long as it goes downwards.\nUsing DFS helps traverse the tree and explore all possible paths.\nA prefix sum technique with a hash map allows quick calculation of the sum of any subpath.\nBacktracking is used to remove a path's prefix sum when moving back up the recursion tree.\nAlthough a brute-force approach exists, it is inefficient compared to the prefix sum method.",
        "complexity":   "Time Complexity: O(n) on average (where n is the number of nodes), though worst-case can be O(n^2) in a skewed tree.\nSpace Complexity: O(n) due to recursion stack and the hash map that stores prefix sums.",
        "solution":     "The solution employs a DFS traversal of the tree along with a hash map that records the frequency of prefix sums encountered so far. At each node, the current prefix sum is updated by adding the node’s value. We then check the hash map for how many times (current prefix sum - targetSum) has occurred, since this value indicates the existence of a subpath summing to targetSum. We update the hash map with the current prefix sum, continue the DFS to traverse child nodes, and then backtrack by decrementing the current prefix sum's count before retreating to the parent node. This ensures that sums from other branches are not incorrectly combined.",
    },
    "boundary-of-binary-tree": {
        "key_insights": "Break down the problem into three parts:\nLeft boundary (excluding leaves).\nAll leaves (do a DFS to find leaves).\nRight boundary (excluding leaves, then reverse the collected list).\nSpecial handling is needed when the tree doesn't have a left or right subtree.\nThe root is always included and is never considered as a leaf, even if it has no children.\nAvoid duplication of leaf nodes in the left/right boundaries.",
        "complexity":   "Time Complexity: O(n) – Every node is visited at most once.\nSpace Complexity: O(n) – O(n) space is used for the output list and recursion stack (in worst case tree is skewed).",
        "solution":     "The solution uses a Depth-First Search (DFS) strategy to traverse the tree and collect the boundary nodes in three steps:\nFor the left boundary, start at root.left and traverse down, adding nodes (skipping leaves).\nFor the leaves, perform a DFS from the root and add any node that has no children.\nFor the right boundary, start at root.right and traverse down, adding nodes (skipping leaves). Since the right boundary must be added in reverse order, we reverse the collected list after traversal.\nFinally, concatenate these lists in the order: root, left boundary, leaves, reversed right boundary.\nThe approach makes use of helper functions to clearly separate the logic for gathering left boundary, leaves, and right boundary. This modular approach simplifies reasoning and avoids edge case mistakes such as including duplicate leaf nodes.",
    },
    "binary-tree-longest-consecutive-sequence-ii": {
        "key_insights": "Use Depth-First Search (DFS) to visit every node.\nFor each node, compute:\nThe longest increasing consecutive path starting from that node.\nThe longest decreasing consecutive path starting from that node.\nCombine the increasing and decreasing paths (subtracting one to avoid double counting the current node) to potentially form a longer consecutive sequence through the node.\nUpdate a global maximum to record the longest path seen.",
        "complexity":   "Time Complexity: O(n), where n is the number of nodes since every node is visited once.\nSpace Complexity: O(h), where h is the height of the tree, due to the recursion stack.",
        "solution":     "The solution employs a DFS traversal. At every node, we calculate two values:\ninc – the length of the longest increasing consecutive sequence starting from that node.\ndec – the length of the longest decreasing consecutive sequence starting from that node.\nFor each child of the current node:\nIf the child's value is node.val + 1, update the increasing sequence.\nIf the child's value is node.val - 1, update the decreasing sequence.\nThen, the longest path through the current node is inc + dec - 1 (subtracting one to avoid counting the current node twice). A global variable maintains the maximum length encountered. This process works regardless of the path direction because it combines both increasing and decreasing parts.",
    },
    "kill-process": {
        "key_insights": "Build a mapping (dictionary/hash table) from a parent process ID to its list of child process IDs.\nUse Depth-First Search (DFS) or Breadth-First Search (BFS) starting from the kill process to traverse the tree and collect all descendant processes.\nThe problem requires handling a tree structure that may have multiple children per node.",
        "complexity":   "Time Complexity: O(n), where n is the number of processes (both building the mapping and the tree traversal take linear time).\nSpace Complexity: O(n), for storing the mapping and the traversal queue/stack.",
        "solution":     "We first construct a mapping from each process's parent ID to its list of child IDs. This allows us to quickly access all children of any process. Once the mapping is built, we perform a tree traversal (either BFS or DFS) starting from the process to kill. During the traversal, we record every process encountered. This collected list represents all processes that will be terminated. The data structures used are a dictionary (for the mapping) and either a stack (for DFS) or a queue (for BFS), making the solution efficient and straightforward.",
    },
    "maximum-width-of-binary-tree": {
        "key_insights": "Perform a level order traversal (BFS) of the tree.\nAssign an index to each node as if the tree were complete (e.g., root is index 1, left child is 2\ni, right child is 2\ni+1).\nFor each level, compute the width as (last_index - first_index + 1).\nUsing indices allows capturing gaps between nodes due to nulls in between.\nEdge cases include handling very skewed trees.",
        "complexity":   "Time Complexity: O(N), where N is the number of nodes in the tree.\nSpace Complexity: O(N), due to the queue used for BFS which in the worst case holds nodes of the last level.",
        "solution":     "We use a Breadth-First Search (BFS) approach with a queue to traverse the tree level by level. At each level, we pair each node with an index representing its position assuming a complete binary tree. This allows us to compute the width of each level using the formula: width = last_index - first_index + 1. We then track the maximum width seen throughout the traversal. The use of indices handles cases where there are null gaps between nodes.",
    },
    "all-nodes-distance-k-in-binary-tree": {
        "key_insights": "Convert the binary tree into an undirected graph so that we can easily traverse both down to the children and up to the parent.\nUse a DFS or simple recursive traversal to create an adjacency list representing the graph.\nPerform a BFS starting from the target node to explore nodes level-by-level until reaching distance k.\nUse a visited set (or equivalent) to avoid revisiting nodes and to prevent cycles.",
        "complexity":   "Time Complexity: O(N), where N is the number of nodes in the tree. Each node is visited once when constructing the graph and once in the BFS.\nSpace Complexity: O(N), for storing the graph and the additional data structures used in BFS and DFS.",
        "solution":     "The solution involves two main steps:\nBuild an undirected graph representation of the binary tree using a DFS. For each node, add the node’s value as a key in an adjacency list and connect it with its children (if any). This creates bidirectional links between parent and child.\nUse a BFS starting from the target node and explore nodes level-by-level. Keep a counter for the current distance, and once the counter reaches k, return all node values found at that level. To avoid processing nodes more than once, maintain a set of visited nodes.",
    },
    "maximum-average-subtree": {
        "key_insights": "Use a depth-first search (DFS) to traverse every subtree.\nAt each node, compute the sum and count of nodes in its subtree.\nCalculate the average for the current subtree and update a global maximum if needed.\nProcessing every node exactly once yields an efficient solution.",
        "complexity":   "Time Complexity: O(n), where n is the number of nodes (each node is visited once).\nSpace Complexity: O(h), where h is the height of the tree (space for recursion stack).",
        "solution":     "We perform a post-order DFS traversal, calculating at each node both the cumulative sum and the count of nodes in its subtree. This enables us to compute the average value in constant time for any subtree. We maintain a global variable to track the maximum average encountered during the traversal. The primary technique is recursive DFS combined with tuple (or pair) aggregation. The simplicity of the recursion and in-place aggregation ensures an efficient and clear solution.",
    },
    "clone-n-ary-tree": {
        "key_insights": "The tree is N-ary, meaning every node can have multiple children.\nA deep copy requires creating new nodes for each node in the tree.\nBoth DFS (recursive) or BFS (iterative) traversals can be used to clone the tree.\nThe overall time complexity is O(N) since each node is visited once.\nWe can follow similar cloning techniques used in cloning graphs, using either recursion or an auxiliary data structure like a queue.",
        "complexity":   "Time Complexity: O(N), where N is the number of nodes in the tree, since each node is visited exactly once.\nSpace Complexity: O(N), accounting for the recursive call stack (or an explicit queue in BFS) and the space needed to store the clone.",
        "solution":     "We perform a DFS traversal starting from the root node. For each node we encounter, we:\nCreate a new node with the same value.\nRecursively clone all its children and attach the cloned children to the new node.\nThis approach ensures that every original node is copied into a new node and that the parent-child relationships are preserved. Edge cases such as an empty tree (null root) are also handled by returning null immediately.",
    },
    "diameter-of-n-ary-tree": {
        "key_insights": "The diameter of the tree can be found by considering the two largest depths from any node among its children.\nUse a depth-first search (DFS) to compute the maximum depth from each node.\nFor each node, the potential diameter is the sum of the two longest depths among its children.\nUpdate a global variable tracking the maximum diameter as you recurse through the tree.",
        "complexity":   "Time Complexity: O(n), where n is the number of nodes, since each node is visited once.\nSpace Complexity: O(h), where h is the height of the tree, due to the recursion stack (worst-case O(n) for a skewed tree).",
        "solution":     "Use a DFS approach to traverse the tree. At each node, calculate the longest and second longest depth among its children. The sum of these two depths gives the candidate diameter through that node. Maintain a global variable to track the maximum diameter found so far. Return the maximum depth from the node (i.e., one plus the largest depth among its children) to be used in parent's calculations.",
    },
    "lowest-common-ancestor-of-a-binary-tree-iii": {
        "key_insights": "Each node has a parent pointer, so you can traverse from any node to the root.\nBy determining the depth of each node, you can “align” the two nodes to the same depth.\nWhen both nodes are aligned, traverse upward simultaneously until they meet.\nThis approach guarantees finding the LCA with O(h) time complexity, where h is the height of the tree.",
        "complexity":   "Time Complexity: O(h), where h is the height of the tree.\nSpace Complexity: O(1)",
        "solution":     "We first compute the depth of both nodes by moving up their parent pointers. If one node is deeper, move it upward until both nodes are at the same level. Then, move both nodes upward one step at a time until they meet; the meeting point is the lowest common ancestor. This approach uses constant extra space and leverages the parent pointer for an efficient solution.",
    },
    "binary-tree-maximum-path-sum": {
        "key_insights": "Use Depth-First Search (DFS) to explore the tree in a post-order fashion.\nFor each node, calculate the maximum gain including that node and optionally one of its subtrees.\nA node’s best contribution to its parent can be either just its own value or its value plus the maximum gain from either the left or right child.\nUpdate a global maximum path sum that considers the possibility of combining both left and right gains with the current node.\nHandle negative values by comparing gains against 0 to avoid decreasing the path sum.",
        "complexity":   "Time Complexity: O(n), where n is the number of nodes in the tree, since each node is visited exactly once.\nSpace Complexity: O(n) in the worst-case scenario due to the recursion stack (O(H) where H is the height of the tree, which can be O(n) for skewed trees).",
        "solution":     "We use a recursive DFS approach (post-order traversal) to compute the maximum gain from each subtree. For every node:\nRecursively compute the maximum gain from the left and right child, ensuring that negative gains are treated as 0.\nThe maximum gain that can be provided to the node’s parent is the node’s value plus the larger gain from its left or right child.\nSimultaneously, the potential maximum path sum including the current node as the highest (or root) node is the node’s value plus both left and right gains.\nUpdate a global variable that holds the maximum path sum found so far.\nReturn the maximum gain (node value plus one best child) to be used by the node’s parent.\nThis strategy ensures that every possible path sum is considered while avoiding double-counting nodes.",
    },
    "serialize-and-deserialize-binary-tree": {
        "key_insights": "The tree can be traversed using either depth-first search (DFS) or breadth-first search (BFS).\nUsing BFS (level-order traversal) simplifies reconstruction since nodes are processed level-by-level.\nPlaceholder markers (e.g., \"null\") are used to indicate missing children.\nBoth serialization and deserialization processes should handle empty trees and edge cases gracefully.\nEnsuring a one-to-one mapping between the tree structure and the serialized string is critical.",
        "complexity":   "Time Complexity: O(n) for both serialization and deserialization, where n is the number of nodes in the tree.\nSpace Complexity: O(n) due to the storage used for the serialized string and additional data structures (queue) during processing.",
        "solution":     "This solution uses level-order traversal (BFS) for both serialization and deserialization. During serialization, each node is processed sequentially with missing children recorded as \"null\". For deserialization, the string is split by commas, and a queue is used to rebuild the tree level by level. The root is created from the first token, then subsequent tokens are assigned as left and right children accordingly, ensuring an accurate reconstruction of the original tree.",
    },
    "permutation-in-string": {
        "key_insights": "- Use a sliding window approach to examine substrings of s2 of length equal to s1.\n- Maintain frequency counts of characters for both s1 and the current window in s2.\n- Compare the frequency counts to determine if a valid permutation exists.\n- Increment the window by adding a new character and removing the leftmost one, keeping the operation efficient.",
        "complexity":   "Time Complexity: O(n) where n is the length of s2 (each character is processed a constant number of times).\nSpace Complexity: O(1) since the frequency count arrays have a fixed size (26 for lowercase English letters).",
        "solution":     "The solution uses two fixed-length arrays (of size 26) to store character frequencies for s1 and the current window in s2. First, initialize the frequency arrays by processing s1 and the first window of s2. Then, slide the window one character at a time, updating the counts by incrementing the count for the new character and decrementing the count for the character that is no longer in the window. After each move, compare both frequency arrays; if they match, s2 contains a permutation of s1. This technique avoids repeatedly computing the frequency distribution from scratch, making the algorithm efficient.",
    },
    "car-fleet": {
        "key_insights": "- Since cars cannot pass each other, the relative order (by starting position) is maintained.\n- Sorting the cars based on their starting positions (descending order) helps to simulate the catching-up process.\n- Computing the time each car takes to reach the target is critical.\n- Compare the current car’s time to the time of the fleet ahead: if it takes longer, it forms a new fleet; otherwise, it gets merged.\n- A stack-like approach (tracking the slowest arrival time seen so far) reduces the problem to a single pass after sorting.",
        "complexity":   "Time Complexity: O(n log n) due to the sorting step.\nSpace Complexity: O(n) for storing the computed times and sorted car information.",
        "solution":     "The solution involves sorting the cars by their starting positions in descending order so that we process the cars from nearest to the target to farthest. For each car, calculate the time to reach the target using the formula (target - position) / speed. As we iterate:\n\n- If a car’s time is greater than the slowest time encountered (tracked by a variable or stack), it cannot catch up to the fleet ahead and forms a new fleet.\n- Otherwise, it merges into the current fleet.\nThis approach ensures that we count each distinct fleet accurately with a single pass through the sorted list.",
    },
    "koko-eating-bananas": {
        "key_insights": "- The required eating speed k lies in the range [1, max(piles)].\n- For a given k, one can calculate the total hours needed by summing up ceil(pile / k) for each pile.\n- The problem can be solved efficiently using binary search to find the minimum k that satisfies the time constraint.\n- Early termination in the hour calculation can be used if the computed hours exceed h.",
        "complexity":   "Time Complexity: O(n log(max(pile))) where n is the number of piles and max(pile) is the maximum bananas in a pile.\nSpace Complexity: O(1) extra space beyond the input.",
        "solution":     "We use binary search on the candidate eating rate k. The search space starts at 1 (minimum possible speed) and goes up to the maximum number of bananas in any pile. For each candidate k, we simulate the number of hours Koko would take by calculating hours += (pile + k - 1) // k for each pile (this formula performs the ceiling division). If the total hours are less than or equal to h, then k might be a valid solution and we try to lower it; otherwise, we increase k. The binary search continues until we converge on the minimum valid eating speed.",
    },
    "copy-list-with-random-pointer": {
        "key_insights": "- Use a hash map (or dictionary) to map each original node to its corresponding copy.\n- Perform two passes: the first to create a copy of each node, and the second to assign the next and random pointers using the hash map.\n- An alternative interleaving approach exists but using a hash table is typically more straightforward.",
        "complexity":   "Time Complexity: O(n) – Each node is processed a constant number of times.\nSpace Complexity: O(n) – A hash map is used to store the mapping from original nodes to their copies.",
        "solution":     "The solution uses a two-pass algorithm. In the first pass, we iterate through the original list and for each node, create a new node (deep copy) while saving the mapping between the original and copy in a hash table. In the second pass, we iterate over the original list again to assign the next and random pointers of the new nodes using the previously built map. This guarantees that every pointer in the copied list corresponds to a new node and not the original.",
    },
    "count-good-nodes-in-binary-tree": {
        "key_insights": "- Perform a tree traversal (DFS or BFS) from the root.\n- Keep track of the maximum value encountered along the current path.\n- A node is \"good\" if its value is greater than or equal to the maximum value seen so far.\n- Update the maximum value as you traverse deeper in the tree.\n- The root is always a good node.",
        "complexity":   "Time Complexity: O(n), where n is the number of nodes, since each node is visited exactly once.\nSpace Complexity: O(h), where h is the height of the tree (O(n) in the worst-case of a skewed tree).",
        "solution":     "We can solve this problem using Depth-First Search (DFS) recursion. Starting from the root, we pass along the current maximum value encountered on the path. For each node, we check if its value is greater than or equal to this maximum. If it is, we count it as a good node and update the maximum value if necessary. This process is continued recursively for both the left and right children of the node. Finally, we sum the counts from both subtrees.",
    },
    "kth-largest-element-in-a-stream": {
        "key_insights": "- Use a min-heap (priority queue) of fixed size k.\n- The heap always contains the k largest elements; the smallest element in the heap is the kth largest overall.\n- When a new test score is added:\nIf the heap has less than k elements, simply add the score.\nIf the heap is full and the new score is greater than the smallest (heap root), replace the root.\n- Each add operation runs in O(log k) time.",
        "complexity":   "Time Complexity: O(log k) per add operation, O(n log k) to build the initial heap.\nSpace Complexity: O(k) for maintaining the heap.",
        "solution":     "We use a min-heap to track the k largest numbers. When initializing, we add numbers from the given list to the heap, ensuring not to exceed a size of k. For each add operation, if the new number is greater than the heap minimum, we replace the minimum. The kth largest number is always accessible as the minimum element in the heap.",
    },
    "last-stone-weight": {
        "key_insights": "- Use a max-heap (or simulate one) to always access the two largest stones quickly.\n- If the two stones are equal, both are removed; otherwise, the difference is pushed back into the heap.\n- Continue the process until the heap has one or no stone left.\n- For languages without a max-heap in their standard library (e.g., Python), simulate one by pushing negative values.",
        "complexity":   "Time Complexity: O(n log n) – Each stone removal and insertion takes O(log n) and in the worst-case scenario, we perform O(n) operations.\nSpace Complexity: O(n) – The heap stores all the stones initially.",
        "solution":     "We solve the problem by using a priority queue (max-heap). In Python, we simulate a max-heap by inserting the negative of each stone's weight into a min-heap. In JavaScript, due to the lack of a built-in heap structure, we sort the array in descending order at each iteration (acceptable given the small constraints). In C++ and Java, we use the standard priority_queue. The algorithm repeatedly extracts the two largest stones, computes their difference, and if the difference is non-zero, it is inserted back into the heap. This process repeats until there is at most one stone left.",
    },
    "design-twitter": {
        "key_insights": "- Use a global timestamp to order tweets as they are posted.\n- Maintain a mapping of each user to the list of tweets they posted.\n- Maintain a mapping of each user to the set of users they follow.\n- For getNewsFeed, merge tweets from the user and their followees using a heap to efficiently retrieve the 10 most recent tweets.\n- Ensure that a user always follows themself to simplify retrieving their own tweets.",
        "complexity":   "Time Complexity:\n\n- postTweet: O(1)\n- follow / unfollow: O(1)\n- getNewsFeed: O(N log K) where N is the number of followees and K is the average number of tweets considered (capped at 10 tweets per merge iteration)\nSpace Complexity:\n- O(U + T) where U is the number of users (for follow mappings) and T is the total number of tweets stored.",
        "solution":     "We use two main data structures: a hash table to store tweets for each user and another hash table for follow relationships. Each tweet is stored as a tuple of (timestamp, tweetId). A global timestamp counter provides a unique ordering. A min-heap (or max-heap using negative timestamp values) is used to merge the tweets from different users efficiently. When retrieving the news feed, we look up the tweet lists for the given user's followees (including self) and use the heap to extract the 10 most recent tweets.",
    },
    "combination-sum-ii": {
        "key_insights": "- Sort the candidates first to easily skip duplicates.\n- Use backtracking to explore all possible combinations.\n- Avoid selecting the same number (at the same recursive level) to avoid duplicate combinations.\n- Terminate recursion when the running sum exceeds the target.\n- Use indices rather than reusing elements to ensure each number is used at most once.",
        "complexity":   "Time Complexity: O(2^n * k) in the worst case, where n is the number of candidates and k is the average length of a combination.\nSpace Complexity: O(k) for the recursion stack and current combination (not including the space occupied by the output).",
        "solution":     "The solution is based on a backtracking algorithm. First, the candidates are sorted to handle duplicates easily. Then, a recursive function is used to build combinations. At each recursion level, iterate through the candidates starting from the current index. For each candidate:\n\n- Skip the candidate if it is the same as the previous one (to avoid duplicates).\n- If the candidate can be added without exceeding the target, add it to the current combination.\n- Recurse with the next index (ensuring each candidate is used only once).\n- Backtrack by removing the candidate from the current combination once completed.\n\nData structures used include:\n\n- An array or list to hold the current combination.\n- A results list to store all valid combinations.\n- Sorting is leveraged to simplify duplicate elimination.",
    },
    "palindrome-partitioning": {
        "key_insights": "- Use backtracking to explore all possible ways to partition the string.\n- At each step, check whether the current substring is a palindrome.\n- If it is, include that substring in the current partition and recursively partition the rest of the string.\n- The limit on the string length (at most 16 characters) allows an exponential backtracking approach.\n- Optimizations like caching palindrome checks can be used, though the simple approach is typically sufficient.",
        "complexity":   "Time Complexity: O(n * 2^n) in the worst case, due to exploring all possible partitions and checking palindromes.\nSpace Complexity: O(n) for recursion stack and temporary storage for current partition.",
        "solution":     "The solution uses the backtracking algorithm:\n\n- Define a helper function that will recursively try to partition the string.\n- For each possible split, check if the substring is a palindrome.\n- If it is, add the substring to the current partition list and recursively call the helper on the remaining substring.\n- When the end of the string is reached, add the current partition list to the final result.\n- Backtrack by removing the last added substring and try the next possible partition.\nThis method systematically explores every possible palindrome partitioning of the string.",
    },
    "redundant-connection": {
        "key_insights": "- The problem can be solved using a Union Find (Disjoint Set Union) data structure.\n- As we iterate over each edge, we check whether the two vertices are already connected.\n- If connecting the two vertices would create a cycle (i.e., they already belong to the same set), then the current edge is redundant.\n- Alternatives like DFS or BFS are possible, but Union Find offers a more straightforward and efficient solution.",
        "complexity":   "Time Complexity: O(n * α(n)) where α(n) is the inverse Ackermann function (almost constant in practice).\nSpace Complexity: O(n), for storing the parent array used in the Union Find structure.",
        "solution":     "We use the Union Find algorithm by maintaining an array where each node points to its parent. For each edge, we perform a find operation on both endpoints:\n\n- If the endpoints belong to the same set, adding this edge would create a cycle and it is marked as redundant.\n- Otherwise, we union the two sets.\nThis algorithm efficiently detects the extra edge that introduces a cycle in the graph.",
    },
    "network-delay-time": {
        "key_insights": "- This is a single-source shortest path problem on a directed graph.\n- Since all edge weights are non-negative, Dijkstra’s algorithm is well-suited for this problem.\n- Construct an adjacency list for the graph.\n- Utilize a min-heap (priority queue) to efficiently select the next node with the smallest cumulative travel time.\n- The answer is the maximum travel time among all reachable nodes; if any node is unreachable, return -1.",
        "complexity":   "Time Complexity: O(E log V) where E is the number of edges and V is the number of nodes.\nSpace Complexity: O(V + E) due to the adjacency list and distance storage.",
        "solution":     "We solve the problem using Dijkstra’s algorithm. First, an adjacency list is built using the input times array. A distance map (or array) records the shortest known distance from the starting node k to all other nodes, initialized to infinity except for k which is set to 0. A min-heap stores nodes prioritized by their current shortest distance.\n\nThe algorithm repeatedly extracts the node with the smallest distance from the heap. For each neighbor of the current node, it checks if a shorter path is found via the current node; if so, the neighbor’s distance is updated and it is added to the heap. Once all nodes are processed, the answer is the maximum distance from the k node. If any node remains unreachable (has distance equal to infinity), return -1.",
    },
    "reconstruct-itinerary": {
        "key_insights": "- Model the tickets as a directed graph where airports are nodes.\n- The problem is akin to finding an Eulerian path through the graph.\n- Use Depth-First Search (DFS) with Hierholzer’s algorithm to construct the itinerary.\n- To ensure lexical order, sort the destination lists in reverse order so that the smallest lexical destination can be popped during DFS.\n- Build the itinerary in reverse during backtracking and then reverse it to get the final order.",
        "complexity":   "Time Complexity: O(E log E), where E is the number of tickets, due to the sorting of edges.\nSpace Complexity: O(E), to store the graph and recursion stack.",
        "solution":     "Construct an adjacency list (graph) mapping each departure airport to a list of destination airports sorted in reverse lexicographical order. Perform DFS starting from \"JFK\". During DFS, traverse while there are destinations available from the current airport. Append airports to the itinerary as you backtrack. Reverse the built itinerary at the end to obtain the correct order.",
    },
    "min-cost-to-connect-all-points": {
        "key_insights": "- The problem can be reinterpreted as finding a Minimum Spanning Tree (MST) on a complete graph.\n- Manhattan distance is used to calculate the weight/cost between every pair of vertices.\n- Prim’s or Kruskal’s algorithm can be applied to solve the MST problem.\n- A direct construction of the complete graph is unnecessary; distances can be computed on the fly, which is efficient given the problem constraints.",
        "complexity":   "Time Complexity: O(n^2)\nSpace Complexity: O(n)",
        "solution":     "We can solve the problem using Prim’s algorithm. We start from an arbitrary point (for example, the first point) and iteratively add the point that has the smallest distance to the growing MST. Instead of explicitly constructing all the edges, we compute the Manhattan distance on the fly between the current MST and the remaining points. An array is used to track the minimum connection cost for each point that is not yet included in the tree. At each iteration, we select the point with the smallest cost to add into the MST and update the costs for the remaining points. This approach guarantees that we connect all points with the minimum total cost.",
    },
    "swim-in-rising-water": {
        "key_insights": "- The problem can be viewed as finding a path from the top-left to bottom-right where the cost is the maximum elevation encountered on that path.\n- A greedy approach using Dijkstra’s algorithm (or a min-heap based BFS) ensures that we always consider the next move with the smallest current elevation requirement.\n- Once the destination is reached, the maximum elevation along that path is the minimum time required.\n- Alternative approaches include binary search on time combined with a DFS/BFS check to see if a valid path exists.",
        "complexity":   "Time Complexity: O(n^2 log(n^2)) which simplifies to O(n^2 log n) since each cell is processed once and we use a priority queue.\nSpace Complexity: O(n^2) due to the visited matrix and the priority queue.",
        "solution":     "We can solve the problem using a min-heap based BFS (Dijkstra’s algorithm). Start at cell (0, 0) with its elevation as the initial cost, and use a min-heap to always expand the cell with the lowest required water level. For each popped cell, update the maximum elevation encountered on the current path. When reaching the target cell (n - 1, n - 1), this value represents the least time needed. We maintain a visited matrix to avoid reprocessing cells.",
    },
    "min-cost-climbing-stairs": {
        "key_insights": "- Use Dynamic Programming to solve overlapping subproblems.\n- The minimum cost to reach a step is the cost of that step plus the minimum cost of reaching one of the two previous steps.\n- The final result is the minimum cost to reach the last or second last step since you can jump from either directly to the top.",
        "complexity":   "Time Complexity: O(n)\nSpace Complexity: O(n) (can be optimized to O(1) by using only two variables)",
        "solution":     "We use a dynamic programming approach. Define a dp array where dp[i] is the minimum cost required to reach step i. Initialize dp[0] with cost[0] and dp[1] with cost[1] because you can start at either step. For each subsequent step, the recurrence is dp[i] = cost[i] + min(dp[i-1], dp[i-2]). Finally, the answer is the minimum of dp[n-1] and dp[n-2] since you can reach the top from either of these steps. This method efficiently computes the answer in one pass through the array.",
    },
    "house-robber-ii": {
        "key_insights": "- The houses are arranged in a circle, meaning the first and last houses are neighbors.\n- Because of the circular arrangement, you cannot rob both the first and last houses.\n- The problem can be reduced into two separate linear House Robber problems:\nOne considering houses from index 1 to n-1 (exclude the first house).\nThe other considering houses from index 0 to n-2 (exclude the last house).\n- Use dynamic programming with constant space (using two variables) to compute the solution for a linear array of houses.",
        "complexity":   "Time Complexity: O(n), where n is the number of houses.\nSpace Complexity: O(1), as only a constant amount of extra space is used.",
        "solution":     "To handle the circular dependency, we solve two linear versions of the problem:\n\n- Exclude the first house and calculate the maximum rob amount for houses from index 1 to n-1.\n- Exclude the last house and calculate the maximum rob amount for houses from index 0 to n-2.\nThen, the final answer is the maximum of these two calculated values. We use dynamic programming by iterating through the list and at each step, calculating the maximum money that can be robbed up to that point with two variables.",
    },
    "palindromic-substrings": {
        "key_insights": "- Every individual character is a palindrome.\n- Palindromes can be expanded from a center point; centers can be single characters (odd length) or between two characters (even length).\n- The \"expand around center\" technique efficiently counts palindromes in O(n^2) time.\n- Dynamic programming could also be used but requires extra space and is more complex to implement.",
        "complexity":   "Time Complexity: O(n^2) where n is the length of the string.\nSpace Complexity: O(1) if using the expand-around-center method (ignoring recursion/stack overhead).",
        "solution":     "The solution employs the expand-around-center approach:\n\n- Iterate over each index of the string considering it as the center of a palindrome.\n- For each center, expand in both directions (left and right) as long as the characters are the same and within bounds.\n- Count every valid palindrome found during the expansion.\n- Repeat for both odd-length centers (a single index) and even-length centers (between two indices).\n\nThis method efficiently checks all potential palindromic substrings without extra space for dynamic programming tables.",
    },
    "coin-change-ii": {
        "key_insights": "- Use dynamic programming to build up the number of ways to form every amount up to the target.\n- A one-dimensional dp array suffices, where dp[j] represents the number of ways to form sum j.\n- When processing each coin, iterate over amounts from the coin value to the target, updating dp.\n- Order of iteration ensures that combinations, not permutations, are counted.",
        "complexity":   "Time Complexity: O(n * amount), where n is the number of coins.\nSpace Complexity: O(amount), due to the dp array used for computations.",
        "solution":     "We solve the problem by using dynamic programming with a one-dimensional dp array. The dp array is sized as amount + 1 with dp[0] initialized to 1, representing the single way to achieve the amount of 0 (by choosing no coins). For each coin in the coins array, we update every dp[j] for j from the coin’s value up to the target amount: dp[j] is increased by dp[j - coin]. This ensures we consider each coin only once per combination, hence avoiding duplicate counts of the same combination in different orders.",
    },
    "target-sum": {
        "key_insights": "- The problem can be converted into a subset sum problem by considering two groups: numbers with a '+' sign and numbers with a '-' sign.\n- Let the sum of numbers with a '+' sign be sum(P) and with a '-' sign be sum(N). Then the problem reduces to solving:\nsum(P) - sum(N) = target\nand sum(P) + sum(N) = totalSum, where totalSum is the sum of all numbers in nums.\n- From these equations, we derive sum(P) = (target + totalSum) / 2.\n- We only proceed if (target + totalSum) is even and not negative; otherwise, there are no valid expressions.\n- Dynamic programming is used to count the number of subsets that sum to sum(P).",
        "complexity":   "Time Complexity: O(n * subsetSum), where n is the length of the nums array and subsetSum = (target + totalSum)/2.\nSpace Complexity: O(subsetSum), as we use a one-dimensional DP array.",
        "solution":     "The solution converts the problem into a subset sum DP problem. First, compute the total sum of the array. If (target + totalSum) is not even or target is greater than totalSum, return 0 since no valid configuration exists. Otherwise, compute the required subset sum (subsetSum) as (target + totalSum) / 2 and use a dynamic programming array to count the number of ways to reach subsetSum. The DP approach iterates through each number in the array, updating the counts using a reverse iteration to ensure each number is only used once.",
    },
    "interleaving-string": {
        "key_insights": "- The problem asks if s3 can be constructed by preserving the order of characters in s1 and s2.\n- A dynamic programming approach is effective by defining dp[i][j] to represent whether s3[0 ... i+j-1] is an interleaving of s1[0 ... i-1] and s2[0 ... j-1].\n- The solution must check that the total lengths match; otherwise, it is immediately false.\n- Transitions are based on whether the current character in s3 matches the next char in s1 or s2.\n- Space optimization to O(s2.length) is possible by reusing a 1D DP array.",
        "complexity":   "Time Complexity: O(n * m) where n = length of s1 and m = length of s2.\nSpace Complexity: O(n * m) with the basic DP solution. (Can be optimized to O(m) with further space reduction.)",
        "solution":     "We use a 2D dynamic programming table where dp[i][j] is true if s3[0 ... i+j-1] can be formed by interleaving s1[0 ... i-1] and s2[0 ... j-1]. First, we check if the lengths of s1 and s2 add up to s3’s length; if not, return false. Then, we initialize dp[0][0] to true and fill in the first row and column based on whether the characters in s2 or s1 correspond to s3. For the rest of the table, dp[i][j] is true if either the previous state dp[i-1][j] is true and s1[i-1] matches s3[i+j-1], or dp[i][j-1] is true and s2[j-1] matches s3[i+j-1]. The answer is found in dp[s1.length][s2.length].",
    },
    "jump-game-ii": {
        "key_insights": "- Utilize a greedy algorithm to make optimal jumps.\n- Track the furthest index that can be reached within the current jump range.\n- When the current index reaches the end of the current range, a jump is made and the range updates to the furthest reachable index.\n- Edge case: if the array has only one element, no jumps are needed.",
        "complexity":   "Time Complexity: O(n) - Single pass through the array.\nSpace Complexity: O(1) - Only a few variables are used for tracking jumps and ranges.",
        "solution":     "We use a greedy approach by iterating through the array and keeping track of:\n\n- The furthest index reachable from the current range (furthest).\n- The end of the current jump's range (currentEnd).\n- The number of jumps made (jumps).\n\nAt each iteration, we update the furthest reachable index. When the current index reaches the currentEnd, it indicates that one jump has been used and we update currentEnd to furthest. This ensures we are always making the optimal jump to cover the longest distance possible until the end of the array is reached.",
    },
    "hand-of-straights": {
        "key_insights": "- The total number of cards must be divisible by groupSize.\n- Use a frequency count (hash table or map) to record the occurrence of each card.\n- Always start forming groups from the smallest available card to maintain the consecutive order.\n- For each card, if its count is non-zero, check that the subsequent groupSize-1 cards exist with at least as many counts.\n- Decrease the counts accordingly for each valid consecutive group.",
        "complexity":   "Time Complexity: O(n log n) due to sorting the unique keys from the hand.\nSpace Complexity: O(n) for storing the frequency count of the cards.",
        "solution":     "The solution uses a greedy approach along with a frequency hash map. The main steps are:\n\n- Verify that the total number of cards is divisible by groupSize.\n- Count the frequency of each card.\n- Sort the keys of the frequency map.\n- Iterate over the sorted keys and for each card with a positive count, try to form a group of groupSize consecutive cards.\n- For each card in the group, decrease the frequency by the number of groups formed. If at any point the required card is missing or its count is insufficient, return false.\n- If all groups can be formed successfully, return true.",
    },
    "merge-triplets-to-form-target-triplet": {
        "key_insights": "- Only consider triplets that are \"valid\" in the sense that none of its values exceed the corresponding target value.\n- For each component of the target, there must be at least one valid triplet that provides that maximum value.\n- By taking the element-wise maximum across all valid triplets, if we can recreate the target, then the target can be formed using a series of allowed operations.",
        "complexity":   "Time Complexity: O(n), where n is the number of triplets.\nSpace Complexity: O(1) extra space.",
        "solution":     "The solution approach involves iterating over the list of triplets and only considering those triplets that do not exceed the target values in any position. For each valid triplet, update a candidate triplet by taking the element-wise maximum with the current candidate. Finally, check if the candidate exactly matches the target triplet. The algorithm uses simple array operations and avoids any extra space beyond a few variables.",
    },
    "partition-labels": {
        "key_insights": "- Determine the last occurrence of every character in the string.\n- Use a greedy approach to extend the current partition until all characters within it do not appear later in the string.\n- When the current index reaches the furthest last occurrence for characters seen so far, a partition can be closed.\n- Repeat the process for the remaining section of the string.",
        "complexity":   "Time Complexity: O(n) where n is the length of the string because we traverse the string twice.\nSpace Complexity: O(1) (or O(26) to be precise) for storing the last occurrences of each character.",
        "solution":     "We first iterate through the string to store the last index at which each character appears. Then, we traverse the string while maintaining two pointers: one for the start of the current partition and one to track the furthest last occurrence (end) of any character seen so far. For each character, we update the end pointer to be the maximum of its current value and the last index of that character. When the current index equals the end pointer, it means that all characters in the current partition do not appear later. We then determine the size of this partition, add it to the result list, and move to the next partition. We repeat this until the entire string is processed.",
    },
    "valid-parenthesis-string": {
        "key_insights": "- The '*' character provides flexibility, allowing multiple interpretations.\n- A greedy approach can track the range of possible open parentheses count.\n- Use two counters to represent the minimum and maximum open count possible at any moment.\n- If the maximum count goes negative at any point, the string cannot be valid.\n- At the end, the minimum count must be zero for a valid string.",
        "complexity":   "Time Complexity: O(n) where n is the length of the string\nSpace Complexity: O(1)",
        "solution":     "We use a greedy algorithm by maintaining two counters:\n\n- minOpen - the minimum number of open parentheses that could be present.\n- maxOpen - the maximum number of open parentheses that could be present.\n\nFor every character in the string:\n\n- For '(', increment both minOpen and maxOpen.\n- For ')', decrement both minOpen and maxOpen.\n- For '*', decrement minOpen (treat as ')') and increment maxOpen (treat as '(').\n\nClamp minOpen to not go below 0, since negative count of open parentheses is not possible in any valid interpretation. If maxOpen drops below 0 at any point, it means there are more closing brackets than can be balanced by available '*' and '(', so the string is invalid. Finally, if minOpen is 0, the string can be balanced, otherwise it is invalid.",
    },
    "minimum-interval-to-include-each-query": {
        "key_insights": "- Sort the intervals by their start values to efficiently determine when an interval becomes relevant for a given query.\n- Process queries in increasing order so that intervals can be added to a data structure (min-heap) as they \"start\" to cover queries.\n- Use a min-heap keyed by the size (length) of the interval to quickly retrieve the smallest interval currently containing the query.\n- Remove any intervals from the heap that no longer cover the current query (i.e., their end is less than the query).\n- Maintain the original order of queries by storing the indices while sorting.",
        "complexity":   "Time Complexity: O((N + Q) log N) where N is the number of intervals and Q is the number of queries. Sorting and heap operations dominate the complexity.\nSpace Complexity: O(N + Q) for storing intervals, queries, and the heap.",
        "solution":     "The solution uses a two-step approach:\n\n- Sort the queries while keeping track of their original indices.\n- Sort intervals by their starting values. For each query (in increasing order), add intervals that start before or at this query to a min-heap, where each entry is the size of the interval along with its end value. Then, remove intervals from the heap that do not cover the query (their end value is less than the query). The heap’s top element (if available) holds the smallest interval size that contains the query.\n- Place the answer in a result array at the position corresponding to the original query order.\n\nData structures used: Sorting arrays and a min-heap for efficient retrieval and removal.",
    },
    "happy-number": {
        "key_insights": "- The process involves repeatedly computing the sum of squares of digits.\n- If the process reaches 1, the number is happy.\n- If the computed values start repeating (cycle detection), the number is not happy.\n- The cycle can be detected using a hash set to record previously seen numbers or using a two pointers (Floyd’s cycle detection) approach.",
        "complexity":   "Time Complexity: O(1) – Although the process appears iterative, the sum of squares quickly reduces any number to a bounded range.\nSpace Complexity: O(1) – The number of unique sums computed is bounded, resulting in constant space usage.",
        "solution":     "The solution involves computing the sum of the squares of a number's digits in each iteration. We use a hash set to track numbers that have already been seen. If a number repeats, it means we are in a cycle and the number is not happy. If the number becomes 1, it is a happy number. Data structures used include a hash set for cycle detection, and the algorithm uses iterative computation.",
    },
    "plus-one": {
        "key_insights": "- The digits are stored from most significant to least significant.\n- Start processing from the least significant digit (end of array) to handle potential carry.\n- Carefully handle the case where all digits are 9, in which case an additional digit is needed.\n- Use an in-place update approach to reduce extra space usage when possible.",
        "complexity":   "Time Complexity: O(n) in the worst-case, where n is the number of digits.\nSpace Complexity: O(n) in the worst-case for the output array (when an extra digit is added).",
        "solution":     "We iterate from the end of the digits array towards the beginning, adding one to the current digit. If the digit becomes 10, we set it to 0 and propagate the carry to the next digit. If no carry is needed, we can return the result immediately. If we finish processing the array and still have a carry (e.g., the number was all 9s), we create a new array with one extra digit at the beginning set to 1.",
    },
    "multiply-strings": {
        "key_insights": "- Simulate the multiplication process similar to hand multiplication.\n- Use an array to store intermediate results where the index correlation is based on the position of digits.\n- Each multiplication may contribute a value that needs to be managed with a carry over in the result array.\n- Handle the special case when either input is \"0\".",
        "complexity":   "Time Complexity: O(n * m), where n and m are the lengths of num1 and num2 respectively.\nSpace Complexity: O(n + m), used for storing the intermediate multiplication results.",
        "solution":     "The solution multiplies each digit of num1 with each digit of num2 by simulating the grade-school multiplication algorithm. An array is used to store the intermediate sums at positions that correspond to the sum of the indices from num1 and num2. For two digits multiplied together, the product is added to the corresponding index in the result array, and any carry is added to the adjacent left cell. Finally, the array is converted back to a string while taking care to remove any leading zeros.\n\nData structures used:\n\n- Array for intermediate multiplication results.\n- Two nested loops to multiply each digit pair.\n\nAlgorithmic steps:\n\n- Initialize an array of size (length of num1 + length of num2) with all zeros.\n- Loop through digits of num1 and num2 in reverse order.\n- Multiply the two digits, add the product to the corresponding position in the result array (taking into account any previous values).\n- Manage the carry for each index.\n- Convert the result array to a string, skipping any leading zeros.\n- Return the final product string.",
    },
    "detect-squares": {
        "key_insights": "- Leverage the fact that an axis-aligned square has vertical and horizontal edges.\n- For a query point (x, y), iterate over all points that share the same y-axis. For each of these points (px, y) (with px ≠ x), determine the potential square's side length, d = |px - x|.\n- Check for the existence of the two other points (x, y+d) and (px, y+d) (or (x, y-d) and (px, y-d)) that would complete the square.\n- Use a hash map (or dictionary) to store each point along with its count since duplicate points are allowed.",
        "complexity":   "Time Complexity:\n• The add operation is O(1).\n• The count operation is O(n) in the worst-case scenario, where n is the number of distinct points with the same y-coordinate as the query point.\n\nSpace Complexity:\n• O(m), where m is the number of added points, since we store counts for each point.",
        "solution":     "We implement the DetectSquares class by maintaining a hash map (or dictionary) to store the frequency count for each point using its (x, y) coordinates as the key.\nIn the add(point) method, we simply increment the count for the given point.\nFor the count(point) method, we iterate through all points that share the same y-coordinate as the query point. For every such point (px, y) (with px ≠ x), we calculate the potential side length of a square (d = px - x). Then, we check for two possible squares: one above the query point and one below it. The count is increased by the number of times these needed points exist multiplied by the count of the current candidate point.\nThis method efficiently counts all possible squares that can be formed with the query point.",
    },
    "sum-of-two-integers": {
        "key_insights": "- Use bitwise XOR (^) to perform addition without carrying.\n- Use bitwise AND (&) followed by a left shift (<<) to calculate the carry.\n- Repeat the process until there is no carry.\n- This method simulates the addition process using binary arithmetic.",
        "complexity":   "Time Complexity: O(1) – The loop runs a fixed number of times based on the number of bits.\nSpace Complexity: O(1) – Only constant extra space is used.",
        "solution":     "The solution uses bit manipulation to add two numbers without using the + or - operators.\n\n- Use XOR (^) on a and b to add the bits where there is no carry.\n- Use AND (&) to find the positions where both a and b have a 1 (i.e., potential carry). Then shift the result left by one to add in the next higher bit.\n- Set a to the XOR result and b to the shifted carry.\n- Repeat steps 1-3 until there is no carry (b equals 0).\n- Return a which holds the final sum.\nThis approach leverages the bitwise operations to effectively simulate binary addition.",
    },
    "max-consecutive-ones": {
        "key_insights": "- Iterate over the array while keeping track of the current sequence length of 1's.\n\n- Reset the counter when a 0 is encountered.\n\n- Update the maximum value during the iteration.\n\n- This direct scanning results in a simple O(n) solution with constant space.",
        "complexity":   "Time Complexity: O(n) - We traverse the array once.\nSpace Complexity: O(1) - Only constant extra variables are used.",
        "solution":     "We iterate through the binary array and maintain two counters: one for the current sequence of consecutive 1's (currentCount) and one for the maximum sequence found so far (maxConsecutive). As we scan the array, we increment currentCount when we see a 1 and update maxConsecutive. When a 0 is found, we reset currentCount to 0. This approach leverages a simple linear pass through the array and uses a constant amount of extra space.",
    },
    "third-maximum-number": {
        "key_insights": "- We need to consider only distinct values in the array.\n\n- Use variables or a data structure to track the top 3 distinct maximum numbers.\n\n- We need an O(n) solution, so we should avoid unnecessary sorting.\n\n- Edge-case: If there are fewer than 3 distinct numbers, the maximum among them should be returned.",
        "complexity":   "Time Complexity: O(n) because we iterate through the array once.\n\nSpace Complexity: O(1) since we only maintain a fixed number of variables.",
        "solution":     "We iterate through the nums array and maintain three variables to store the first, second, and third distinct maximum numbers. For each number in the array, we check whether it is already one of the three maximums (to handle duplicates). If not, we update the maximums accordingly:\n\n- If the current number is greater than the first maximum, then update the first, second, and third maximums.\n\n- Else if the current number is between the first maximum and the second, update the second and third maximums accordingly.\n\n- Else if the current number is between the second maximum and the third, update the third maximum.\n\nAfter processing all elements, if the third maximum is still unassigned (i.e., there are less than three distinct numbers), return the first maximum. This approach uses constant extra space and only one pass through the input array.",
    },
    "shuffle-the-array": {
        "key_insights": "- The array is split into two halves: the first n elements and the last n elements.\n\n- Each element from the first half pairs with the corresponding element in the second half.\n\n- A single iteration from 0 to n-1 is sufficient to interleave the elements correctly.",
        "complexity":   "Time Complexity: O(n)\n\nSpace Complexity: O(n)",
        "solution":     "The solution involves iterating over the first half of the array and interleaving elements from both halves. Create an empty result array and for each index i from 0 to n-1, add nums[i] from the first half and nums[i+n] from the second half to the result array. This approach leverages a simple loop ensuring a linear time complexity while using extra space to store the final array.",
    },
    "remove-duplicates-from-sorted-array": {
        "key_insights": "- The array is sorted in non-decreasing order, meaning duplicate values appear consecutively.\n\n- Use a two-pointer approach:\n\n- One pointer to iterate through the array.\n\n- Another pointer to track the position to place the next unique element.\n\n- The solution modifies the array in place with O(1) extra space.",
        "complexity":   "Time Complexity: O(n), where n is the length of the input array.\n\nSpace Complexity: O(1) since the modification is done in-place.",
        "solution":     "The approach uses two pointers. The first pointer (uniqueIndex) tracks the position for the next unique element, starting from index 1. The second pointer iterates over the array from index 1 to the end. Whenever a new element (different from the previous element) is encountered, it is placed at uniqueIndex, and uniqueIndex is incremented. Finally, uniqueIndex represents the count of unique elements and denotes how many elements are valid in the modified array.",
    },
    "remove-element": {
        "key_insights": "- Use a two-pointer approach: one pointer iterates through the array while the other keeps track of the position for the next valid element.\n\n- Modify the array in-place, which ensures constant extra space usage.\n\n- The order of elements is not preserved, which allows swapping elements or simply overwriting the unwanted ones.",
        "complexity":   "Time Complexity: O(n)\nSpace Complexity: O(1)",
        "solution":     "The idea is to maintain a write pointer that indicates where the next valid element (not equal to val) should be placed. Iterate through the array with a read pointer. For every element that is not equal to val, write it to the position indicated by the write pointer and increment the write pointer. Once the loop completes, the write pointer will represent the number of valid elements (k) and the array's first k elements will be the desired result.",
    },
    "majority-element-ii": {
        "key_insights": "- There can be at most 2 elements that appear more than ⌊ n/3 ⌋ times.\n\n- A modified Boyer-Moore Voting Algorithm can be applied to track at most two potential candidates in one pass.\n\n- A second pass is needed to validate the candidates count.",
        "complexity":   "Time Complexity: O(n)\n\nSpace Complexity: O(1)",
        "solution":     "The solution uses a modified Boyer-Moore Voting Algorithm. Since an element must appear more than ⌊ n/3 ⌋ times to be valid, there can be at most two such candidates.\n\n- First, iterate through the array to select up to two candidates based on vote counts.\n\n- Then, re-iterate through the array to count the frequency of the selected candidates.\n\n- Finally, output the candidates that meet the threshold.\n\nThis approach ensures linear time complexity and constant extra space.",
    },
    "remove-duplicates-from-sorted-array-ii": {
        "key_insights": "- The array is already sorted, which allows for efficient duplicate detection.\n\n- Use a two-pointer technique: one pointer (slow) to track the position for the next valid element and another pointer (fast) to iterate through the array.\n\n- Since each element is allowed up to two occurrences, compare the current element with the element two positions before in the processed section.\n\n- In-place modifications ensure O(1) extra space usage.",
        "complexity":   "Time Complexity: O(n), where n is the length of the array, as we traverse the array once.\n\nSpace Complexity: O(1), because the modifications are done in-place with constant extra space.",
        "solution":     "We use two pointers to solve the problem in one pass. The slow pointer holds the index where the next valid element should be placed. The fast pointer scans each element in the array.\n\n- For indices less than 2, we simply copy them, since they are automatically valid.\n\n- For each subsequent element at index i, we compare it with the element at index (slow - 2). If they are not equal, it means adding the current element will not exceed the allowed two occurrences.\n\n- We then update the array in-place by writing the element at the slow pointer and incrementing the slow pointer.\n\n- Finally, all valid elements are stored in the first part of the array, and the slow pointer's value represents the new length.",
    },
    "best-time-to-buy-and-sell-stock-ii": {
        "key_insights": "- You can make as many transactions as needed as long as you are not holding more than one share.\n\n- The best strategy is to capture every upward movement in price.\n\n- Instead of finding the best pair of days for each transaction, sum the differences between consecutive days that indicate an increase.\n\n- This greedy approach avoids complex dynamic programming structures while still ensuring maximum profit.",
        "complexity":   "Time Complexity: O(n), where n is the length of the prices array, since we traverse the list once.\nSpace Complexity: O(1), as we only need a few integer variables for tracking profit.",
        "solution":     "The solution uses a greedy approach by iterating over the prices list and adding up profits each time there is a rise from one day to the next. If prices[i] is lower than prices[i+1], buying on day i and selling on day i+1 will add to the overall profit. The idea is that accumulating all these small profits yields the maximum profit possible by making as many transactions as necessary. The approach avoids explicit tracking of all transactions, requiring only constant extra space.",
    },
    "number-of-zero-filled-subarrays": {
        "key_insights": "- A subarray is contiguous. For every sequence of consecutive 0's of length L, there are L * (L + 1) / 2 subarrays.\n\n- Iterating through the array and counting consecutive zeros is an efficient approach.\n\n- Once a non-zero element is encountered, compute the contributions from the current streak and then reset the count.\n\n- Handle the edge case where the array ends with a sequence of zeros.",
        "complexity":   "Time Complexity: O(n), where n is the length of the array.\nSpace Complexity: O(1), as only a few counter variables are used.",
        "solution":     "We use a simple linear scan with an integer counter to track consecutive zeros. Every time we see a zero, we increase the count. When a non-zero is encountered (or at the end of the array), we calculate the number of subarrays for that block using the formula L * (L + 1) / 2 and add it to our total. This way, we ensure all zero-filled subarrays are considered efficiently.",
    },
    "increasing-triplet-subsequence": {
        "key_insights": "- You are asked to find three numbers in increasing order.\n\n- The goal is to achieve this in O(n) time and O(1) space.\n\n- Maintaining two variables for the smallest and second smallest numbers found so far allows checking if a greater third number exists.\n\n- Each element is compared to update these candidates based on a greedy approach.",
        "complexity":   "Time Complexity: O(n) — traverse the list once\n\nSpace Complexity: O(1) — only constant extra space is used",
        "solution":     "The algorithm scans through the array while keeping two variables: \"first\" representing the smallest value encountered so far and \"second\" for the next smallest value greater than \"first\". For each number:\n\n- If it is less than or equal to \"first\", update \"first\".\n\n- Else if it is less than or equal to \"second\", update \"second\".\n\n- Otherwise, a valid triplet is found and the function returns true.\nThis efficient approach uses the greedy method to maintain candidates for the first two numbers and automatically finds a valid third number if it exists.",
    },
    "reverse-string": {
        "key_insights": "- Use the two-pointer technique, with one pointer starting at the beginning and the other at the end of the array.\n\n- Swap the elements at these pointers and move them towards the center.\n\n- Stop when the pointers cross (or meet in the middle).",
        "complexity":   "Time Complexity: O(n), where n is the number of characters in the array.\n\nSpace Complexity: O(1) since only a constant amount of extra memory is used.",
        "solution":     "The solution uses the two-pointer approach to reverse the string in-place. By initializing one pointer at the start and one at the end of the array, we repeatedly swap the characters at these positions and then move them towards the center. This continues until the pointers meet or cross, ensuring the string is completely reversed without using additional space.",
    },
    "length-of-last-word": {
        "key_insights": "- Remove any trailing spaces to handle edge cases.\n\n- Find the last space in the string to isolate the last word.\n\n- The length of the last word is the difference between the total string length (after trimming) and the position of the last space.",
        "complexity":   "Time Complexity: O(n), where n is the length of the string (single pass to trim and another pass to count last word characters).\nSpace Complexity: O(1), using constant extra space.",
        "solution":     "We first trim the string to remove trailing spaces which may cause incorrect identification of the last word. Then, we locate the last space character in the trimmed string. The length of the last word is calculated as the difference between the total string length and the index of the last space minus one. This approach uses basic string operations and requires constant space while processing the string in linear time.",
    },
    "is-subsequence": {
        "key_insights": "- Use a two-pointer approach: one pointer traversing s and one traversing t.\n\n- As you iterate through t, if the current character matches the current character in s, move the pointer in s.\n\n- If the pointer in s reaches the end, all characters were matched in order, so s is a subsequence of t.\n\n- In the follow-up scenario with many s strings and one t, preprocess t by mapping each character to its indices, enabling binary search for the next occurence in t for each character of s.",
        "complexity":   "Time Complexity: O(n + m), where n is the length of t and m is the length of s.\nSpace Complexity: O(1) for the simple two-pointer approach. The preprocessing method (follow-up) requires extra space for the mapping, i.e., O(n).",
        "solution":     "We use a two-pointer technique to solve the problem. One pointer goes through the string s while the other goes through t. Every time a matching character is found, move the pointer in s. If by the end of t the pointer in s has reached the end, then s is a subsequence of t. For the follow-up scenario with many s queries, pre-process t by creating a mapping from each character to a list of its positions in t. Then for each s, use binary search to quickly find a valid next position for each character.",
    },
    "valid-palindrome-ii": {
        "key_insights": "- Use two pointers starting from the beginning and end of the string.\n\n- Compare characters; if they match, move both pointers inward.\n\n- When a mismatch is found, you have two possibilities: skip the left character or the right character.\n\n- Check if either of the resulting substrings is a palindrome.\n\n- The approach effectively leverages a greedy method and operates in linear time.",
        "complexity":   "Time Complexity: O(n), where n is the length of the string.\nSpace Complexity: O(1), using only constant extra space.",
        "solution":     "The solution uses a two-pointer technique—one pointer starts at the beginning of the string and another at the end. They move towards each other while comparing characters. If a mismatch occurs, the algorithm tries skipping either the left or right character and then checks if the remaining substring forms a palindrome. This additional check is done using a helper function that verifies if a given substring is a palindrome. This strategy ensures that the algorithm only allows one deletion, conforming to the constraint.",
    },
    "rotate-string": {
        "key_insights": "- Both strings must be of equal length for s to be rotated into goal.\n\n- By concatenating s with itself (s + s), all possible rotations of s are represented as continuous substrings.\n\n- We simply need to check if goal is a substring of s + s.",
        "complexity":   "Time Complexity: O(n) on average (n is the length of s) assuming an efficient substring search.\nSpace Complexity: O(n) due to storing the concatenated string.",
        "solution":     "The solution leverages string concatenation. If s and goal have the same length, concatenating s with itself yields a new string that contains every rotation of s as a contiguous substring. Therefore, we only need to check if goal exists within this concatenated string. This solution uses simple string operations and takes advantage of built-in substring searching.",
    },
    "find-the-index-of-the-first-occurrence-in-a-string": {
        "key_insights": "- Search for a substring (needle) within a larger string (haystack).\n\n- A straightforward approach is to iterate and compare potential starting positions.\n\n- The naive approach checks each possible starting index in haystack, while more advanced techniques (like KMP) can improve efficiency, but the naive approach is often sufficient given the constraints.\n\n- Special edge-case: if needle is an empty string (as typically defined in some similar problems) but here constraints guarantee positive length.",
        "complexity":   "Time Complexity: O(n * m) in the worst case, where n is the length of haystack and m is the length of needle.\nSpace Complexity: O(1) as no additional space is used.",
        "solution":     "The solution uses a brute force method with two pointers. We iterate over each possible starting position in haystack and check if the substring starting there matches the needle. If we find a match, we return the current index as the first occurrence. If no match is found after checking all possible positions, we return -1. This approach is simple and directly implements the problem's requirement.",
    },
    "zigzag-conversion": {
        "key_insights": "- The zigzag pattern can be simulated by iterating over the characters and appending them to rows.\n\n- Maintain an index for the current row and a direction (up or down) for adding characters.\n\n- When the direction reaches the top or bottom row, the traversal direction reverses.\n\n- Concatenating all rows produces the final result.",
        "complexity":   "Time Complexity: O(n), where n is the length of the string.\nSpace Complexity: O(n), used to store characters in rows.",
        "solution":     "The solution involves simulating the zigzag writing process by iterating over the string once while keeping track of the current row and the direction of movement. An array (or list) of strings is used where each element corresponds to a row in the zigzag pattern. As we iterate through the string:\n\n- Append the current character to the string corresponding to the current row.\n\n- Reverse the direction if the current row is either the top or bottom row.\n\n- Adjust the current row index accordingly.\n\n- Finally, join all row strings to produce the output.\n\nThis simulation approach efficiently arranges the characters and is intuitively easy to follow.",
    },
    "count-and-say": {
        "key_insights": "- The problem is essentially generating a sequence where each term is produced by describing the previous term.\n\n- Use run-length encoding to count consecutive digits.\n\n- The sequence is generated iteratively from 1 up to n.\n\n- Iterative approach is straightforward and favored over recursion in this follow-up.",
        "complexity":   "Time Complexity: O(n * m) where m is the average length of the sequence string generated at each step.\n\nSpace Complexity: O(m) for maintaining the current string and the next generated string.",
        "solution":     "We start with the base string \"1\" for n=1. For each subsequent term from 2 to n, iterate over the current string to count consecutive identical digits (run-length encoding). Append the count followed by the digit to form the next term. This iterative process continues until we reach the nth term.\n\nKey data structures include:\n\n- Strings for storing the current term.\n\n- Temporary string builders (or string concatenation) for constructing each new term.\nThe algorithm uses a simple loop through the string and conditionally appends to the new string when a digit changes, representing a group.",
    },
    "reverse-words-in-a-string": {
        "key_insights": "- Use built-in functions to trim and split the string by spaces.\n\n- Reverse the list of words.\n\n- Join the reversed list using a single space.\n\n- Handle edge cases such as multiple spaces between words and leading/trailing spaces.\n\n- In languages with mutable strings, consider an in-place algorithm for O(1) extra space as a follow-up.",
        "complexity":   "Time Complexity: O(n), where n is the length of the input string.\nSpace Complexity: O(n), to store the split words (unless an in-place algorithm is implemented).",
        "solution":     "The main idea is to clean up the string by removing extra spaces and then split the string into words. After obtaining a list of words, we simply reverse the list and join the words with a single space between them. This approach leverages string trimming, splitting, reversing, and joining, which are available in many programming languages. A potential gotcha is ensuring that no extra spaces are left in the output, which is handled by trimming and using the join function appropriately.",
    },
    "determine-if-two-strings-are-close": {
        "key_insights": "- The set of unique characters in both strings must be identical; if they differ, no series of transformations can make the strings equal.\n\n- The frequency distribution (multiset of frequencies) of characters can be rearranged via transformations; hence, sorting the list of frequencies should yield the same result if the strings are close.\n\n- Swapping can rearrange characters arbitrarily, so positions don't matter—only the counts do.\n\n- Use hash maps (or frequency counters) to count occurrences and then compare.",
        "complexity":   "Time Complexity: O(n + m + k log k) where n and m are the lengths of the two strings and k is the number of unique characters (at most 26).\n\nSpace Complexity: O(k), for storing the frequencies.",
        "solution":     "The solution involves two main checks:\n\n- Ensure both strings have exactly the same unique set of characters.\n\n- Compare the sorted frequency counts of each string.\n\nIf both conditions are met, the strings can be transformed into each other using the allowed operations. Hash maps (or dictionaries) are used to count character frequencies, and sorting is applied on their values to compare frequency distributions.",
    },
    "guess-the-word": {
        "key_insights": "- Use a strategy based on eliminating words from the candidate set based on the number of matching letters returned by the Master.guess call.\n\n- Define a helper function to calculate the number of matching character positions between any two words.\n\n- Each guess helps you narrow down the candidate words: you only keep words in the list that would give the same matching count with the guessed word as the Master.guess output.\n\n- Although brute-force could work given a small wordlist, a strategy such as minimax or simply narrowing down the candidate set by eliminating non-conforming words is sufficient.\n\n- This problem is interactive in nature; careful management of the guess count and candidate filtering is paramount.",
        "complexity":   "Time Complexity: In the worst-case scenario, each guess requires checking against all candidates with O(n) comparisons and each comparison takes O(word_length), leading to O(n^2 * word_length).\nSpace Complexity: O(n) for storing the candidate list.",
        "solution":     "The solution involves iteratively guessing a word from the current candidate list and then filtering the candidate list based on the number of matching letters reported by Master.guess. A helper function computes the number of matching letters between two words. After each guess, the candidate list is reduced to only those words that would have produced the same matching score with the guessed word. This process continues until the secret word is found or the allowed number of guesses is exceeded. The use of candidate elimination is a key trick here, ensuring that each guess reduces the possible search space.",
    },
    "set-mismatch": {
        "key_insights": "- The array is of length n and is intended to contain numbers from 1 to n.\n\n- One number appears twice while another number is missing entirely.\n\n- A simple way is to use a frequency count (often implemented via an array or hash table) to detect the duplicate.\n\n- Alternatively, arithmetic properties (sum and sum of squares) or XOR can be exploited to solve the problem, but using a hash table is straightforward for an easy constraint.",
        "complexity":   "Time Complexity: O(n)\n\nSpace Complexity: O(n)  (due to using an extra data structure for frequency count)",
        "solution":     "We leverage a hash table (or an auxiliary array) to count the occurrences of each number in the array. As we iterate through the array, we detect the number that occurs twice. Then, by iterating through the numbers from 1 to n, we can determine which number is missing since it will have a count of 0.\n\nAlgorithm:\n\n- Initialize an empty hash table (or an auxiliary list) to keep track of counts.\n\n- Iterate through nums and update the count of each number.\n\n- While updating counts, identify the number that appears twice.\n\n- Iterate from 1 through n to find the missing number (the one with 0 count).\n\n- Return the duplicate and missing numbers as an array.",
    },
    "power-of-two": {
        "key_insights": "- A power of two in binary representation contains exactly one set bit.\n\n- Negative numbers and zero cannot be powers of two.\n\n- The bit manipulation trick n &#x26; (n - 1) can be used to determine if there's exactly one set bit (for n > 0).",
        "complexity":   "Time Complexity: O(1)\n\nSpace Complexity: O(1)",
        "solution":     "The algorithm checks whether n is a power of two by leveraging the fact that powers of two have exactly one set bit. First, the check n > 0 ensures the number is positive because only positive numbers can be powers of two. Then, by performing n &#x26; (n - 1), we verify that the number has only one set bit. If the result is 0, then n is a power of two; otherwise, it is not. This solution executes in constant time and uses constant space.",
    },
    "hamming-distance": {
        "key_insights": "- The Hamming distance between two integers can be obtained by computing the XOR of x and y.\n\n- The XOR operation highlights the bits that differ between the two numbers.\n\n- Counting the number of set bits (1s) in the XOR result gives the required Hamming distance.\n\n- An efficient method to count set bits is to repeatedly clear the least significant bit set using bit manipulation (n = n &#x26; (n - 1)).",
        "complexity":   "Time Complexity: O(1) (since the numbers are 32-bit, the operation takes constant time)\n\nSpace Complexity: O(1)",
        "solution":     "The solution involves computing the XOR of the two given integers. The resulting number will have bits set only at the positions where x and y differ. The next step is to count the number of 1s in this XOR result. This count directly represents the Hamming distance. We can count the set bits by iteratively removing the lowest set bit (using the operation n = n &#x26; (n - 1)) until the number becomes zero. This approach is efficient and leverages basic bit manipulation techniques.",
    },
    "bitwise-and-of-numbers-range": {
        "key_insights": "- The bitwise AND operation only retains a bit if that bit is 1 in every number in the given range.\n\n- The key observation is that the range of numbers will eventually force a bit position to flip from 1 to 0; hence, only the common prefix (the leftmost bits that do not change when moving from left to right) contributes to the final result.\n\n- The solution involves shifting both left and right until they match, tracking the number of shifts, and then shifting back to obtain the common bits.",
        "complexity":   "Time Complexity: O(log n), where n is the number represented by right, since we process a fixed number of bits (typically 32 bits for 32-bit integers).\nSpace Complexity: O(1), since only a few integer variables are used.",
        "solution":     "The main idea is to discard the differing bits between left and right. We do this by shifting both numbers to the right until they become equal, which effectively removes the non-common bits at the right. The number of shifts performed tells us how many trailing zeros must be added back by shifting left. This approach efficiently finds the common prefix shared by both numbers, which represents the bitwise AND of all numbers in the given range.\n\nData Structures Used:\n\n- Only integer variables are used.\n\nAlgorithmic Approach:\n\n- While left is not equal to right, right-shift both left and right (i.e., remove the least significant bit).\n\n- Count the number of shifts performed.\n\n- Once left equals right, shift back to the left by the counted amount to restore the common prefix in its original bit positions.\n\n- The final number is the bitwise AND of all numbers in the range.",
    },
    "single-number-ii": {
        "key_insights": "- Use bit manipulation to solve the problem in O(n) time with O(1) extra space.\n\n- The core idea is to track the bit counts across all numbers and use modulo operations to cancel out bits that appear three times.\n\n- Instead of using extra arrays for bit counts, maintain two accumulator variables to represent bits seen once and twice.\n\n- Leverage bitwise XOR, AND, and NOT operations to update and clear bits accordingly.",
        "complexity":   "Time Complexity: O(n)\nSpace Complexity: O(1)",
        "solution":     "The approach uses two accumulators, named ones and twos, to track the bits that have appeared once and twice, respectively. For each number in the array, we update these accumulators:\n\n- Calculate new values for ones by XOR-ing with the current number and removing any bits that are already in twos.\n\n- Update twos accordingly using bits that appear and are not newly reintroduced into ones.\nAfter processing all numbers, the variable ones will contain the unique single number because the bits corresponding to numbers that appear three times cancel out.",
    },
    "single-number-iii": {
        "key_insights": "- XOR of two identical numbers is 0.\n\n- XOR of all numbers results in the XOR of the two unique numbers.\n\n- A set bit in the XOR results (xorTotal) indicates a bit position where the two unique numbers differ.\n\n- Partitioning the array based on this set bit allows isolation of each unique number.",
        "complexity":   "Time Complexity: O(n)\n\nSpace Complexity: O(1)",
        "solution":     "Use XOR to combine all numbers, which cancels out the pairs and leaves the XOR of the two unique numbers. Identify any set bit in this XOR to differentiate between the two unique numbers. Partition the numbers based on whether they have that bit set and XOR each group separately. The result are the two unique numbers. This approach leverages bit manipulation with constant extra space and a single pass (or two passes) over the input.",
    },
    "word-pattern": {
        "key_insights": "- We need a one-to-one mapping between characters in the pattern and words in s.\n\n- Use two hash maps (or dictionaries): one to map characters to words and another to map words to characters.\n\n- Ensure that the number of words in s equals the length of the pattern.\n\n- Check for any conflict during the mapping process.",
        "complexity":   "Time Complexity: O(n) where n is the number of words (or the length of the pattern), as we iterate through both simultaneously.\nSpace Complexity: O(n) for the mappings stored in the hash maps.",
        "solution":     "The approach is to first split the string s into words and verify that its length matches the length of the pattern. Then, iterate through each letter of the pattern along with the corresponding word, and do the following:\n\n- If the pattern character is not in the char-to-word mapping, assign it, but ensure that the corresponding word is also not already assigned to a different character in the word-to-char mapping.\n\n- If the pattern character already exists in the mapping, check that the mapped word matches the current word.\n\n- Return false at any point a conflict is found; otherwise, return true if the entire pattern is processed without error.\n\nThis dual-check via two hash maps ensures the bijection property is maintained.",
    },
    "first-unique-character-in-a-string": {
        "key_insights": "- Use a hash table (or fixed-size array) to count the frequency of each character in the string.\n\n- Iterate over the string a second time to find the first character with a frequency of one.\n\n- The solution handles the case where no unique character exists by returning -1.",
        "complexity":   "Time Complexity: O(n) - two passes over the string.\nSpace Complexity: O(1) - only 26 lowercase letters are stored, which is a constant space.",
        "solution":     "This solution leverages a frequency counting approach with a hash table (or fixed-size array for the 26 lowercase letters). In the first pass, the frequency of each character in the string is recorded. In the second pass, the string is iterated again to locate the first character with a count of one. If found, its index is returned; if not, -1 is returned. This strategy provides an efficient way to solve the problem with just two passes over the input string.",
    },
    "find-all-numbers-disappeared-in-an-array": {
        "key_insights": "- The input array contains integers from 1 to n, so each value can be mapped to an index in the array.\n\n- In-place modification can be achieved by marking the values at these mapped indices to indicate presence.\n\n- By iterating over the array after marking, the indices that were not marked (i.e., still positive) correspond to the missing numbers.\n\n- This solution avoids using extra space beyond the output array.",
        "complexity":   "Time Complexity: O(n)\nSpace Complexity: O(1) (excluding the space for the output array)",
        "solution":     "We iterate through the array and for each number, we determine its corresponding index (by taking the absolute value of the number and subtracting one). We then mark the number at that index as negative to indicate that the number (index + 1) exists in the array. Finally, we iterate through the array once more and collect all indices that still hold positive values; these indices correspond to numbers that did not appear in the original array.",
    },
    "maximum-number-of-balloons": {
        "key_insights": "- Count the frequency of each character in the text.\n\n- The target word \"balloon\" requires:\n\n- b: 1\n\n- a: 1\n\n- l: 2\n\n- o: 2\n\n- n: 1\n\n- The number of times \"balloon\" can be formed is limited by the minimum value among these frequencies (taking into account that l and o are needed twice).",
        "complexity":   "Time Complexity: O(n), where n is the length of text.\nSpace Complexity: O(1), as the frequency count uses constant space (only lower-case English letters).",
        "solution":     "We first build a frequency map of characters in the input text using a hash table (or dictionary). For the word \"balloon\", we calculate the possible counts by taking:\n\n- The count of 'b'\n\n- The count of 'a'\n\n- The count of 'n'\n\n- The count of 'l' divided by 2 (since two are needed)\n\n- The count of 'o' divided by 2 (since two are needed)\n\nThe answer is the minimum of all these values, ensuring that all necessary characters are available in the required quantities. This approach is efficient due to single-pass counting and constant extra space.",
    },
    "number-of-good-pairs": {
        "key_insights": "- Use counting to determine how many times each number appears.\n\n- For every occurrence of a number, the number of good pairs contributed by that occurrence is the count of that number seen so far.\n\n- A hash table is ideal for tracking frequencies quickly.\n\n- The pair counting can be done in one pass through the array.",
        "complexity":   "Time Complexity: O(n) where n is the length of the input array.\nSpace Complexity: O(n) in the worst case due to the frequency hash table.",
        "solution":     "We utilize a hash table (or dictionary) to map each number to its frequency of occurrence as we iterate through the list. Each time we encounter a number, we add its current frequency (which represents the number of times it has appeared before) to the overall count of good pairs. Then, we increment its frequency in the hash table. This method counts the pairs in a single pass, making the solution efficient in both time and space.",
    },
    "isomorphic-strings": {
        "key_insights": "- Use a mapping (dictionary/hash table) from characters in s to characters in t.\n\n- Use a second mapping from characters in t back to s to ensure a one-to-one correspondence.\n\n- If any mapping is inconsistent, the strings are not isomorphic.\n\n- Traverse both strings simultaneously.",
        "complexity":   "Time Complexity: O(n), where n is the length of the strings, as we iterate through each character once.\nSpace Complexity: O(1) under the assumption that the character set is fixed (ASCII), otherwise O(n) in the worst case.",
        "solution":     "The algorithm iterates over each character pair from s and t. For each pair, it checks two mappings:\n\n- If the current character from s has been previously mapped, it must map to the current character in t.\n\n- If the current character from t has been previously mapped, it must map to the current character in s.\nIf both conditions are satisfied for all character pairs, then the strings are isomorphic.\nData structures used include two dictionaries (or hash tables) to keep track of the mappings from s to t and t to s.",
    },
    "contains-duplicate-ii": {
        "key_insights": "- Use a hash table (or a sliding window) to keep track of the most recent index where each number appears.\n\n- For every element, if it is already in the hash table, check the difference between the current index and its previously stored index.\n\n- If the difference is within k, return true immediately.\n\n- Otherwise, update the index for the element.\n\n- Alternatively, maintain a sliding window (using a set) of size at most k to check for duplicates in the current window.",
        "complexity":   "Time Complexity: O(n) — where n is the number of elements in nums, since each element is processed once.\nSpace Complexity: O(min(n, k)) — using a hash table or set that stores at most k elements at any time.",
        "solution":     "We leverage a hash table to store numbers and their most recent indices. As we iterate through the array, we check if the current number is already in the table. If so, we examine the distance between the current index and the saved index. Should this distance be at most k, we can immediately return true. If not, we update the stored index for that number. This method ensures only a single pass through the array is required. An alternative approach is to use a sliding window implemented via a set, where we maintain a window of size k. If a duplicate is encountered within the window, we return true.",
    },
    "intersection-of-two-arrays-ii": {
        "key_insights": "- Use a hash table to count the occurrences of each number in one array.\n\n- Iterate through the second array, and for each number, check if it exists in the hash table with a non-zero count.\n\n- If it does, add the number to the result and decrement the count in the hash table.\n\n- For sorted arrays, a two-pointer approach can be used for optimal performance.\n\n- Consider memory-efficient methods when one of the arrays is too large to fit in memory.",
        "complexity":   "Time Complexity: O(n + m), where n and m are the lengths of the two arrays.\nSpace Complexity: O(min(n, m)), since the hash table stores counts for the smaller array.",
        "solution":     "We solve the problem by first storing the frequency of each element from one input array using a hash table (dictionary). Then, we iterate through the second array and check if the element is present in the hash table with a positive count. If it is, we add it to the result array and reduce the count. This ensures that each common element is added as many times as it appears in both arrays. If the arrays are sorted, a two-pointer technique can be used where both arrays are traversed simultaneously, providing an alternative approach with potentially lower overhead in specific scenarios.",
    },
    "encode-and-decode-tinyurl": {
        "key_insights": "- Use a hash table (or dictionary/map) to store the mapping between the tiny URL code and the original long URL.\n\n- Generate a unique key (e.g., a random 6-character string) to serve as the shortened identifier.\n\n- Handle potential collisions by regenerating the key when necessary.\n\n- Both encoding and decoding operations should be efficient (preferably O(1) time complexity).",
        "complexity":   "Time Complexity: O(1) for both encode and decode operations on average.\n\nSpace Complexity: O(n), where n is the number of URLs stored.",
        "solution":     "We use a hash map to maintain the mapping between a randomly generated 6-character code and its corresponding long URL. For the encode function, a random code is generated and then concatenated with a base URL (e.g., http://tinyurl.com/). If the generated code already exists in the hash map (collision), we generate a new one until a unique code is obtained. The decode function simply extracts the code part from the tiny URL and looks up the original URL in the hash map.",
    },
    "reorganize-string": {
        "key_insights": "- Count the frequency of each character.\n\n- A valid reorganization is impossible if any character’s frequency exceeds half of the string length (rounded up).\n\n- Use a max heap (priority queue) to always choose the character with the highest remaining frequency.\n\n- Place the most frequent character and then pick the next most frequent; add the previous character back if it still has remaining frequency.\n\n- This greedy strategy ensures that the most frequent characters are always spaced apart.",
        "complexity":   "Time Complexity: O(n log k), where n is the length of the string and k is the number of unique characters.\nSpace Complexity: O(k) for the frequency map and heap.",
        "solution":     "The solution begins by counting the frequency of each character in the string. If any character appears more than (n + 1) / 2 times, then a valid arrangement is impossible.\n\nNext, use a max heap to always select the character with the highest remaining frequency. Repeatedly extract the top two characters from the heap, append them to the result, and then reduce their frequency. If a character still has a positive frequency after decrementing, push it back into the heap.\n\nThis greedy strategy guarantees that no two adjacent characters are the same, because by always pairing the most frequent remaining characters, they are distributed as evenly as possible.",
    },
    "split-array-into-consecutive-subsequences": {
        "key_insights": "- Use a greedy approach to form the subsequences.\n\n- Utilize a frequency map to track how many times each number appears.\n\n- Use an additional map to track how many subsequences ending with a certain number need to be extended.\n\n- For each number, either extend an existing subsequence or start a new one if possible.\n\n- If neither is possible, then it is not possible to form valid subsequences.",
        "complexity":   "Time Complexity: O(n) where n is the number of elements in nums\n\nSpace Complexity: O(n) for storing the frequency and subsequence extension maps",
        "solution":     "We first count the frequency of every number in the array. Then, we iterate through the array and try to place each number:\n\n- If a number is already part of an existing subsequence (tracked via the extension map), then extend that subsequence.\n\n- Otherwise, check if the number can start a new subsequence (i.e., the next two consecutive numbers are available).\n\n- If neither condition holds, return false immediately.\nThis greedy algorithm ensures that subsequences are extended whenever possible, helping to avoid wasteful starts of new subsequences that might block later extensions.",
    },
    "number-of-matching-subsequences": {
        "key_insights": "- Instead of checking each word individually against s, leverage the fact that s is fixed and iterate through it to \"advance\" pointers for each word.\n\n- Use a mapping (or bucket) from characters to lists of iterators/pointers representing the next character needed by each word.\n\n- As we iterate over s, update the waiting lists and count when a word is fully matched.\n\n- This approach avoids redundant scanning and significantly reduces time complexity.",
        "complexity":   "Time Complexity: O(n + m) where n is the length of s and m is the total number of characters in all words\n\nSpace Complexity: O(m) for storing the waiting pointers for each character in all words",
        "solution":     "We initialize a dictionary (hash map) that maps each letter (a-z) to a list of word iterators or pointers. Each word in words is associated with its starting character. Then, iterate through each character in s. For the current character, retrieve and clear its waiting list and advance each word’s pointer. If a word is completely matched (i.e., all its characters have been processed), increment the result counter. Otherwise, push the word pointer into the bucket corresponding to its next required character. This technique efficiently checks for subsequences in a single pass over s with minimal repeated scanning.",
    },
    "number-of-good-ways-to-split-a-string": {
        "key_insights": "- Use two passes: one forward to compute the running count of distinct characters from the beginning, and one backward to compute the running count from the end.\n\n- By precomputing the distinct letter count for every possible split point, the count for each potential split can be quickly compared.\n\n- A simple iteration through potential split indices lets us count the number of valid splits.",
        "complexity":   "Time Complexity: O(n), where n is the length of the string, as we traverse the string twice.\nSpace Complexity: O(n), used for storing the distinct character counts for each prefix and suffix.",
        "solution":     "We solve the problem in multiple steps. First, we create an array, leftDistinct, where leftDistinct[i] represents the number of unique characters from the start of the string to index i. We then create a similar array, rightDistinct, where rightDistinct[i] represents the number of unique characters from index i to the end of the string. Finally, we iterate over all valid split positions (from 0 to n-2) and count the splits where leftDistinct[i] equals rightDistinct[i+1]. The main trick here is the efficient counting of distinct characters using a set in a single pass, making the solution linear in time complexity.",
    },
    "minimum-deletions-to-make-character-frequencies-unique": {
        "key_insights": "- Count the frequency of each character in the string.\n\n- Use a greedy approach to ensure that each frequency is unique.\n\n- Utilize a set (or similar data structure) to keep track of frequencies already used.\n\n- For each character’s frequency, if it conflicts with an already used frequency, reduce the frequency (simulate deletion) until it becomes unique or zero.\n\n- The problem leverages sorting or iterating over a small fixed alphabet (26 letters) for efficiency.",
        "complexity":   "Time Complexity: O(n) where n is the length of the string (plus a negligible factor for iterating over at most 26 unique characters).\n\nSpace Complexity: O(1) since the frequency dictionary and set have at most 26 keys.",
        "solution":     "The solution begins by counting the frequency of every character in the string. Then, iterate over these frequencies and use a set to check for uniqueness. If a frequency already exists in the set, decrement the frequency (and count a deletion) until you either find a unique frequency or drop to 0. This greedy adjustment minimizes the total number of deletions required. The main data structures used are a hash map (or dictionary) for frequency counting and a set to track used frequencies.",
    },
    "merge-strings-alternately": {
        "key_insights": "- Use two pointers to iterate through both strings.\n\n- Append characters alternately until one string is exhausted.\n\n- After the loop, append any remaining characters from the longer string.\n\n- Ensure an O(n + m) time complexity where n and m are the lengths of the two strings.",
        "complexity":   "Time Complexity: O(n + m)\n\nSpace Complexity: O(n + m)",
        "solution":     "The solution involves initializing two pointers (or indices) for the two strings and iterating over them simultaneously. At each iteration, add the character from word1 followed by the character from word2. When one pointer reaches the end of its string, append the remainder of the other string to the result. This approach employs a straightforward two-pointer technique and string concatenation.",
    },
    "count-binary-substrings": {
        "key_insights": "- The substrings we are looking for are periods where a block of 0's is directly followed by a block of 1's (or vice versa).\n\n- The valid substrings are determined by the minimum length of two consecutive groups.\n\n- We can solve this problem by compressing the string into a list of counts representing consecutive groups of the same character.\n\n- The answer is the sum of the min(count[i], count[i+1]) for all valid consecutive groups.",
        "complexity":   "Time Complexity: O(n) where n is the length of the string.\nSpace Complexity: O(n) in the worst-case scenario (when all characters are alternating) which can be optimized to O(1) by processing on the fly.",
        "solution":     "We first iterate over the string to count consecutive character groups. Once we have these counts, for each pair of adjacent groups, the number of valid substrings that can be formed is the minimum of the two counts. This works because if a group of 0's is of size x and the following group of 1's is of size y, then there are min(x, y) valid substrings that start in one group and extend into the next.\n\nThe solution leverages a two-pass approach (or even a single-pass approach by keeping track of the previous group count) and uses simple arithmetic to compute the final answer.",
    },
    "4sum": {
        "key_insights": "- Sort the array to simplify handling duplicates and employ two pointers efficiently.\n\n- Use nested loops to fix the first two numbers and then use the two-pointer technique for the remaining two numbers.\n\n- Carefully skip duplicates at each step to ensure all quadruplets in the result are unique.\n\n- Manage potential overflow or large input sizes by careful index manipulation.",
        "complexity":   "Time Complexity: O(n^3)\n\nSpace Complexity: O(n) for the output space assuming sorting space does not count.",
        "solution":     "The solution begins by sorting the input array to make duplicate elimination easier. Two nested loops are used to select the first and second elements of the quadruplet. For the remaining two numbers, the two-pointer approach is applied by initializing pointers at the start and the end of the remaining subarray. The sum of the four numbers is compared to the target:\n\n- If the sum equals the target, the quadruplet is added to the results and the pointers move inward while skipping duplicate numbers.\n\n- If the sum is less than the target, the left pointer is moved right to increase the sum.\n\n- If the sum is greater than the target, the right pointer is moved left to decrease the sum.\nThis strategy efficiently finds all unique quadruplets that meet the condition.",
    },
    "string-compression": {
        "key_insights": "- Use a two-pointer approach: one pointer for reading the original characters and one for writing the compressed result.\n\n- Iterate through the array, counting consecutive duplicates.\n\n- Write the character to the result followed by its count (if greater than 1), converting the count to its string representation.\n\n- The algorithm works in-place with constant extra space aside from the counter and temporary variables.",
        "complexity":   "Time Complexity: O(n), where n is the number of characters in the array.\nSpace Complexity: O(1) (in-place compression with only a few extra variables).",
        "solution":     "The solution leverages a two-pointer technique:\n\n- A \"read\" pointer traverses the array to identify groups of repeated characters.\n\n- A \"write\" pointer updates the array with the compressed characters.\n\n- For each group, after writing the character, if the group size is more than 1, the count is converted to a string and each digit is written sequentially.\n\n- This in-place modification ensures constant extra space and the entire array is scanned once, resulting in O(n) time complexity.",
    },
    "boats-to-save-people": {
        "key_insights": "- Sort the list of people's weights.\n\n- Use the two pointers technique: one pointer at the beginning (lightest) and one at the end (heaviest).\n\n- If the sum of the weights at both pointers is within the boat's limit, pair them and move both pointers.\n\n- Otherwise, assign the heavier person alone and move the pointer for the heaviest.\n\n- Continue until all people are accounted for.",
        "complexity":   "Time Complexity: O(n log n) due to the sorting step.\nSpace Complexity: O(1) additional space (ignoring the space taken by sorting, which may be O(log n) depending on the implementation).",
        "solution":     "The solution uses a greedy approach combined with the two pointers technique. After sorting the array, we initialize two pointers: one at the start (pointing to the lightest person) and one at the end (pointing to the heaviest person). In each iteration, we check if the combined weight of the persons at these pointers is within the limit. If so, we pair them in a single boat and move both pointers. Otherwise, the heavier person boards a boat alone, and we move the pointer for the heaviest person. This process repeats until all people have been allocated a boat. This approach ensures that we use the minimum number of boats.",
    },
    "count-subarrays-with-fixed-bounds": {
        "key_insights": "- Use a sliding window to consider only valid segments where every element is between minK and maxK.\n\n- Keep track of the most recent indices where minK and maxK appeared.\n\n- Reset the window when an invalid element (less than minK or greater than maxK) is encountered.\n\n- The number of valid subarrays ending at a position can be determined by the distance from the most recent invalid index to the minimum of the last occurrences of minK and maxK.",
        "complexity":   "Time Complexity: O(n), where n is the length of the array since we make a single pass.\nSpace Complexity: O(1), as we use only a constant amount of extra space.",
        "solution":     "Use a single traversal of the array while maintaining three pointers:\n\n- lastInvalid: index of the most recent element that is outside the [minK, maxK] range.\n\n- lastMin: index of the most recent occurrence of minK.\n\n- lastMax: index of the most recent occurrence of maxK.\n\nFor each element:\n\n- If it is invalid (not in [minK, maxK]), update lastInvalid.\n\n- Update lastMin or lastMax if the element is equal to minK or maxK.\n\n- The number of valid subarrays ending at the current index is max(0, min(lastMin, lastMax) - lastInvalid).\n\nAccumulate this count over the array.",
    },
    "running-sum-of-1d-array": {
        "key_insights": "- The problem requires computing cumulative sums, also known as prefix sums.\n\n- Iterating over the array while maintaining a running total yields the answer.\n\n- In-place modification is possible if allowed, but creating a new array maintains immutability.\n\n- The constraints are small enough to allow a simple O(n) solution.",
        "complexity":   "Time Complexity: O(n) because we process each element once.\nSpace Complexity: O(n) if we store the running sum in a new array; O(1) extra space if modified in-place.",
        "solution":     "The solution uses a simple iterative approach. Initialize a variable to keep track of the cumulative sum. Then iterate over the input array, updating the cumulative sum with each element and storing it in the resulting array. This approach uses the array itself (or an additional output array) to build the running sum in a single pass through the data. The algorithm leverages the concept of prefix sum which allows for an efficient calculation with minimal additional memory overhead.",
    },
    "range-sum-query-immutable": {
        "key_insights": "- Precompute a prefix sum array during initialization.\n\n- The sum for any given range can be computed in O(1) by subtracting the prefix sum at the starting index from the prefix sum at the ending index + 1.\n\n- This approach makes use of extra O(n) space but provides fast queries.",
        "complexity":   "Time Complexity: O(n) for preprocessing and O(1) per query.\n\nSpace Complexity: O(n) for storing the prefix sum array.",
        "solution":     "We build a prefix sum array such that each element at index i+1 represents the sum of the first i elements. For any query (left, right), the sum of elements between left and right is given by prefix[right+1] - prefix[left]. This approach leverages preprocessing to optimize query performance.",
    },
    "find-pivot-index": {
        "key_insights": "- Use a prefix sum strategy by first calculating the total sum of the array.\n\n- As you iterate over the array, maintain the sum of numbers seen so far (left sum).\n\n- At any index, the right sum can be derived by subtracting the left sum and the current element from total sum.\n\n- When left sum equals right sum, you've found the pivot index.\n\n- Only one pass through the array is needed, resulting in O(n) time complexity.",
        "complexity":   "Time Complexity: O(n)\n\nSpace Complexity: O(1)",
        "solution":     "We start by computing the total sum of the array. Then, we use a single loop to traverse the array and maintain a running sum (left sum). For each index, the right sum is computed as total sum minus left sum minus the current element. If the left sum equals the right sum, we return the current index as the pivot index. If no index satisfies this condition, we return -1.",
    },
    "maximum-population-year": {
        "key_insights": "- Use a difference array (or prefix sum technique) to mark changes in population over the years.\n\n- Increment the population counter at the birth year and decrement at the death year.\n\n- Traverse through the years (from 1950 to 2050) to compute the prefix sum, which gives the population for each year.\n\n- Track the maximum population and update the earliest year whenever a new maximum is found.",
        "complexity":   "Time Complexity: O(R) where R is the range of years (a constant range from 1950 to 2050, so effectively O(1)).\nSpace Complexity: O(R) for the difference array (again constant space with respect to input size).",
        "solution":     "We use a difference array technique to track the population changes. For each log, we increase the population count at the birth year and decrease it at the death year (since the person is not alive during the death year). We then iterate through the years from 1950 to 2050, accumulating the changes to compute the population per year. By tracking the maximum population encountered and choosing the earliest year with that population, we can determine the required answer.",
    },
    "range-addition": {
        "key_insights": "- Use a difference array (prefix sum technique) to update ranges efficiently.\n\n- Instead of updating every element for each range (which could be slow), update the start index and subtract after the end index.\n\n- Compute the final array by taking the cumulative sum of the difference array.\n\n- This approach reduces the time complexity from potentially O(n * number_of_updates) to O(n + number_of_updates).",
        "complexity":   "Time Complexity: O(n + k), where n is the length of the array and k is the number of updates.\n\nSpace Complexity: O(n) for the difference array (and output array).",
        "solution":     "We use a difference array to efficiently perform range updates. The idea is to add the increment at the start index and subtract the increment just after the end index. After processing all updates, a single pass of cumulative sum across the array gives the final result. This method avoids updating every element within the range for each query, making it efficient for large inputs.",
    },
    "subarray-sums-divisible-by-k": {
        "key_insights": "- Use the concept of prefix sums where prefix[i] represents the sum of elements from the start up to index i.\n\n- A subarray sum from index i+1 to j is divisible by k if and only if prefix[j] % k equals prefix[i] % k.\n\n- Use a hash table (or dictionary/map) to count occurrences of each remainder.\n\n- Initialize the hash table with the remainder 0 count set to 1 to account for subarrays that start from index 0.\n\n- Handle negative numbers by adjusting mod results to ensure they are non-negative.",
        "complexity":   "Time Complexity: O(n) where n is the length of the array, since we traverse the array once.\nSpace Complexity: O(k) in the worst-case scenario, due to storing frequency counts of remainders.",
        "solution":     "We solve the problem using prefix sums and a hash table (or map) to track the frequency of each modulo value (prefix sum mod k). For each element in the array, we update the cumulative sum and compute its modulo with k. The number of valid subarrays ending at the current index is equal to the number of times this modulo value has been seen before (since the difference between two prefix sums with the same modulo is divisible by k). Finally, sum up these counts to obtain the answer.",
    },
    "continuous-subarray-sum": {
        "key_insights": "- Use the prefix sum technique to accumulate sums.\n\n- Instead of storing full sums, store the remainder (mod k) when divided by k.\n\n- If two prefix sums have the same remainder, the subarray between these indices sums to a multiple of k.\n\n- Ensure that the subarray has at least two elements; thus, check that the distance between the indices is at least 2.",
        "complexity":   "Time Complexity: O(n)\n\nSpace Complexity: O(min(n, k))",
        "solution":     "The approach involves using prefix sums and taking advantage of the modulo property. As you compute the cumulative sum for each element, calculate the remainder of the sum modulo k. If the same remainder has been seen before and the subarray between the two indices is of length at least 2, then the subarray's sum is a multiple of k.\n\nWe store these remainders in a hash table (dictionary/map) mapping the remainder to the earliest index where this remainder was encountered. Special care is taken to initialize the hash table with the remainder 0 mapped to index -1 to handle cases where the subarray starting at index 0 has a valid sum.",
    },
    "range-sum-query-2d-immutable": {
        "key_insights": "- Precompute a prefix sum matrix where each cell accumulates the sum of elements above and to the left.\n\n- Use the inclusion-exclusion principle to efficiently calculate the sum of any submatrix using precomputed sums.\n\n- Preprocessing the matrix takes O(m * n) time, but each query is answered in constant time (O(1)).",
        "complexity":   "Time Complexity:\n\n- Preprocessing: O(m * n)\n\n- Each Query: O(1)\n\nSpace Complexity: O(m * n) for storing the prefix sum matrix.",
        "solution":     "The solution involves building a prefix sum matrix, dp, where dp[i+1][j+1] holds the sum of elements in the submatrix from (0, 0) to (i, j). This allows summing any rectangular region using the inclusion-exclusion principle. Specifically, for a query defined by (row1, col1) to (row2, col2), the region sum can be computed as:\n\n  dp[row2+1][col2+1] - dp[row1][col2+1] - dp[row2+1][col1] + dp[row1][col1]\n\nThis approach ensures that after an O(m * n) preprocessing step, each query is computed in constant time.",
    },
    "increment-submatrices-by-one": {
        "key_insights": "- Instead of updating each element within a query's submatrix (which could be costly), use a 2D difference array (prefix sum technique) for efficient range updates.\n\n- For each query, update only the borders in the difference matrix:\n\n- Add 1 at (row1, col1).\n\n- Subtract 1 at (row1, col2 + 1) if within bounds.\n\n- Subtract 1 at (row2 + 1, col1) if within bounds.\n\n- Add 1 at (row2 + 1, col2 + 1) if within bounds.\n\n- After processing all queries, convert the difference matrix into the final matrix using two-pass prefix sums (first row-wise then column-wise).",
        "complexity":   "Time Complexity: O(q + n^2), where q is the number of queries.\n\nSpace Complexity: O(n^2) for the extra difference matrix used.",
        "solution":     "We use a 2D difference (or prefix sum) approach to handle the range updates efficiently. Instead of incrementing every element in a queried submatrix directly, we update only the corners of the submatrix in a difference matrix. After processing every query, we compute prefix sums to convert the difference matrix to the actual updated matrix.\n\nSteps:\n\n- Initialize a 2D difference matrix of size (n+1) x (n+1) with zeros.\n\n- For each query [row1, col1, row2, col2]:\n\n- Add 1 at diff[row1][col1].\n\n- Subtract 1 at diff[row1][col2 + 1] if col2+1 &#x3C; n.\n\n- Subtract 1 at diff[row2 + 1][col1] if row2+1 &#x3C; n.\n\n- Add 1 at diff[row2 + 1][col2 + 1] if both row2+1 and col2+1 are &#x3C; n.\n\n- Compute the prefix sum row-wise and then column-wise to form the final matrix.\n\n- Return the updated matrix up to n x n.\n\nThis approach allows each query to be processed in constant time and handles large inputs efficiently.",
    },
    "matrix-block-sum": {
        "key_insights": "- Brute-force summing every block for each element is inefficient for larger matrices.\n\n- Using a prefix sum (cumulative sum) matrix greatly optimizes the process.\n\n- With a prefix sum matrix, we can compute the sum of any submatrix in constant time.\n\n- Boundary management is crucial since the k-neighborhood may extend beyond the matrix edges.",
        "complexity":   "Time Complexity: O(m * n)\n\nSpace Complexity: O(m * n)",
        "solution":     "We compute a prefix sum matrix where each element at (i, j) represents the sum of all elements in mat from (0, 0) to (i, j). With the prefix sum matrix, the sum of any submatrix defined by its corners can be efficiently computed using the inclusion-exclusion principle. For each element in the result matrix, the boundaries of the submatrix are determined by the limits imposed by k and the matrix size. This approach ensures that each matrix cell’s block sum is calculated in constant time after an initial O(m * n) preprocessing step for building the prefix sum matrix.",
    },
    "maximum-average-subarray-i": {
        "key_insights": "- Use a sliding window to efficiently compute the sum of every subarray of length k.\n\n- Maintain the maximum sum encountered while sliding the window across the array.\n\n- Finally, calculate the average of the subarray with the maximum sum by dividing by k.",
        "complexity":   "Time Complexity: O(n) - We compute the initial sum in O(k) and then update the sum in O(n-k).\nSpace Complexity: O(1) - Only a few extra variables are used regardless of input size.",
        "solution":     "Use the sliding window technique. Start by computing the sum of the first k elements. As you slide the window forward one index at a time, subtract the element that is leaving the window and add the new element that enters the window. Maintain a variable to track the maximum sum seen so far. Finally, the maximum average is the maximum sum divided by k.",
    },
    "maximum-number-of-vowels-in-a-substring-of-given-length": {
        "key_insights": "- Use the sliding window technique to efficiently evaluate substrings of length k.\n\n- Maintain a running count of vowels within the current window.\n\n- When sliding the window, subtract the count if the exiting character is a vowel and add if the new incoming character is a vowel.\n\n- The approach is efficient with a time complexity of O(n).",
        "complexity":   "Time Complexity: O(n), where n is the length of string s.\nSpace Complexity: O(1), since only constant extra space is used.",
        "solution":     "We solve the problem by implementing a sliding window. First, count the vowels in the initial window of size k. Then continue sliding the window one character at a time. For each slide, if the character that exits is a vowel, decrement the count; if the entering character is a vowel, increment the count. Track the maximum count during this process. This method ensures each character is processed once for an efficient solution.",
    },
    "maximum-sum-of-distinct-subarrays-with-length-k": {
        "key_insights": "- Use a sliding window of fixed length k to iterate through the array.\n\n- Maintain a frequency map (or hash table) for elements in the current window to easily check for duplicate elements.\n\n- Instead of recalculating the sum for each window, update the current sum by subtracting the element that is exiting the window and adding the new element.\n\n- Only consider the current window’s sum if the frequency map size is equal to k (no duplicates).",
        "complexity":   "Time Complexity: O(n), where n is the number of elements in nums, as each element is added and removed from the sliding window exactly once.\nSpace Complexity: O(k) in the worst-case scenario for maintaining the frequency map.",
        "solution":     "The problem is solved using a sliding window technique. We maintain a window of size k along with a hash table (or dictionary) to count the occurrence of elements in the window. For each move of the window:\n\n- Add the new element and update the current window sum.\n\n- If the window size exceeds k, remove the leftmost element and update the frequency map and current sum.\n\n- Check if the current window contains distinct elements by verifying if the size of the frequency dictionary equals k.\n\n- If valid, update the maximum sum.\nThis approach ensures that each element is processed in constant time on average, and the sliding window guarantees that we only look at contiguous segments.",
    },
    "substring-with-concatenation-of-all-words": {
        "key_insights": "- Use the fact that all words are of the same length to slide through s in fixed-size segments.\n\n- Create a frequency map for the words in the array to know the required counts.\n\n- Use a sliding window approach with different starting offsets (0 to wordLength-1) to maintain alignment.\n\n- Expand the window by checking word-sized chunks and adjust (slide) the window when the frequency of a word exceeds its allowed count.\n\n- When the window contains exactly all words, record the starting index.",
        "complexity":   "Time Complexity: O(n * m) where n is the length of s and m is the number of words (each word check takes constant time due to fixed word length splitting).\nSpace Complexity: O(m) for the frequency map used for the words.",
        "solution":     "The solution uses a sliding window exhibiting multiple starting offsets to ensure each word chunk is properly aligned. For each offset, the window slides in increments of wordLength. A hash map tracks the current counts of words found. When a word's count exceeds its allowed frequency (from the input words array), the window is shrunk from the left until the condition is met. If the window size matches total words, the starting index is recorded. This approach efficiently checks each possible concatenation substring.",
    },
    "minimum-size-subarray-sum": {
        "key_insights": "- All numbers are positive, which allows for a sliding window approach.\n\n- A sliding window maintains a dynamic range [left, right] that expands or contracts to find candidate subarrays.\n\n- The window is expanded until the sum meets/exceeds the target, then contracted to seek the minimal candidate.\n\n- An O(n) solution is achievable with the sliding window technique; an alternative O(n log n) solution exists using prefix sums and binary search.",
        "complexity":   "Time Complexity: O(n)\n\nSpace Complexity: O(1)",
        "solution":     "We use the sliding window approach:\n\n- Initialize two pointers, left and right, and a variable to maintain the current window sum.\n\n- As you iterate with the right pointer, add the current element to the window sum.\n\n- While the window sum is greater than or equal to target, try to reduce the window size by moving the left pointer forward and update the minimal length found.\n\n- If no valid window is found throughout the loop, return 0.\nThis method works efficiently because adding an element increases the sum and removing it decreases the sum, so we maintain validity within a single pass.",
    },
    "max-consecutive-ones-iii": {
        "key_insights": "- The problem can be solved efficiently using the sliding window technique.\n\n- Maintain a window that can contain at most k zeros.\n\n- When the number of zeros in the window exceeds k, move the left pointer to shrink the window.\n\n- Record the maximum window length during the iteration which gives the maximum consecutive ones.",
        "complexity":   "Time Complexity: O(n), where n is the length of the array, since we process each element at most once.\nSpace Complexity: O(1), only constant extra space is used.",
        "solution":     "We use a sliding window approach:\n\n- Use two pointers (left and right) that define the current window.\n\n- Traverse the array using the right pointer.\n\n- Count the number of zeros encountered in the current window.\n\n- When the count exceeds k, increment the left pointer until there are at most k zeros in the window.\n\n- Throughout the process, the length of the current valid window is recorded, and the maximum window length is updated.\n\n- The maximum window length gives the maximum number of consecutive 1's achievable with at most k flips.",
    },
    "fruit-into-baskets": {
        "key_insights": "- Use a sliding window to maintain a contiguous subarray with at most two distinct fruit types.\n\n- A hash table (or dictionary) is used to count occurrences of fruit types in the current window.\n\n- Expand the window to include new fruits and shrink it from the left when more than two types are present.\n\n- This approach is similar to solving the \"longest substring with at most K distinct characters\" for K = 2.",
        "complexity":   "Time Complexity: O(n), where n is the length of the fruits array.\nSpace Complexity: O(1), as the hash table stores at most 3 keys in worst-case scenarios (effectively bounded by 2 unique types).",
        "solution":     "We use a sliding window technique. By iterating through the fruits array with a right pointer, we update a hash table that tracks the count of each fruit type in the window. When the window contains more than two unique fruit types, we move the left pointer to reduce the window size until it becomes valid again (only two types remain). The maximum valid window size over all iterations gives the maximum number of fruits that can be picked.",
    },
    "maximum-points-you-can-obtain-from-cards": {
        "key_insights": "- Instead of choosing which k cards to take, think in reverse: find the contiguous subarray of length n - k (where n is the total number of cards) that has the minimum sum.\n\n- The maximal points you can obtain is the total sum of the cards minus the sum of the subarray that you did not choose.\n\n- A sliding window technique can efficiently find the minimum sum subarray of a given fixed length.\n\n- This approach changes a two-ended selection problem into a single contiguous subarray problem.",
        "complexity":   "Time Complexity: O(n)\n\nSpace Complexity: O(1)",
        "solution":     "The idea is to first compute the total sum of the cardPoints array. If k equals the number of cards, then the answer is simply the total sum. Otherwise, since you are required to take k cards and the remaining cards form a contiguous block of length n - k, find that contiguous block with the minimum sum using the sliding window technique. Subtract this minimum window sum from the total sum, which gives the maximum points possible.\n\nThe algorithm uses the following steps:\n\n- Compute the total sum of the array.\n\n- Compute the sum of the first window of size n - k.\n\n- Slide the window across the array while updating the current window sum and tracking the minimum window sum encountered.\n\n- Subtract the minimum window sum from the total sum to obtain the maximum points obtainable by taking k cards from the ends.\n\nData structures used: variables for sum tracking; no additional data structures beyond constant space is required.",
    },
    "subarray-product-less-than-k": {
        "key_insights": "- Using a sliding window approach is effective when dealing with contiguous subarrays and maintaining a running product.\n\n- Since the problem involves multiplying numbers, as soon as the running product exceeds or equals k, shrink the window from the left.\n\n- The number of valid subarrays ending at the current right pointer is given by the length of the window.",
        "complexity":   "Time Complexity: O(n) because each element is visited at most twice.\nSpace Complexity: O(1) as only a few extra variables are used.",
        "solution":     "The solution uses a sliding window technique to maintain a running product of the subarray.\n\n- Initialize pointers for the window (left) and variables for the running product (prod) and count of valid subarrays (ans).\n\n- Iterate through the array with a right pointer, multiplying the current element to the product.\n\n- If the product becomes greater than or equal to k, move the left pointer rightwards while dividing the product by each element passed.\n\n- The number of subarrays ending at the current index that satisfy the condition is equal to (right - left + 1). Add this count to ans.\n\n- Return the final count stored in ans.\n\nThis method efficiently tracks valid subarrays and avoids checking all possible combinations.",
    },
    "frequency-of-the-most-frequent-element": {
        "key_insights": "- Sorting the array simplifies the problem by allowing us to work with contiguous subarrays.\n\n- A sliding window approach efficiently finds the largest window where all elements can be made equal with at most k operations.\n\n- Maintain a running sum of the current window to compute the cost of increasing all elements to match the current maximum.\n\n- If the cost exceeds k, shrink the window from the left until the cost is within limits.",
        "complexity":   "Time Complexity: O(n log n) due to sorting, plus O(n) for the sliding window traversal.\nSpace Complexity: O(1) if sorting is done in-place (or O(n) otherwise).",
        "solution":     "Sort the input array and use a sliding window approach. For each new element considered at the right pointer, calculate the cost to convert all elements within the window to match the current element (nums[right]). This cost is computed as:\ncost = nums[right] * window_length - sum(window)\n\nIf the cost is greater than k, move the left pointer to shrink the window until the cost is acceptable. The length of the window at any point reflects the frequency of the most frequent element that can be obtained. Update the maximum frequency accordingly.",
    },
    "longest-substring-with-at-least-k-repeating-characters": {
        "key_insights": "- The valid substring must have every character's frequency greater than or equal to k.\n\n- A sliding window approach can be employed by controlling the number of unique characters within the window.\n\n- Iterating over all possible unique character counts (from 1 to 26) allows us to systematically explore all valid substrings.\n\n- Alternatively, a divide and conquer approach splits the string on characters that fail the k-occurrence criteria.",
        "complexity":   "Time Complexity: O(n * 26) = O(n), since we iterate over the string for each unique character count.\nSpace Complexity: O(26) = O(1), for the frequency counter array.",
        "solution":     "We use a sliding window approach that iterates over the possible counts of unique characters (from 1 to 26). For each unique count:\n\n- Expand the window while updating the frequency counts.\n\n- Maintain the number of unique characters and the number meeting the k threshold.\n\n- Contract the window when the number of unique characters exceeds the current target.\n\n- If the number of unique characters equals the number meeting the k threshold, update the maximum length.\n\nThis approach efficiently explores all possible windows that can form a valid substring.",
    },
    "minimum-window-subsequence": {
        "key_insights": "- The problem is different from the classic \"minimum window substring\" because the characters of s2 must appear in order.\n\n- A two-pointer approach can be used to first \"fast-forward\" to find a valid window end where the last character of s2 is matched.\n\n- Once the end is found, a reverse scan is applied to find the beginning of the window by matching s2 in reverse order.\n\n- Updating the result when a smaller window is found is crucial.\n\n- The approach runs in O(n * m) time, with n and m being the lengths of s1 and s2 respectively.",
        "complexity":   "Time Complexity: O(n * m), where n is the length of s1 and m is the length of s2.\nSpace Complexity: O(1) additional space is used apart from the input strings.",
        "solution":     "The solution uses a two-phase approach for each candidate window:\n\n- Use a forward pointer to traverse s1 until we find a valid window ending at an index where the last character of s2 is matched.\n\n- Once a valid end is detected, use a backward pointer starting at that index and move in reverse to find the start index of the window where s2 fully matches as a subsequence.\n\nThe algorithm repeatedly checks every possible window end where the match is complete and updates the minimal window to return the left-most minimal substring if multiple windows of the same length exist.",
    },
    "subarrays-with-k-different-integers": {
        "key_insights": "- Count subarrays with exactly k distinct integers by leveraging the sliding window technique.\n\n- Use the trick: the number of subarrays with exactly k distinct = subarrays with at most k distinct - subarrays with at most (k-1) distinct.\n\n- Maintain a hash map (or dictionary) to count frequency of array elements as you expand and contract the sliding window.\n\n- Carefully update your window boundaries to ensure the count is maintained correctly.",
        "complexity":   "Time Complexity: O(n) for each helper sliding window (total O(n) since each element is processed a constant number of times)\nSpace Complexity: O(n) in the worst case, due to the hash map storing frequencies of unique numbers.",
        "solution":     "To solve the problem, we use a sliding window technique combined with a helper function that counts subarrays with at most k distinct integers. The idea is:\n\n- Create a helper function countAtMost(K) that returns the number of subarrays with at most K distinct integers.\n\n- In the helper, maintain two pointers (left and right) for the sliding window and a frequency map.\n\n- For each position of right pointer, update the frequency map. If the number of distinct integers exceeds K, move the left pointer to reduce it.\n\n- The total subarrays ending at the current right are added to the answer.\n\n- The final answer is: countAtMost(k) - countAtMost(k - 1).\n\nThis approach ensures we efficiently count valid subarrays without needing to re-calculate subarray properties repeatedly.",
    },
    "maximum-sum-circular-subarray": {
        "key_insights": "- Use Kadane's algorithm to find the maximum sum of a contiguous non-circular subarray.\n\n- Apply a modified Kadane's algorithm to find the minimum subarray sum.\n\n- The maximum circular subarray sum can be computed as (total sum of array - minimum subarray sum).\n\n- Handle the edge case where all numbers are negative by returning the non-circular maximum.",
        "complexity":   "Time Complexity: O(n)\n\nSpace Complexity: O(1)",
        "solution":     "The solution involves two passes over the array. In the first pass, we apply Kadane’s algorithm to find the maximum subarray sum. In the second pass, we compute the minimum subarray sum using a similar approach. The maximum circular subarray sum is then the maximum of the non-circular (standard) maximum or (total sum - minimum subarray sum). However, if all numbers are negative, the non-circular maximum is returned to avoid an incorrect result caused by subtracting the minimum subarray sum.",
    },
    "maximum-subarray-sum-with-one-deletion": {
        "key_insights": "- Use dynamic programming to track two states: the maximum subarray sum ending at index i without deletion, and the maximum subarray sum ending at index i with one deletion.\n\n- The state with no deletion uses the classic Kadane’s algorithm.\n\n- For the state with one deletion, either use the current element added to a previously \"one-deleted\" state or delete the current element by taking the previous \"no deletion\" sum.\n\n- Careful handling is required when all numbers are negative, in which case no deletion might yield the maximum sum.",
        "complexity":   "Time Complexity: O(n)\n\nSpace Complexity: O(n) (Can be reduced to O(1) with optimized variables)",
        "solution":     "We solve this problem using dynamic programming with two arrays/variables. One (dp_no_del) tracks the maximum subarray sum ending at the current index without any deletion, while the other (dp_del) tracks the maximum subarray sum ending at the current index with one deletion allowed.\n\nAt each index:\n\n- For dp_no_del: We either continue the previous subarray or start a new subarray with the current element.\n\n- For dp_del: We either delete the current element (thus carry over the previous dp_no_del) or add the current element to the previous dp_del.\n\nThe answer is the maximum value found in both dp_no_del and dp_del across all indices.",
    },
    "maximum-absolute-sum-of-any-subarray": {
        "key_insights": "- The problem can be reduced to finding two quantities: the maximum subarray sum and the minimum subarray sum.\n\n- The maximum absolute sum will be the maximum of these two values, since taking the absolute ensures both positive and negative extremes are accounted for.\n\n- Kadane’s algorithm can be used to efficiently find both the maximum subarray sum and the minimum subarray sum in O(n) time.",
        "complexity":   "Time Complexity: O(n), where n is the number of elements in the array.\nSpace Complexity: O(1), since only a constant amount of extra space is used.",
        "solution":     "The solution uses Kadane's algorithm twice. First, we calculate the maximum subarray sum by iteratively updating the current maximum and overall maximum. Then, we can modify the algorithm to get the minimum subarray sum by tracking the current minimum and overall minimum. The answer is then the maximum of the overall maximum and the absolute value of the overall minimum. This approach is efficient and uses constant extra space.",
    },
    "longest-turbulent-subarray": {
        "key_insights": "- A turbulent subarray requires alternating \"greater than\" and \"less than\" comparisons.\n\n- A sliding window or dynamic programming approach can be used to solve the problem in O(n) time.\n\n- Reset the subarray length when encountering equal adjacent elements or when the alternating condition fails.\n\n- Handle the case where the array has only one element.",
        "complexity":   "Time Complexity: O(n)\n\nSpace Complexity: O(1)",
        "solution":     "We can solve the problem using a dynamic programming / sliding window approach:\n\n- If the array has only one element, return 1.\n\n- Initialize a variable (currentLength) to keep track of the current turbulent subarray length.\n\n- Traverse the array from the second element and:\n\n- If the current element is equal to the previous element, reset currentLength to 1.\n\n- Otherwise, if the current and previous comparisons create a valid alternating pattern when compared to the one before them, increment currentLength.\n\n- If the alternating pattern breaks, reset currentLength to 2 (since the current two elements may start a new turbulent subarray).\n\n- Track the maximum length seen during the traversal.\n\nKey trick: Instead of precomputing comparison signs, we check the alternating condition on the fly using the relationship between three consecutive elements.",
    },
    "best-sightseeing-pair": {
        "key_insights": "- The score can be reformulated as (values[i] + i) + (values[j] - j), decoupling the contributions from the two indices.\n\n- While iterating through the array, maintain the maximum value of (values[i] + i) seen so far.\n\n- For every new spot j, combine the previously stored maximum with (values[j] - j) to update the answer.\n\n- This one-pass strategy ensures an O(n) time complexity.",
        "complexity":   "Time Complexity: O(n)\n\nSpace Complexity: O(1)",
        "solution":     "The solution involves a single pass through the array, maintaining a variable (best_left) to record the maximum value of (values[i] + i) encountered so far. For each index j (starting from 1), calculate the score by adding best_left and (values[j] - j). Update the max score whenever a higher score is found, and also update the best_left for future calculations. This efficient approach leverages dynamic programming by keeping track of cumulative optimal sub-results.",
    },
    "game-of-life": {
        "key_insights": "- The update must happen simultaneously, so changes in one cell cannot immediately affect its neighbors.\n\n- Use an in-place strategy by encoding the transition states within the board.\n\n- Introduce temporary values to represent state changes: for example, mark a cell that was live but now dead, and a cell that was dead but now live.\n\n- When counting live neighbors, consider the cell's original state, which can be recovered using the absolute value or a defined mapping.",
        "complexity":   "Time Complexity: O(m * n) – we check each cell and its at most 8 neighbors.\nSpace Complexity: O(1) – updates are performed in-place without extra auxiliary storage.",
        "solution":     "We can solve the problem in-place by using intermediate states:\n\n- Use -1 to represent a cell that was originally live (1) but becomes dead (0).\n\n- Use 2 to represent a cell that was originally dead (0) but becomes live (1).\n\nDuring the first pass, for each cell count the live neighbors (treating a cell as originally live if its value is 1 or -1). Then, update the cell using the Game of Life rules:\n\n- If a cell is dead (0) and has exactly 3 live neighbors, mark it as 2 (becomes live).\n\n- If a cell is live (1) and has fewer than 2 or more than 3 live neighbors, mark it as -1 (dies).\n\nIn a second pass, finalize the board by converting cells:\n\n- If the value is > 0 (either 1 or 2), set the cell to 1.\n\n- Otherwise, set the cell to 0.\n\nThis method leverages in-place modifications while correctly computing neighbor counts based on original states.",
    },
    "intersection-of-two-linked-lists": {
        "key_insights": "- The intersection point is defined by reference (i.e., the same memory location), not by the node value.\n\n- Using two pointers that traverse both lists can help align the pointers so they traverse an equal number of nodes.\n\n- When one pointer reaches the end of its list, it switches to the head of the other list.\n\n- If the linked lists intersect, the pointers will eventually meet at the intersection node; if not, both will become null.",
        "complexity":   "Time Complexity: O(m + n)\n\nSpace Complexity: O(1)",
        "solution":     "We use a two-pointer approach. Initialize two pointers, one at headA and one at headB. Traverse the lists by moving each pointer one step at a time. When a pointer reaches the end of its list, reset it to the head of the other list. This aligns the pointers so that if there is an intersection, they will meet at the intersecting node. If not, both pointers will eventually become null, indicating no intersection. This approach runs in O(m + n) time and uses constant extra space.",
    },
    "remove-duplicates-from-sorted-list": {
        "key_insights": "- The linked list is already sorted, so any duplicates will be adjacent.\n\n- A single pass through the list is sufficient to remove duplicates.\n\n- The algorithm modifies the list in place without needing extra memory.",
        "complexity":   "Time Complexity: O(n) - where n is the number of nodes in the list\n\nSpace Complexity: O(1) - in-place modification with constant extra space",
        "solution":     "We traverse the linked list using a pointer. For each node, we check if the next node has the same value. If it does, we adjust the pointer to skip the duplicate node. Otherwise, we move our pointer to the next node. This approach efficiently removes duplicates in a single pass without requiring additional data structures.",
    },
    "delete-the-middle-node-of-a-linked-list": {
        "key_insights": "- Use the two pointers technique (slow and fast pointers) to find the middle node in a single pass.\n\n- Track the node previous to the slow pointer node to efficiently remove the middle node by changing the 'next' pointer.\n\n- Handle the edge case when the list contains only one node, in which case the result is an empty list.",
        "complexity":   "Time Complexity: O(n) because we traverse the list once.\nSpace Complexity: O(1) as only a constant amount of extra space is used.",
        "solution":     "We use the two pointers technique where the slow pointer moves one step at a time while the fast pointer moves two steps. When the fast pointer reaches the end, the slow pointer will be at the middle node. We also maintain a pointer to the previous node of the slow pointer so we can remove the middle node by linking the previous node directly to the next node of the slow pointer. If the list only has one node, we return null since the single node is the middle node to be removed.",
    },
    "remove-duplicates-from-sorted-list-ii": {
        "key_insights": "- The list is sorted, so duplicate nodes appear consecutively.\n\n- Use a dummy node to simplify edge cases (e.g., when the first node is a duplicate).\n\n- Two pointers (prev and current) can be used to detect and skip over duplicate nodes.\n\n- If a node has duplicates, skip all nodes with that value and connect the previous valid node to the next distinct node.",
        "complexity":   "Time Complexity: O(n) - We traverse the list once.\nSpace Complexity: O(1) - Only a few pointers are used, regardless of the input size.",
        "solution":     "We iterate through the linked list with a pointer called current while maintaining a previous pointer (prev) initialized to a dummy node. Whenever we detect that the current node has a duplicate (i.e., its next node has the same value), we record that value and advance current until all nodes with that duplicate value are skipped. Then, we link the previous node to the current node (which is now the next distinct node). If no duplicate is found, we simply move both pointers forward. This approach efficiently removes all duplicates in one pass with constant extra space.",
    },
    "partition-list": {
        "key_insights": "- Use two separate lists: one for nodes less than x and one for nodes greater than or equal to x.\n\n- Create dummy nodes for each list to simplify edge case handling.\n\n- Traverse the original list once, partitioning nodes into the two lists.\n\n- Connect the two lists at the end to obtain the final partitioned list.",
        "complexity":   "Time Complexity: O(n) because we traverse the list once.\n\nSpace Complexity: O(1) since we only use a constant number of pointers.",
        "solution":     "We maintain two dummy head nodes to start the lists for nodes less than x and nodes greater than or equal to x. As we traverse the original list, we append each node to the appropriate list. After the traversal, we link the end of the less-than list to the head of the greater-than or equal list and set the tail of the greater list to None (or null) to avoid forming a cycle. Finally, we return the new head from the less-than list.",
    },
    "add-two-numbers-ii": {
        "key_insights": "- Use stacks to simulate processing the digits from least significant (end of the list) to most significant (start of the list).\n\n- By pushing each list’s values onto a stack, you can pop them off in reverse order and perform addition with a carry.\n\n- Create new nodes for the resulting linked list as you compute the sum, linking them appropriately in reverse order.\n\n- Careful handling of the carry value is critical for correct summation.",
        "complexity":   "Time Complexity: O(n + m) where n and m are the lengths of the two linked lists.\nSpace Complexity: O(n + m) due to the use of stacks to hold the nodes' values and the space needed for the result list.",
        "solution":     "We begin by pushing the values of each linked list onto two separate stacks, which allows us to retrieve the digits in reverse order for addition. Then we pop elements from both stacks, add them along with any carry from the previous addition, and create a new node for each digit of the resultant sum. Finally, the new nodes are prepended to the resultant list. This approach avoids modifying the input lists (i.e., no reversal is performed on the original linked lists) and handles different list lengths and carry propagation gracefully.",
    },
    "delete-node-in-a-linked-list": {
        "key_insights": "- You are given direct access to the node to be deleted, not the head.\n\n- The node to be deleted is guaranteed not to be the tail.\n\n- The solution involves copying the next node’s value into the current node.\n\n- Then, bypass the next node by updating the pointer of the current node to point to the next node’s next pointer.",
        "complexity":   "Time Complexity: O(1)\n\nSpace Complexity: O(1)",
        "solution":     "The key idea is to simulate deletion by copying the next node's data into the current node and then deleting the next node. Since you don't have access to the node before the one to be deleted, you can't adjust its pointer. Instead, you overwrite the target node’s value with the next node’s value and update the pointer to skip the next node. This in-place modification effectively removes the original node value from the list without requiring traversal from the head.",
    },
    "merge-in-between-linked-lists": {
        "key_insights": "- Traverse list1 to find the node right before index a.\n\n- Continue traversal to locate the node immediately after index b.\n\n- Find the tail of list2.\n\n- Reconnect these parts: connect the node before a to the head of list2 and connect list2's tail to the node after b.",
        "complexity":   "Time Complexity: O(n1 + n2) where n1 is the length of list1, and n2 is the length of list2.\nSpace Complexity: O(1) assuming we perform pointer manipulation in-place.",
        "solution":     "We solve the problem by performing the following steps:\n\n- Traverse list1 to obtain the node just before the ath node (let's call it prevA).\n\n- Continue traversing list1 from prevA to get to the node right after the bth node (postB).\n\n- Traverse list2 to locate its tail.\n\n- Link prevA.next to list2's head.\n\n- Link list2's tail to postB.\nThis reconnects list1 with list2 in-between, effectively removing the nodes from a to b.",
    },
    "reverse-linked-list-ii": {
        "key_insights": "- Use a dummy node to simplify edge cases.\n\n- Traverse the list to locate the node immediately before the reversal section.\n\n- Reverse the sublist in-place by adjusting pointers iteratively.\n\n- Reconnect the reversed sublist back to the original list.\n\n- Achieve the reversal in one pass through the list.",
        "complexity":   "Time Complexity: O(n)\n\nSpace Complexity: O(1)",
        "solution":     "The solution uses an in-place reversal technique. A dummy node is created to handle cases where the reversal starts at the head. First, traverse to the node immediately before the left-th node. Then, reverse the sublist by iteratively moving the next node in the sublist to the front of the reversed portion. Finally, reconnect the reversed section with the remainder of the list. The key trick here is the iterative reversal within the sublist which ensures that the overall reversal is done in a single pass with constant extra space.",
    },
    "baseball-game": {
        "key_insights": "- Use a stack (list) to keep track of valid scores.\n\n- Process operations sequentially, updating the stack accordingly.\n\n- Handle different operations by manipulating the stack: push new scores, pop invalid scores.\n\n- Final result is the sum of scores left in the stack.",
        "complexity":   "Time Complexity: O(n), where n is the number of operations.\nSpace Complexity: O(n), in the worst-case the stack stores all operation results.",
        "solution":     "The solution uses a stack-based approach. Iterate through each operation and:\n\n- If the operation is a numeric value, convert it to an integer and push it onto the stack.\n\n- If it is \"C\", remove the most recent score by popping from the stack.\n\n- If it is \"D\", double the most recent score and push the result.\n\n- If it is \"+\", calculate the sum of the two most recent scores and push that value.\nFinally, sum all the values in the stack to obtain the final score.",
    },
    "remove-all-adjacent-duplicates-in-string": {
        "key_insights": "- Utilize a stack to efficiently process and remove adjacent duplicates.\n\n- Iterate through each character in the string, comparing it with the top of the stack.\n\n- If the current character matches the top of the stack, pop the stack (i.e., remove the duplicate pair).\n\n- If there is no match, push the current character onto the stack.\n\n- Finally, join the characters remaining in the stack to obtain the result.",
        "complexity":   "Time Complexity: O(n), where n is the length of the string, because each character is processed at most once.\nSpace Complexity: O(n), in the worst-case scenario where no duplicates are found and all characters are stored in the stack.",
        "solution":     "The solution uses a stack-based approach. As we iterate through the input string, each character is compared with the top element of the stack. If they are the same, it means we have found an adjacent duplicate, so we remove the top element. Otherwise, we push the current character onto the stack. This method ensures that duplicate adjacent pairs are removed in a single pass through the string. Finally, the remaining characters in the stack are combined to form the final string, which is then returned as the unique answer.",
    },
    "maximum-nesting-depth-of-the-parentheses": {
        "key_insights": "- Use a counter to track the current nesting level.\n\n- Increment the counter when encountering an opening parenthesis '('.\n\n- Update the maximum depth if the current counter exceeds the previous maximum.\n\n- Decrement the counter when encountering a closing parenthesis ')'.\n\n- Since the input is guaranteed to be a valid parentheses string, no additional checks are required.",
        "complexity":   "Time Complexity: O(n), where n is the length of the string.\nSpace Complexity: O(1)",
        "solution":     "The approach involves iterating over every character in the string once. We maintain two variables: one to count the current level of nesting (currentDepth) and one to store the maximum depth encountered (maxDepth). For every character:\n\n- If it is an opening parenthesis '(', increment currentDepth. Then check and update maxDepth if currentDepth is greater.\n\n- If it is a closing parenthesis ')', decrement currentDepth.\nThis simple counter approach ensures that we determine the maximum nesting depth efficiently using constant extra space.",
    },
    "validate-stack-sequences": {
        "key_insights": "- Use a stack to simulate the push and pop operations.\n\n- Iterate over the pushed array, pushing each element to the stack.\n\n- After each push, check if the top of the stack equals the current element in the popped array. If so, perform pop operations.\n\n- Continue this process until either all elements are processed or a mismatch occurs.\n\n- The simulation confirms the validity of the popped sequence if the stack is empty at the end.",
        "complexity":   "Time Complexity: O(n) where n is the number of elements, as each element is pushed and popped at most once.\nSpace Complexity: O(n) for the auxiliary stack used during the simulation.",
        "solution":     "We simulate the process using a stack:\n\n- Initialize an empty stack and an index pointer for the popped array.\n\n- Traverse the pushed sequence, pushing each element onto the stack.\n\n- After each push, while the stack is not empty and the element on top of the stack equals the current element in popped (indicated by the index pointer), pop from the stack and increment the pointer.\n\n- If all operations complete and the stack is empty, the sequence is valid; otherwise, it is not.\nThe key trick is continuously comparing the top of the stack with the next element in popped and performing pop operations as long as they match.",
    },
    "minimum-remove-to-make-valid-parentheses": {
        "key_insights": "- Use a stack to record indices of unmatched '('.\n\n- Traverse the string once to identify unmatched ')' and push indices of '('.\n\n- Mark indices that need removal and reconstruct the valid string.\n\n- Greedy, one-pass validation is efficient.",
        "complexity":   "Time Complexity: O(n) where n is the length of the string\n\nSpace Complexity: O(n) in the worst case for the stack and additional storage",
        "solution":     "We can solve the problem by iterating over the string with a stack to match parentheses:\n\n- For every character in the string:\n\n- If the character is '(', push its index onto the stack.\n\n- If it is ')', check if there is a matching '(' in the stack. If so, pop the top; otherwise, mark this index for removal.\n\n- After the iteration, any indices left in the stack (representing unmatched '(') should also be marked for removal.\n\n- Build the final string by skipping characters at marked indices.\nThis approach ensures we only remove the minimum number of parentheses to achieve a valid string.",
    },
    "remove-duplicate-letters": {
        "key_insights": "- Use a monotonic stack to build the resulting string.\n\n- Store the last occurrence of each character to know if it can be safely removed.\n\n- Use a set (or boolean array) to track which characters have been added to avoid duplication.\n\n- Greedily remove characters from the stack if the current character is smaller and the top of the stack appears again later in the string.",
        "complexity":   "Time Complexity: O(n), where n is the length of the string, because each character is processed once.\nSpace Complexity: O(n) for the stack and auxiliary data structures.",
        "solution":     "We iterate over the string while maintaining a stack and a record of whether each character is already in the result. For each character, if it is already in the result, we skip it. Otherwise, we check if the current character is smaller than the last character in the stack and if that last character appears later in the string. If both conditions are met, we pop it from the stack so that the overall result remains lexicographically minimal. Then we add the current character to the stack and mark it as seen.",
    },
    "removing-stars-from-a-string": {
        "key_insights": "- Use a stack data structure to simulate the removal operation.\n\n- When encountering a non-star character, push it onto the stack.\n\n- When encountering a star, pop the top character from the stack (which represents the nearest non-star character to its left).\n\n- The final answer is the concatenation of the characters remaining in the stack.",
        "complexity":   "Time Complexity: O(n), where n is the length of the string s, since we process each character once.\n\nSpace Complexity: O(n), in the worst case when no stars are encountered and all characters are stored in the stack.",
        "solution":     "The solution employs a stack to efficiently simulate the removal process. As you iterate through the string, push every non-star character onto the stack. When you encounter a star, pop the last character from the stack which represents removing the closest non-star character to its left along with the star. After processing the entire string, join the remaining characters in the stack to form the final result. Critical points include ensuring the operation is performed only when there is a character to remove (guaranteed by problem constraints) and handling the string traversal in a single pass.",
    },
    "simplify-path": {
        "key_insights": "- Use a stack to handle the directory names.\n\n- Ignore empty strings or '.' as they represent no change in the directory.\n\n- When encountering '..', pop the top of the stack if it's not empty.\n\n- All other valid strings are treated as directory names and pushed onto the stack.\n\n- Finally, join the elements in the stack with '/' and prepend a slash to form the canonical path.",
        "complexity":   "Time Complexity: O(n) where n is the length of the path string.\nSpace Complexity: O(n) in the worst-case scenario where all directories are valid and stored in the stack.",
        "solution":     "We approach the problem by:\n\n- Splitting the input path by the '/' delimiter.\n\n- Iterating through the resulting tokens:\n\n- Skip tokens that are empty or equal to '.'.\n\n- When encountering '..', pop an element from the stack if it exists.\n\n- Otherwise, push the token onto the stack as a valid directory name.\n\n- After processing all tokens, the stack contains the valid path components. We then construct the simplified path by concatenating these components with '/' in between, making sure it starts with a '/'.\n\n- Edge cases include handling the root directory where the stack might be empty.",
    },
    "exclusive-time-of-functions": {
        "key_insights": "- Use a stack to simulate function calls; the top of the stack represents the currently executing function.\n\n- When a function starts while another is running, update the exclusive time of the currently running function.\n\n- For an \"end\" log, add the elapsed time (including the current timestamp) to the function on top of the stack.\n\n- Keep track of the previous timestamp to calculate elapsed durations correctly.",
        "complexity":   "Time Complexity: O(n) where n is the number of log entries.\nSpace Complexity: O(n) in the worst-case scenario due to the stack.",
        "solution":     "We use a stack to track the active functions. As we iterate through each log:\n\n- For a \"start\" log, if there is a function currently executing (top of the stack), update its exclusive time with the time elapsed since the last timestamp.\n\n- Then push the new function onto the stack and update the previous timestamp.\n\n- For an \"end\" log, pop the completed function from the stack and update its exclusive time by adding the elapsed time plus one (to account for the inclusive end timestamp). Then update the previous timestamp to one more than the current timestamp.\nThis approach efficiently tracks the execution time intervals for each function.",
    },
    "next-greater-element-i": {
        "key_insights": "- Use a monotonic decreasing stack to precompute the next greater element for every number in nums2.\n\n- Use a hash map to store the mapping from element to its next greater element.\n\n- For each element in nums1, look up the next greater element from the hash map.\n\n- This approach avoids a nested loop by processing nums2 only once.",
        "complexity":   "Time Complexity: O(n) where n is the length of nums2 (plus O(m) for nums1, overall O(n + m)).\nSpace Complexity: O(n) for the stack and hash map.",
        "solution":     "We iterate through nums2 once while maintaining a stack that keeps numbers in decreasing order. When a new number is encountered, it is the next greater element for all numbers in the stack that are smaller than it. These elements are popped from the stack and the mapping is recorded in a hash map. Afterwards, for each element in nums1, we simply fetch the answer from the hash map. This ensures an optimal O(n + m) solution.",
    },
    "final-prices-with-a-special-discount-in-a-shop": {
        "key_insights": "- A brute-force approach would check each subsequent item for every index which can be inefficient.\n\n- Using a monotonic stack helps in efficiently finding the next smaller or equal price for each item.\n\n- Traverse the array once, while maintaining a stack of indices where the discount has not yet been applied.\n\n- When a price qualifies as the discount for the items stored in the stack, update those items.",
        "complexity":   "Time Complexity: O(n) - Each element is pushed and popped from the stack at most once.\nSpace Complexity: O(n) - In the worst-case scenario, all elements are stored in the stack.",
        "solution":     "The solution uses a monotonic stack to track the indices of prices that have not yet found their discount. As we iterate over the prices:\n\n- Check if the current price can act as a discount for any previously seen elements (stored in the stack) by comparing it with the price at the index on the top of the stack.\n\n- If the current price is less than or equal to the price at the top of the stack, it qualifies as a discount; subtract it from that price and pop that index from the stack.\n\n- Push the current index onto the stack.\nOnce the iteration is complete, any index left in the stack did not encounter a valid discount and remains at its original price.",
    },
    "next-greater-element-ii": {
        "key_insights": "- Because the array is circular, we simulate the wrap-around by iterating over the array twice.\n\n- A monotonic stack (decreasing order) is used to efficiently track the next greater element.\n\n- Process elements in reverse order to handle dependencies correctly.\n\n- Each element is pushed and popped at most once, yielding an efficient solution.",
        "complexity":   "Time Complexity: O(n), since each element is processed a constant number of times.\nSpace Complexity: O(n), used for the result array and the stack.",
        "solution":     "This solution employs a monotonic stack to find the next greater element efficiently. We traverse the array twice (using modulo arithmetic to simulate circular behavior) from right to left. For each element, we pop elements from the stack until we find one that is greater than the current element. If such an element exists, it is the next greater element; otherwise, the result remains -1. Finally, we push the current index to the stack. This approach guarantees that every element is considered only a few times.",
    },
    "online-stock-span": {
        "key_insights": "- The problem can be solved efficiently using a monotonic stack.\n\n- Store pairs of (price, span) in the stack, where span is the number of consecutive days whose prices are less than or equal to that price.\n\n- For every new price, pop elements from the stack while they are less than or equal to the current price, and accumulate their spans.\n\n- This approach ensures that each price is processed only once, leading to an amortized O(1) time per operation.",
        "complexity":   "Time Complexity: Amortized O(1) per call to next\nSpace Complexity: O(n) where n is the number of price quotes stored in the stack",
        "solution":     "We use a monotonic stack to keep track of prices and their associated spans. Each time the next price is introduced, we:\n\n- Initialize a span value of 1 for the current day.\n\n- While the stack is not empty and the price at the top of the stack is less than or equal to the current price, pop the pair and add its span to the current span.\n\n- Push the current price and its computed span onto the stack.\n\n- Return the span.\n\nThis works because the popped elements represent a sequence of consecutive days where the stock prices were less than or equal to the current price.",
    },
    "132-pattern": {
        "key_insights": "- The challenge is to efficiently detect a subsequence that satisfies nums[i] &#x3C; nums[k] &#x3C; nums[j].\n\n- A brute force approach checking all triples would be too slow given the constraints.\n\n- Using a monotonic stack in reverse order allows us to keep track of potential candidates for nums[k] and check for a valid pattern.\n\n- We maintain a variable to store the possible \"2\" element (candidate for nums[k]) as we move from right to left.",
        "complexity":   "Time Complexity: O(n) where n is the number of elements in the array.\nSpace Complexity: O(n) in the worst case for the stack.",
        "solution":     "We solve the problem by iterating through the list in reverse order while using a stack to maintain potential candidates for the \"middle\" element (nums[j]). We also use a variable (let’s call it candidate) to represent the best candidate for nums[k] that we have seen so far. For each element in the reversed array:\n\n- If the current element is less than the candidate, we have found a valid 132 pattern.\n\n- Otherwise, while the stack is not empty and the current element is greater than the top of the stack, we update the candidate by popping from the stack.\n\n- Push the current element onto the stack as a potential future candidate.\nThis approach efficiently identifies whether a 132 pattern exists.",
    },
    "remove-k-digits": {
        "key_insights": "- Use a greedy approach that removes digits to make the number as small as possible.\n\n- Utilize a stack (monotonic increasing sequence) to decide whether to keep or remove a digit.\n\n- Remove any excess digits from the end if k > 0 after the primary loop.\n\n- Handle edge cases including leading zeros and the possibility of removing all digits.",
        "complexity":   "Time Complexity: O(n), where n is the length of the input string.\nSpace Complexity: O(n), needed for the stack.",
        "solution":     "We apply a greedy algorithm using a monotonic stack. For each digit in the number, if the current digit is smaller than the last digit on the stack and we still have removals (k > 0), we pop from the stack. This removal helps in making the constructed number smaller. After iterating over the entire number, if any removals remain, we trim the number from the end. Finally, we join the resulting digits, strip leading zeros, and if the result is empty, we return \"0\".",
    },
    "max-chunks-to-make-sorted": {
        "key_insights": "- Since the input is a permutation of 0 through n-1, the correctly sorted array is exactly [0, 1, 2, ..., n-1].\n\n- While iterating through the array, if the maximum value seen so far is equal to the current index, then all elements up to the current index can form one chunk.\n\n- A greedy approach by iterating and making a cut whenever max_so_far equals the current index ensures the maximum number of chunks.",
        "complexity":   "Time Complexity: O(n) - Single pass through the array.\nSpace Complexity: O(1) - Only a few auxiliary variables are used.",
        "solution":     "The algorithm uses a greedy approach with a single linear scan of the array. A variable is maintained to track the maximum value seen so far during the iteration. At each index, if the maximum value equals the current index, this indicates that all numbers up to that index are exactly the set {0, 1, ..., i}, thus permitting a chunk division. The process continues until the end of the array, and the count of chunk boundaries is returned as the maximum number of chunks.",
    },
    "sum-of-subarray-minimums": {
        "key_insights": "- Leverage a monotonic stack to determine the span over which an element is the minimum.\n\n- For each element, compute its \"left\" span (number of subarrays ending at the element where it is the minimum) and \"right\" span (number of subarrays starting at the element where it remains the minimum).\n\n- The contribution of an element is its value multiplied by its left and right spans.\n\n- Use modular arithmetic to handle large numbers.",
        "complexity":   "Time Complexity: O(n)\n\nSpace Complexity: O(n)",
        "solution":     "The approach involves two passes using a monotonic stack. In the first pass, for each element, we count how many contiguous elements on the left (including the current one) are greater than (or equal, with a twist for handling duplicates) this element; this gives the left span. In the second pass, we do a similar traversal from the right to obtain the right span, ensuring that while moving right the criteria for maintaining the minimum includes strict comparisons in order to correctly handle duplicates. Finally, the element's overall contribution is the product of its value, its left span, and its right span. Summing up these contributions for all elements yields the answer modulo 10^9 + 7.",
    },
    "sum-of-subarray-ranges": {
        "key_insights": "- Instead of enumerating all subarrays (which is O(n²)), calculate the contribution of each element as the maximum and minimum in subarrays.\n\n- Use monotonic stacks to compute, for each element, the number of subarrays where it is the minimum and where it is the maximum.\n\n- Specifically, determine the distance to previous and next less elements (for minimum) and previous and next greater elements (for maximum).\n\n- The overall sum is the aggregated difference of the contributions: (element * count_as_max) - (element * count_as_min).",
        "complexity":   "Time Complexity: O(n)\n\nSpace Complexity: O(n)",
        "solution":     "We solve the problem by breaking it into two parts: calculating the total contribution when each element acts as the maximum and as the minimum in subarrays. For each index i:\n\n- Compute left and right boundaries indicating how far we can extend while keeping nums[i] as the unique maximum or minimum using monotonic stacks.\n\n- Multiply these boundaries to determine the number of subarrays in which nums[i] is the maximum or minimum.\n\n- Accumulate the aggregate sum by adding nums[i] * (number of subarrays where it is maximum) and subtracting nums[i] * (number of subarrays where it is minimum).\n\nThis approach leverages monotonic stacks to avoid an O(n²) enumeration, delivering an O(n) time solution.",
    },
    "shortest-unsorted-continuous-subarray": {
        "key_insights": "- The sorted order of the array determines the positions where the actual array deviates from the sorted order.\n\n- A straightforward solution is to compare the array with a sorted copy and identify the first and last indices where they differ.\n\n- A more optimal O(n) approach involves traversing the array from left-to-right and right-to-left:\n\n- Left-to-right pass: Track the maximum value seen. Whenever the current number is less than this maximum, it indicates the elements are not in order; update the right boundary accordingly.\n\n- Right-to-left pass: Track the minimum value seen. Whenever the current number is greater than this minimum, update the left boundary.\n\n- If the array is already sorted, the boundaries never update, and the result is 0.\n\n- This approach uses constant extra space (besides a few variables), making it efficient for large inputs.",
        "complexity":   "Time Complexity: O(n) – The array is traversed twice.\nSpace Complexity: O(1) – Only constant extra space is used.",
        "solution":     "The solution uses a two-pointer greedy approach:\n\n- In the first pass (left-to-right), track the maximum value encountered so far. When a current element is found smaller than this maximum, mark its position as a potential right boundary.\n\n- In the second pass (right-to-left), track the minimum value encountered so far. When a current element is larger than this minimum, mark its position as a potential left boundary.\n\n- The difference between these boundary indices (plus one) yields the length of the shortest unsorted subarray.\n\n- If no such boundaries are found, then the array is already sorted, and the answer is 0.\n\nThis method avoids extra space for a sorted copy and achieves optimal time performance.",
    },
    "next-greater-node-in-linked-list": {
        "key_insights": "- Convert the linked list into an array for easier index manipulation.\n\n- Use a monotonic (decreasing) stack to store indices of nodes that haven't found their next greater element.\n\n- As you traverse the array, compare the current value with values corresponding to indices stored in the stack.\n\n- When the current value is larger, update the result for those indices and pop them off the stack.\n\n- Unresolved indices in the stack will have a next greater value of 0.",
        "complexity":   "Time Complexity: O(n)\n\nSpace Complexity: O(n)",
        "solution":     "The solution begins by converting the linked list into an array, which allows for straightforward index-based traversal. A stack is used to keep track of indices for which we have not yet found a next greater element. As we iterate through the array, if the current element's value is greater than the value at the index on the top of the stack, it means the current element is the next greater element for that index. We update the result at that index and pop it from the stack. This process continues until the stack is either empty or the current element is not greater than the element referenced by the stack's top index. Finally, any indices remaining in the stack are assigned a value of 0 as there is no greater element after them.",
    },
    "number-of-visible-people-in-a-queue": {
        "key_insights": "- The problem requires determining the visibility count for each person based on height comparisons.\n\n- A monotonic stack is an effective tool to “simulate” the view from each person.\n\n- By iterating from right-to-left, we can efficiently maintain a stack containing potential visible persons.\n\n- For each current person, pop all shorter persons from the stack (they are visible) and if the stack is not empty afterward, it means the next taller person is still visible.\n\n- The use of a monotonic stack allows the solution to run in O(n) time by processing each element only once.",
        "complexity":   "Time Complexity: O(n) – each element is pushed and popped at most once.\nSpace Complexity: O(n) – in the worst-case scenario, the stack can hold all n elements.",
        "solution":     "We solve this problem by iterating through the queue from right to left while maintaining a monotonic stack. The stack stores the heights of people in a decreasing order (from top to bottom). For each person, we perform the following steps:\n\n- Initialize a count for visible persons.\n\n- While the stack is not empty and the current person's height is greater than the height on the top of the stack, we pop the stack. Each popped element represents a person that the current person can see.\n\n- If the stack remains non-empty after the popping process, the current person can also see the next taller person (the one currently at the top of the stack).\n\n- Store the computed count in the result array and push the current person's height onto the stack.\nThis approach guarantees that each person’s visibility count is computed efficiently in a single pass using a monotonic stack.",
    },
    "create-maximum-number": {
        "key_insights": "- Use a greedy, monotonic stack approach to select the maximum subsequence of a given length from a single array.\n\n- Merge two maximum subsequences by lexicographically comparing their remaining parts.\n\n- Try all possible splits of k digits between the two arrays to ensure the overall maximum is obtained.",
        "complexity":   "Time Complexity: O((m+n) * k²) in the worst case due to iterating splits and comparing subsequences during merging\n\nSpace Complexity: O(k) for storing subsequences and the merged result",
        "solution":     "The solution consists of three main steps:\n\n- For each array, utilize a helper function to compute the maximum subsequence of a given length using a monotonic stack. This guarantees that the highest possible digits are chosen while preserving order.\n\n- Merge two subsequences into the maximum possible result by repeatedly selecting the lexicographically larger sequence among the remaining parts.\n\n- Iterate over all valid ways to split k between the two arrays (from max(0, k - length(nums2)) to min(k, length(nums1))) and record the best merged result.\n\nData structures employed include arrays and stacks. The algorithmic approach is greedy and leverages lexicographical comparison to merge sequences optimally.",
    },
    "number-of-recent-calls": {
        "key_insights": "- Use a queue (or deque) to store the timestamps of the recent pings.\n\n- Since the ping times are guaranteed to be strictly increasing, the oldest time is always at the front of the queue.\n\n- For each new ping, remove elements from the front of the queue that fall outside the desired time window.\n\n- The remaining elements in the queue represent the pings in the time range [t - 3000, t].",
        "complexity":   "Time Complexity: O(1) amortized per ping, since each timestamp is added and removed at most once.\nSpace Complexity: O(n), where n is the number of pings stored (at most the number of pings within any 3000 ms window).",
        "solution":     "The solution uses a queue data structure to efficiently track and manage the timestamps of the ping requests. When a ping is made:\n\n- Add the new timestamp to the queue.\n\n- Remove all timestamps from the front of the queue that are less than t - 3000.\n\n- The size of the queue then gives the count of recent pings.\n\nThis approach leverages the fact that the incoming timestamps are strictly increasing. The removal process ensures that no outdated ping remains in the queue, keeping the data structure clean and efficient.",
    },
    "time-needed-to-buy-tickets": {
        "key_insights": "- The problem simulates a round-robin process where every person buys one ticket per turn.\n\n- Instead of simulating the entire queue operation, it is possible to compute the time using counts of rounds each person goes through.\n\n- For each person, compare the number of tickets they need with the tickets needed by the kth person to determine if they buy a ticket in the final round.\n\n- The kth person might only receive as many turns as tickets[k] while others ahead or after might receive an extra turn if they still have tickets left when kth person finishes in the round.",
        "complexity":   "Time Complexity: O(n) where n is the number of people in the queue.\nSpace Complexity: O(1) as only constant extra space is used.",
        "solution":     "The idea is to count how many seconds (or turns) each person in the queue spends buying tickets. For every person i in the queue, the number of turns they get to buy a ticket is the minimum between their required tickets and tickets[k] (the kth person's tickets). However, for person i where the index is less than or equal to k, if they require exactly tickets[k] tickets, they get to buy a ticket in the final round; otherwise, if i is after k, they only get turns up to tickets[k] - 1 since the kth person finishes during the kth round.\n\nThis insight leads to a formula. The total time is the sum over all i of:\nmin(tickets[i], tickets[k] - (i > k ? 1 : 0))\n\nUsing this, we avoid explicit simulation and compute the answer directly.",
    },
    "reveal-cards-in-increasing-order": {
        "key_insights": "- The final revealed sequence must be the sorted order of the deck.\n\n- Instead of simulating the process forward, we simulate it in reverse.\n\n- Use a queue (or deque) to efficiently perform operations on both ends.\n\n- Start from the largest card and reverse the process to reconstruct the initial order.",
        "complexity":   "Time Complexity: O(n log n) due to sorting the deck.\nSpace Complexity: O(n) for storing the recreated ordering and using a deque.",
        "solution":     "To solve the problem, sort the deck in ascending order. Then, simulate the revealing process in reverse: Initialize an empty deque. Iterate over the sorted deck in descending order. For each card, if the deque is non-empty, remove the last element and insert it at the front to simulate reversing the \"move next card to bottom\" step. Then, insert the current card at the front. The resulting deque represents the required deck order that, when processed by the given steps, reveals the cards in increasing order.",
    },
    "number-of-people-aware-of-a-secret": {
        "key_insights": "- Use dynamic programming to simulate the number of new people who learn the secret each day.\n\n- Track when people become eligible to share (after delay days) and when they forget the secret (after forget days).\n\n- Utilize prefix sums (cumulative sums) to efficiently compute the range sum of contributions for each day.\n\n- The final answer is the sum of new persons who still remember the secret (i.e., those whose discovery day is >= n - forget + 1).",
        "complexity":   "Time Complexity: O(n) – We iterate through days 1 to n once.\nSpace Complexity: O(n) – We store dp and prefix arrays for days 1 through n.",
        "solution":     "We simulate day by day using two arrays:\n\n- dp[i]: the number of people who learn the secret on day i.\n\n- prefix[i]: the cumulative sum of dp from day 1 to day i.\n\nFor each day i (starting from day 2), the number of new people who learn the secret, dp[i], is calculated as the sum of contributions from those who learned the secret between day i-delay and i-1 (inclusive), minus those who have already forgotten (i.e., those from day i-forget and earlier). The prefix array allows us to quickly compute these segment sums. Finally, the answer is the sum of dp values for days when individuals still remember the secret at day n (i.e., those from day n-forget+1 to day n).",
    },
    "find-the-winner-of-the-circular-game": {
        "key_insights": "- The problem is a variation of the Josephus Problem.\n\n- A recurrence relation exists: f(1, k) = 0 and f(n, k) = (f(n - 1, k) + k) mod n.\n\n- The final answer must be adjusted to 1-indexed by adding 1 to the result.\n\n- The mathematical recurrence yields an O(n) time solution with O(1) space.\n\n- While simulation using a list or queue can solve the problem, it may be less efficient.",
        "complexity":   "Time Complexity: O(n) using the mathematical recurrence.\nSpace Complexity: O(1) as it only requires a few variables.",
        "solution":     "The optimal solution leverages the mathematical recurrence relation of the Josephus problem. The recurrence relation is defined as:\n\n- Base case: When there is only one friend, the winning index is 0.\n\n- Recurrence: For a circle of size n, the new winning index can be computed by (previous winner + k) mod n.\nAfter computing the solution using zero-based indexing, add 1 to convert it to one-based indexing.\n\nWe maintain a running winner position and update it as we iterate from 2 to n. This approach requires constant space and runs in linear time.",
    },
    "jump-game-vi": {
        "key_insights": "- The problem can be modeled using dynamic programming where dp[i] represents the maximum score to reach index i.\n\n- The recurrence becomes dp[i] = nums[i] + max(dp[j]) for all indices j such that i-k ≤ j &#x3C; i.\n\n- A monotonic queue can be used to maintain the maximum dp value in the sliding window efficiently.\n\n- Removing indices that fall outside the current window is crucial to ensure consistent window size.",
        "complexity":   "Time Complexity: O(n) because each element is processed at most twice.\nSpace Complexity: O(n) for the dp array and the deque, though the deque holds at most k elements.",
        "solution":     "We use a dynamic programming approach where we define dp[i] as the maximum score achievable when reaching index i. The transition is defined as dp[i] = nums[i] + max(dp[i-1], dp[i-2], ..., dp[i-k]).\n\nTo efficiently get the maximum in the sliding window of size k, we use a monotonic deque that stores indices in decreasing order of their dp values. For every index i:\n\n- Remove indices from the front if they are out-of-window (i.e., index &#x3C; i - k).\n\n- The front of the deque gives the index of the maximum dp value in the current window.\n\n- Calculate dp[i] using dp[deque.front()] + nums[i].\n\n- Before adding i to the deque, remove all indices from the back whose dp values are less than dp[i] to maintain a decreasing order.\n\nThis method ensures that each dp value is computed in constant amortized time.",
    },
    "continuous-subarrays": {
        "key_insights": "- The condition on the subarray can be rephrased as ensuring that the difference between the maximum and minimum value in the subarray is at most 2.\n\n- A sliding window technique is ideal because subarrays are contiguous.\n\n- Use two monotonic queues (or deques) to efficiently track the minimum and maximum values in the current window.\n\n- When the condition breaks (i.e., max - min > 2), adjust the window by moving the left pointer appropriately.\n\n- The number of subarrays ending at the current right pointer is the window's length (right - left + 1).",
        "complexity":   "Time Complexity: O(n) because each element is processed at most twice (once when added and once when removed).\nSpace Complexity: O(n) in the worst case for the deques, though typical use is much lower.",
        "solution":     "We use a sliding window approach where we maintain two deques: one that keeps track of the maximum values (in decreasing order) and one for the minimum values (in increasing order) within the current window. For each new element at the right pointer, we update both deques. If the difference between the front elements of the maximum and minimum deques exceeds 2, we slide the left pointer until the condition is restored. The count of valid subarrays ending at the current right index is the size of the window, which is then added to the overall answer.",
    },
    "find-the-most-competitive-subsequence": {
        "key_insights": "- Use a greedy algorithm with a monotonic stack to continuously build the smallest lexicographical sequence.\n\n- Ensure that after potential removals, there are still enough elements remaining to complete a subsequence of size k.\n\n- At each step, compare the current number with the last element in the stack and decide whether to pop from the stack.",
        "complexity":   "Time Complexity: O(n), where n is the length of nums.\nSpace Complexity: O(k), since the stack holds at most k elements.",
        "solution":     "The solution iterates over the array while maintaining a monotonic stack. For each element, if the current element is smaller than the last element in the stack and there are enough elements left to reach a total of k, the algorithm pops from the stack to remove suboptimal choices. This greedy process guarantees that the resulting subsequence is the most competitive (i.e., lexicographically smallest possible). Once the iteration is finished, the stack contains the desired subsequence.",
    },
    "longest-continuous-subarray-with-absolute-diff-less-than-or-equal-to-limit": {
        "key_insights": "- Use a sliding window approach to efficiently find the longest valid subarray.\n\n- Maintain the current window's minimum and maximum values using two monotonic deques.\n\n- One deque (minDeque) will keep track of elements in increasing order to quickly access the minimum.\n\n- The other deque (maxDeque) will track elements in decreasing order to quickly access the maximum.\n\n- Slide the right pointer to expand the window and when the condition (max - min > limit) is violated, move the left pointer to shrink the window.\n\n- The deques are updated by removing indices that are no longer in the window or are not useful for maintaining the monotonic order.",
        "complexity":   "Time Complexity: O(n) – Each element is processed at most twice (once when added and once when removed from the window).\nSpace Complexity: O(n) – In the worst case, the deques may hold all the elements.",
        "solution":     "We use the sliding window technique with two deques to store indices for the current window's maximum and minimum values. As we iterate through nums with a right pointer, we update:\n\n- The maxDeque by removing elements from the back that are less than the current element.\n\n- The minDeque by removing elements from the back that are greater than the current element.\nThen, if the current window (from left to right) does not satisfy the condition (i.e., the difference between the front values of maxDeque and minDeque is greater than limit), we increment the left pointer and update the deques by discarding indices that fall out of the window. The maximum window length encountered is the answer.",
    },
    "max-value-of-equation": {
        "key_insights": "- The equation can be rewritten as (y_i + x_i) + (y_j - x_j) since x_i &#x3C; x_j.\n\n- For every current point j, we are looking to pair it with a previous point i that maximizes (y_i + x_i), provided that x_j - x_i &#x3C;= k.\n\n- Use a sliding window approach based on x-values since points are sorted.\n\n- A deque (monotonic queue) is well-suited to maintain potential candidates for (y_i + x_i) in decreasing order while ensuring the window constraint.\n\n- As you iterate through points, remove points that are outside the valid window and update the maximum answer accordingly.",
        "complexity":   "Time Complexity: O(n) - Each point is processed once and each is added/removed from the deque at most once.\n\nSpace Complexity: O(n) - In the worst-case scenario, the deque can contain all points.",
        "solution":     "The solution involves the following steps:\n\n- Transform the equation into a form where for each point j, you want to maximize (y_i + x_i) from a previous point i that satisfies x_j - x_i &#x3C;= k.\n\n- Use a deque to store indices (or the computed value along with x_i) such that the values (y_i + x_i) are maintained in decreasing order.\n\n- For the current point j, first remove any points from the front of the deque that are no longer within the valid x-window (i.e., where x_j - x_i > k).\n\n- If the deque is not empty, compute a candidate answer as (current y_j - current x_j) + (front value in deque) and update the maximum answer.\n\n- Before moving to the next point, maintain the monotonicity of the deque by removing from the back any point whose (y_i + x_i) value is less than or equal to the current point’s value (y_j + x_j) as they will never be optimal for future points.\n\n- Append the current point to the deque.\n\n- Return the maximum answer found.",
    },
    "shortest-subarray-with-sum-at-least-k": {
        "key_insights": "- Use a prefix-sum array to quickly compute the sum of any subarray.\n\n- The difference between two prefix sums gives the sum of the subarray between their indices.\n\n- Maintain a monotonic deque that stores indices of the prefix-sum array in increasing order.\n\n- As you iterate through the prefix sums, check and update the answer when the difference meets or exceeds k.\n\n- Remove indices from the deque that are no longer potential candidates (either because they yield a subarray that is too long or their prefix sum is not optimal).",
        "complexity":   "Time Complexity: O(n) where n is the number of elements in nums.\nSpace Complexity: O(n) due to storing the prefix sum array and the deque.",
        "solution":     "We solve the problem by first constructing a prefix-sum array where each element p[i+1] equals p[i] plus nums[i]. For any two indices i and j (with i &#x3C; j), the sum of the subarray nums[i] to nums[j-1] is p[j] - p[i]. We need this difference to be at least k. By using a deque, we can efficiently track and check for the shortest valid subarray.\n\nThe deque is maintained in increasing order of prefix sums:\n\n- As we iterate through the prefix sums, if the current prefix sum minus the smallest prefix sum from the deque is at least k, we update the minimum length.\n\n- We remove indices from the back if they offer no benefit (i.e., having a larger prefix sum when a smaller one has already been seen).\n\n- This ensures that we always consider the best candidate indices for forming a valid subarray.",
    },
    "sort-characters-by-frequency": {
        "key_insights": "- Use a hash map/dictionary to count the frequency of each character.\n\n- Sort the characters by their frequency in descending order.\n\n- Reconstruct the final string by repeating characters based on their frequency.\n\n- The problem allows any valid ordering when frequencies are equal.",
        "complexity":   "Time Complexity: O(n log n) in the worst case due to sorting (or O(n) if bucket sort is applied).\nSpace Complexity: O(n) for storing the frequency counts and the final output.",
        "solution":     "The solution involves:\n\n- Iterating through the string to compute the frequency of each character using a hash table/dictionary.\n\n- Sorting the unique characters based on their frequency in descending order.\n\n- Building the result string by repeating each character based on its frequency.\nA key trick here is that the problem guarantees that the order among characters with equal frequency can be arbitrary, so a simple sort is sufficient.",
    },
    "maximum-gap": {
        "key_insights": "- Sorting the array explicitly would take O(n log n) time, which is not acceptable; instead, we can use an optimal bucket sort-based approach.\n\n- The idea is to determine the minimum and maximum values in the array.\n\n- The maximum gap will occur not within a bucket but between adjacent buckets.\n\n- Compute appropriate bucket size (gap) and then distribute numbers across these buckets.\n\n- Track the minimum and maximum value within each bucket.\n\n- The maximum gap is the maximum difference between the minimum value of the current non-empty bucket and the maximum value of the previous non-empty bucket.",
        "complexity":   "Time Complexity: O(n)\nSpace Complexity: O(n)",
        "solution":     "The solution involves these steps:\n\n- Find the minimum and maximum value in the array. If they are equal, return 0 because all numbers are the same.\n\n- Compute the bucket size using the formula: bucket_size = max(1, (max_val - min_val) / (n - 1)).\n\n- Calculate the number of buckets required: bucket_count = (max_val - min_val) / bucket_size + 1.\n\n- Initialize each bucket to store its minimum and maximum values.\n\n- Iterate over the array and place each element in its corresponding bucket.\n\n- Finally, iterate through the buckets and compute the maximum gap by comparing the minimum value of the current non-empty bucket with the maximum value of the previous non-empty bucket.\n\nThis bucket-based strategy ensures that the maximum gap is found without performing a full sort, maintaining linear time complexity.",
    },
    "contains-duplicate-iii": {
        "key_insights": "- Use a sliding window of size indexDiff to ensure the index condition.\n\n- To efficiently check the value condition, maintain a data structure with near-ordering.\n\n- Bucket sort (or using an ordered set / BST) can help check for any element within [num - valueDiff, num + valueDiff] in O(1) average time.\n\n- Use a bucket size of (valueDiff + 1) to map numbers to buckets. Two numbers in the same or adjacent buckets may satisfy the condition.",
        "complexity":   "Time Complexity: O(n) on average (each element is processed once and bucket operations take constant time on average).\nSpace Complexity: O(min(n, indexDiff)) since we only store the current sliding window’s elements.",
        "solution":     "The solution leverages bucket sort along with a sliding window. We use a hash map to maintain buckets, where each bucket size is defined as (valueDiff + 1). For each element:\n\n- Compute its bucket ID.\n\n- Check if this bucket already has a number (this guarantees the value difference is within valueDiff).\n\n- Check the adjacent buckets (bucket-1 and bucket+1) for potential candidates.\n\n- Insert the current number into its bucket.\n\n- Once the sliding window exceeds the size indexDiff, remove the element that falls outside the window.\nThis approach ensures both conditions (index and value constraints) are met efficiently.",
    },
    "special-binary-string": {
        "key_insights": "- The problem has an inherent recursive structure where each valid special string can be broken down into smaller special strings.\n\n- The inner structure between a matching pair of 1 and 0 (i.e. a segment \"1\" + sub-string + \"0\") is itself a special binary string.\n\n- Once you recursively transform the inner segments, sorting these segments in descending order helps maximize the lexicographical order of the overall string.\n\n- The crucial trick is recognizing that by recursively processing the inner parts and then reassembling them after sorting, you capture all possible valid swaps.",
        "complexity":   "Time Complexity: O(n^2 * log n) in the worst-case scenario due to the recursive partitioning and sorting of segments.\nSpace Complexity: O(n) for recursion depth and storage of sub-segments.",
        "solution":     "We solve the problem using a recursive approach. The algorithm works as follows:\n\n- Initialize a counter and traverse string s.\n\n- For every substring that forms a valid special binary string (when the counter reaches zero), recursively process the inner substring (after removing the leading '1' and trailing '0').\n\n- Wrap the processed substring with '1' and '0' and add it to a list of candidate segments.\n\n- Sort these segments in descending (lexicographically largest) order.\n\n- Concatenate the sorted segments and return the result.\n\nKey data structures used include:\n\n- A list (or array) to hold candidate special substrings.\n\n- Recursive function calls to process inner special strings.\n\nThis recursive decomposition ensures we explore all possible swaps implicitly, and sorting the segments yields the lexicographically largest result possible.",
    },
    "integer-to-english-words": {
        "key_insights": "- The number is processed in groups of three digits (hundreds, tens, and ones), mapping these groups to their word equivalents.\n\n- Special handling is required for numbers between 10 and 19 (teens) because they have unique names.\n\n- Larger groups beyond the basic hundred (thousand, million, billion) need to be appended with the appropriate scale.\n\n- The solution is recursive or iterative in nature, processing each three-digit block separately and then combining them.\n\n- Edge case: when the number is 0, the output should simply be \"Zero\".",
        "complexity":   "Time Complexity: O(1) — Although it appears to depend on the number of digits, the maximum input size is fixed (at most 10 digits for 2^31 - 1).\nSpace Complexity: O(1) — Only a constant amount of extra space is used.",
        "solution":     "The solution involves breaking the integer into segments of three digits starting from the least significant digits. For each segment:\n\n- Convert the hundreds, tens, and ones place into words.\n\n- Handle the special case for numbers between 10 and 19.\n\n- Append the appropriate scale (thousand, million, billion) based on the segment’s position.\nThe final result is constructed by concatenating the word representations of these segments (ignoring empty segments) in reverse order (most significant segment first).\n\nData Structures used:\n\n- Arrays or lists for mapping digits, teens, tens and scales.\n\nAlgorithmic Approach:\n\n- Define helper functions that convert a three-digit number into words.\n\n- Loop through each segment (every three digits) and convert if non-zero.\n\n- Concatenate segments with proper scale values.\n\n- Trim any extra spaces from the final output.",
    },
    "longest-nice-substring": {
        "key_insights": "- A substring is considered nice if for every character, both its uppercase and lowercase forms are present.\n\n- To check if a substring is nice, using a set to quickly verify the presence of both cases is effective.\n\n- Due to the small constraint (s.length &#x3C;= 100), an O(n^3) brute-force solution is acceptable, but an efficient divide-and-conquer method can also be applied.\n\n- The divide-and-conquer technique works by identifying an offending character (one missing its counterpart) and then recursively solving for the segments on both sides.\n\n- Keeping track of the earliest occurrence for substrings of the same length is necessary when multiple valid candidates exist.",
        "complexity":   "Time Complexity: O(n^2) to O(n^3) in the worst-case scenario if using brute force; however, the divide and conquer approach typically performs faster in practice.\nSpace Complexity: O(n) due to recursion and auxiliary data structures (sets).",
        "solution":     "We can solve the problem using a recursive divide-and-conquer approach:\n\n- Check if the current string is nice. We do this by creating a set of characters in the string and confirming for each character that its opposite case also exists.\n\n- If the string is nice, return it as a candidate.\n\n- If not, find the first offending character that violates the niceness condition.\n\n- Recursively call the function for the two substrings split at the offending character, and return the longer of the two valid nice substrings.\n\n- This approach ensures that we consider every possibility while naturally skipping segments that cannot form a nice substring.",
    },
    "convert-sorted-list-to-binary-search-tree": {
        "key_insights": "- The list is sorted in ascending order, which allows us to apply a divide and conquer approach.\n\n- The middle element of the list should be the root of the BST to ensure the tree is balanced.\n\n- Recursively repeating the process for the left and right segments of the list constructs the left and right subtrees.\n\n- Two popular approaches are:\n\n- Convert the list to an array and then use two pointers for recursion.\n\n- Use slow and fast pointers to find the middle element without extra space.",
        "complexity":   "Time Complexity: O(n) if using the in-order simulation approach; O(n log n) if repeatedly finding the middle with two pointers.\n\nSpace Complexity: O(log n) for the recursion stack (assuming the conversion to array uses O(n) extra space in that approach).",
        "solution":     "We solve the problem using a divide and conquer method by first converting the linked list into an array, which allows for easy access to the middle element. The middle element is chosen as the root of the BST, and the left half of the array is used to build the left subtree while the right half constructs the right subtree. This method guarantees that the height difference between the subtrees is minimized, resulting in a height-balanced BST.\n\nKey data structures used:\n\n- Array (to store linked list values)\n\n- Tree nodes to build the BST\n\nThe algorithm recursively computes the middle index and creates tree nodes until the entire array is processed.",
    },
    "construct-quad-tree": {
        "key_insights": "- Use a divide and conquer approach: recursively check if the current grid region is uniform.\n\n- A region is uniform if all entries are the same; if so, create a leaf node.\n\n- If not uniform, split the region into four equal quadrants and recursively construct the Quad-Tree for each.\n\n- Careful handling of grid boundaries is required during recursion.",
        "complexity":   "Time Complexity: O(n^2) in the worst-case scenario since every cell might be inspected.\nSpace Complexity: O(n^2) in the worst-case due to recursion stack and node storage.",
        "solution":     "We use recursion to examine each sub-grid of the matrix. First, we check if the current grid region has a uniform value. If it does, we return a leaf node with that value. If not, we partition the grid into four smaller quadrants (topLeft, topRight, bottomLeft, bottomRight) and recursively construct the corresponding Quad-Tree nodes for each quadrant. Finally, we create an internal node combining these four children. This strategy guarantees we process all parts of the grid and correctly form the Quad-Tree.",
    },
    "maximum-binary-tree": {
        "key_insights": "- The maximum element in any subarray becomes the root of the (sub)tree.\n\n- The problem can be naturally solved using a recursive, divide and conquer approach.\n\n- Alternatively, a monotonic stack can be used to achieve an O(n) solution.\n\n- Handling base cases, such as an empty array, is crucial to prevent errors.",
        "complexity":   "Time Complexity: O(n^2) in the worst-case (when the array is sorted in ascending or descending order) using the recursive approach; O(n) using a monotonic stack approach.\nSpace Complexity: O(n) due to recursion stack or explicit stack usage.",
        "solution":     "We solve the problem recursively:\n\n- Identify the maximum element in the current array segment.\n\n- Create a tree node with this maximum value.\n\n- Recursively build the left subtree with the subarray left of the maximum element.\n\n- Recursively build the right subtree with the subarray right of the maximum element.\nThis approach leverages the divide and conquer technique to build the tree in a structured manner.",
    },
    "sort-an-array": {
        "key_insights": "- Must not use built-in functions.\n\n- Achieve O(nlog(n)) worst-case time complexity.\n\n- Aim for minimum additional space; using an in-place algorithm is ideal.\n\n- Heap sort is a good candidate because it offers O(nlog(n)) time with O(1) additional space.",
        "complexity":   "Time Complexity: O(nlog(n))\nSpace Complexity: O(1) (in-place)",
        "solution":     "The chosen approach is Heap Sort. Heap sort works by first building a max heap from the input array. A max heap is a complete binary tree where the value in each node is greater than or equal to the values in its children. After the max heap is constructed, the largest element is at the root of the heap. Then, we repeatedly swap the root of the heap with the last element of the heap and reduce the heap size by one. We then re-heapify the root to restore the max heap property. This process continues until the heap is empty and the array is sorted in ascending order.\n\nKey steps in the algorithm:\n\n- Build the max heap in-place by calling a helper function (heapify) for each non-leaf node.\n\n- Iteratively swap the first element (largest in the heap) with the last element and reduce the heap size.\n\n- Re-heapify the array from the root index to ensure the max heap property is maintained.\n\nThis approach avoids the extra space required for algorithms like merge sort and guarantees O(nlog(n)) worst-case time.",
    },
    "reverse-pairs": {
        "key_insights": "- The brute force solution checks every possible pair, resulting in O(n^2) time complexity, which is too slow for the given constraints.\n\n- A modified merge sort can be used to both sort the array and count reverse pairs efficiently.\n\n- During the merge process, since the two halves are sorted, a two-pointer technique can efficiently count how many numbers in the right half satisfy the reverse pair condition for each number in the left half.\n\n- Alternative approaches include Binary Indexed Trees or Segment Trees, but merge sort provides a clean divide and conquer solution.",
        "complexity":   "Time Complexity: O(n log n) – Each level of the merge sort takes O(n) time and there are O(log n) levels.\nSpace Complexity: O(n) – Additional space is required for the temporary array during merging.",
        "solution":     "We solve the problem using a modified merge sort. The idea is to recursively split the array into halves, count reverse pairs in each half, and then count reverse pairs across the two halves before merging them. When merging two sorted halves, a two-pointer approach can be used to count the reverse pairs quickly. After counting, the two halves are merged in sorted order.",
    },
    "count-of-range-sum": {
        "key_insights": "- Transform the problem using prefix sums. Define prefix[i] as the sum of elements nums[0] to nums[i-1] which simplifies S(i, j) to prefix[j+1] - prefix[i].\n\n- The problem then reduces to counting, for every prefix[j+1], how many earlier prefix[i] satisfy: lower ≤ prefix[j+1] - prefix[i] ≤ upper.\n\n- Use a modified merge sort (or other data structures like Binary Indexed Tree or Segment Tree) to count the number of valid pairs while sorting the prefix sum array.\n\n- The merge step cleverly counts the number of valid prefix differences before merging two sorted halves.",
        "complexity":   "Time Complexity: O(n log n) due to merge sort style divide and conquer.\nSpace Complexity: O(n) for the prefix sum and temporary arrays used in merging.",
        "solution":     "We transform the input array nums into a prefix sum array. For each new prefix sum, we search the sorted list of previous prefix sums to count how many fall into the range [current - upper, current - lower]. This is efficiently done during the merge sort process where each merge step counts the valid pairs between the two sorted subarrays. The merge sort guarantees that the array segments are sorted, which allows binary-counting of valid ranges by maintaining two pointers. This approach eliminates the need for nested loops and achieves O(n log n) complexity.",
    },
    "find-the-kth-largest-integer-in-the-array": {
        "key_insights": "- Since each number is represented as a string and can be very long (up to 100 digits), converting them to numeric types might be impractical in some languages; instead, compare using string properties.\n\n- The key observation is that one integer is greater than another if its string length (number of digits) is larger; if lengths are equal, lexicographical order gives the order.\n\n- Sorting the array in descending order using a custom comparator (first comparing lengths, then lexicographically) allows direct access to the kth largest element.\n\n- Alternatively, selection algorithms (e.g., Quickselect) or heaps can be used, but a sort is simple and effective given the input constraints.",
        "complexity":   "Time Complexity: O(n log n * m) where m is the maximum string length (for comparing two numbers)\n\nSpace Complexity: O(n) (space used by sorting)",
        "solution":     "The solution involves sorting the array of strings in descending order based on their numeric value. We define a custom comparator that:\n\n- Compares the lengths of the strings. A longer string indicates a larger integer.\n\n- If the lengths are equal, compares them lexicographically.\n\nAfter sorting, the kth largest integer is simply found at the k-1 index in the sorted array. This approach avoids pitfalls with very large numbers since it relies only on string properties.",
    },
    "valid-perfect-square": {
        "key_insights": "- Recognize that if num is a perfect square, it will have an exact integer square root.\n\n- Use binary search between 1 and num/2 (when num >= 4) to efficiently determine if there is an integer mid such that mid * mid == num.\n\n- The binary search stops when the search space is exhausted, meaning num is not a perfect square if no integer mid found.",
        "complexity":   "Time Complexity: O(log(num))\nSpace Complexity: O(1)",
        "solution":     "We solve the problem using binary search. By checking mid * mid at each step, we narrow down the interval where the square root could lie, eventually determining if an exact integer square root exists. The trick is setting the correct search boundaries: numbers less than 4 can be handled directly, and for larger numbers, the square root can never be more than num/2. This avoids using built-in functions and deals gracefully with large inputs.",
    },
    "find-smallest-letter-greater-than-target": {
        "key_insights": "- The array is sorted in non-decreasing order, which enables an efficient binary search solution.\n\n- The binary search is used to identify the first character greater than the target.\n\n- Account for the wrap-around scenario by returning the first character when the target is greater than or equal to all elements in the array.",
        "complexity":   "Time Complexity: O(log n) where n is the length of the letters array\n\nSpace Complexity: O(1)",
        "solution":     "We can solve this problem using binary search. Initialize two pointers, left and right, to define the search boundaries. Compute the middle index and compare the character at that index with the target. If the current character is less than or equal to the target, move the left pointer to mid + 1; otherwise, move the right pointer to mid - 1. After the loop, use left % n (where n is the size of the array) to handle the wrap-around case and return the correct character.",
    },
    "search-in-rotated-sorted-array-ii": {
        "key_insights": "- The array is rotated, so one of the two halves will always be sorted.\n\n- Duplicates can complicate the binary search by obscuring which half is sorted.\n\n- When duplicates cause ambiguity (i.e., when nums[low], nums[mid], and nums[high] are equal), the search space is reduced by incrementally moving the boundaries.\n\n- A modified binary search can efficiently search the array on average, though the worst-case runtime can degrade to O(n) due to duplicates.",
        "complexity":   "Time Complexity: Worst-case O(n), Average-case O(log n)\n\nSpace Complexity: O(1)",
        "solution":     "The solution employs a modified binary search technique. The main algorithm works by maintaining two pointers (low and high) and calculating a mid-point. If the target is equal to the element at the mid, the algorithm returns true. If duplicates are detected at the low, mid, and high indices, the boundaries are adjusted to avoid ambiguity. Otherwise, the algorithm determines if the left or right side is sorted; if the target lies within the sorted portion, it moves the boundaries accordingly. This method accounts for potential duplicate values influencing the decision process.",
    },
    "find-peak-element": {
        "key_insights": "- Use a binary search strategy to achieve the required O(log n) time complexity.\n\n- At each step, compare the middle element with its right neighbor to decide which half to search.\n\n- Due to the -∞ boundaries, there is always at least one peak in the array.",
        "complexity":   "Time Complexity: O(log n)\nSpace Complexity: O(1)",
        "solution":     "We use a binary search approach by maintaining two pointers: low and high. We calculate a mid index and check if it is a peak by comparing it with its neighbors. If the element to the right of mid is greater, then a peak must exist in the right half of the array; otherwise, it will be in the left half. This greedy move is valid due to the boundary conditions where the ends are effectively -∞. By continually narrowing the search space, we can efficiently find a peak element.",
    },
    "heaters": {
        "key_insights": "- Sort both the houses and the heaters arrays to efficiently search for the nearest heater for any given house.\n\n- For each house, use binary search to locate the heater that is closest.\n\n- The minimum required radius is the maximum distance a house is from its nearest heater.\n\n- The problem leverages binary search for each house, leading to an overall efficient solution.",
        "complexity":   "Time Complexity: O(n log m) where n is the number of houses and m is the number of heaters.\nSpace Complexity: O(1) (ignoring the space required for input sorting, which can be done in-place)",
        "solution":     "The solution first sorts the houses and heaters arrays. For each house, binary search is applied to the heaters array to find the position where the heater value is just greater than or equal to the house's position. The distance to the closest heater is then the minimum of the distance from the house to the heater just before or just after this position. Finally, the answer is the maximum of these minimum distances, ensuring that every house is within the coverage of some heater.",
    },
    "minimum-number-of-days-to-make-m-bouquets": {
        "key_insights": "- Use binary search on the range of possible days since waiting more days only increases the number of available flowers.\n\n- At each day guess, simulate whether it is possible to form m bouquets by iterating over the bloomDay array.\n\n- Count only bouquets formed by k consecutive flowers (reset count when encountering a flower that hasn't bloomed by the considered day).\n\n- Early termination: if the total number of flowers is insufficient to form m bouquets (i.e. if m * k > n), return -1 immediately.",
        "complexity":   "Time Complexity: O(n * log(max(bloomDay))) - For each binary search iteration, we scan the entire array.\nSpace Complexity: O(1) - Constant extra space is used.",
        "solution":     "The problem can be efficiently solved using a binary search approach. The idea is to determine a minimum day such that there are enough adjacent flowers (k consecutive ones) to form m bouquets. We define a helper function to check if for a given day, at least m bouquets are possible.\n\nThe binary search is performed over the range of days [min(bloomDay), max(bloomDay)]. For each mid value (representing a day), iterate through the bloomDay array to count how many bouquets can be created if only flowers with bloomDay &#x3C;= mid are considered bloomed. The count resets if a flower that hasn't bloomed is encountered. If the bouquet count meets or exceeds m, then this day is a potential answer, and we try to find a smaller day. Otherwise, we search in the higher day range.\n\nData structures used include simple counters and pointers for iterating over the array. The algorithmic approach is a combination of greedy checking and binary search.",
    },
    "search-a-2d-matrix-ii": {
        "key_insights": "- Each row and column is sorted, which allows us to eliminate multiple rows or columns with one comparison.\n\n- Starting from the top-right corner (or bottom-left) is beneficial because at this position, one can decide to move left or down based on how the current value compares with the target.\n\n- This \"staircase\" search leads to a linear time algorithm relative to the sum of dimensions of the matrix.",
        "complexity":   "Time Complexity: O(m + n) where m is the number of rows and n is the number of columns.\nSpace Complexity: O(1)",
        "solution":     "The algorithm starts from the top-right corner of the matrix. At each step, compare the current element with the target:\n\n- If the current element equals the target, return true.\n\n- If the current element is greater than the target, move left because all elements below are larger.\n\n- If the current element is smaller than the target, move down because all elements to the left are smaller.\nThis method ensures that in each step one row or one column is eliminated from consideration, leading to an efficient search.",
    },
    "magnetic-force-between-two-balls": {
        "key_insights": "- Sort the basket positions to enable efficient checking.\n\n- Use binary search on the “answer” (the minimum force) rather than iterating over all possible forces.\n\n- For a given minimum force, use a greedy strategy to place balls sequentially, placing a ball whenever the distance from the last placed ball meets the minimum requirement.\n\n- Adjust the search boundaries based on whether the placement was possible.",
        "complexity":   "Time Complexity: O(n log(max_position - min_position))\nSpace Complexity: O(1) (ignoring the space used by sorting in-place)",
        "solution":     "The solution starts by sorting the basket positions. Then, the maximum possible minimum force is determined using binary search. The binary search explores all possible force values between 1 and the difference between the maximum and minimum basket positions. For each candidate force, a helper function checks if it is possible to place m balls such that each ball is at least the candidate force apart. This greedy placement starts from the first basket and places subsequent balls only when the current basket’s position is at least the candidate force away from the last placed ball. Adjust the binary search boundaries based on whether the placement is possible or not, and finally, return the largest force for which placement is possible.",
    },
    "find-in-mountain-array": {
        "key_insights": "- Use binary search to minimize the number of MountainArray.get calls.\n\n- First, identify the peak (maximum element) using a modified binary search.\n\n- Once the peak is found, perform two separate binary searches: one on the increasing part (left side) and one on the decreasing part (right side).\n\n- Return the index from the left side search if found (since we want the minimum index); otherwise, check the right side.\n\n- Be mindful of the interactive constraint where total number of get calls must be under 100.",
        "complexity":   "Time Complexity: O(log n) – binary searches are used to identify the peak and target index.\nSpace Complexity: O(1) – only a constant amount of extra memory is used.",
        "solution":     "The solution consists of three main steps:\n\n- Peak Finding: Use binary search over the mountain array to locate the peak. In each step, compare element at mid and mid+1 to determine if the peak is to the right or left.\n\n- Target Search in the Ascending Part: Conduct a standard binary search in the increasing segment (from index 0 to peak). Since the sequence is sorted in ascending order, a regular binary search works.\n\n- Target Search in the Descending Part: If the target is not found on the left, perform a binary search on the decreasing segment (from peak+1 to the end) where comparisons are inverted (i.e., larger values come before smaller ones).\n\n- Return the index from the left search if found, otherwise if found on the right, return that index. If not found in either part, return -1.",
    },
    "find-k-th-smallest-pair-distance": {
        "key_insights": "- Sorting the array simplifies the calculation since the absolute difference is monotonic when traversing sequentially.\n\n- Instead of generating all pair distances, use binary search on the range of possible distances.\n\n- A two-pointer technique can efficiently count the number of pairs with a distance less than or equal to a given middle value.\n\n- The binary search converges on the kth smallest distance based on the count of valid pairs.",
        "complexity":   "Time Complexity: O(n log(max - min) + n log n) where n is the number of elements. O(n log n) for sorting and O(n log(max - min)) for binary search across the distance range.\nSpace Complexity: O(1) extra space (ignoring the space required for sorting).",
        "solution":     "The approach starts by sorting the input array to leverage the ordered property of the numbers. The possible pair distances range between 0 and (max(nums) - min(nums)). Use binary search over this range to find the smallest distance d such that there are at least k pairs with a distance less than or equal to d. A helper function uses a two-pointer method on the sorted array to count the number of pairs satisfying the condition. Adjust the binary search boundaries based on this count until convergence.",
    },
    "permutations-ii": {
        "key_insights": "- Sort the input array to identify and easily skip duplicates.\n\n- Use backtracking to generate all candidate permutations.\n\n- Maintain a visited or used array to track elements already added to the current permutation.\n\n- When encountering a duplicate element, only use it if the previous identical element was used in the current recursion branch.\n\n- Backtrack by removing elements and resetting their used status when moving to another branch.",
        "complexity":   "Time Complexity: O(n * n!) in the worst-case scenario, where n is the length of the input array.\nSpace Complexity: O(n) for the recursion stack and used data structure, plus O(n * n!) to store the permutations.",
        "solution":     "The solution employs a backtracking algorithm. First, sort the nums array to ensure that duplicates are adjacent. Then, use a recursive function to build permutations by iterating through the array and choosing elements that have not been used yet. To handle duplicates, if the current element is the same as the previous one and the previous one was not used in the current recursive path, skip the current element. This ensures that duplicate permutations are never constructed. Data structures used include:\n\n- A list (or array) to hold the current permutation path.\n\n- A boolean visited (or used) array to keep track of whether a number is already in the current permutation.\n\nAfter constructing a permutation of the same length as nums, add it to the results list, then backtrack to explore alternative possibilities.",
    },
    "combination-sum-iii": {
        "key_insights": "- Use a backtracking approach to explore all potential combinations.\n\n- Only numbers from 1 to 9 are available.\n\n- Each number can be used only once.\n\n- The solution must consist of exactly k numbers summing to n.\n\n- Early pruning is possible if the current sum plus the next candidate exceeds n.",
        "complexity":   "Time Complexity: O(2^9) in the worst-case scenario, since we are exploring subsets of 9 numbers.\nSpace Complexity: O(k) for the recursion stack, where k is the number of elements in the combination.",
        "solution":     "We solve this problem using the backtracking algorithm. The solution involves a recursive helper function that tries to build combinations from numbers 1 through 9. At each recursive call, if the current path contains k numbers and their sum equals n, the path is added to the result. We use early pruning by checking if the running sum exceeds n, and we start with numbers greater than the last included to avoid duplicate combinations.",
    },
    "combinations": {
        "key_insights": "- The problem requires generating all unique combinations (order does not matter) from 1 to n.\n\n- Backtracking (depth-first search) is ideal for exploring all possible combinations.\n\n- We build combinations incrementally and backtrack when the current combination reaches a dead end or a complete combination of k numbers is formed.\n\n- Pruning the recursion early by limiting the candidate range helps improve efficiency.",
        "complexity":   "Time Complexity: O(k * C(n, k))\n\nSpace Complexity: O(k) for the recursion stack (not including space for the output)",
        "solution":     "We use a backtracking approach with depth-first search to generate all possible combinations. Start with an empty combination and recursively add numbers from a candidate range ensuring each new element is greater than the last added, which maintains the increasing order and avoids duplicates. When the length of the current combination equals k, add it to the result list. After exploring each path, backtrack by removing the last element and try the next number. The candidate range can be pruned since if the remaining numbers are insufficient to reach k, no further recursive calls are made.",
    },
    "restore-ip-addresses": {
        "key_insights": "- Use a backtracking approach to explore different segmentations of the input string.\n\n- IP addresses contain exactly four segments; once four segments are generated, the entire string must have been used.\n\n- Validate each segment to ensure it is within the range [0, 255] and does not have leading zeros (unless the segment is \"0\").\n\n- Prune the search if the remaining characters do not allow forming a valid IP address (too short or too long).",
        "complexity":   "Time Complexity: O(N^4) in the worst-case scenario where N is the length of the string, though N is bounded by string length constraints.\nSpace Complexity: O(N) for the recursion call stack and temporary storage, where N is the length of the string.",
        "solution":     "We use a backtracking recursive approach to partition the string into four segments. At each recursive call, we try all possible segment lengths (1 to 3 characters) while ensuring that:\n\n- The chosen segment does not exceed the string's bounds.\n\n- The segment does not have any invalid leading zeros (unless the segment is \"0\").\n\n- The numeric value of the segment lies between 0 and 255.\n\nOnce four segments are collected, we check if all characters in the string have been consumed. If so, we join the segments with dots and add the resulting IP address to the final answer list.",
    },
    "unique-paths-iii": {
        "key_insights": "- Count all non-obstacle cells (including start and end) to know the route length required.\n\n- Use Depth First Search (DFS) to explore all possible paths from the starting cell.\n\n- Mark cells as visited during recursion to avoid revisiting and backtrack afterwards.\n\n- Only count a path when reaching the ending cell and the number of visited cells equals the total non-obstacle count.",
        "complexity":   "Time Complexity: O(3^(mn)) in the worst-case scenario due to branching factor in DFS.\nSpace Complexity: O(mn) due to recursion call stack and in-place modifications of the grid.",
        "solution":     "We solve the problem by performing a DFS starting from the unique starting cell. Before DFS, count the total number of non-obstacle cells. As DFS explores neighbors recursively, mark the cell as visited (by setting it to -1) to prevent cycles and restore its value after exploring (backtracking). When the ending cell is reached, check if we have visited all non-obstacle cells. If yes, increment the valid path count.\n\nData structures and algorithms used:\n\n- Recursion (DFS) for exploring all valid paths.\n\n- In-place grid marking for tracking visited cells.\n\n- Backtracking for restoring grid state.\nThis approach ensures that every possible valid route is considered while correctly handling obstacles and ensuring each non-obstacle cell is visited exactly once.",
    },
    "remove-invalid-parentheses": {
        "key_insights": "- Use BFS to generate all possible strings by removing one parenthesis at a time.\n\n- Stop the BFS once valid strings are found at the current level (to ensure minimum removals).\n\n- Use a set to avoid processing the same string multiple times.\n\n- Use a helper function to check if a string has valid parentheses.",
        "complexity":   "Time Complexity: O(n * 2^n) in the worst-case, as each character could be removed leading to exploring 2^n possibilities.\nSpace Complexity: O(2^n) for storing all unique intermediate strings in the worst-case.",
        "solution":     "We solve this problem using a Breadth-First Search (BFS) approach. Start with the original string and generate all possible strings by removing one parenthesis at each step. For every generated string, check if it is valid using a helper function that traverses the string and maintains a balance counter. As soon as valid expressions are found at a particular level of BFS, add them to the result list and do not proceed to deeper levels. This ensures we remove the minimal number of parentheses. Data structures used include a queue for BFS and a set for visited strings to avoid duplicates.",
    },
    "average-of-levels-in-binary-tree": {
        "key_insights": "- Use level-order traversal (BFS) to process each level of the tree.\n\n- A queue helps to efficiently traverse the tree level by level.\n\n- For each level, track the number of nodes and the cumulative sum to compute the average.\n\n- Edge cases include handling an empty tree (though given constraints confirm at least one node).",
        "complexity":   "Time Complexity: O(n), where n is the number of nodes in the tree because each node is processed exactly once.\n\nSpace Complexity: O(n) in the worst-case (e.g., when the tree is completely balanced, the last level might contain O(n/2) nodes).",
        "solution":     "The solution employs a breadth-first search (BFS) using a queue to traverse the tree level by level. For each level, it calculates the sum of node values and the count of nodes. By dividing the cumulative sum by the count, it obtains the average for that level, which is then added to the result list. This approach efficiently computes the required averages with a clear focus on level-wise processing.",
    },
    "populating-next-right-pointers-in-each-node": {
        "key_insights": "- The tree is perfect, meaning that if a node exists, both its left and right children exist (except at the leaf level).\n\n- At each node, we can set:\n\n- node.left.next to node.right.\n\n- node.right.next to node.next.left (if node.next exists).\n\n- We can use a level-order traversal without extra space by linking nodes from left to right on the same level.\n\n- The approach only uses constant extra space (excluding recursion stack if used).",
        "complexity":   "Time Complexity: O(n), where n is the number of nodes in the tree.\nSpace Complexity: O(1) extra space (ignoring recursion stack or pointers used to traverse levels).",
        "solution":     "We can solve this problem by taking advantage of the perfect tree property. Starting from the root, we traverse each level using the already established \"next\" pointers. For each node at a given level, set the left child's \"next\" pointer to the right child. For the right child, if the node has a \"next\" pointer, link it to the left child of the node's next neighbor. After processing a level, move to the leftmost node of the next level. This iterative process continues until we reach a level without children.",
    },
    "populating-next-right-pointers-in-each-node-ii": {
        "key_insights": "- The tree is not necessarily a perfect binary tree.\n\n- The problem requires an algorithm using constant extra space.\n\n- A level order traversal using established next pointers allows linking nodes on the next level without additional data structures.\n\n- Use pointers to keep track of the current level, the head of the next level, and the tail to build the next connections.",
        "complexity":   "Time Complexity: O(n) where n is the number of nodes, because every node is processed once.\n\nSpace Complexity: O(1) extra space (ignoring the recursion stack if a recursive approach were used) by leveraging the next pointers for level order traversal.",
        "solution":     "The solution uses a level order traversal without using an explicit queue. Instead, it uses three pointers:\n\n- current: iterates over nodes in the current level.\n\n- nextHead: points to the first node in the next level.\n\n- nextTail: used to connect nodes in the next level.\nFor each node in the current level, if it has a left child, attach it to nextTail. Similarly, if it has a right child, attach it to nextTail. After processing the entire level, move to nextHead and continue the process. This guarantees constant extra space usage while linking all nodes appropriately.",
    },
    "check-completeness-of-a-binary-tree": {
        "key_insights": "- A complete binary tree requires every level (except the last) to be fully occupied.\n\n- In the last level, nodes must fill from the left.\n\n- A level order traversal (BFS) effectively checks these conditions.\n\n- Once a missing child is encountered during BFS, all subsequent nodes must be leaves.",
        "complexity":   "Time Complexity: O(n), where n is the number of nodes, since each node is visited once.\nSpace Complexity: O(n) in the worst-case scenario due to the queue used for BFS traversal.",
        "solution":     "We solve this problem using a breadth-first search (BFS) traversal. Traverse the tree level by level using a queue. When a node is encountered that does not have a left or right child, set a flag indicating that all following nodes must have no children. If any subsequent node has a child, the tree is not complete. This approach efficiently ensures the tree maintains the complete tree properties.",
    },
    "binary-tree-preorder-traversal": {
        "key_insights": "- Preorder traversal follows the order: Root -> Left -> Right.\n\n- A recursive solution is straightforward but the problem asks for an iterative solution.\n\n- Use a stack to simulate the recursive function call behavior.\n\n- When using a stack, push the right child first so that left is processed before right.",
        "complexity":   "Time Complexity: O(N), where N is the number of nodes, because every node is visited once.\nSpace Complexity: O(N) in the worst case due to the stack when the tree is unbalanced.",
        "solution":     "We use an iterative approach with a stack. Start by pushing the root to the stack. While the stack is not empty, pop the top node from the stack, record its value, and push its right child (if exists) followed by its left child (if exists) to the stack. This ensures that the left child is processed before the right child, adhering to the preorder traversal order.",
    },
    "sum-of-left-leaves": {
        "key_insights": "- Use recursion or iteration (DFS/BFS) to traverse the binary tree.\n\n- At each node, check if the left child is a leaf (has no children).\n\n- Sum up the values of all left leaves.\n\n- Ensure all nodes are checked, even if the immediate left child is not a leaf.",
        "complexity":   "Time Complexity: O(n) where n is the number of nodes in the tree, because every node is visited exactly once.\nSpace Complexity: O(n) in the worst case, due to recursion stack space if the tree is skewed.",
        "solution":     "We use a Depth-First Search (DFS) recursive approach. At each node, check if the left child exists and is a leaf. If so, add its value to the sum. Regardless, recursively call the function for both the left and right children (if applicable) to explore the entire tree. The base case for recursion is when the current node is null. This method ensures that all nodes are properly evaluated while maintaining clarity and simplicity.",
    },
    "binary-tree-paths": {
        "key_insights": "- Use a Depth-First Search (DFS) or backtracking approach to explore all paths from the root to leaves.\n\n- Build the path as you traverse the tree.\n\n- Once a leaf node is reached, append the constructed path to the result list.\n\n- The problem requires returning all paths in any order.",
        "complexity":   "Time Complexity: O(N), where N is the number of nodes in the tree, since every node is visited once.\nSpace Complexity: O(H), where H is the height of the tree, which is the space used on the recursion stack. In the worst case (skewed tree), H can be O(N).",
        "solution":     "We perform a DFS traversal starting from the root. At each node, we append the node's value to the current path. If the current node is a leaf (i.e. it does not have left or right children), we add the current path (constructed as a string separated by \"->\") to the result. Otherwise, we recursively process the left and right children. The recursion naturally handles backtracking since the current path is passed (or extended) at each recursive call.",
    },
    "count-complete-tree-nodes": {
        "key_insights": "- A complete binary tree has properties that allow us to calculate parts of the tree quickly.\n\n- Compute the depth of the left-most path and the right-most path:\n\n- If they are equal, the tree is perfect and the number of nodes is 2^depth - 1.\n\n- If not equal, recursively count nodes in the left and right subtrees.\n\n- This approach reduces the time complexity to O((log n)^2) versus a straightforward O(n) traversal.",
        "complexity":   "Time Complexity: O((log n)^2) – each level requires O(log n) comparisons and the height is O(log n).\n\nSpace Complexity: O(log n) – due to recursive stack if using recursion.",
        "solution":     "We use a recursive strategy:\n\n- Define two helper functions to determine the depth along the left-most and right-most branches.\n\n- Compare these depths:\n\n- If equal, the tree is a perfect binary tree and the number of nodes can be calculated using the formula 2^depth - 1.\n\n- If not equal, the tree is not fully perfect, so we recursively count the nodes in the left and right subtrees and add one for the root.\n\n- This algorithm uses the properties of a complete binary tree to avoid traversing every node, achieving a faster solution.\n\nData Structures and Algorithms:\n\n- Binary tree traversal (depth-first search style).\n\n- Recursion.\n\n- Bit manipulation (via power of two computation when the tree is perfect).\n\nKey trick: Quickly determine subtree perfection by comparing leftmost and rightmost depths.",
    },
    "longest-univalue-path": {
        "key_insights": "- Use Depth-First Search (DFS) to traverse the tree in postorder.\n\n- For each node, compute the longest path with the same value that starts from its left child and right child.\n\n- Extend the path if the child node's value equals the current node's value.\n\n- Combine the left and right paths at the current node to consider the situation where the path goes through the node.\n\n- Maintain a global variable to track the maximum path length found.",
        "complexity":   "Time Complexity: O(n), where n is the number of nodes in the tree.\nSpace Complexity: O(n) in the worst case due to the recursion stack.",
        "solution":     "Perform a DFS traversal of the tree. At each node, calculate the longest univalue path from its left and right children that can be connected with the current node (if the child's value is the same as the current node's value). Update a global maximum using the sum of the left and right potential paths at each node. Return the maximum edge count obtained amongst all nodes.",
    },
    "maximum-difference-between-node-and-ancestor": {
        "key_insights": "- Use Depth-First Search (DFS) to traverse the tree.\n\n- Track the minimum and maximum values encountered along the current path from the root.\n\n- At each node, update the global maximum difference using the current node’s value and the tracked minimum and maximum.\n\n- The solution leverages recursion which implicitly uses the call stack to manage state.",
        "complexity":   "Time Complexity: O(n), where n is the number of nodes in the tree.\nSpace Complexity: O(h), where h is the height of the tree. In the worst case (skewed tree) this could be O(n).",
        "solution":     "The problem can be solved using a DFS traversal that passes along two values: the current minimum and maximum encountered on the path from the root to the current node. At each node, compute the potential differences from the current node’s value with both the minimum and maximum. Update the overall maximum difference if these values yield a larger difference. Then, update the current path’s minimum and maximum values, and recursively traverse the left and right children. When the traversal backtracks, the previously computed values guarantee the differences are correctly calculated for all ancestor-descendant relations.",
    },
    "construct-binary-tree-from-inorder-and-postorder-traversal": {
        "key_insights": "- The last element of the postorder array is always the root.\n\n- Finding the index of the root in the inorder array splits the tree into left and right subtrees.\n\n- Recursively reconstruct the tree using the divided inorder arrays and corresponding segments of the postorder array.\n\n- Use a hash table (or dictionary) to quickly locate the index of any value in the inorder array.",
        "complexity":   "Time Complexity: O(n)\n\nSpace Complexity: O(n) (Space is used for the recursive call stack and extra hash table)",
        "solution":     "We can solve the problem by leveraging the observation that in postorder traversal, the last element is the root of the current subtree. After identifying the root, we locate its index in the inorder array which partitions the array into left and right subtrees. A hash table is used for fast lookup of the root index in the inorder sequence. We then recursively apply the same process for left and right subtrees, carefully managing indices to maintain alignment between the inorder and postorder arrays.",
    },
    "binary-tree-inorder-traversal": {
        "key_insights": "- Inorder traversal pattern: left subtree → node → right subtree.\n\n- An iterative solution uses a stack to simulate the recursive process.\n\n- Continuously move to the leftmost node and store nodes in the stack.\n\n- After reaching a null, backtrack by popping from the stack, process the node, and then explore its right subtree.\n\n- Handle edge cases such as an empty tree.",
        "complexity":   "Time Complexity: O(n) - Every node is visited once.\nSpace Complexity: O(n) - In the worst-case scenario (skewed tree), the stack may store all nodes.",
        "solution":     "The problem can be solved using an iterative approach that simulates the behavior of recursion with a stack. Start with the root node, then push each left child onto the stack until reaching a null node. Once you hit null, pop the last node from the stack, process it, and then move to its right child. Repeat this process until there are no nodes left to process. This method ensures that nodes are processed in their correct inorder sequence.",
    },
    "minimum-distance-between-bst-nodes": {
        "key_insights": "- The in-order traversal of a BST produces a sorted list of node values.\n\n- The minimum difference will be found between two consecutive values in this sorted sequence.\n\n- Use a variable to track the previous node value during traversal.\n\n- Update the minimum difference as you visit each node.",
        "complexity":   "Time Complexity: O(n), where n is the number of nodes in the BST, since every node is visited once.\nSpace Complexity: O(n) in the worst-case scenario due to recursion stack (or O(h) where h is the tree height for balanced trees).",
        "solution":     "The solution uses an in-order traversal of the BST to get the nodes in a sorted order. As we traverse the tree, we maintain a variable to store the previous node's value. The absolute difference between the current node's value and the previous node's value is computed, and if it is smaller than the current minimum difference, it is updated. This approach leverages the BST's inherent characteristics and ensures that the minimum difference is found efficiently.",
    },
    "minimum-absolute-difference-in-bst": {
        "key_insights": "- Inorder traversal of a BST yields the node values in sorted order.\n\n- The minimum absolute difference will always be between two consecutive values in this sorted order.\n\n- A single traversal (inorder) is sufficient to compute the answer by comparing the current node’s value with the previous node’s value.\n\n- Using an extra variable to store the previous node’s value avoids the need for extra space to store the sorted list.",
        "complexity":   "Time Complexity: O(n), where n is the number of nodes in the BST.\nSpace Complexity: O(n) in the worst-case for the call stack (tree height) for a skewed tree; O(log n) on average for a balanced tree.",
        "solution":     "The solution performs an inorder traversal of the BST to retrieve the node values in sorted order. During the traversal, it keeps track of the previously visited node’s value. At each node, the difference between the current node’s value and the previous node’s value is computed, and the minimum difference is updated accordingly. This method leverages the BST’s inherent property of ordered traversal to efficiently calculate the desired result without additional data structures.",
    },
    "balance-a-binary-search-tree": {
        "key_insights": "- Perform an in-order traversal to obtain a sorted list of node values.\n\n- Use the sorted list to reconstruct the BST by choosing the middle element as the root, ensuring balanced subtrees.\n\n- Recursively build left and right subtrees from the corresponding halves of the array.\n\n- Leveraging the BST's in-order property guarantees a sorted array, making the reconstruction straightforward.",
        "complexity":   "Time Complexity: O(n), where n is the total number of nodes, due to the in-order traversal and balanced BST construction.\nSpace Complexity: O(n), for storing node values in an array and the recursion stack.",
        "solution":     "The problem is solved in two primary steps. First, perform an in-order traversal of the BST and store the node values in a sorted list. Then, build a balanced BST from this sorted list by recursively picking the middle element as the root. This ensures that the tree remains balanced because the heights of the left and right subtrees are kept as equal as possible.",
    },
    "binary-tree-postorder-traversal": {
        "key_insights": "- Use an iterative approach to avoid recursion.\n\n- A two-stack strategy can reverse the order of node processing to simulate postorder traversal.\n\n- One stack can be used too, though the two-stack method is intuitive: first stack for processing nodes, second stack to reverse the order.\n\n- Edge case: if the tree is empty, return an empty list.",
        "complexity":   "Time Complexity: O(n) – Each node is processed once.\nSpace Complexity: O(n) – In worst case, both stacks can contain up to all the nodes in the tree.",
        "solution":     "The solution uses a two-stack approach. The first stack is used for traversing the tree in a modified pre-order (root, right, left) such that when nodes are transferred into the second stack, they will be in the postorder (left, right, root) order. Finally, the values in the second stack are collected and returned as the result.",
    },
    "sum-root-to-leaf-numbers": {
        "key_insights": "- Utilize a Depth-First Search (DFS) or recursion strategy to traverse the tree.\n\n- Carry forward a running value that represents the number formed from the root to the current node.\n\n- When a leaf node is reached, the currently formed number is complete and can be added to the total sum.\n\n- Use the mathematical transformation current_value * 10 + node.val to update the number along the path.",
        "complexity":   "Time Complexity: O(n) where n is the number of nodes in the tree.\nSpace Complexity: O(h) where h is the height of the tree because of the recursion call stack.",
        "solution":     "The problem can be solved using recursive DFS. Start at the root with an initial value of 0. For each node, update the accumulated number by multiplying the current sum by 10 and adding the node's digit. When a leaf is encountered (a node with no children), add the current number to the total sum. Finally, return the sum of numbers formed from all root-to-leaf paths. This approach guarantees that every node is visited exactly once and uses only the call stack proportional to the height of the tree.",
    },
    "delete-nodes-and-return-forest": {
        "key_insights": "- Use a recursive depth-first search (DFS) to traverse the tree.\n\n- At each node, determine if it should be deleted based on the to_delete set.\n\n- When deleting a node, ensure that its non-deleted children are added as new roots in the forest.\n\n- Postorder traversal (processing children before the current node) is beneficial so that children are processed before potentially deleting the parent.\n\n- Using a set for to_delete items allows for fast lookup.",
        "complexity":   "Time Complexity: O(N), where N is the number of nodes since each node is visited once.\nSpace Complexity: O(N) for the recursion stack and the set used to store deletion values and resulting forest.",
        "solution":     "We employ a DFS approach to traverse the tree in a postorder manner, processing left and right children first. For each node:\n\n- Check if the node should be deleted by looking it up in the to_delete set.\n\n- If the node is flagged for deletion, add its non-null children as potential new roots to the forest.\n\n- Return null for a deleted node and the node itself if it is kept.\nThis recursive process ensures that any subtree whose root gets deleted properly adds any potential subtrees to the answer list before the node is removed.",
    },
    "find-duplicate-subtrees": {
        "key_insights": "- Use a postorder traversal (DFS) so that each subtree is serialized after processing its children.\n\n- Represent each subtree uniquely (e.g., as a string serialization) to detect duplicates.\n\n- Use a hash table (dictionary) to count the occurrence of each serialized subtree.\n\n- When a serialized subtree's count becomes two, add its root node to the result list (to ensure each duplicate subtree is reported only once).",
        "complexity":   "Time Complexity: O(n), where n is the number of nodes in the tree. (In the worst-case scenario, serialization of each subtree might lead to higher complexity, but typically each node is processed once.)\nSpace Complexity: O(n), for storing the serialization of each subtree and the hash table.",
        "solution":     "The solution adopts a DFS (postorder traversal) approach where each subtree is serialized into a string based on its node value and the serializations of its left and right children. This unique serialization acts as a key in a hash table that records how many times a particular subtree has been seen. When a subtree's serialization appears for the second time, we record its root in the result array. This method ensures that we only return one instance for each group of duplicate subtrees.",
    },
    "flatten-binary-tree-to-linked-list": {
        "key_insights": "- The goal is to rearrange the tree without using extra space for new nodes or data structures.\n\n- A pre-order traversal order must be maintained, meaning the current node is processed before its left and right children.\n\n- One in-place solution involves iterating over nodes, finding the rightmost node in the left subtree (if it exists), attaching the original right subtree to this node, then moving the left subtree to the right.\n\n- Using an iterative approach can achieve O(1) extra space, while a recursive solution may use stack space proportional to the tree height.",
        "complexity":   "Time Complexity: O(n) where n is the number of nodes (each node is processed a constant number of times).\n\nSpace Complexity: O(1) extra space in the iterative solution (ignoring recursion stack in the recursive approach).",
        "solution":     "We use an iterative approach to flatten the tree. The idea is to start from the root and for each node:\n\n- If a left child exists, locate the rightmost node of the left subtree.\n\n- Attach the node’s existing right subtree to the right pointer of this rightmost node.\n\n- Move the left subtree to the right pointer of the current node and set the left pointer to null.\n\n- Move to the next node (which is now the right child) and repeat until all nodes have been processed.\n\nThis approach effectively re-links all nodes following a pre-order traversal and avoids the overhead of extra space (aside from loop variables).",
    },
    "distribute-coins-in-binary-tree": {
        "key_insights": "- Each node’s balance is defined as (number of coins at the node) - 1, which indicates the surplus (if positive) or deficit (if negative).\n\n- Use a Depth-First Search (DFS) traversal (post-order) to process children before handling the parent.\n\n- At each node, the number of moves required is the sum of the absolute balances from its left and right subtrees.\n\n- The DFS returns the net balance for each subtree so that these balances can be accumulated at the parent.\n\n- The total moves required is the sum of the absolute values of the surplus/deficits transferred across each edge.",
        "complexity":   "Time Complexity: O(n) since each node is processed once.\nSpace Complexity: O(h), where h is the height of the tree (due to recursion stack).",
        "solution":     "We solve the problem by performing a post-order DFS traversal on the tree. At every node, compute the net coin balance (node.val + left balance + right balance - 1). The moves needed at that node are the absolute values of the left and right balances, reflecting the coins that must be moved into or out of that subtree. Accumulate these moves during the traversal. Finally, the sum of all moves gives the minimum number of moves required to achieve the desired coin distribution.",
    },
    "step-by-step-directions-from-a-binary-tree-node-to-another": {
        "key_insights": "- Find the path from the root to the start node and the root to the destination node using DFS.\n\n- Identify the last common node (i.e. the lowest common ancestor, LCA) by comparing the two paths.\n\n- The moves from the start node to the LCA are represented by ‘U’s.\n\n- The remaining part of the destination path (from the LCA to the destination node) provides the directions (‘L’ or ‘R’).\n\n- Concatenate the appropriate number of ‘U’s with the unique portion of the destination path to obtain the final direction string.",
        "complexity":   "Time Complexity: O(n) – Traversing the tree using DFS and comparing paths takes linear time.\nSpace Complexity: O(n) – In the worst-case scenario, the recursion stack and path storage will use linear space.",
        "solution":     "We solve the problem by:\n\n- Using DFS to obtain the path from the root to the start node and from the root to the destination node. During DFS, we accumulate the direction taken (either ‘L’ or ‘R’).\n\n- Comparing the two generated paths from the beginning to determine where they diverge. This divergence point corresponds to the lowest common ancestor (LCA).\n\n- For the portion of the start path after the common part, each step is replaced by a ‘U’ (to move upward from the start node to the LCA).\n\n- After reaching the LCA, append the remaining steps from the destination path to reach the destination node.\n\n- Finally, return the concatenated instruction string.\n\nThe key data structures used are:\n\n- Vectors/arrays (or lists) to store the path for each target node.\n\n- Recursive DFS for tree traversal.\n\nThis approach ensures that we correctly translate the downward path information into the required “upward then downward” directions.",
    },
    "search-in-a-binary-search-tree": {
        "key_insights": "- Utilize the BST property: for any node, all values in the left subtree are smaller and all values in the right subtree are larger.\n\n- This property allows efficient searching by choosing the correct subtree to continue the search.\n\n- The problem can be recursively or iteratively solved.\n\n- Recursion typically leads to a cleaner, more intuitive implementation.",
        "complexity":   "Time Complexity: O(h), where h is the height of the tree.\n\nSpace Complexity: O(h) due to recursion stack in the recursive approach, or O(1) if an iterative approach is used.",
        "solution":     "We traverse the BST starting at the root. For each node, we compare the target value with the current node's value:\n\n- If the values match, we return the current node.\n\n- If the target value is less, we recursively (or iteratively) search the left subtree.\n\n- If the target value is greater, we proceed to search the right subtree.\nBy making use of the BST properties, we avoid checking every node, which makes the approach efficient.",
    },
    "two-sum-iv-input-is-a-bst": {
        "key_insights": "- Utilize the BST property to traverse the tree while checking for the complementary value (k - current node's value).\n\n- A hash set can be used to store seen node values which facilitates constant time lookups.\n\n- Alternatively, an in-order traversal converts the BST into a sorted array, allowing a two-pointer approach.\n\n- The recursive DFS solution is both intuitive and easy to implement for this problem.",
        "complexity":   "Time Complexity: O(n) where n is the number of nodes, since each node is visited once.\nSpace Complexity: O(n) in the worst case due to the extra space used by the hash set (or recursion stack in the worst-case scenario of an unbalanced tree).",
        "solution":     "The solution uses a depth-first search (DFS) traversal while maintaining a hash set to remember the values of visited nodes. For each node, check if k minus the node's value exists in the hash set. If it does, return true. Otherwise, add the node's value to the set and continue the DFS on both the left and right children. This approach efficiently finds a pair of nodes that sum up to k without needing to traverse the tree more than once.",
    },
    "range-sum-of-bst": {
        "key_insights": "- Utilize the BST property to prune unnecessary branches (i.e., if a node's value is less than low, then its left subtree can be skipped; if greater than high, its right subtree can be skipped).\n\n- A Depth-First Search (DFS) traversal (recursive or iterative) is appropriate to visit only the relevant nodes.\n\n- Keep a running sum as you traverse nodes that fall within the range [low, high].",
        "complexity":   "Time Complexity: O(n) in the worst-case scenario where every node is visited.\n\nSpace Complexity: O(n) in the worst-case due to recursive call stack or if an iterative approach uses a stack, especially in the case of a skewed tree.",
        "solution":     "We solve the problem using DFS traversal (recursion) leveraging the BST properties. At each node:\n\n- If the node is null, return 0.\n\n- If the node's value is within the range [low, high], add it to the sum.\n\n- If the node's value is greater than low, explore the left subtree because it might contain nodes in range.\n\n- If the node's value is less than high, explore the right subtree for potential nodes in range.\nThis algorithm reduces unnecessary traversals by pruning off subtrees that cannot contribute to the result.",
    },
    "inorder-successor-in-bst-ii": {
        "key_insights": "- If the given node has a right child, the successor is the leftmost node in the right subtree.\n\n- If the node does not have a right child, move up the tree using the parent pointer until you find a node that is a left child of its parent. That parent node is the successor.\n\n- If no suitable parent is found, the given node is the largest element and its successor is null.",
        "complexity":   "Time Complexity: O(h), where h is the height of the tree.\nSpace Complexity: O(1) using a constant amount of extra space.",
        "solution":     "We leverage the properties of a BST along with parent pointers. The algorithm first checks if the node has a right subtree. If yes, it finds the leftmost node in that subtree which is the in-order successor. If there is no right subtree, we traverse up using parent pointers until we find a node that is the left child of its parent, which indicates that the parent's value is the next greater element in the in-order traversal.",
    },
    "insert-into-a-binary-search-tree": {
        "key_insights": "- A BST has the property that for any node, all the nodes in its left subtree are smaller and all the nodes in its right subtree are larger.\n\n- Insertion into a BST involves following the BST property and finding the appropriate null location to attach the new node.\n\n- The process can be implemented recursively or iteratively.\n\n- The problem guarantees uniqueness so no duplicate checks are needed.",
        "complexity":   "Time Complexity: O(h) where h is the height of the BST (in the worst case, for a skewed tree, O(n)).\nSpace Complexity: O(h) for the recursion stack in the recursive approach (or O(1) with an iterative solution).",
        "solution":     "We use the BST property to insert the new node. Starting from the root, if the value to insert is less than the current node's value, we proceed to the left subtree; otherwise, we proceed to the right subtree. This continues until we find a null pointer where we can insert the new value as a new node.\nKey steps:\n\n- Check if the current node is null; if yes, create a new node with the given value.\n\n- Recurse (or iterate) down the tree until the correct insertion point is found.\n\n- Update the left or right pointers accordingly.\nThis approach uses recursion for clarity, though an iterative implementation is also straightforward.",
    },
    "delete-node-in-a-bst": {
        "key_insights": "- If the key is not found in the BST, return the original tree.\n\n- There are three deletion cases:\n\n- The node is a leaf (no children).\n\n- The node has one child (left or right).\n\n- The node has two children.\n\n- For a node with two children, find the inorder successor (smallest node in the right subtree) or the inorder predecessor (largest node in the left subtree) to replace the node's value.\n\n- Recursively adjust the tree to preserve the BST invariants.",
        "complexity":   "Time Complexity: O(h), where h is the height of the tree (in the worst-case O(n) for a skewed tree).\n\nSpace Complexity: O(h), due to the recursion stack.",
        "solution":     "We use a recursive approach to traverse the tree to find the node with the given key. When the node is found:\n\n- If it has zero or one child, return the non-null child or null.\n\n- If it has two children, find the inorder successor (the smallest node in the right subtree), copy its value to the current node, and then delete the successor recursively.\nThis ensures that the BST property is maintained after deletion.",
    },
    "trim-a-binary-search-tree": {
        "key_insights": "- If a node's value is less than low, then its left subtree will also be less than low due to the BST property and can be discarded.\n\n- If a node's value is greater than high, then its right subtree can be discarded.\n\n- Use depth-first search (DFS) recursively to trim both the left and right subtrees.\n\n- Maintain the original tree structure for the retained nodes.",
        "complexity":   "Time Complexity: O(n), where n is the number of nodes, since each node is visited once.\nSpace Complexity: O(n) in the worst-case for the recursion call stack (O(log n) on average for a balanced tree).",
        "solution":     "The solution employs a recursive DFS approach on the binary search tree. For each node, we:\n\n- Check if the current node is null; if it is, return null.\n\n- If the node's value is smaller than low, move to its right subtree (since all nodes in its left subtree are automatically out of range).\n\n- If the node's value is greater than high, move to its left subtree.\n\n- Otherwise, recursively trim the left and right subtrees.\nThis approach takes advantage of the BST properties to efficiently discard entire subtrees that are out of the allowed range without examining every node in those subtrees.",
    },
    "recover-binary-search-tree": {
        "key_insights": "- In a valid BST, an in-order traversal produces a sorted sequence.\n\n- A swapped node pair causes a deviation from the sorted order. There can be one or two such violations.\n\n- By tracking the previous node during an in-order traversal, we can identify nodes where the value order is incorrect.\n\n- After finding the two nodes, swap their values to fix the tree.\n\n- While the straightforward recursive in-order traversal uses O(n) space (due to the recursion stack), the Morris traversal technique can achieve O(1) space.",
        "complexity":   "Time Complexity: O(n)\n\nSpace Complexity: O(n) in the recursive approach (or O(1) using Morris Traversal)",
        "solution":     "The solution involves performing an in-order traversal of the BST to detect the two nodes that have been swapped. During the traversal, we maintain a pointer to the previously visited node. When we encounter a node that is less than its previous node, we record the two nodes that violate the BST invariant. In cases where the swapped nodes are not adjacent, we will see two such violations; otherwise, we see only one. Finally, we swap the values of the two incorrect nodes, thereby restoring the BST.",
    },
    "convert-binary-search-tree-to-sorted-doubly-linked-list": {
        "key_insights": "- Use an in-order traversal to process nodes in sorted order.\n\n- Maintain pointers to connect the current node with the previously visited node.\n\n- After traversal, connect the head and tail of the list to make it circular.\n\n- The in-place constraint means no new nodes (or extra data structures for node storage) are created.",
        "complexity":   "Time Complexity: O(n) – Each node is visited once during the in-order traversal.\nSpace Complexity: O(n) – Due to the recursion stack in the case of an unbalanced tree. For a balanced tree, it can be O(log n).",
        "solution":     "We solve the problem using an in-order depth-first search (DFS) traversal to ensure nodes are accessed in sorted order. While traversing:\n\n- Use a helper recursive function to perform the in-order traversal.\n\n- Maintain a reference to the previously processed node (prev) and to the head of the list.\n\n- For each node, connect it with the previous node by setting prev.right to the current node and current node.left to prev.\n\n- Once traversal is complete, connect the head and tail (last node) to make the list circular.\nThe algorithm uses recursion, and the key trick is to update the pointers while unwinding the recursion.",
    },
    "construct-binary-search-tree-from-preorder-traversal": {
        "key_insights": "- The preorder sequence always starts with the root of the tree.\n\n- For any node, elements in the left subtree must be less than the node’s value, while elements in the right subtree must be greater.\n\n- Use recursion with bounds (or an iterative approach with a stack) to determine where a new value fits in the BST.\n\n- Maintain an index into the preorder array to build the tree sequentially.",
        "complexity":   "Time Complexity: O(n) where n is the number of nodes since each node is processed exactly once.\nSpace Complexity: O(n) in the worst case due to the recursion call stack (for a skewed tree).",
        "solution":     "We can solve this problem using a recursive approach. The recursive function will have lower and upper bounds which restrict the valid range for node values at each stage. Starting with the entire range (-∞, +∞), we process each value in the preorder list in order. If the next value lies within the bounds, it is accepted as a node. We then recursively construct the left subtree with an updated upper bound (current node's value) and the right subtree with an updated lower bound (current node's value). This ensures that the BST property is maintained throughout.\n\nThe key data structure used is the recursion call stack, which inherently manages the bounds for a valid BST construction. The algorithm processes each element of the preorder sequence exactly once, leading to optimal time complexity.",
    },
    "my-calendar-i": {
        "key_insights": "- Use a list (or array) to store currently booked time intervals.\n\n- An event [start, end) does not conflict with another if its end time is less than or equal to the other's start time or its start time is greater than or equal to the other's end time.\n\n- With at most 1000 booking attempts, a simple linear scan for overlapping intervals is acceptable.\n\n- Optionally, for efficiency or scalability, maintain the intervals in a sorted order and use binary search, checking only immediate neighbors for overlaps.",
        "complexity":   "Time Complexity: O(n) per booking in the worst-case scenario, where n is the number of existing events.\n\nSpace Complexity: O(n), as all events are stored.",
        "solution":     "The solution maintains a list of tuples (or pairs) representing booked events. When booking a new event, iterate through the list to check if the new interval overlaps with any already booked interval. The overlap condition is evaluated by ensuring that for every booked interval [s, e), the new event [start, end) satisfies (end &#x3C;= s or start >= e). If it does, append the new event and return true. If there is any overlap, return false without adding the event.",
    },
    "my-calendar-ii": {
        "key_insights": "- Maintain two separate collections: one for all booked events and one for the periods that are already double booked.\n\n- When booking a new event, first check if it conflicts with any of the double booked intervals. If it does, adding the event would create a triple booking.\n\n- Otherwise, for every existing event, compute and record any new overlap created by the new event with that event. These overlaps form the new double booking intervals.\n\n- Finally, add the new event to the collection of all bookings.",
        "complexity":   "Time Complexity: O(n) per booking call in the worst case, where n is the number of events booked so far.\nSpace Complexity: O(n) to store all the bookings and overlaps.",
        "solution":     "We use a two-list approach:\n\n- A list (or array) to record all booked events.\n\n- A list (or array) to record all intervals where double bookings have occurred.\n\nFor each new event:\n\n- First check the new event interval against all intervals in the second list (overlaps). If any overlap exists, then a triple booking would occur, and we return false.\n\n- Otherwise, for each event already booked, if there is an overlap with the new event, compute that overlapping interval and add it to the overlaps list.\n\n- Finally, add the new event to the booked events list and return true.\n\nThis approach leverages the idea of interval intersection and simple linear scans over the events. The challenge lies in correctly computing the overlapping intervals and ensuring that triple overlaps are not introduced.",
    },
    "stock-price-fluctuation": {
        "key_insights": "- Use a hash table (or dictionary) to map each timestamp to its latest price.\n\n- Maintain two heaps (a max heap and a min heap) to quickly retrieve the maximum and minimum prices.\n\n- Since updates can change historical prices, use lazy deletion in the heaps: when the top element does not match the current price in the hash table, repeatedly pop it.\n\n- Track the latest timestamp separately to efficiently return the current/latest price.",
        "complexity":   "Time Complexity:\n\n- update: O(log n) due to pushing into two heaps.\n\n- current: O(1).\n\n- maximum/minimum: O(log n) in the worst-case (when cleaning outdated heap entries).\n\nSpace Complexity:\n\n- O(n) to store the hash table and both heaps.",
        "solution":     "We use a hash table (or dictionary) to store the mapping from timestamp to price. For maximum and minimum queries, we use a max heap (storing negative prices for a max-oriented comparison) and a min heap. When updating a timestamp, we update the hash table and push the new (price, timestamp) pair into both heaps. For queries, we repeatedly check the top of the heap and compare with the hash table – if it is outdated (i.e. the hash table’s price for the timestamp does not match the heap entry), we pop the invalid entry until we find a valid one. We keep track of the latest timestamp in a variable so that the current price query is efficient.",
    },
    "search-suggestions-system": {
        "key_insights": "- Sorting the products lexicographically simplifies finding candidates.\n\n- Using binary search helps efficiently locate the starting index for each prefix.\n\n- Limiting suggestions to at most three candidates for every prefix ensures optimal performance in both time and space.\n\n- Alternative methods include Trie-based search which can be efficient for prefix queries, but binary search after sorting is straightforward given the problem constraints.",
        "complexity":   "Time Complexity: O(n log n + m log n) where n is the number of products and m is the length of searchWord. Sorting takes O(n log n) and for each of the m prefixes, binary search takes O(log n).\nSpace Complexity: O(m + k) where m is the output size (list of lists for each prefix) and k is the additional space used by the sorting algorithm (which can be O(n) in worst-case depending on implementation).",
        "solution":     "The approach starts by sorting the input products lexicographically. For each prefix of searchWord, use binary search to quickly find the first product that could match the current prefix. Then, iterate through at most three products from that position and, if they share the prefix, add them to the suggestions for that stage. This process is repeated for every prefix in searchWord. This method leverages efficient sorting and binary search to meet the problem's requirements while ensuring the suggestions are lexicographically minimal.",
    },
    "replace-words": {
        "key_insights": "- A Trie (prefix tree) is an ideal data structure to quickly find the shortest prefix match.\n\n- Insert all dictionary words into the Trie.\n\n- For each word in the sentence, traverse the Trie character by character until a complete root is found or no further matching branch exists.\n\n- Using a space split for the sentence enables individual word processing.",
        "complexity":   "Time Complexity: O(m + n * l) where m is the total number of characters in all dictionary roots, n is the number of words in the sentence, and l is the average length of a word.\nSpace Complexity: O(m) for storing the Trie.",
        "solution":     "We insert each root from the dictionary into a Trie. For each word in the sentence, we iterate character by character checking for a matching prefix. Once we find a node that marks the end of a dictionary word (a valid root), we replace the current word with that root. If no such prefix exists for a word, we leave it unchanged. This approach optimally leverages the Trie to quickly determine prefix matches and minimizes unnecessary comparisons.",
    },
    "maximum-xor-of-two-numbers-in-an-array": {
        "key_insights": "- The XOR operation is bitwise, so the maximum result is primarily influenced by the higher-order bits.\n\n- A Trie (prefix tree) can be used to store the binary representation of the numbers enabling efficient bit-by-bit comparison.\n\n- By inserting numbers into the Trie and then checking for the complement bit (i.e., if bit is 0 look for 1 and vice versa), one can greedily maximize the XOR at each bit-level.\n\n- The solution runs in O(n * L) time, where L is the number of bits (typically 32 for standard integers).",
        "complexity":   "Time Complexity: O(n * L) where L = 32, so essentially O(n).\n\nSpace Complexity: O(n * L) in the worst-case scenario for storing nodes in the Trie.",
        "solution":     "We use a Trie data structure to store the binary representations of the numbers in the array. For each number, we traverse the Trie trying to take the opposite branch (complement bit) at each level to maximize the resulting XOR. We then update the maximum XOR seen so far. This approach efficiently checks for the best possible XOR pair for every element in nums.",
    },
    "remove-sub-folders-from-the-filesystem": {
        "key_insights": "- Sorting the folders lexicographically guarantees that any parent folder comes before its sub-folders.\n\n- While iterating through the sorted list, only add a folder to the result if it is not a sub-folder of the previously added folder.\n\n- Checking if a folder is a sub-folder is done by confirming whether the folder string starts with the previous folder string plus a '/'.",
        "complexity":   "Time Complexity: O(n log n * L) where n is the number of folders and L is the average length of the folder strings (sorting cost and prefix checking).\nSpace Complexity: O(n) for storing the result list.",
        "solution":     "The approach starts by sorting the folder list lexicographically so that parent folders come before any of their sub-folders. Then, iterate through each folder in the sorted list. For each folder, check if it is a sub-folder of the last folder added to the result. This is done by verifying that the folder does not start with the previous folder path followed by a '/'. If it does not, add the folder to the result. This method avoids unnecessary comparisons and efficiently filters out sub-folders.",
    },
    "find-the-length-of-the-longest-common-prefix": {
        "key_insights": "- Convert each integer into its string representation to easily extract and compare prefixes.\n\n- For any integer, you can derive all possible prefixes (e.g., \"12345\" gives \"1\", \"12\", \"123\", \"1234\", \"12345\").\n\n- Instead of comparing every pair (which would be inefficient), generate a set of all prefixes for arr1 and another set for arr2, then find the intersection of these sets.\n\n- The result is the maximum length among all common prefixes.",
        "complexity":   "Time Complexity: O((n + m) * d), where n and m are the lengths of arr1 and arr2 respectively, and d is the maximum number of digits (at most 9).\n\nSpace Complexity: O((n + m) * d) for storing all prefixes in sets.",
        "solution":     "The approach involves converting the numbers in each array into strings and generating all possible prefixes for each number. We then store these prefixes in two sets (one for each array) and compute the intersection to obtain the common prefixes. The length of the longest prefix in this intersection is the answer. This method avoids checking every pair individually and leverages set operations for efficiency.",
    },
    "words-within-two-edits-of-dictionary": {
        "key_insights": "- For each query word, check every dictionary word to count the number of differing positions.\n\n- A query word is valid if there exists at least one dictionary word with at most two differing characters.\n\n- The problem can be solved using a brute-force approach since the maximum size of queries and dictionary, along with word length, is small.\n\n- Early stopping during letter comparisons can improve performance by breaking out as soon as the edit count exceeds two.",
        "complexity":   "Time Complexity: O(q * d * n), where q is the number of queries, d is the number of dictionary words, and n is the length of each word.\nSpace Complexity: O(1) additional space (excluding the space for the output list).",
        "solution":     "The approach involves iterating through each word in the queries array. For each query word, we iterate through every word in the dictionary and compare them character by character to count the differences. If the number of differences is two or fewer for any dictionary word, the query word is added to the result list. By using early termination in the character comparison loop, the algorithm improves efficiency when a word exceeds the allowed edit threshold. This brute-force method is acceptable given the constraints.",
    },
    "stream-of-characters": {
        "key_insights": "- Instead of checking all possible suffixes against every word, we store words in a Trie data structure.\n\n- Since we are matching suffixes, it is efficient to insert the words in reversed order. This way, when a new character is queried, we traverse the stream backward and the Trie forward.\n\n- Limit the size of the stored stream to the length of the longest word.\n\n- The worst-case query cost is proportional to the maximum length of words.",
        "complexity":   "Time Complexity: O(M) per query, where M is the maximum length of the words.\nSpace Complexity: O(Total characters in all words) for the Trie plus O(M) for the stream buffer.",
        "solution":     "We construct a reverse Trie where each word from the list is stored in reverse order. With every query, append the new character to a stream buffer and traverse this list backwards while matching against the Trie. If at any point you hit a Trie node marked as the end of a word, return true. The stream buffer length is capped at the length of the longest word to ensure efficient memory usage. This method drastically reduces unnecessary comparisons and quickly identifies valid suffixes.",
    },
    "concatenated-words": {
        "key_insights": "- Sort the words by length so that smaller words, which can be used to build larger ones, are processed first.\n\n- Use dynamic programming to determine if a given word can be segmented into two or more words from a pre-built dictionary (set).\n\n- Ensure that when checking a word, you do not consider the word itself; only use previously seen (shorter) words.\n\n- Optimize by limiting the inner loop based on the maximum possible word length (given constraint is 30).",
        "complexity":   "Time Complexity: O(n * l^2) where n is the number of words and l is the average length of the words.\nSpace Complexity: O(n * l) for storing words and the DP array for each word.",
        "solution":     "The solution involves first sorting the list of words by their length. For each word, use a DP array where dp[i] indicates whether the substring word[0:i] can be constructed from words in a set of previously seen words. If a word can be segmented into at least two parts based on the dictionary, it qualifies as a concatenated word. After processing each word, add it to the set so that it can be used to form longer words. This approach effectively builds up from the smallest words to the largest.",
    },
    "word-squares": {
        "key_insights": "- Use backtracking to construct candidate word squares one row at a time.\n\n- For each new row, determine the required prefix (formed from the kth letters of previously chosen words) that the candidate word must match.\n\n- Precompute a mapping (or Trie) from prefixes to the list of words that share that prefix. This allows efficient lookup and pruning.\n\n- The problem leverages the fact that all words have the same length and the board is square.",
        "complexity":   "Time Complexity: In the worst-case, it can be exponential due to backtracking. However, using prefix lookups significantly prunes the search. With L as the word length and N as the number of words, the effective time complexity is approximately O(N * L * (number of candidates per prefix)^(L)).\nSpace Complexity: O(N * L) for the prefix mapping, plus O(L) for the recursion stack and building the current square.",
        "solution":     "This solution uses backtracking combined with a prefix mapping technique. First, we precompute a dictionary that maps every possible prefix to the list of words with that prefix. Then we start forming a word square row-by-row. At each recursive call, we derive the prefix for the next word based on the current square (by taking the ith character from each of the already chosen words where i is the current depth). We then only consider words that match this prefix. This drastically reduces the number of possibilities, making the backtracking efficient even for multiple words. The algorithm uses recursive DFS (depth-first search) and backtracking to explore valid combinations and build complete word squares.",
    },
    "furthest-building-you-can-reach": {
        "key_insights": "- Use a greedy approach to prioritize ladders for the largest jumps.\n\n- Use bricks for smaller height differences.\n\n- Maintain a min-heap (priority queue) to track the height differences where you chose to use bricks initially.\n\n- When the number of climbs exceeds the available ladders, the smallest jump (from the heap) is replaced by using bricks.\n\n- The simulation stops when the bricks run out before you can cover the required jump.",
        "complexity":   "Time Complexity: O(n log n) in the worst-case scenario due to the heap operations, where n is the number of buildings.\nSpace Complexity: O(n) for the worst-case heap storage.",
        "solution":     "We solve the problem by simulating the journey from the first building to the last. For each jump, if the next building is taller than the current one, we calculate the height difference. We push this difference into a min-heap. If the number of required climbs (i.e., the heap's size) exceeds the available ladders, we remove the smallest jump from the heap and use bricks to cover it instead. If at any point the bricks become insufficient (i.e., bricks drop below zero), we cannot progress further and return the current building index. This approach optimally allocates ladders to the largest jumps while using bricks for the smaller ones.",
    },
    "single-threaded-cpu": {
        "key_insights": "- Sort tasks by enqueueTime while preserving original indices.\n\n- Use a min-heap (priority queue) to efficiently choose the task with the shortest processing time.\n\n- If the heap is empty and tasks are yet to arrive, jump the current time to the next task's enqueueTime.\n\n- Process tasks until all tasks have been scheduled.",
        "complexity":   "Time Complexity: O(n log n) due to sorting and heap operations.\nSpace Complexity: O(n) for storing tasks and the heap.",
        "solution":     "We first augment each task with its original index and sort the tasks based on their enqueue times. We then use a min-heap to keep track of tasks that are available. At each step, we:\n\n- Add all tasks available at the current time to the heap.\n\n- If the heap is not empty, pop the task with the smallest processing time (using the original index as a tie breaker) and update the current time by its processing time.\n\n- If the heap is empty, advance the current time to the next task's enqueue time.\nThis process ensures that the CPU always processes the appropriate task and the order is maintained correctly.",
    },
    "process-tasks-using-servers": {
        "key_insights": "- Use a min-heap to track free servers sorted by weight and index.\n\n- Use another min-heap to manage busy servers, prioritized by when they become free.\n\n- Keep track of the current time, taking into account task arrival times.\n\n- If no server is free at the time of task arrival, fast-forward time to when the next server becomes available.\n\n- Always reassign servers from the busy heap to the free heap when they complete their tasks.",
        "complexity":   "Time Complexity: O(m log n), where m is the number of tasks and n is the number of servers, since each task assignment may require heap operations.\nSpace Complexity: O(n + m) primarily for the heaps and the result array.",
        "solution":     "We use two heaps:\n\n- Free Servers Heap: stores tuples (server weight, server index), sorted so that the server with the smallest weight (and then smallest index) is always at the top.\n\n- Busy Servers Heap: stores tuples (finish time, server weight, server index), ordered by finish time. When a server is busy processing a task, it resides in this heap.\nThe algorithm maintains a current time variable. For each task arriving at time j, time is updated to ensure it is at least j. Servers that become free on or before the current time are moved from the busy heap to the free heap. If no free server is available, the current time jumps to the finish time of the next server to be available. The task is then assigned to the free server at the top of the free heap, and the server's next free time is computed and added to the busy heap. This continues until all tasks are assigned.",
    },
    "maximum-performance-of-a-team": {
        "key_insights": "- Sort the engineers by efficiency in descending order so that when processing an engineer, their efficiency is the minimum for the current selection.\n\n- Use a min-heap to maintain the top speeds among the selected engineers. This helps in efficiently removing the engineer with the smallest speed when the team exceeds k members.\n\n- Keep a running sum of the speeds currently in the team.\n\n- At each iteration, calculate the potential performance as the current speed sum multiplied by the current engineer’s efficiency (which is the minimum efficiency for the team so far) and update the maximum performance if it is higher.",
        "complexity":   "Time Complexity: O(n log k)\n\nSpace Complexity: O(k)",
        "solution":     "The solution involves the following steps:\n\n- Pair each engineer’s speed and efficiency then sort these pairs in descending order based on efficiency.\n\n- Initialize a min-heap (or priority queue) to store speeds and a variable to keep the running sum of speeds.\n\n- Iterate through the sorted engineers. For each engineer:\n\n- Add the engineer’s speed to the min-heap and update the running sum.\n\n- If the size of the heap exceeds k, remove the smallest speed from the heap and subtract it from the running sum.\n\n- Compute the current performance as the running sum multiplied by the current engineer's efficiency.\n\n- Update the maximum performance if the current performance is greater.\n\n- Return the maximum performance modulo 10^9 + 7.\n\nUsing a min-heap ensures that when the team size exceeds k, the smallest speed can be removed in O(log k) time, keeping the overall complexity efficient.",
    },
    "longest-happy-string": {
        "key_insights": "- Use a greedy strategy to always pick the letter with the highest remaining count unless it would result in three consecutive identical letters.\n\n- Use a max-heap (or priority queue) to efficiently retrieve the character with the largest remaining frequency.\n\n- If the top element would cause an invalid sequence, select the next best option.\n\n- Continue this process until no valid moves remain.",
        "complexity":   "Time Complexity: O((a+b+c) * log 3) ≈ O(a+b+c) since the heap contains at most 3 elements.\n\nSpace Complexity: O(1) additional space, aside from the output string.",
        "solution":     "The solution utilizes a greedy algorithm combined with a max-heap (priority queue) to always choose the letter with the highest count. At each step, we check if appending the selected letter causes three consecutive occurrences. If it does, we attempt to use the next most frequent letter instead. This ensures that the longest possible string is constructed without violating the happy string constraints.",
    },
    "ipo": {
        "key_insights": "- Sort the projects by their required capital so that you can easily know which projects are feasible given current capital.\n\n- Use a max heap (priority queue) to always select the feasible project with the highest profit.\n\n- Iterate up to k times, each time adding all newly feasible projects (those whose capital requirement is at most the current capital) to the max heap.\n\n- Terminate early if there are no projects available in the heap.",
        "complexity":   "Time Complexity: O(n log n + k log n) where n is the number of projects.\nSpace Complexity: O(n) for storing project information and the heap.",
        "solution":     "The approach starts by pairing each project’s required capital with its profit, then sorting these pairs based on the capital requirement. For each of the k rounds, add all projects that are affordable (given the current capital) to a max heap that prioritizes profit. Then select the project with the maximum profit, update the available capital and continue. This greedy selection process ensures that the best immediate profit boost is always taken, eventually maximizing the total capital.",
    },
    "sliding-window-median": {
        "key_insights": "- Use the sliding window technique to sequentially add and remove elements.\n\n- Maintain two heaps:\n\n- A max heap (implemented via negatives) for the lower half of the data.\n\n- A min heap for the higher half of the data.\n\n- The two heaps allow efficient median retrieval in O(1) time.\n\n- Lazy removal and balancing of heaps are crucial to ensure the correctness when elements exit the window.",
        "complexity":   "Time Complexity: O(n log k) because each insertion and removal in the heaps takes O(log k).\nSpace Complexity: O(k) to maintain the two heaps.",
        "solution":     "We solve the problem using a DualHeap approach. We maintain two heaps to divide the sliding window into lower and upper halves. When a new element is added, it is placed into one of the heaps based on its value relative to the current median. When an element falls out of the window, we mark it for lazy removal in the appropriate heap. We then balance the heaps to ensure the sizes differ by at most one, allowing quick computation of the median.",
    },
    "summary-ranges": {
        "key_insights": "- Since the array is sorted and unique, we can simply iterate through it.\n\n- Track the start of a new range, and expand the range as long as the next number is consecutive.\n\n- When a break in consecutiveness is noticed, format the current range accordingly and start a new range.\n\n- Finalize the last range after exiting the loop.",
        "complexity":   "Time Complexity: O(n) where n is the length of the nums array.\nSpace Complexity: O(1) additional space (excluding the space for the output list).",
        "solution":     "We iterate through the array while maintaining a start pointer for the current range. For each element, check if the next element is exactly one greater. If not, the current range ends here. Format the range as \"start->end\" if there is more than one element in the range, otherwise just as \"start\". This algorithm leverages the sorted property of the array to accomplish the task in one pass using constant extra space.",
    },
    "meeting-scheduler": {
        "key_insights": "- Sort both time slot arrays by their start times to simplify the overlap checking process.\n\n- Use a two-pointer approach to traverse the sorted arrays concurrently.\n\n- For each pair of slots examined, compute the intersection by taking the maximum of the start times and the minimum of the end times.\n\n- Check if this overlapping duration is at least as long as the required meeting duration; if so, return the earliest valid meeting interval.\n\n- Advance the pointer corresponding to the slot that ends earlier to look for a potentially better overlapping slot further on.",
        "complexity":   "Time Complexity: O(n log n + m log m), where n and m are the number of slots in slots1 and slots2, respectively, due to sorting. The merging process itself runs in O(n + m).\nSpace Complexity: O(1) additional space (ignoring the space used for sorting if done in-place).",
        "solution":     "The solution involves first sorting both slots1 and slots2 by their start times. Then, using a two-pointer approach, iterate over both sorted lists simultaneously. For each pair of slots, calculate the possible overlapping time interval by taking max(slot1_start, slot2_start) as the start and min(slot1_end, slot2_end) as the end. If the duration of the overlap is at least as long as the required duration, the earliest feasible meeting time is found and we return the interval starting at this overlap start and ending at (overlap start + duration). If the overlap is insufficient, advance the pointer of the slot that ends first since that slot cannot provide a sufficient overlap with any future slots from the other list.",
    },
    "car-pooling": {
        "key_insights": "- Use a sweep-line algorithm by converting each trip into two events: one for picking up passengers (adding to the load) and one for dropping them off (subtracting from the load).\n\n- Sort the events by location. For events occurring at the same location, process drop-offs before pick-ups to free up capacity.\n\n- At each event point, update the current passenger count and verify it does not exceed the car’s capacity.\n\n- This simulation technique efficiently handles overlapping intervals with a difference array concept.",
        "complexity":   "Time Complexity: O(N log N) due to sorting the events, where N is the number of trips.\nSpace Complexity: O(N) for storing the events.",
        "solution":     "The idea is to transform each trip into two events. For each trip:\n\n- Create an event at the 'from' location to add passengers.\n\n- Create an event at the 'to' location to subtract passengers.\nSort these events by the location value. When events coincide at the same location, process the drop-off events (negative changes) before the pick-up events (positive changes) to ensure capacity is freed before being assigned.\nThen, iterate over the sorted events, update the cumulative number of passengers, and check against the available capacity. If at any point the current passenger count exceeds the capacity, return false. If the entire journey is simulated without breaching capacity, return true.",
    },
    "remove-covered-intervals": {
        "key_insights": "- Sort intervals by their start points in ascending order and by end points in descending order when starts are equal.\n\n- By sorting this way, if two intervals have the same start, the longer interval comes first, making it easier to detect coverage.\n\n- Keep track of the maximum end encountered so far.\n\n- When iterating, if the current interval's end is less than or equal to the tracked maximum end, it is covered by a previous interval.",
        "complexity":   "Time Complexity: O(n log n) due to sorting, with an additional O(n) for a single pass through the intervals.\nSpace Complexity: O(1) extra space if the sort is in-place.",
        "solution":     "The solution involves a greedy approach:\n\n- Sort the intervals by start coordinate in ascending order; if two intervals share the same start, sort them by their end coordinate in descending order.\n\n- Initialize a counter for the number of non-covered intervals and a variable to record the farthest end encountered.\n\n- Iterate through the sorted list. For each interval, if its end is greater than the maximum end seen so far, it is not covered and should be counted; update the maximum end.\n\n- Return the count.\n\nThis technique ensures each interval is processed only once after sorting, which yields an efficient and optimal solution.",
    },
    "minimum-number-of-arrows-to-burst-balloons": {
        "key_insights": "- Sort the balloon intervals by their ending coordinate.\n\n- Start by shooting an arrow at the end of the first interval.\n\n- Iterate through the sorted intervals and only shoot a new arrow if the next balloon's start is greater than the position where the last arrow was shot.\n\n- This greedy approach minimizes arrows by overlapping as many balloons as possible with one arrow.",
        "complexity":   "Time Complexity: O(n log n) due to sorting the intervals.\nSpace Complexity: O(1) additional space if sorting is done in-place (ignoring the input storage).",
        "solution":     "The solution uses a greedy algorithm combined with sorting. First, sort the array of balloon intervals based on their x_end values. Initialize the arrow count to 1 with the first arrow shot at the end of the first interval. Then, for each subsequent balloon, check if its x_start is greater than the x position of the last shot arrow. If so, shoot another arrow at the current balloon's end and update the arrow position. This ensures that each arrow covers the maximum number of overlapping balloons.",
    },
    "maximum-number-of-events-that-can-be-attended": {
        "key_insights": "- Sort events by their start day.\n\n- Use a min-heap (priority queue) to dynamically store events that have started but not yet ended.\n\n- On each day, add all events starting on that day to the heap.\n\n- Remove events from the heap that have already ended.\n\n- Always attend the event that ends the earliest (pop from the heap) to free up future days.\n\n- Iteratively move through each day until all events are processed.",
        "complexity":   "Time Complexity: O(n log n)\n\nSpace Complexity: O(n)",
        "solution":     "The algorithm starts by sorting the events by their start day. Then, using a min-heap, it processes each day from the earliest start day up to the latest possible day among the events. For each day, it adds all events that begin on that day into the min-heap. The heap is used to keep track of the current events by their end days. Events that have ended before the current day are removed from the heap. On each day, if there is any event in the heap, the one with the smallest end day (which gives the best chance for future days) is attended and removed from the heap. This greedy strategy maximizes the total number of events attended.",
    },
    "two-best-non-overlapping-events": {
        "key_insights": "- Sort events by start time. This allows for binary search to quickly find the next non-overlapping event.\n\n- Precompute a suffix maximum array where each position stores the maximum event value among all events starting from that index.\n\n- For each event, via binary search, determine the earliest event that can be attended after the current one, and combine the current event’s value with the best value from the remainder of the list.\n\n- Consider the case where taking a single event is optimal.",
        "complexity":   "Time Complexity: O(n log n) – O(n log n) for sorting and binary search over each event.\nSpace Complexity: O(n) – for the auxiliary arrays used (start times and suffix maximum values).",
        "solution":     "The algorithm sorts the events by their start time. Then, it builds an array that holds the maximum event value from any index to the end (suffix max). For each event, a binary search is used on the sorted start times to find the next event that can be attended (i.e., the event whose start time is at least the current event's end time plus one). The value of the current event is added to the precomputed best value from that next event, and the maximum sum is updated accordingly. This approach ensures that the best combination of two non-overlapping events is found while keeping complexity under control.",
    },
    "meeting-rooms-iii": {
        "key_insights": "- Use a min-heap to track currently available rooms by room number.\n\n- Use another min-heap to manage ongoing meetings, keyed by their finish times.\n\n- Process the meetings in order of their start times since they are unique.\n\n- When no room is available at the meeting’s start time, delay the meeting to the earliest finish time available and update its finish time by adding its original duration.\n\n- Count how many meetings are allocated per room and return the one with the maximum count (resolving ties by room number).",
        "complexity":   "Time Complexity: O(m log n) where m is the number of meetings, since each meeting may require heap operations on a heap of size at most n.\nSpace Complexity: O(m + n) due to storing meetings for sorting and maintaining the heaps (the busy heap is O(n) and the sort uses O(m)).",
        "solution":     "We simulate the meeting room allocation with two heaps:\n\n- Heap for available rooms (storing just the room numbers so that the smallest numbered available room is taken first).\n\n- Heap for ongoing meetings (storing pairs of [end_time, room_number]).\nFor each meeting (processed in order by start time):\n\n- Free any rooms that have finished their meetings by comparing the meeting’s start time with the top of the ongoing meetings heap.\n\n- If there is an available room, assign the meeting immediately.\n\n- Otherwise, delay the meeting until the earliest room is free, update the meeting’s start and finish times accordingly.\n\n- Update the count for each room’s usage.\n\n- Finally, select the room with the maximum meeting count (choosing the smallest room number in case of ties).",
    },
    "find-k-pairs-with-smallest-sums": {
        "key_insights": "- Both arrays are sorted, making it possible to efficiently traverse and pick the smallest candidate pairs.\n\n- A min-heap (priority queue) can be used to always access the next smallest sum pair.\n\n- Instead of generating all possible pairs (which could be very large), we only generate candidates and push new pairs from nums2 as needed.",
        "complexity":   "Time Complexity: O(k log(min(k, n1))) where n1 is the size of nums1, since we push at most min(k, n1) elements into the heap.\nSpace Complexity: O(min(k, n1)) for storing the heap elements.",
        "solution":     "We use a min-heap (priority queue) to efficiently track and extract the pair with the smallest sum. Initially, we push pairs composed of each element in nums1 (up to k elements) paired with the first element of nums2. Each element in the heap is a tuple containing (sum, index in nums1, index in nums2). Every time we extract the smallest pair, we add the next pair from nums2 (if available) for that index in nums1. This approach ensures we always have the next smallest pair available and avoids creating all possible pairs upfront.",
    },
    "kth-smallest-element-in-a-sorted-matrix": {
        "key_insights": "- The matrix is sorted both row-wise and column-wise.\n\n- Instead of flattening and sorting the entire matrix (which uses O(n^2) space), take advantage of the sorted properties.\n\n- Binary search can be applied over the range of values in the matrix.\n\n- Count the number of elements less than or equal to a midpoint to determine if the kth element is on the left or right side of the search range.\n\n- Alternatively, a min-heap approach can be used, though it generally leads to higher space complexity.",
        "complexity":   "Time Complexity: O(n * log(max - min)), where max and min represent the range of values in the matrix.\nSpace Complexity: O(1) extra space (ignoring recursion and input).",
        "solution":     "We solve the problem by performing binary search on the value range between the smallest and largest elements in the matrix. For a mid-value during the binary search, we count how many elements in the matrix are less than or equal to mid by scanning from the bottom-left corner. If the count is less than k, we move to the right half of the range; otherwise, we adjust the range to the left. This iterative process continues until the range is narrowed down to a single element, which is the kth smallest.",
    },
    "logger-rate-limiter": {
        "key_insights": "- Use a hash table (dictionary) to store the most recent timestamp when each message was printed.\n\n- Check if the current timestamp is at least 10 seconds later than the stored timestamp before printing the message.\n\n- The messages are in chronological order, so no need for extra data structures to handle reordering.\n\n- The approach efficiently handles streaming input with constant time lookups.",
        "complexity":   "Time Complexity: O(1) average per message (dictionary lookup and update)\n\nSpace Complexity: O(n) in the worst case, where n is the number of unique messages",
        "solution":     "Maintain a hash table where keys are messages and values are the next allowed timestamp when the message can be printed. When a new message arrives with a given timestamp, check:\n\n- If the message is not stored in the hash table, print it and set the allowed timestamp as current timestamp + 10.\n\n- If the current timestamp is greater than or equal to the stored allowed timestamp, update the allowed timestamp and print the message.\n\n- Otherwise, do not print the message.\nThis approach is efficient because it only requires constant time operations per call and uses minimal additional space.",
    },
    "implement-stack-using-queues": {
        "key_insights": "- We simulate a stack (LIFO) behavior using two queues (FIFO).\n\n- For the push operation, use one queue (q2) to hold the new element and then transfer all elements from the other queue (q1) to q2 so that the new element is at the front.\n\n- Swap the roles of the two queues after each push so that q1 always holds the stack in the correct order.\n\n- The pop and top operations can then be implemented by simply removing or peeking at the front of q1.",
        "complexity":   "Time Complexity:\n\n- push: O(n), where n is the number of elements in the stack (due to transferring all elements between queues).\n\n- pop: O(1)\n\n- top: O(1)\nSpace Complexity: O(n), as we store all elements in the two queues.",
        "solution":     "We implement the stack using two queues. During each push operation, we enqueue the new element into an empty auxiliary queue (q2) and then move all elements from the primary queue (q1) to q2. This way, the new element comes to the front of the queue, mimicking stack behavior. Finally, we swap q1 and q2 so that q1 always represents the current stack. For pop, we simply dequeue from q1; for top, we peek at the front of q1; and for empty, we check if q1 is empty.",
    },
    "design-circular-deque": {
        "key_insights": "- Use a fixed-size array to simulate a circular buffer.\n\n- Maintain two pointers (front and rear) to track the positions for insertion/deletion.\n\n- Use modulo arithmetic for pointer adjustments to create the circular effect.\n\n- Track the number of elements to easily determine if the deque is full or empty.\n\n- Ensure that operations such as getFront and getRear return -1 when the deque is empty.",
        "complexity":   "Time Complexity: O(1) for all operations\n\nSpace Complexity: O(k), where k is the size of the deque",
        "solution":     "The solution employs a circular array as the underlying data structure to represent the deque. Two pointers, front and rear, are used to manage the insertion and deletion from both ends of the deque. When inserting at the front, the front pointer is decremented modulo the capacity; and for insertion at the rear, the element is added at the current rear pointer and then the rear pointer is incremented modulo the capacity. A size counter tracks the current number of elements, easing the detection of full or empty states. Deletion operations adjust the pointers similarly without shifting elements, thereby achieving constant time complexity for all operations.",
    },
    "design-browser-history": {
        "key_insights": "- Use a list (or similar data structure) to store the browsing history.\n\n- Maintain an index pointer to track the current page.\n\n- When visiting a URL, remove all forward history beyond the current page.\n\n- For the back operation, decrement the pointer by the given steps, ensuring it does not go below 0.\n\n- For the forward operation, increment the pointer by the given steps, ensuring it does not exceed the last visited page.",
        "complexity":   "Time Complexity:\n\n- visit: O(1) on average (slicing the forward history may take O(n) in worst-case but steps are bounded).\n\n- back and forward: O(1) per operation.\n\nSpace Complexity:\n\n- O(n) where n is the number of visited URLs.",
        "solution":     "We maintain an array (or list) for the history and an integer pointer to mark the current page.\n\nWhen visiting a new URL, we remove any forward history, append the new URL, and update the pointer.\n\nFor back, we decrement the pointer ensuring it does not go below zero; for forward, we increase the pointer ensuring it does not exceed the available history.\n\nThis approach ensures that all operations run efficiently with minimal overhead.",
    },
    "snapshot-array": {
        "key_insights": "- Use per-index storage to maintain history of changes; record only when updates occur.\n\n- Each index stores a list of (snap_id, value) pairs, sorted by snap_id.\n\n- Utilize binary search during a get operation to efficiently find the most recent update not exceeding the provided snap_id.\n\n- Keep a current snap_id counter and update it with each snap() call.",
        "complexity":   "Time Complexity:\n\n- set: O(1) (amortized)\n\n- snap: O(1)\n\n- get: O(log S), where S is the number of times an index was set (binary search over updates)\nSpace Complexity: O(n + total number of set operations), since we store only updates per index.",
        "solution":     "We maintain a history for each array index as a list of pairs with each pair containing a snapshot id and its corresponding value. When setting a value, if the latest snapshot id in the history for that index matches the current snap_id, update the value. Otherwise, append a new (snap_id, value) pair. The snap() method will increment the snap_id counter and return the previous counter. The get() method performs a binary search on the history list for the given index to locate the most recent update where snap_id is less than or equal to the queried snap_id.",
    },
    "design-a-food-rating-system": {
        "key_insights": "- Use a hash map to store each food's current rating and cuisine.\n\n- Maintain a max heap for each cuisine to retrieve the highest-rated food quickly.\n\n- Store tuples of (-rating, food) in the heap. The negative rating turns the min-heap (default in many languages) into a max-heap.\n\n- Handle rating updates with lazy deletion: insert the updated rating into the heap and, during retrieval, discard stale entries whose rating does not match the latest value in the hash map.\n\n- Lexicographical order is naturally supported by storing the food name in the tuple and relying on tuple comparison.",
        "complexity":   "Time Complexity:\n\n- changeRating: O(log n) due to heap insertion.\n\n- highestRated: O(log n) amortized as stale entries are lazily removed from the heap.\n\nSpace Complexity: O(n) for storing the maps and heaps.",
        "solution":     "We create three primary data structures:\n\n- A map from food to its current rating (foodToRating).\n\n- A map from food to its cuisine (foodToCuisine).\n\n- A map from cuisine to a max heap (cuisineToHeap) that stores pairs of (-rating, food).\n\nWhen a food’s rating is updated, we update the foodToRating map and push the new (-rating, food) tuple onto the appropriate heap. When a query for the highest-rated food is made, we repeatedly check the top of the heap for that cuisine. If the top entry’s rating (after sign inversion) matches the current rating in foodToRating, it is returned; otherwise, we pop the stale value and continue. This lazy deletion avoids the overhead of directly removing outdated entries from the heap.",
    },
    "range-module": {
        "key_insights": "- Use a sorted data structure (like an ordered list or balanced BST) to maintain non-overlapping intervals.\n\n- When adding an interval, merge it with overlapping intervals.\n\n- Query by checking if an interval is fully covered by one of the existing intervals.\n\n- Removing an interval requires splitting overlapping intervals accordingly.\n\n- Binary search can be used to quickly locate intervals that might overlap with the query range.",
        "complexity":   "Time Complexity: Each addRange, queryRange, and removeRange operation can take O(n) in the worst-case as merging or splitting intervals might require scanning through intervals.\nSpace Complexity: O(n) where n is the number of disjoint intervals stored.",
        "solution":     "We maintain a sorted list of intervals, where each interval is a pair [start, end) and the intervals are non-overlapping. For the addRange operation, we locate all intervals that overlap with [left, right) and merge them together. For queryRange, we use binary search to check if an interval exists that completely covers [left, right). For removeRange, overlapping intervals are modified or split to remove the range [left, right).\n\nThe key is to update the intervals list while ensuring it remains sorted and non-overlapping. This data structure avoids tracking every single number and is efficient in handling intervals up to large input sizes.",
    },
    "design-a-text-editor": {
        "key_insights": "- Use two separate data structures (stacks) to represent the text to the left and right of the cursor.\n\n- The left stack holds characters before the cursor, while the right stack holds characters after the cursor.\n\n- Each operation is executed in O(k) time where k is the number of characters to process in that operation.\n\n- Cursor movement is simulated by transferring characters between the left and right stacks.\n\n- For retrieving the last min(10, len) characters left of the cursor, simply extract from the left stack.",
        "complexity":   "Time Complexity: O(k) per call for addText, deleteText, cursorLeft, and cursorRight where k is the number of characters processed.\nSpace Complexity: O(n) overall, where n is the total number of characters maintained in both stacks.",
        "solution":     "We simulate a text editor using two stacks. One stack, leftStack, contains characters to the left of the cursor; the other, rightStack, contains characters to the right. When adding text, push characters onto leftStack. For deleting text, pop characters from leftStack. Moving the cursor left pops characters from leftStack into rightStack (up to k times or until leftStack is empty), and moving right pops from rightStack back into leftStack. After each cursor move, the solution returns the last 10 characters from leftStack (or whatever is available). This approach guarantees that each operation processes only the relevant k characters.",
    },
    "assign-cookies": {
        "key_insights": "- Sort both the greed factor array and the cookie sizes array.\n\n- Use a two-pointer approach to iterate through both arrays.\n\n- If the current cookie can satisfy the current child's greed, assign it and move to the next child.\n\n- Continue until either all children are checked or all cookies are used.",
        "complexity":   "Time Complexity: O(n log n + m log m) where n is the number of children and m is the number of cookies (due to sorting).\nSpace Complexity: O(1) if sorting is done in-place, otherwise O(n + m) if extra space is used to copy lists.",
        "solution":     "The approach is based on greedy allocation. By sorting the children based on their greed factors and sorting the cookies by size, we can efficiently match the smallest available cookie that is sufficient for a given child. The two-pointer technique helps in traversing both arrays simultaneously, ensuring that we don’t waste a potentially larger cookie on a child with lower greed if a smaller cookie would suffice. This strategy maximizes the number of content children.",
    },
    "apple-redistribution-into-boxes": {
        "key_insights": "- Compute the total number of apples by summing the values in the apple array.\n\n- To minimize the number of boxes, choose boxes with the largest capacities first.\n\n- Sort the capacity array in descending order.\n\n- Recursively add box capacities until the cumulative capacity is greater than or equal to the total apples.\n\n- This greedy approach ensures the optimal solution using minimal boxes.",
        "complexity":   "Time Complexity: O(m log m) due to sorting the capacity array, where m is the number of boxes.\n\nSpace Complexity: O(1) extra space if sorting is done in-place.",
        "solution":     "The solution starts by summing all apples in the input array apple. The capacity array is then sorted in descending order, allowing the selection of the largest boxes first. Iterate through this sorted capacity list, accumulating the capacity until it is at least equal to the total apple count. The number of iterations (boxes) used will be the answer. The key data structures include simple integer variables and an array. The greedy algorithm is optimal here because it ensures that the highest capacities are chosen to reach the required sum as quickly as possible.",
    },
    "jump-game-vii": {
        "key_insights": "- A straightforward BFS or DFS would be too slow for large inputs due to overlapping subproblems.\n\n- Use dynamic programming with a sliding window (prefix sum) to efficiently track the cumulative reachability.\n\n- Instead of checking each jump individually, maintain a sliding window sum over the dp values that denote whether previous positions are reachable.\n\n- When the sliding window moves, add the new dp value entering the window and subtract the dp value leaving the window.\n\n- The cell is marked reachable only if it is '0' and there exists at least one previous reachable index within the jump range.",
        "complexity":   "Time Complexity: O(n) where n is the length of the string.\nSpace Complexity: O(n) for the dp array.",
        "solution":     "The solution uses a dynamic programming approach combined with a sliding window technique. We create a boolean array dp where dp[i] indicates whether index i is reachable. We initialize dp[0] as true because we start from index 0. A variable (reachable) is used to maintain the count of reachable indices in the current window. For each index i, if i is within minJump distance from a previous index, we add dp[i - minJump] to reachable. If the index is beyond maxJump, we subtract dp[i - maxJump - 1] from reachable as its contribution is no longer valid. If the current character s[i] is '0' and reachable is greater than zero, we mark dp[i] as true. Finally, dp[s.length - 1] gives the answer.",
    },
    "minimum-add-to-make-parentheses-valid": {
        "key_insights": "- Use a counter to track the number of unmatched '(' characters.\n\n- When encountering a ')', if there is no preceding unmatched '(', it indicates an imbalance that requires inserting an '('.\n\n- After processing the entire string, any remaining unmatched '(' will require inserting the same number of closing parentheses.\n\n- The total moves is the sum of insertions needed for unmatched ')' and unmatched '('.",
        "complexity":   "Time Complexity: O(n), where n is the length of the string.\nSpace Complexity: O(1)",
        "solution":     "The solution iterates through the entire string using a single pass. A counter (named count) is incremented for every '(' encountered, representing an imbalance. When a ')' is encountered, if the counter is greater than zero, it implies a matching '(' exists, so the counter is decremented; if not, it indicates an unmatched ')' and requires an insertion, so a moves counter is incremented. After processing all characters, any remaining count of '(' represents unmatched openings, and the same number of moves must be added. This greedy approach ensures we account for all imbalances with a minimum number of moves using constant space.",
    },
    "maximum-swap": {
        "key_insights": "- Use a greedy strategy to get the highest value number.\n\n- Record the last occurrence index of each digit (0 through 9) in the number.\n\n- Iterate through the digits. For each digit, check if there is a larger digit available later in the number.\n\n- Swap the current digit with the rightmost occurrence of the larger digit if it makes the entire number larger.\n\n- Only one swap is allowed, so the first valid swap is the optimal solution.",
        "complexity":   "Time Complexity: O(n) where n is the number of digits (since we scan the digits a constant number of times).\nSpace Complexity: O(n) for storing the digits and the occurrence mapping.",
        "solution":     "We first convert the input number into a list (or equivalent structure) of digits. Then, we create a mapping of each digit (0-9) to its last occurrence index in the list. The key idea is that for each digit (from leftmost to rightmost), we look for a larger digit that occurs later. Starting from 9 down to one greater than the current digit, we check if such a digit exists in the mapping at an index beyond the current position. If found, we swap and return the new number immediately. If no such digit is found for any position, the number is already maximized, so we return the original number.",
    },
    "queue-reconstruction-by-height": {
        "key_insights": "- Sort the array in descending order by height; for people with equal height, sort in ascending order by k.\n\n- When inserting a person into the result list, their k value indicates the index at which they should be placed.\n\n- This greedy approach guarantees that each person is inserted after all taller or equal height persons have already been placed.",
        "complexity":   "Time Complexity: O(n^2) in the worst case due to the insertion process in the list.\nSpace Complexity: O(n) for storing the output queue.",
        "solution":     "The solution leverages a greedy algorithm. First, sort the input array such that taller people come first and, for the same height, the person with a smaller k comes first. By doing this, when you iterate over the sorted list and insert each person at the index equal to their k value, the relative order of people already inserted (who are taller or of the same height) remains valid, fulfilling the condition of exactly k people in front who are taller or equal.",
    },
    "maximum-score-from-removing-substrings": {
        "key_insights": "- The order of removal matters because once a substring is removed, the remaining parts of the string can combine to form new removable pairs.\n\n- It is optimal to first remove the substring with the higher reward. This prevents potentially losing higher points if a higher rewarded pair is formed later.\n\n- Use a greedy approach with a stack to efficiently remove substrings in one pass.\n\n- Process the string twice:\n\n- First pass: Remove the higher-value substring while using a stack to track characters.\n\n- Second pass: On the resulting string, remove the lower-value substring.",
        "complexity":   "Time Complexity: O(n) per pass, hence overall O(n).\n\nSpace Complexity: O(n) in worst-case scenario for the stack.",
        "solution":     "We use a greedy algorithm with stacks:\n\n- Determine which substring removal gives more points.\n\n- For the substring with higher reward:\n\n- Traverse the string, using a stack to check if the current character and the top of the stack form the high-reward pair.\n\n- If they do, pop from the stack and add the respective points.\n\n- For the remaining substring, repeat a similar process using a new stack.\n\n- Sum the points from both passes to get the maximum score.\n\nThis approach ensures that we remove the most valuable pairs first without missing any formation of the less valuable pair later.",
    },
    "avoid-flood-in-the-city": {
        "key_insights": "- When it rains on a lake, mark that lake as full along with the current day index.\n\n- If the same lake is rained upon again without being dried, a flood occurs.\n\n- On a dry day (rains[i] == 0), choose to dry a lake that is scheduled to receive rain in the future and is currently full.\n\n- Use a data structure to keep track of available dry days (and their indices) and search for a dry day that occurs after the previous rain for a given lake.\n\n- A greedy algorithm with binary search (or a balanced tree structure) is a natural fit to make the optimal drying decision.",
        "complexity":   "Time Complexity: O(n log n) – Each dry day lookup involves a binary search in the sorted list of dry day indices.\nSpace Complexity: O(n) – To maintain the mapping of lakes to their last rain day and the list of available dry days.",
        "solution":     "The solution is based on a greedy strategy combined with binary search. We maintain:\n\n- A dictionary (or hash table) to keep track of the last day each lake was rained upon.\n\n- A sorted list (or tree set) to store indices of days where no rain occurs (dry days).\n\nFor each day:\n\n- If it rains (rains[i] > 0):\n\n- If the lake is already full (exists in our dictionary), we try to find the earliest dry day (using binary search) that is after the previous rain day for that lake.\n\n- If such a dry day does not exist, a flood is inevitable, so we return an empty array.\n\n- Otherwise, we assign that dry day to dry the lake and update our data structures.\n\n- If it is a dry day, we simply add the day's index to our sorted list of available dry days and, by default, assign an arbitrary value (e.g., 1) in the result. This value will be overridden if this day gets used to dry a specific lake.\n\nThis approach ensures that we always use the best available dry day for a lake that might cause a future flood.",
    },
    "maximum-matrix-sum": {
        "key_insights": "- Flipping the sign of two adjacent numbers does not affect the overall parity (even/odd nature) of the count of negative numbers.\n\n- You can effectively change the signs in pairs, which means if the total count of negative numbers is even, you can transform all values to positive.\n\n- If the count of negative numbers is odd, one number will remain negative, so you want that to be the smallest (by absolute value) to minimize the reduction in the maximum sum.\n\n- The answer thus becomes the sum of the absolute values of all elements, adjusted by subtracting twice the smallest absolute value if an odd number of negatives exists.",
        "complexity":   "Time Complexity: O(n^2) – We traverse every element in the matrix once.\nSpace Complexity: O(1) – Only constant extra space is needed (aside from input storage).",
        "solution":     "The approach leverages a greedy strategy:\n\n- Iterate through the matrix and compute the sum of the absolute values of each element.\n\n- Count the number of negative numbers and track the smallest absolute value.\n\n- If the count of negatives is even, all negatives can be flipped to positive, so the maximum sum is the sum of absolute values.\n\n- If the count of negatives is odd, one number will remain negative after optimal operations. To maximize the sum, choose the smallest absolute value to be negative. Therefore, subtract twice this minimum value from the total sum of absolute values.\n\n- Use simple loops and arithmetic to achieve the solution.",
    },
    "most-profit-assigning-work": {
        "key_insights": "- Pair each job's difficulty and profit and sort them by difficulty.\n\n- Compute a running maximum of profit for increasing difficulties; this allows you to quickly determine the best profit available for any worker ability.\n\n- For each worker, use binary search in the sorted job list to find the highest difficulty job they can perform.\n\n- Sum the best profits for all workers, handling workers that cannot perform any job by adding zero.",
        "complexity":   "Time Complexity: O(n log n + m log n), where n is the number of jobs and m is the number of workers.\nSpace Complexity: O(n), used for storing and processing the sorted job pairs and the running max profit list.",
        "solution":     "The solution involves first pairing up job difficulties with their respective profits and then sorting these pairs based on difficulty. As you process this sorted list, maintain a running maximum profit to ensure that at any given difficulty level, you know the best profit you can earn from any job up to that difficulty. For each worker, perform a binary search on the sorted job difficulties to quickly determine the best profit that the worker can achieve. By summing these values for all workers, you achieve the maximum total profit. This algorithm leverages sorting, prefix maximum, and binary search to efficiently compute the result.",
    },
    "minimum-cost-to-hire-k-workers": {
        "key_insights": "- Determine the wage-to-quality ratio for each worker, which represents the minimum acceptable rate per unit quality.\n\n- By sorting workers by their ratio, you can ensure that if a worker is the highest ratio in the chosen group, then paying every worker according to this ratio meets the minimum wage requirement.\n\n- Use a max-heap (priority queue) to maintain the smallest total quality sum among the chosen workers. If the group exceeds size k, remove the worker with the highest quality (thus the highest contribution to cost) to minimize the total cost.\n\n- At each step (when heap size reaches k), compute the total cost as the current sum of qualities multiplied by the current worker’s ratio and update the minimum cost answer.",
        "complexity":   "Time Complexity: O(n log n + n log k)\nSpace Complexity: O(n)",
        "solution":     "This solution calculates the effective wage-to-quality ratio for each worker and sorts the workers by this ratio. The sorted order guarantees that any chosen group has a maximum ratio that enforces the proportional payment rule without violating any minimal wage constraints.\n\nA max-heap is then used to keep track of the largest qualities among the selected workers. As we traverse through the sorted list, we add each worker's quality into the heap and update a running sum of qualities. If the heap size exceeds k, we remove the worker with the highest quality to keep only the k smallest qualities (thus minimizing cost). The current total cost candidate is then computed as the running sum multiplied by the current worker's ratio. The minimum such candidate is the solution.\n\nThe key data structures used are:\n\n- Array for storing worker data (ratios and quality).\n\n- Sorting algorithm to order workers by the computed ratio.\n\n- Max-heap (implemented using a min-heap of negative values in languages like Python) to efficiently maintain the current group’s total quality.",
    },
    "candy": {
        "key_insights": "- Every child must get at least one candy.\n\n- A left-to-right pass ensures that each child has more candies than the left neighbor if their rating is higher.\n\n- A right-to-left pass ensures that each child has more candies than the right neighbor if their rating is higher.\n\n- The final candy distribution for each child is determined by taking the maximum candies assigned by the two passes.",
        "complexity":   "Time Complexity: O(n) - Two passes over the array, each in linear time.\nSpace Complexity: O(n) - An auxiliary array is used to store the candy counts for each child.",
        "solution":     "We start by initializing an array with 1 candy for each child, since each child must receive at least one candy. Then, perform a left-to-right pass: if a child’s rating is higher than the previous child, assign one more candy than the previous child. Next, perform a right-to-left pass: if a child’s rating is higher than the next child, update the candy count to be the maximum of its current value or one more than the next child's candy count. The answer is the sum of the adjusted candy counts.",
    },
    "minimum-number-of-refueling-stops": {
        "key_insights": "- The car's fuel depletes as it travels, so fuel management is critical.\n\n- Using a greedy strategy with a max-heap (priority queue) helps decide from which station to refuel.\n\n- At each station, if the current fuel is insufficient to reach the next station (or the destination), refuel from the station with the maximum fuel among all previously passed stations.\n\n- Dynamic programming can also be used, but a greedy approach with a heap usually yields a simpler solution.\n\n- Edge cases: no stations available, or fuel exactly reaches a station/destination with 0 fuel remaining.",
        "complexity":   "Time Complexity: O(n log n) where n is the number of stations. Each station may be pushed or popped from the heap.\nSpace Complexity: O(n) due to the storage used by the heap for up to n elements.",
        "solution":     "The solution uses a greedy approach combined with a max-heap. As the car moves towards the target, it subtracts the distance traveled from its current fuel. Whenever the fuel is insufficient to travel to the next station or destination, the algorithm refuels using the station that offered the most fuel among those passed (available in the max-heap). If no such station exists and the car cannot proceed further, the destination is unreachable (return -1). This approach minimizes the number of stops since we always choose the best available fuel option when needed.",
    },
    "set-intersection-size-at-least-two": {
        "key_insights": "- Use a greedy strategy by processing intervals in order of increasing end points.\n\n- Sort the intervals by end point (and in case of tie, by start in descending order) so that choosing numbers near the end maximizes coverage for future intervals.\n\n- For each interval, count the numbers already chosen that fall into the interval.\n\n- If fewer than two numbers are present, add the necessary number(s) (always choosing the largest possible values within the interval to maximize overlap with future intervals).",
        "complexity":   "Time Complexity: O(n log n) due to the sorting step where n is the number of intervals.\nSpace Complexity: O(n) for storing the chosen numbers (in worst-case scenarios) plus auxiliary space for sorting.",
        "solution":     "The solution uses a greedy approach after sorting the intervals. First, the intervals are sorted by their end boundary, with a tie-breaker on the start boundary (in descending order) to ensure that when intervals share an end, the one with the larger start is handled last. For each interval, we check how many selected numbers (already added in our result set) fall inside the interval. If fewer than two, we add additional numbers at the end of the interval. This ensures that the numbers we add are as large as possible within the current interval, thereby maximizing the chance they will cover future intervals as well. The key idea is that by always “covering” an interval from the end, we are potentially covering as many subsequent intervals as possible.",
    },
    "time-needed-to-inform-all-employees": {
        "key_insights": "- The company structure forms a tree with the head as the root.\n\n- We need to propagate the news from the head down through all branches.\n\n- The time taken for the news to fully propagate is determined by the longest path from the head to any leaf in the tree.\n\n- A Depth-First Search (DFS) efficiently traverses the tree, accumulating inform times along each branch.",
        "complexity":   "Time Complexity: O(n) – Each employee (node) is visited once.\nSpace Complexity: O(n) – In the worst-case, the recursion stack may hold all n nodes.",
        "solution":     "We construct an adjacency list (or tree) that maps each manager to their list of direct subordinates. Then, we perform a DFS starting from the head. For each employee, we calculate the time required to inform all their subordinates by taking the maximum time among all children and adding the current employee’s informTime. This approach correctly computes the time to disseminate the news along the longest chain in the tree.",
    },
    "all-paths-from-source-to-target": {
        "key_insights": "- The graph is a DAG, so there are no cycles. This makes a recursive Depth-First Search (DFS) or backtracking approach viable without needing to worry about infinite loops.\n\n- Every decision (choosing a neighbor) is independent because paths do not cycle back.\n\n- Backtracking is essential to explore different branches, adding a node, and later removing it to reset the current path.\n\n- Since the output requires listing complete paths from the source (0) to the target (n - 1), we need to store and eventually copy the current path once a valid path is found.",
        "complexity":   "Time Complexity: O(2^n * n) in the worst-case scenario where exponential number of paths may exist, each path requiring O(n) to copy.\nSpace Complexity: O(n) for the recursion stack and current path, excluding the space required for the output.",
        "solution":     "We can solve this problem using a Depth-First Search (DFS) algorithm with backtracking. The basic idea is to start at node 0 and explore each neighbor recursively. When the target node (n - 1) is reached, a copy of the current path is added to the result list. Once the recursive call returns, we backtrack by removing the last node so that we can continue exploring other possible paths from the previous node.\n\nThe following solutions illustrate how to implement this approach in various programming languages.",
    },
    "employee-importance": {
        "key_insights": "- Map employee IDs to their corresponding employee object for quick lookup.\n\n- The problem can be modeled as a tree or graph where nodes represent employees.\n\n- Traverse the tree (using DFS or BFS) starting from the given employee ID.\n\n- Sum the importance values along the traversal path.\n\n- Handle potential negative importance values.",
        "complexity":   "Time Complexity: O(n), where n is the number of employees, since in the worst case every employee will be visited.\nSpace Complexity: O(n) in the worst case due to recursion call stack (DFS) or queue (BFS) and additional hashmap for storing employee lookup.",
        "solution":     "We first build a hashmap (or dictionary) mapping each employee's ID to the employee object for O(1) access during traversal. Then, using either Depth First Search (DFS) or Breadth First Search (BFS), we traverse from the target employee, and at each node we add its importance to a running total. The DFS approach can be implemented recursively or iteratively with a stack, while the BFS approach makes use of a queue. The key idea is ensuring that each employee is processed exactly once.",
    },
    "number-of-enclaves": {
        "key_insights": "- Any land cell that is connected to the boundary can reach the edge and should not be counted.\n\n- Use DFS/BFS to traverse and mark all land cells connected to the boundary as water.\n\n- Once the boundary-connected cells are removed, count the remaining land cells in the grid.",
        "complexity":   "Time Complexity: O(m * n) - Each cell is processed at most once.\nSpace Complexity: O(m * n) - In worst-case scenarios (e.g., deep recursion in DFS), the auxiliary space used can be proportional to the number of cells.",
        "solution":     "The approach involves a two-step process:\n\n- Traverse the boundaries of the grid and perform a DFS/BFS from each boundary cell that is a land cell. During traversal, mark all reachable land cells (i.e., cells that can potentially reach the boundary) as sea.\n\n- After processing the boundaries, count all remaining land cells in the grid. These are the enclaves that cannot reach the boundary.\n\nKey data structures include the grid itself and, depending on the implementation, either a recursion stack (DFS) or an explicit queue (BFS) for traversal. The main trick is to remove (or mark) all boundary-connected land cells before counting, effectively isolating the enclaves.",
    },
    "making-a-large-island": {
        "key_insights": "- First, identify and label every existing island using DFS/BFS.\n\n- Maintain a mapping from island id to its size.\n\n- For each 0, examine its four neighbors. Sum the size of distinct adjacent islands and add 1 (for the flipped cell) to compute a candidate island size.\n\n- Be careful to avoid counting the same island more than once when summing.\n\n- If the grid is full of 1s (i.e. no 0 to flip), then the largest island is the whole grid.",
        "complexity":   "Time Complexity: O(n^2) because each cell is visited a constant number of times.\nSpace Complexity: O(n^2) for the recursion/queue in DFS/BFS in the worst case and for storing island information.",
        "solution":     "The solution uses a two-phase algorithm. In the first phase, we use DFS to label each island with a unique id (starting from 2 to avoid confusion with 0 and 1) and record its area in a mapping (dictionary). In the second phase, for each cell that is 0, we look at its 4-directional neighbors to find distinct islands. We then sum the sizes of these islands and add 1 (for converting the current 0 into a 1), tracking the maximum island size possible. This approach efficiently calculates the largest island that can be created after a single modification.",
    },
    "longest-path-with-different-adjacent-characters": {
        "key_insights": "- The tree structure guarantees a unique path between any two nodes.\n\n- A depth-first search (DFS) can be used to explore each subtree and aggregate the maximum valid branch lengths.\n\n- At each node, only child paths with a different character than the current node can be extended.\n\n- Combining the two longest valid branches stemming from a node gives the candidate longest path that passes through that node.\n\n- A global variable is maintained to track the overall longest valid path found during traversal.",
        "complexity":   "Time Complexity: O(n) - Each node is visited once.\nSpace Complexity: O(n) - For recursion stack and storage of the tree structure.",
        "solution":     "We solve the problem using a DFS traversal. First, we build an adjacency list (child list) from the parent array to represent the tree. For each node, we perform a DFS that returns the length of the longest valid path starting from that node (including itself) where the adjacent characters differ. For every node, we consider all its children and update the two longest valid branch lengths only if their characters differ from the current node. The candidate longest path passing through a node is the sum of the two longest valid branch lengths plus 1 (for the current node). We update a global maximum with this value at every node. Finally, the DFS returns the overall maximum path length.",
    },
    "open-the-lock": {
        "key_insights": "- This problem can be modeled as a graph where each node represents a 4-digit combination.\n\n- Each valid move from one node generates up to 8 neighboring combinations (one for each wheel moving one step forward or backward).\n\n- Breadth-First Search (BFS) is used to explore the graph level by level, which naturally finds the shortest path.\n\n- Deadends must be avoided during the traversal.\n\n- The initial state \"0000\" must be checked against the deadends.",
        "complexity":   "Time Complexity: O(10^N) where N is the number of wheels (N = 4 gives 10^4 combinations in worst-case scenario).\nSpace Complexity: O(10^N) due to storing visited states and the queue in BFS.",
        "solution":     "We use a Breadth-First Search (BFS) to systematically explore all potential combinations starting from \"0000\". Each combination represents a state or node in our search space. From each state, we generate all valid next states by incrementing or decrementing each digit (with wraparound behavior). We keep track of combinations that are either deadends or already visited to avoid cycles and unnecessary processing.\n\nKey steps:\n\n- Start at \"0000\" if it is not a deadend.\n\n- Use a queue to process one level of combinations at a time, representing the number of moves.\n\n- For each combination, generate all possible next moves (neighbors). Skip any that are deadends or that have been visited.\n\n- If the target combination is reached, return the level (move count).\n\n- If the queue empties without finding the target, return -1.",
    },
    "snakes-and-ladders": {
        "key_insights": "- Use Breadth-First Search (BFS) to explore all possible moves from the start while tracking the number of dice rolls.\n\n- Convert the one-dimensional square number into board coordinates accounting for the alternating direction in each row.\n\n- When landing on a snake or ladder, follow it immediately, but only take one jump per roll.\n\n- Maintain a visited set or array to avoid re-processing cells and prevent infinite loops.",
        "complexity":   "Time Complexity: O(n²) – each of the n² cells is processed at most once during the BFS.\nSpace Complexity: O(n²) – used for the queue and visited set to store positions.",
        "solution":     "The problem is solved via a Breadth-First Search (BFS) approach. Starting from square 1, we explore all reachable squares (up to six moves ahead) for each dice roll. For each move, we convert the square number into board coordinates while considering the alternating order of rows. When a move encounters a snake or ladder (board value not equal to -1), we move directly to its destination. By using BFS, we ensure that the first time we reach the final square (n²) it is with the fewest moves possible. To guarantee no repeated work, we track visited squares.",
    },
    "minimum-genetic-mutation": {
        "key_insights": "- Use Breadth-First Search (BFS) as it finds the shortest path (minimum mutations) in an unweighted graph.\n\n- Mutations are generated by changing each character of the gene string one by one to one of the valid characters: A, C, G, or T.\n\n- Use a set for the bank for O(1) membership checks.\n\n- Avoid revisiting the same gene by marking mutations as visited (or removing them from the bank).",
        "complexity":   "Time Complexity: O(N * L * 4) where N is the number of genes in the bank and L is the length of the gene (here L=8), due to checking each position with 4 possible mutations.\nSpace Complexity: O(N) for the queue and visited set, with additional O(N) for the bank set.",
        "solution":     "We solve the problem using a Breadth-First Search (BFS) algorithm. The idea is to start from the initial gene and generate all possible one-character mutations. Each valid mutation (i.e., one that exists in the bank) is added to the BFS queue. For each level of BFS, we increment the mutation count. When the end gene is reached, we return the current mutation count.\n\nWe store the bank in a set to ensure constant time membership checks and avoid revisiting states. This solution guarantees finding the minimum number of mutations needed if a valid sequence exists, otherwise, it returns -1.",
    },
    "shortest-path-in-binary-matrix": {
        "key_insights": "- Use Breadth-First Search (BFS) to guarantee the shortest path in an unweighted grid.\n\n- Movement is allowed in 8 directions, so each cell has up to 8 neighbors.\n\n- Check boundary conditions and ensure only unvisited cells with value 0 are processed.\n\n- Mark cells as visited to avoid reprocessing and infinite loops.\n\n- If the start or finish is blocked (==1), the answer is immediately -1.",
        "complexity":   "Time Complexity: O(n^2), as in the worst-case scenario every cell in the grid is visited once.\nSpace Complexity: O(n^2), due to the queue used for BFS and storage for visited cells.",
        "solution":     "We approach this problem using BFS, which is ideal for finding the shortest path in a grid when each move has the same cost. Initialize the BFS from the top-left cell if it is 0. For every cell dequeued, explore its 8 neighboring cells. If a neighboring cell is within grid bounds, has a value of 0, and hasn’t been visited, mark it as visited and enqueue it with an incremented path length. Continue this process until the bottom-right cell is reached. If the queue is exhausted without reaching the destination, return -1.",
    },
    "path-with-maximum-minimum-value": {
        "key_insights": "- The score of a path is determined by its lowest value. Therefore, the objective is to maximize this minimum value over all possible paths.\n\n- A greedy strategy that always considers the next step with the highest value works well.\n\n- Using a max-heap (priority queue) allows for exploring high-value paths first.\n\n- Once the destination is reached, the minimum value along the chosen path is the answer.\n\n- Alternative methods include binary search over the possible answer space with a connectivity check (using DFS/BFS) or Union-Find to simulate connectivity with a threshold.",
        "complexity":   "Time Complexity: O(m * n * log(m * n)) because each cell might be pushed into the heap once and each heap operation costs log(m*n).\nSpace Complexity: O(m * n) for the heap and visited cells tracking.",
        "solution":     "The solution uses a greedy algorithm implemented with a max-heap:\n\n- Start at cell (0, 0) and initialize the score as the value at the starting cell.\n\n- Use a max-heap to store cells with their corresponding values in descending order.\n\n- Repeatedly pop the cell with the current highest value. Update the score as the minimum value encountered so far along the path.\n\n- If the destination cell is reached, return the current score.\n\n- Otherwise, push all valid neighboring cells that have not been visited into the heap.\nThis method ensures that paths that are more promising (i.e., with higher current minimum values) are explored first, thus guaranteeing the maximum possible score upon reaching the destination.",
    },
    "shortest-path-in-a-grid-with-obstacles-elimination": {
        "key_insights": "- Use a Breadth-First Search (BFS) approach to explore the grid level by level.\n\n- Maintain a state that includes the current position (row and column) and the number of obstacles eliminated so far.\n\n- Use a 3-dimensional visited data structure (or a 2D array with obstacle elimination count) to avoid reprocessing states that have been reached with fewer eliminations.\n\n- The BFS ensures that the first time you reach the target, it is via the shortest path.",
        "complexity":   "Time Complexity: O(m * n * k), where m and n are grid dimensions and k is the maximum obstacles that can be eliminated.\nSpace Complexity: O(m * n * k), due to storing the state (position and obstacles eliminated).",
        "solution":     "The solution utilizes a modified BFS that tracks each state as a tuple (row, column, obstaclesEliminated). Starting from (0,0) with 0 obstacles eliminated, we explore all possible moves (up, down, left, right). When moving to an adjacent cell:\n\n- If the cell is empty, continue without increasing the elimination count.\n\n- If the cell is an obstacle and you have remaining eliminations (i.e., obstaclesEliminated &#x3C; k), increase the elimination count.\nOnly states that offer a better (i.e., lower elimination count) path to a cell compared to any previously recorded state are added to the BFS queue. This prevents unnecessary reprocessing and reduces time complexity. When the destination is reached, the current step count from the BFS is returned. If the queue depletes without reaching the destination, the function returns -1.",
    },
    "minimum-obstacle-removal-to-reach-corner": {
        "key_insights": "- The grid can be interpreted as a weighted graph where moving through an empty cell (0) has cost 0 and moving through an obstacle (1) has cost 1.\n\n- The goal is to find the path with the minimum total cost.\n\n- Use a 0-1 BFS or a modified Dijkstra's algorithm to efficiently process the grid.\n\n- 0-1 BFS optimizes the traversal by using a deque: pushing 0-cost moves to the front and 1-cost moves to the back.",
        "complexity":   "Time Complexity: O(m * n) since each cell is processed at most once.\nSpace Complexity: O(m * n) for the deque and the visited/cost matrix.",
        "solution":     "We solve the problem by leveraging 0-1 BFS. The grid is modeled as a weighted graph where:\n\n- Moving to a neighbor cell with value 0 has no additional cost.\n\n- Moving to a neighbor cell with value 1 incurs a cost of 1 (obstacle removal).\n\nA deque (double-ended queue) is used:\n\n- When moving to an empty cell (0), push the new position to the front of the deque.\n\n- When moving to an obstacle (1), push the new position to the back.\n\nThis ensures positions that cost 0 are prioritized, and we maintain an array or matrix to keep track of the minimum obstacles removed to reach each cell.\n\nThe algorithm terminates when the destination cell is dequeued with the minimum cost, or when all cells are processed.",
    },
    "course-schedule-iv": {
        "key_insights": "- Represent courses and prerequisites as a directed acyclic graph (DAG).\n\n- Use graph traversal techniques (DFS or BFS) or compute the transitive closure.\n\n- The Floyd Warshall algorithm can precompute reachability between all courses.\n\n- After precomputation, each query can be answered in constant time by checking the reachability matrix.",
        "complexity":   "Time Complexity: O(N^3 + Q) where N = numCourses (Floyd Warshall algorithm) and Q = number of queries\n\nSpace Complexity: O(N^2) for storing the reachability matrix",
        "solution":     "We solve the problem by converting it into computing reachability in a DAG. The approach is to use the Floyd Warshall algorithm to compute a transitive closure.\n\nSteps:\n\n- Build a boolean reachability matrix and mark direct prerequisites.\n\n- Use a triple-nested loop (Floyd Warshall) to update the matrix such that if course i can reach course k and course k can reach course j, then course i can reach course j.\n\n- Answer each query in constant time by checking the precomputed reachability matrix.",
    },
    "find-eventual-safe-states": {
        "key_insights": "- Identify cycles: Nodes involved in cycles are unsafe because there exists a path that does not terminate.\n\n- DFS with state tracking: Use three states for each node (unvisited, visiting, and safe) to detect cycles.\n\n- Memorization: Once a node's safety is determined, reuse this result to avoid redundant computations.\n\n- Alternate approach: Reverse the graph and perform a topological sort, though DFS is more straightforward here.",
        "complexity":   "Time Complexity: O(V + E) where V is the number of nodes and E is the number of edges.\n\nSpace Complexity: O(V) due to the recursion stack and additional storage for tracking node states.",
        "solution":     "We perform a depth-first search (DFS) and use an array to mark each node with one of three states:\n\n- 0: unvisited\n\n- 1: visiting (currently in recursion stack)\n\n- 2: safe (confirmed safe node)\n\nFor each node, if during DFS a node is encountered that is already marked as visiting, a cycle is detected and the node is unsafe. If all reachable neighbors of a node are safe, then the node itself is safe. Finally, we collect and return all safe nodes sorted in ascending order.",
    },
    "find-all-possible-recipes-from-given-supplies": {
        "key_insights": "- Use a graph (dependency graph) where each recipe has incoming edges from its required ingredients.\n\n- Apply topological sorting: start with the initial supplies and progressively \"create\" recipes when all their dependencies are met.\n\n- Track in-degrees for recipes. When an ingredient (or recipe) becomes available, decrease the in-degree of all recipes that depend on it.\n\n- When a recipe’s in-degree reaches zero, it can be created and added to the result (and used as an ingredient for others).",
        "complexity":   "Time Complexity: O(R + total_ingredients) where R is the number of recipes and total_ingredients sums up all ingredient instances.\nSpace Complexity: O(R + I) where I is the total unique ingredients used in the dependency mapping.",
        "solution":     "We use a topological sort based on Breadth-First Search (BFS). First, we map every ingredient to the list of recipes that need it. We compute the in-degree (number of ingredients required) for each recipe. Initialize the process with the supplied ingredients. For each available ingredient, visit all recipes that depend on it, decrease their in-degree, and add any recipe with an in-degree of zero to the queue. This recipe then becomes an available ingredient for further recipes.",
    },
    "sort-items-by-groups-respecting-dependencies": {
        "key_insights": "- Items belonging to no group (group[i] == -1) should be assigned unique group ids.\n\n- Build two graphs: one for items (to handle intra-group ordering) and one for groups (to handle inter-group ordering).\n\n- For every dependency (j -> i): if items j and i belong to the same group, add an edge in the item graph; if they belong to different groups, add an edge in the group graph.\n\n- Use topological sort (Kahn’s algorithm or DFS based) to determine a valid order for both groups and items.\n\n- Combine the ordered items as per the sorted order of groups to get the final answer.",
        "complexity":   "Time Complexity: O(n + E) where n is the number of items and E is the total number of dependency edges.\n\nSpace Complexity: O(n + E) for storing graphs, indegree arrays, and auxiliary data structures.",
        "solution":     "We first reassign group ids for ungrouped items (group[i] == -1) to ensure every item is assigned a group. Then, we build two graphs:\n\n- A graph for items, connecting nodes within the same group using dependency constraints.\n\n- A graph for groups, connecting different groups based on dependency relations across items.\n\nUsing topological sort, we obtain an ordering of groups. Then, for each group, we topologically sort the items in that group. Finally, we concatenate the sorted items in the order given by the group sort.\n\nIf any cycle is detected (either at the group level or within any group), the function returns an empty list.",
    },
    "number-of-provinces": {
        "key_insights": "- The matrix represents an undirected graph where each node is a city.\n\n- Direct and indirect connections imply finding connected components in the graph.\n\n- DFS, BFS, or Union-Find can be used to traverse and count connected components.\n\n- Each traversal starting from an unvisited city represents a new province.",
        "complexity":   "Time Complexity: O(n^2) – Every cell of the matrix is inspected.\nSpace Complexity: O(n) – For the visited array (and recursion stack in DFS worst-case).",
        "solution":     "To solve the problem, we use a Depth-First Search (DFS) approach. Start by iterating over each city. When encountering an unvisited city, initiate a DFS to mark all cities within the same province as visited. Increment the province counter for each DFS initiation. This effectively counts the number of connected components (provinces) in the graph.",
    },
    "evaluate-division": {
        "key_insights": "- The problem can be modeled as a graph where each variable is a node.\n\n- Each equation A / B = k creates a directed edge from A to B with weight k and from B to A with weight 1/k.\n\n- To answer a query, perform a DFS or BFS from the source node and try to reach the target node, multiplying the weights along the path.\n\n- If a variable in the query does not exist in the graph or no path exists, return -1.0.",
        "complexity":   "Time Complexity: O(V + E) per query in the worst case since we might traverse the entire graph.\nSpace Complexity: O(V + E) for storing the graph and the recursion stack or queue for the search.",
        "solution":     "We can solve this problem by first constructing a graph using a hashmap or dictionary where each node points to its neighbors with the corresponding multiplication factors. For each query, we perform a depth-first search (DFS) starting from the dividend. We keep track of the cumulative product along the path. If we eventually reach the divisor, we return the computed product. If not, the query is unsolvable and we return -1.0.\nImportant points:\n\n- Use a visited set during DFS to prevent cycles.\n\n- Ensure to handle cases where either node in the query is not present in the graph.\n\n- The same approach works with BFS, but DFS may be simpler to implement in these cases.",
    },
    "most-stones-removed-with-same-row-or-column": {
        "key_insights": "- This problem can be modeled as a graph where each stone is a node. An edge exists between two stones if they share the same row or column.\n\n- In each connected component of the graph, you can remove all stones except one.\n\n- Thus, the maximum number of stones removed equals the total number of stones minus the number of connected components.\n\n- Depth-First Search (DFS) or Union Find can be used to count the number of connected components.",
        "complexity":   "Time Complexity: O(n^2) in the worst case when each stone is compared with every other stone (can be optimized with Union Find).\nSpace Complexity: O(n) for storing visited status and mapping structures.",
        "solution":     "We solve the problem using Depth-First Search (DFS). The algorithm first builds mappings from rows and columns to stone indices. Then, it iterates through each stone; if the stone hasn't been visited, it initiates a DFS which marks all stones in the same connected component (those sharing a row or column) as visited. The number of DFS calls equals the number of connected components. Finally, the maximum stones removed is given by the total number of stones minus the number of connected components.",
    },
    "minimize-malware-spread": {
        "key_insights": "- The graph is undirected and fully connected in the sense that if two nodes are in the same connected component, an infection in one will eventually infect all nodes in that component.\n\n- By removing a node from the initial list, the remaining initially infected nodes will still infect entire connected components.\n\n- Use connected components detection (via DFS or Union-Find) to group nodes.\n\n- For each component, count its size and the number of initially infected nodes within it.\n\n- Only if a component has exactly one initially infected node, removal of that node would save the rest of the component from infection.\n\n- The final answer is the node whose removal saves the largest number of nodes (largest component size), with ties broken by smallest index. If no such node exists, return the smallest index from the initial list.",
        "complexity":   "Time Complexity: O(n^2) – We traverse the entire n x n graph to find connected components.\nSpace Complexity: O(n) – We store component information and visited flags for up to n nodes.",
        "solution":     "We solve the problem by first using DFS (or Union-Find) to find all connected components in the graph. For each component, we compute its total number of nodes and the count of initially infected nodes. If a component has exactly one initially infected node, then removing that particular node prevents the spread to the rest of the component. We track, for each initially infected node, how many nodes can be saved by its removal (specifically, the component size of its unique component). Finally, we return the candidate with the maximum saving; if no candidate exists (i.e., every component has multiple infections), we return the smallest index from the initial list.",
    },
    "connecting-cities-with-minimum-cost": {
        "key_insights": "- This is a Minimum Spanning Tree (MST) problem.\n\n- Kruskal’s algorithm can be used by sorting the edges by cost and using a Union-Find (Disjoint Set Union) structure to efficiently check if adding an edge forms a cycle.\n\n- Alternatively, Prim’s algorithm with a priority queue can solve this, but Kruskal’s is straightforward given edge-based input.\n\n- The key is to add edges one by one in increasing order of cost until all cities are connected.",
        "complexity":   "Time Complexity: O(E log E) due to sorting the connections, where E is the number of connections.\nSpace Complexity: O(n) for the Union-Find data structure, plus O(E) space for storing the edges.",
        "solution":     "We use Kruskal's algorithm to build a Minimum Spanning Tree. First, sort the connections by cost. Then, iterate through each edge and use a Union-Find data structure to determine if the current edge connects two previously disconnected components. If it does, add the edge’s cost to the total cost and union the two components. Finally, if we have connected all n cities (i.e., used n-1 edges), return the total cost; otherwise, return -1.",
    },
    "optimize-water-distribution-in-a-village": {
        "key_insights": "- Transform the problem by introducing a virtual node (node 0) where connecting house i to this node costs wells[i - 1].\n\n- Build a graph where the edges are both the actual pipes between houses and edges from the virtual node to each house representing the well cost.\n\n- Use a Minimum Spanning Tree (MST) approach (either Kruskal’s or Prim’s algorithm) to connect all houses with the minimum cost.\n\n- The MST will automatically choose the cheaper option between building a well and laying a connecting pipe.",
        "complexity":   "Time Complexity: O((n + p) log(n + p)), where n is the number of houses and p is the number of pipes (for sorting the edges in the MST algorithm).\nSpace Complexity: O(n + p) to store the union-find data structures and the edge list.",
        "solution":     "We solve the problem by modeling it as an MST problem. First, we add a virtual node (0) to represent a water source. Then, we connect this node to each house i with an edge that has a cost equal to wells[i - 1]. Next, we add all the given pipe connections between houses to the graph. With this graph setup, the problem reduces to finding the minimum spanning tree that connects all nodes (including the virtual node). We can then use Kruskal’s algorithm with a union-find data structure to build this MST. The union-find data structure helps efficiently determine whether adding an edge will create a cycle, while sorting the edges ensures that we always add the smallest available edge that connects two components of the graph.",
    },
    "find-critical-and-pseudo-critical-edges-in-minimum-spanning-tree": {
        "key_insights": "- Use Kruskal's algorithm to compute the MST.\n\n- Sort the edges by weight and use Union-Find to efficiently detect cycles.\n\n- Compute the baseline MST weight.\n\n- For each edge, test if excluding it increases the MST weight (critical edge).\n\n- For each edge, test if forcing its inclusion can still produce an MST with the same weight (pseudo-critical edge).",
        "complexity":   "Time Complexity: O(E^2 * α(N)) where E is the number of edges and α is the inverse Ackermann function.\nSpace Complexity: O(N + E) for the Union-Find data structure and edge storage.",
        "solution":     "The solution uses Kruskal's algorithm along with a Union-Find (Disjoint Set Union) data structure:\n\n- Compute the baseline MST weight by sorting the edges and applying Kruskal’s algorithm.\n\n- For every edge:\n\n- To check if it is critical, recompute the MST without the edge. If the resulting weight increases or the MST cannot be formed, the edge is critical.\n\n- To check if it is pseudo-critical, force the inclusion of the edge and then compute the MST. If the resulting total weight equals the baseline MST weight, the edge is pseudo-critical.\nThis requires careful manipulation of the Union-Find structure and multiple MST computations.",
    },
    "path-with-maximum-probability": {
        "key_insights": "- The problem can be modeled as a graph traversal where the \"distance\" is defined in terms of cumulative probability (i.e., product of probabilities).\n\n- Instead of summing edge weights (like in a standard shortest path problem), multiply probabilities.\n\n- Dijkstra's algorithm can be adapted to maximize the product by using a max-heap (priority queue) to always extend the path with the highest probability so far.\n\n- An alternative trick is to transform the multiplication problem into an addition problem by using logarithms, but here a direct multiplication approach with proper priority update is more natural.\n\n- Maintain an array holding the best probability reached for each node to avoid unnecessary processing.",
        "complexity":   "Time Complexity: O(E log V), where E is the number of edges and V is the number of vertices.\nSpace Complexity: O(V + E) for the graph representation and auxiliary data structures.",
        "solution":     "We use a modified Dijkstra's algorithm:\n\n- Build an adjacency list representation of the graph.\n\n- Use a max-heap (priority queue) where each element is a pair (current probability, node). The heap always gives the node with the highest probability path computed so far.\n\n- Initialize the probability of reaching the start node as 1 and 0 for all other nodes.\n\n- While the heap is not empty, pop the node with the highest probability. If it is the end node, return its probability.\n\n- For each neighbor, compute the new probability by multiplying the current probability with the probability of the edge connecting to that neighbor. If this new probability is higher than the best known probability for the neighbor, update it and push the neighbor into the heap.\n\n- If the end node is never reached, return 0.",
    },
    "path-with-minimum-effort": {
        "key_insights": "- The problem asks for a path minimizing the maximum step difference instead of the sum of weights.\n\n- This can be modeled as a graph problem where each cell is a node connected to its neighbors with edge weights equal to the absolute difference in heights.\n\n- A modified Dijkstra’s algorithm fits well since the \"cost\" of a path is determined by the maximum weight along the path rather than the sum.\n\n- Alternatively, binary search combined with a flood-fill (BFS/DFS) or Union-Find could determine the smallest threshold that connects the start and end.",
        "complexity":   "Time Complexity: O(m * n * log(m * n)) where m and n are the dimensions of the grid (each cell processed and maintained in a priority queue).\n\nSpace Complexity: O(m * n) for the distance matrix and priority queue.",
        "solution":     "The solution uses a modified Dijkstra’s algorithm. The grid is treated as a graph where each cell is a node. The cost to move from one cell to an adjacent cell is the absolute difference in their heights. However, instead of summing up costs, the cost associated with a path is determined by the maximum single-step cost encountered along that path. Using a min-heap (priority queue), we always expand the least costly (in terms of maximum step encountered so far) path. We update the cost for neighboring cells if a path with a smaller maximum difference is found. The process continues until the destination cell is reached.",
    },
    "minimum-cost-to-convert-string-i": {
        "key_insights": "- Model the allowed transformations as a directed weighted graph where nodes represent letters.\n\n- For each allowed transformation, update the edge with the minimum cost if multiple operations exist for the same pair.\n\n- Use the Floyd Warshall algorithm to compute the minimum conversion cost between every pair of letters.\n\n- For each position in the source string, if the minimal cost to convert the source letter to the target letter is not available (infinite), the conversion is impossible.\n\n- Sum up the conversion cost for each character position, applying the transformation cost computed via the graph.",
        "complexity":   "Time Complexity: O(26^3 + n), where n is the length of the strings. The 26^3 part comes from the Floyd Warshall algorithm on 26 English letters.\nSpace Complexity: O(26^2) for the cost matrix, plus O(n) for input strings.",
        "solution":     "The problem can be solved by first modeling the transformations as a graph where each node is a letter ('a'-'z'). Every allowed operation creates a directed edge from letter x to letter y with a weight equal to the operation's cost. If there are multiple operations between the same pair, we take the one with the smallest cost.\n\nAfter constructing the graph, we apply the Floyd Warshall algorithm to compute the minimum cost path between every pair of letters. This gives us the cheapest way to transform any letter into any other letter (possibly via intermediate transformations).\n\nFinally, we iterate over each index of the source string. For each character, if the source and target letters are the same, no cost is incurred. Otherwise, we add the minimum cost to transform the source letter to the target letter based on our computed graph. If any transformation is impossible (i.e., remains infinite), we return -1.",
    },
    "find-minimum-time-to-reach-last-room-i": {
        "key_insights": "- Model this as a shortest path problem on a grid where each move costs 1 second, but you might incur waiting time.\n\n- Use Dijkstra’s algorithm with a min-heap (priority queue) to efficiently track the earliest arrival times.\n\n- For each move, the effective arrival time at a neighbor is max(current time + 1, moveTime at the neighbor).\n\n- Avoid processing outdated routes by checking if a better time for a cell has already been found.",
        "complexity":   "Time Complexity: O(n * m * log(n*m)) due to processing each cell with a min-heap.\nSpace Complexity: O(n * m) for storing the distance (time) matrix.",
        "solution":     "We use a Dijkstra-like algorithm that leverages a min-heap (or priority queue) to always choose the room that can be reached the earliest. For each neighboring room, we calculate the new arrival time using 1 second for movement and possibly waiting until the room's moveTime if necessary. If this new time is better than the previously recorded time, we update and add the neighbor to our heap.",
    },
    "minimum-cost-to-make-at-least-one-valid-path-in-a-grid": {
        "key_insights": "- The problem can be viewed as finding the shortest path in a grid where moving in the designated arrow direction costs 0 and any change (i.e., moving in any other direction) costs 1.\n\n- This is a weighted graph problem with two edge costs: 0 and 1, making it a natural candidate for a 0-1 Breadth First Search (BFS) algorithm.\n\n- With 0-1 BFS, we use a dequeue (double-ended queue) to efficiently process nodes ensuring that 0-cost moves are prioritized.\n\n- The grid dimensions are limited (m, n ≤ 100), so using a BFS-based algorithm leads to a manageable number of states.",
        "complexity":   "Time Complexity: O(m * n) – Each cell is processed, and there can be at most a constant number of operations per cell with 0-1 BFS.\nSpace Complexity: O(m * n) – The auxiliary space for the cost matrix and the dequeue is proportional to the number of grid cells.",
        "solution":     "We use a 0-1 BFS approach to solve the problem. We view each cell as a node in a graph. For a given cell (i, j):\n\n- Moving in the direction indicated by the cell is free (cost 0).\n\n- Moving in any other direction requires modifying the cell’s direction (cost 1).\nA deque is used to store the cells to be processed. When a move has 0 cost, we add that cell to the front of the deque so that it is processed sooner; moves with cost 1 are added to the back. This guarantees that we always expand the current least-cost path. We maintain a cost matrix to keep track of the minimum cost achieved to reach each cell, stopping when the target cell (m-1, n-1) is reached.",
    },
    "minimum-weighted-subgraph-with-the-required-paths": {
        "key_insights": "- The problem essentially requires two paths: one from src1 to dest and one from src2 to dest.\n\n- Instead of independently finding two paths, we can leverage the observation that there exists an intermediate node v such that:\n\n- There is a shortest path from src1 to v.\n\n- There is a shortest path from src2 to v.\n\n- And there is a shortest path from v to dest.\n\n- By reversing the graph, the path from any node v to dest in the original graph becomes a path from dest to v in the reversed graph.\n\n- Running three instances of Dijkstra's algorithm (from src1, from src2, and from dest on the reversed graph) gives the minimum distance from the respective source nodes to every other node.\n\n- The answer is the minimum over all nodes v of (dist(src1, v) + dist(src2, v) + dist(v, dest)). If any part is unreachable for a given v, skip that candidate.",
        "complexity":   "Time Complexity: O(E log N), where E is the number of edges and N is the number of nodes because Dijkstra's algorithm is run three times.\nSpace Complexity: O(N + E) for the graph representation and distance arrays.",
        "solution":     "To solve the problem, we use Dijkstra's algorithm three times:\n\n- From src1 on the original graph to get distances from src1 to every node.\n\n- From src2 on the original graph to get distances from src2 to every node.\n\n- From dest on the reversed graph to get distances from every node to dest.\nFor each node v, we calculate totalCost = dist1[v] + dist2[v] + dist3[v] (where dist3[v] is the distance from v to dest calculated on the reversed graph). The answer is the minimum totalCost among all nodes v that are reachable from the respective sources. If no such candidate exists, we return -1.",
    },
    "cracking-the-safe": {
        "key_insights": "- The problem is equivalent to generating a de Bruijn sequence for n-length strings over an alphabet of size k.\n\n- Every possible n-digit combination appears exactly once as a substring in the de Bruijn sequence.\n\n- The idea is to view the problem through the lens of graph theory where nodes represent the (n-1) digit suffixes and edges correspond to possible n-digit combinations.\n\n- An Eulerian circuit (or DFS based approach) can be used to traverse every edge exactly once ensuring that all combinations are covered.",
        "complexity":   "Time Complexity: O(k^n) – The algorithm essentially explores all possible n-length combinations.\nSpace Complexity: O(k^n) – The space is primarily used to store visited edges and recursion stack.",
        "solution":     "We can solve this problem using a DFS approach to generate a de Bruijn sequence. The steps are:\n\n- Create a starting node with (n-1) zeros.\n\n- Use DFS to traverse every possible edge from the current node. An edge is represented by appending a digit to the current node.\n\n- Mark each combination (edge) as visited when you traverse it.\n\n- Append the digit to the result once the DFS call for that branch completes (backtracking).\n\n- Finally, append the starting node to the result to complete the sequence.\nThis method guarantees that every possible combination appears exactly once.",
    },
    "valid-arrangement-of-pairs": {
        "key_insights": "- Interpret each pair as a directed edge from start to end.\n\n- The problem reduces to finding an Eulerian path (or circuit) in a directed graph.\n\n- If a vertex exists with out-degree exactly one greater than in-degree, it should be the starting vertex; otherwise, any vertex can serve as a valid starting node.\n\n- Hierholzer’s algorithm is ideal to reconstruct an Eulerian path by performing a modified DFS that “peels off” cycles and ensures all edges get used exactly once.",
        "complexity":   "Time Complexity: O(N), where N is the number of pairs since every edge is visited exactly once.\nSpace Complexity: O(N) for storing the graph and the recursion/stack space for DFS.",
        "solution":     "We first build a graph mapping each start node to a list of its end nodes. To efficiently obtain and remove edges, sort each list in reverse order so that we can “pop” from the end. Next, determine a valid starting node: if there is a vertex whose out-degree is exactly one more than its in-degree, start from there. Otherwise, we pick any vertex that exists in the graph.\n\nUsing Hierholzer’s algorithm, perform a DFS:\n\n- While the current vertex has outgoing edges, pop the last edge (which is efficient thanks to our reverse-sorted order) and recursively visit the end node.\n\n- Append the current vertex to the path once all its edges are visited.\n\n- Finally, reverse the recorded path to obtain the Eulerian path.\n\nThis path will be a list of vertices. To convert it back into a list of pairs, form pairs from consecutive vertices. Since each pair is exactly the edge used in the Eulerian path and all pairs are unique, this fulfills the problem's requirements.",
    },
    "fibonacci-number": {
        "key_insights": "- The Fibonacci sequence is defined recursively.\n\n- A direct recursive solution can lead to redundant calculations.\n\n- An iterative or dynamic programming (DP) approach can efficiently compute the result.\n\n- For n in the range [0, 30], an iterative solution with O(n) time complexity is both simple and effective.",
        "complexity":   "Time Complexity: O(n)\n\nSpace Complexity: O(1)",
        "solution":     "We solve the problem using an iterative approach which computes the Fibonacci numbers starting from 0 and 1 and then iteratively builds up to F(n). This method uses constant space by only keeping track of the last two computed Fibonacci values. The algorithm is efficient and avoids the pitfalls of recursive stack overflow or repeated calculations that would result from a naive recursion.",
    },
    "maximum-length-of-pair-chain": {
        "key_insights": "- Sort the pairs based on their second (right) element to make a greedy selection.\n\n- The greedy approach ensures that by choosing the pair with the smallest end, you leave maximum space for subsequent pairs.\n\n- After sorting, iterate through the list and select a pair if its start is greater than the end value of the last selected pair.\n\n- This strategy results in the optimal solution.",
        "complexity":   "Time Complexity: O(n log n) due to the sorting step.\n\nSpace Complexity: O(1) if we ignore the space used to store the input.",
        "solution":     "The solution involves sorting the pairs by their right endpoints. Start with a variable to keep track of the current end of the chain (initialized to negative infinity) and a counter for the chain length. Iterate over the sorted pairs; if the current pair's start is greater than the current end, it can be added to the chain. Update the counter and the current end accordingly. This greedy method ensures the longest possible chain is built by always choosing the next available pair that leaves room for as many subsequent pairs as possible.",
    },
    "delete-and-earn": {
        "key_insights": "- Transform the problem by aggregating points for each unique number, similar to grouping.\n\n- Realize that if you earn points from number x, you cannot earn points from x-1 and x+1.\n\n- The problem resembles the House Robber problem where adjacent houses (numbers) cannot be robbed (taken).\n\n- Use dynamic programming to decide whether to take or skip each unique number.",
        "complexity":   "Time Complexity: O(max + n) where max is the maximum number in nums (since we iterate through a frequency array up to max) and n is the length of nums.\nSpace Complexity: O(max) for the frequency and dp arrays.",
        "solution":     "The solution begins by using a hash table (or frequency array) to compute the total points that can be earned from each unique number (i.e., points = number * frequency). Then, we convert the problem to one similar to the House Robber problem: for each number i, decide to either take dp[i-2] plus the current total points at i or skip i and take dp[i-1]. This decision is captured in the recurrence relation dp[i] = max(dp[i-1], dp[i-2] + total[i]). Finally, return dp[max_num] as the result.",
    },
    "integer-break": {
        "key_insights": "- For maximizing product, breaking the integer into smaller parts can yield a higher product than not splitting it.\n\n- Greedy insight: Breaking n into as many 3's as possible generally gives the optimal solution, with adjustment for remainders (e.g. when a remainder of 1 occurs, it is beneficial to convert 3+1 into 2+2).\n\n- A dynamic programming approach can also be used: compute maximum products for smaller integers and build up to n by considering all partitions.\n\n- Be cautious with base cases (n = 2, n = 3) to ensure correctness.",
        "complexity":   "Time Complexity: O(n^2) in the dynamic programming approach since for each integer from 2 to n, we consider all possible splits.\nSpace Complexity: O(n) because we use an array dp of size n+1 to store intermediate results.",
        "solution":     "We can solve this problem using a dynamic programming approach. Define dp[i] to be the maximum product obtainable by breaking the integer i. For each i from 3 to n, iterate through possible first splits and update dp[i] by taking the maximum of:\n\n- The product of the two parts (j * (i - j))\n\n- The product of j and the optimal product for (i - j) stored in dp[i - j]\n\nThis covers the scenario where further breaking the second part yields a higher product.\n\nAn alternative greedy approach is based on the mathematical insight that partitioning n into as many 3's as possible (with a check on the remainder) yields the maximum product. However, the DP approach is simple and direct, especially given the constraint that n is relatively small.",
    },
    "greatest-sum-divisible-by-three": {
        "key_insights": "- The problem focuses on maximizing a sum that is divisible by three.\n\n- Instead of trying all subsets which is inefficient, we can use dynamic programming to store the best possible sums for each remainder modulo 3.\n\n- The idea is to maintain a state for each remainder (0, 1, 2).\n\n- When processing each number, update the potential maximum sum for each remainder by considering the number added to previous state values.\n\n- Use a temporary copy for state update to avoid interfering with the ongoing iteration.",
        "complexity":   "Time Complexity: O(n), where n is the length of the nums array.\nSpace Complexity: O(1), since we only maintain a fixed-size array (size 3) for the dynamic programming state.",
        "solution":     "We use a dynamic programming approach where we maintain an array, dp, of size 3. Each index i in dp represents the maximum sum we can achieve that has a remainder i when divided by three. Initially, dp[0] is 0 (since a sum of 0 is divisible by three) and the other elements are set to a very low number to indicate that those remainders have not been achieved. For each number in the input list, we compute new possible sums for each remainder by adding this number to the existing sums. We update our dp array accordingly while ensuring that we do not override the results of the current iteration with the updated values immediately. Finally, dp[0] will contain the maximum sum divisible by three.",
    },
    "number-of-ways-to-paint-n-3-grid": {
        "key_insights": "- Each row of 3 cells (with the horizontal-adjacency restriction) can be painted in 12 valid ways.\n\n- These 12 colorings fall into two categories:\n\n- Type A: Patterns in which all 3 cells have distinct colors (6 possibilities).\n\n- Type B: Patterns in which the first and third cell share the same color (6 possibilities).\n\n- The valid painting for the next row depends on the pattern category of the previous row. By carefully analyzing the vertical constraints (avoiding same color in the same column) alongside the horizontal condition within the row, we can derive transition rules between the two types:\n\n- From a Type A row:\n\n- Next row can be Type A in 3 ways.\n\n- Next row can be Type B in 2 ways.\n\n- From a Type B row:\n\n- Next row can be Type A in 2 ways.\n\n- Next row can be Type B in 2 ways.\n\n- Use dynamic programming to update the number of ways row by row based on these transitions.",
        "complexity":   "Time Complexity: O(n) – we iterate once through the n rows.\nSpace Complexity: O(1) – only a constant amount of memory is used to store the counts for the two groups.",
        "solution":     "We solve the problem using a dynamic programming approach with two state variables:\n\n- dpA: number of ways to paint the current row with a Type A (all different) pattern.\n\n- dpB: number of ways to paint the current row with a Type B (first and third same) pattern.\n\nFor the first row, both dpA and dpB are 6 (total 12 valid patterns). For each subsequent row, we calculate:\n\n- nextA = 3 * dpA (transitions from a previous Type A) + 2 * dpB (transitions from a previous Type B).\n\n- nextB = 2 * dpA (transitions from a previous Type A) + 2 * dpB (transitions from a previous Type B).\n\nAfter updating these values for each row, the final answer is (dpA + dpB) modulo 10^9 + 7. This solution uses constant space and processes each row in constant time.",
    },
    "last-stone-weight-ii": {
        "key_insights": "- The problem can be reinterpreted as a partition problem where we divide the stones into two groups, and the result is the difference between the sums of these two groups.\n\n- The optimal result is achieved when the difference between the two group sums is minimized.\n\n- By framing the problem this way, it becomes a variant of the classic subset sum / knapsack problem.\n\n- Use Dynamic Programming (DP) to find the best achievable subset sum that does not exceed half of the total sum of all stones.",
        "complexity":   "Time Complexity: O(n * S) where n is the number of stones and S is the total sum of stones.\nSpace Complexity: O(S), where S is the total sum of stones (DP array space).",
        "solution":     "We convert the problem into a subset sum problem:\n\n- Compute the total sum of the stones.\n\n- Use a DP array to record which subset sums are possible.\n\n- Iterate through each stone and update the DP array in reverse order to ensure each stone is used only once.\n\n- Find the largest subset sum (s1) that does not exceed half of the total sum.\n\n- The answer is the difference between the total sum and twice this subset sum (total - 2 * s1), representing the minimum remaining weight.",
    },
    "ones-and-zeroes": {
        "key_insights": "- Recognize this problem as a 0/1 knapsack variant with two dimensions (number of zeros and ones acting as weight limits).\n\n- Use dynamic programming with a 2D dp array where dp[i][j] represents the maximum subset size achievable with i zeros and j ones.\n\n- Process each binary string by counting its zeros and ones, and update the dp array in reverse to avoid interference.\n\n- Using reverse iteration ensures that each string is considered only once (mimics the 0/1 knapsack pattern).",
        "complexity":   "Time Complexity: O(l * m * n) where l is the number of strings, and m, n are the limits for zeros and ones respectively.\nSpace Complexity: O(m * n) due to the 2D dp array used for dynamic programming.",
        "solution":     "We use a dynamic programming (DP) approach with a two-dimensional dp array of size (m+1) x (n+1). The entry dp[i][j] holds the maximum number of strings that can be included in the subset with at most i zeros and j ones. For each string in the array, count the number of zeros and ones. Then, update the dp state in reverse order to ensure that each string is used only once. This reverse update is crucial to prevent overcounting since each state depends on previous states before the string was considered. The algorithm ensures that by the end, dp[m][n] holds the size of the largest possible subset.",
    },
    "profitable-schemes": {
        "key_insights": "- Use dynamic programming to model the state as (members used, current profit).\n\n- Since profit can exceed minProfit, cap the profit dimension at minProfit (any excess is treated as meeting the requirement).\n\n- Iterate over the list of crimes in a reverse order (to avoid overwriting needed previous values).\n\n- The DP state update involves including or excluding the current crime from the scheme.\n\n- The answer is the sum over all states where profit is at least minProfit with any number of members.",
        "complexity":   "Time Complexity: O(number_of_crimes * n * minProfit)\n\nSpace Complexity: O(n * minProfit)",
        "solution":     "We use a 2D DP approach where dp[member][profit] represents the number of schemes using exactly 'member' people and achieving exactly 'profit' (capped at minProfit). For each crime with required people and profit, we update the dp table in reverse order with respect to the number of people (to avoid double counting usages within the same iteration). The profit index is updated as: newProfit = min(minProfit, current profit + profit from crime) so that any profit above the threshold is treated the same as exactly minProfit. Finally, summing over dp[i][minProfit] for all valid i (i is range [0, n]) gives the final result.",
    },
    "perfect-squares": {
        "key_insights": "- The problem is similar to making change with coins, where the \"coins\" are the perfect squares.\n\n- Dynamic Programming (DP) is an effective approach by building up the solution for each number from 1 to n.\n\n- Breadth-First Search (BFS) can also be used to treat each sum as a state and find the shortest path.\n\n- The constraint 1 &#x3C;= n &#x3C;= 10^4 allows an O(n * sqrt(n)) solution to be efficient.",
        "complexity":   "Time Complexity: O(n * sqrt(n))\n\nSpace Complexity: O(n)",
        "solution":     "The solution uses dynamic programming. We construct an array dp where dp[i] represents the minimum number of perfect squares that sum up to i. For each number i from 1 to n, we iterate over all perfect squares jj that are less than or equal to i and update dp[i] to be the minimum of its current value or 1 + dp[i - jj]. This builds up the solution from the base case dp[0] = 0.",
    },
    "minimum-cost-for-tickets": {
        "key_insights": "- Use dynamic programming to build up a solution using the minimum cost required up to each day.\n\n- For each travel day, consider purchasing a 1-day, 7-day, or 30-day pass and choose the one that minimizes the total cost.\n\n- Skip days without travel since they do not affect the overall cost.\n\n- Use a DP array keyed by day or simulate the process using pointers with the travel days list.",
        "complexity":   "Time Complexity: O(n + last_day) where n is the number of travel days and last_day is the maximum day value. In practice, O(365) due to the year constraint.\nSpace Complexity: O(last_day) which is O(365) in the worst-case scenario.",
        "solution":     "The solution uses dynamic programming. We maintain a DP array (or dictionary) where dp[d] represents the minimum cost to cover all travel days up to day d. For each day d from 1 to the last travel day:\n\n- If d is not a travel day, then dp[d] = dp[d-1] because no additional cost is incurred.\n\n- If d is a travel day, then we calculate:\n\n- The cost if a 1-day pass is purchased today: dp[d-1] + costs[0].\n\n- The cost if a 7-day pass is used: dp[max(0, d-7)] + costs[1].\n\n- The cost if a 30-day pass is used: dp[max(0, d-30)] + costs[2].\n\n- We then set dp[d] as the minimum of these three computed values.\n\nThis DP approach ensures that we account for the overlapping intervals provided by the different passes and choose the optimal cost at each travel day.",
    },
    "number-of-longest-increasing-subsequence": {
        "key_insights": "- Use dynamic programming to keep track of both the length of the longest increasing subsequence (LIS) ending at each index and the number of ways to achieve that length.\n\n- Maintain two arrays: one (dp) for the length of the LIS ending at the current index, and another (count) for the number of such LIS ending at that index.\n\n- For each element, iterate over all previous elements; if a previous element is less than the current one, update the dp and count arrays.\n\n- After processing, the result is the sum of counts for indices where the dp value equals the maximum length found.",
        "complexity":   "Time Complexity: O(n²)\n\nSpace Complexity: O(n)",
        "solution":     "The solution uses dynamic programming with two auxiliary arrays. The dp array stores the length of the longest increasing subsequence ending at each index. The count array stores the number of ways to achieve that length at each index.\n\nFor every index i, loop through all previous indices j. If nums[j] is less than nums[i], meaning an increasing sequence can be formed:\n\n- If extending the sequence from j gives a longer subsequence than previously recorded for i (i.e., dp[j] + 1 > dp[i]), update dp[i] and set count[i] equal to count[j].\n\n- If it equals the already found length (i.e., dp[j] + 1 == dp[i]), then add count[j] to count[i] because another sequence of the same length is found.\n\nFinally, the answer is computed by summing all count[i] for which dp[i] equals the maximum value in dp.",
    },
    "longest-string-chain": {
        "key_insights": "- Sort the words by length so that any potential predecessor appears before its longer successor.\n\n- Use dynamic programming where for each word, you calculate the maximum chain ending with that word by checking all possible words formed by deleting one letter.\n\n- A hash table (map/dictionary) allows you to quickly store and update the chain lengths for each word.\n\n- The final answer is the maximum chain length found over all words.",
        "complexity":   "Time Complexity: O(N * L^2) where N is the number of words and L is the maximum word length.\nSpace Complexity: O(N * L) due to storage in the hash table for chain lengths and intermediate strings.",
        "solution":     "The approach is to use dynamic programming. First, sort the words by their lengths so that any possible predecessor comes before the word itself. Then, initialize a hash table where the key is the word and the value is the longest chain ending at that word. For each word, generate all possible predecessors by deleting one character at a time, and if the predecessor exists in our hash table, update the current word’s chain length by taking the maximum chain value from its predecessors plus one. Finally, the answer is the maximum value stored in the hash table.",
    },
    "longest-arithmetic-subsequence": {
        "key_insights": "- Use dynamic programming where dp[i][diff] represents the length of the longest arithmetic subsequence ending at index i with difference diff.\n\n- For every pair of indices (j, i) with j &#x3C; i, compute the difference diff = nums[i] - nums[j].\n\n- If there exists a subsequence ending at j with the same difference, extend it; otherwise, start a new subsequence with length 2.\n\n- Finally, keep track of the maximum subsequence length encountered.",
        "complexity":   "Time Complexity: O(n^2) where n is the number of elements in the array.\nSpace Complexity: O(n^2) in the worst case due to storing differences for each index.",
        "solution":     "We iterate over the array with a nested loop. For each pair (j, i), we calculate the difference diff = nums[i] - nums[j] and update our dp array: if there is an entry dp[j][diff], then dp[i][diff] = dp[j][diff] + 1; otherwise, dp[i][diff] is set to 2 (since any two numbers form an arithmetic sequence). The answer is the maximum value in our dp structure. We use a hash table (dictionary) for each index to store the counts of arithmetic subsequences by their common difference.",
    },
    "russian-doll-envelopes": {
        "key_insights": "- Sort envelopes by width in ascending order and, if the widths are equal, by height in descending order.\n\n- Sorting in this manner allows us to avoid conflicts when envelopes share the same width.\n\n- After sorting, the problem reduces to finding the Longest Increasing Subsequence (LIS) based solely on heights.\n\n- Utilize binary search within the LIS algorithm to achieve an efficient O(n log n) solution.",
        "complexity":   "Time Complexity: O(n log n), where n is the number of envelopes.\n\nSpace Complexity: O(n), required for the dynamic list used in the LIS solution.",
        "solution":     "The algorithm first sorts the envelopes by their width in increasing order. For envelopes with the same width, sorting by height in descending order ensures that only one envelope of the same width can be considered in the nesting sequence. Then, we extract the heights and use a binary search-based approach to compute the longest increasing subsequence (LIS) of heights, which represents the maximum number of envelopes we can nest. The list maintained during the LIS calculation stores the smallest possible tail for increasing subsequences of different lengths, allowing efficient updating and extension of potential sequences.",
    },
    "unique-paths-ii": {
        "key_insights": "- Use Dynamic Programming (DP) to build up the solution by calculating the number of paths to reach each cell.\n\n- For any cell, if it is not an obstacle, the number of paths to reach it equals the sum of the paths from the cell directly above and the cell to the left.\n\n- Cells containing obstacles are set to 0, meaning no path can go through them.\n\n- Special handling is required for the first row and first column since the robot can only come from one direction for those cells.\n\n- If the starting cell has an obstacle, the answer is immediately 0.",
        "complexity":   "Time Complexity: O(m * n) - Each cell is processed once.\n\nSpace Complexity: O(m * n) - Using an auxiliary grid for DP. An optimization to O(n) space is possible by reusing a single row.",
        "solution":     "We use a 2D dynamic programming approach where a dp array is maintained with the same dimensions as the grid. Initially, dp[0][0] is set to 1 if the starting cell is free, otherwise 0. The first row and first column are filled based on the absence of obstacles until a blockage is met. Then, for every other cell, if it is free, the value in dp[i][j] is computed as the sum of dp[i-1][j] (from above) and dp[i][j-1] (from left). If the cell is an obstacle, the value is 0. This continues until the bottom-right cell is computed.",
    },
    "triangle": {
        "key_insights": "- Use dynamic programming to reduce overlapping subproblems.\n\n- A bottom-up approach is most efficient by calculating the minimum path sums from the bottom row upward.\n\n- This method reduces extra space usage to O(n) where n is the number of rows.\n\n- The solution iteratively updates a dp array that eventually holds the minimum path sum at its first index.",
        "complexity":   "Time Complexity: O(n^2) where n is the number of rows (each element is processed once).\nSpace Complexity: O(n) extra space is used for the dp array storing one row of results.",
        "solution":     "A bottom-up dynamic programming approach is used to handle this problem. First, initialize a dp array as a copy of the last row of the triangle. Then, starting from the second-to-last row, update each element in the dp array by adding the current triangle element to the minimum of the two adjacent numbers from the row below (already stored in dp). By the time you reach the top row, dp[0] holds the minimum total path sum. The dp array is updated in place to ensure an O(n) space solution.",
    },
    "count-square-submatrices-with-all-ones": {
        "key_insights": "- Use dynamic programming to solve the problem.\n\n- Create a dp table where each dp[i][j] represents the size of the largest square that ends at cell (i, j).\n\n- For a cell containing one, the potential square size is determined by taking the minimum of its top, left, and top-left neighbors, then adding one.\n\n- Accumulate the value of dp[i][j] to get the total count of square submatrices with all ones.\n\n- Handling boundary conditions where the cell is in the first row or column is essential.",
        "complexity":   "Time Complexity: O(m * n)\n\nSpace Complexity: O(m * n) (this can be optimized to O(n) if modifying the original matrix is allowed)",
        "solution":     "We solve the problem using dynamic programming. For each cell in the matrix that contains a one, we determine the size of the maximal square submatrix ending at that cell by looking at its top, left, and top-left neighbors in the dp table. The cell's dp value will be one plus the minimum of these three values. The final answer is the sum of all values in the dp table, which represent the count of squares ending at each position.",
    },
    "maximum-number-of-points-with-cost": {
        "key_insights": "- Use dynamic programming to store the best achievable score ending at each column.\n\n- Transition between rows must incorporate the cost of changing columns, given by abs(c1 - c2).\n\n- Precompute two helper arrays (left and right) for each row to capture the maximum score obtainable from left-to-right and right-to-left, effectively handling the absolute difference cost in O(n) time per row.\n\n- This optimized transition allows for solving the problem efficiently without using a nested loop for cost computation.",
        "complexity":   "Time Complexity: O(m * n), where m is the number of rows and n is the number of columns.\nSpace Complexity: O(n), since only one-dimensional arrays (dp, left, right) are maintained for computations.",
        "solution":     "The solution involves dynamic programming by tracking the maximum score achievable at each cell of the current row. For each row beyond the first, two helper arrays, left and right, are computed:\n\n- The left array is built by iterating from left to right, adjusting the score by subtracting 1 for each move to the right.\n\n- The right array is built by iterating from right to left, similarly subtracting 1 for each move to the left.\nThen, for every cell in the new row, the best possible previous score is added from either the left or right helper array along with the current cell's points. This trick avoids the need for an inner loop to compute the absolute difference cost for every pair of columns, thereby optimizing the transition.",
    },
    "minimum-falling-path-sum": {
        "key_insights": "- Use dynamic programming to calculate the minimum path sum from bottom to top.\n\n- For each cell, only consider the three possible moves in the next row (down, down-left, down-right), making sure to handle boundary conditions.\n\n- A bottom-up approach allows us to build the solution by using the already computed results of the subproblems.\n\n- The final result is the minimum value in the computed first row.",
        "complexity":   "Time Complexity: O(n^2) because each of the n*n elements is processed once.\nSpace Complexity: O(n^2) using an auxiliary DP matrix, though the solution can be optimized to O(1) extra space by updating the matrix in-place.",
        "solution":     "We solve this problem using dynamic programming with a bottom-up approach. We initialize a DP structure (or update the matrix in-place) that stores the minimum falling path sum from each cell to the bottom row. Starting from the second last row, we iterate upward. For each cell (i, j), we add the current cell value with the minimum of the allowed moves from the row below (directly below, diagonally left, and diagonally right). Consider boundary conditions for the first and last columns. The answer is the minimum value found in the top row after processing.",
    },
    "remove-boxes": {
        "key_insights": "- This is a hard optimization problem with overlapping sub-problems.\n\n- The heart of the solution is dynamic programming with memoization.\n\n- The key is to consider not just the boxes between indices l and r, but also how many same-colored boxes adjacent to the segment can be combined (parameter k).\n\n- Use a 3-dimensional DP state dp(l, r, k) representing the maximum points achievable from range [l, r] with k extra boxes (of the same color as box l) appended from the left.\n\n- When the first box has consecutive boxes of the same color, they can be aggregated to simplify the state.\n\n- Properly splitting the array at optimal positions where a future same-colored box appears is essential to combine points.",
        "complexity":   "Time Complexity: O(n^3) in the worst-case scenario due to the three nested parameters (l, r, and k), where n is the length of boxes.\nSpace Complexity: O(n^3) for memoization storage.",
        "solution":     "The solution uses recursion with memoization (a top-down dynamic programming approach). For a subarray from index l to r with k extra boxes of the same color as boxes[l] appended, calculate the maximum points as follows:\n\n- First, merge all consecutive boxes at the beginning that are the same as boxes[l] by increasing k and moving l forward.\n\n- Remove the merged block immediately and calculate points as (k+1)^2 plus the result of solving the remaining subarray.\n\n- Try to find any index m in [l+1, r] where boxes[m] equals boxes[l]. By not removing boxes[l] immediately, we first remove the boxes in between (l+1 to m-1) and then merge boxes[l] with boxes[m]. This may yield a higher score.\n\n- Use memoization to avoid repeated calculations for the same state (l, r, k).\n\nThe trick is in the merge step that optimally leverages same-colored boxes that are separated by other boxes.",
    },
    "maximal-rectangle": {
        "key_insights": "- Transform each row into a histogram, where the height at each column is the count of consecutive '1's encountered so far.\n\n- Use a monotonic stack algorithm to compute the largest rectangle area within this histogram.\n\n- Iterate row by row, updating histogram heights and computing the maximal area based on the updated histogram.\n\n- This reduces a 2D problem into repeatedly solving the Largest Rectangle in Histogram problem.",
        "complexity":   "Time Complexity: O(m*n), where m is the number of rows and n is the number of columns, since each histogram is processed in O(n) for each row.\nSpace Complexity: O(n), needed for the histogram and the stack used in the algorithm.",
        "solution":     "The algorithm works by processing each row of the matrix and updating a histogram that represents the number of consecutive '1's for each column up to that row. For every row, we compute the largest rectangle area in the histogram by using a monotonic stack:\n\n- Iterate through each column index in the histogram.\n\n- Use a stack to store column indices such that the heights are in non-decreasing order.\n\n- When a height less than the current height is encountered, pop indices from the stack and calculate the area using the height at the popped index and the current index as the right boundary.\n\n- After processing, ensure remaining indices in the stack are handled.\n\n- The maximum area found over all rows is returned as the answer.",
    },
    "cherry-pickup": {
        "key_insights": "- Transforming the two trips (out and back) into one simultaneous traversal by having two agents start at (0,0) and move to (n-1,n-1) concurrently.\n\n- Since both agents make the same number of moves, their positions satisfy r1+c1 = r2+c2, which allows reducing the state dimension.\n\n- Use dynamic programming with a 3-dimensional state (r1, c1, r2) while computing c2 as (r1+c1-r2).\n\n- Avoid double counting the cherry if both agents land on the same cell.\n\n- Return 0 if no valid path exists (i.e. if the computed maximum is negative).",
        "complexity":   "Time Complexity: O(n^3) where n is the grid dimension.\nSpace Complexity: O(n^3) for the DP memoization table.",
        "solution":     "The solution employs a dynamic programming approach by simulating two agents moving simultaneously from the start to the destination. Each state is defined by (r1, c1, r2) with c2 computed from the invariant r1+c1 = r2+c2. The algorithm explores all the valid combinations of right and down moves for both agents. When both agents are on the same cell, its cherry (if present) is only counted once. If any move leads to an invalid cell or a thorn, that path is considered as -infinity. A memoization table caches computed states to avoid redundant calculations. The final answer is the maximum cherries collected along the optimal path from start to finish, or 0 if no valid path exists.",
    },
    "dungeon-game": {
        "key_insights": "- Use Dynamic Programming with a reverse (bottom-up) approach starting from the princess’s cell.\n\n- At each cell, compute the minimum health needed to ensure survival until reaching the princess.\n\n- The required health at any cell is max(minimum necessary health from the right or down cell minus the current cell value, 1).\n\n- Boundary initialization is key; set extra row and column cells to a high value (or infinity) except the positions adjacent to the princess’s cell set to 1.",
        "complexity":   "Time Complexity: O(m * n)\n\nSpace Complexity: O(m * n)",
        "solution":     "We solve the problem by initializing a DP table where dp[i][j] represents the minimum health needed at cell (i, j) to eventually reach the princess. We initialize the dp table for boundaries to simulate an “exit” from the dungeon cell with a minimum requirement of 1 health. Then we iterate from the bottom-right cell (princess’s cell) backwards to the top-left cell. For each cell, compute the minimum health needed based on the minimum of the health requirements of the right and bottom neighbors, ensuring that the value is at least 1. This approach guarantees that the knight’s health never falls to 0 or below at any point.",
    },
    "painting-a-grid-with-three-different-colors": {
        "key_insights": "- Since m is small (m ≤ 5) but n can be large (n ≤ 1000), it is efficient to treat each column as a state.\n\n- Precompute all valid color configurations for a single column (ensuring vertical adjacent cells differ).\n\n- For any two consecutive columns, ensure that colors in corresponding rows (horizontal adjacent cells) are different.\n\n- Use dynamic programming where dp[i] represents the number of ways to paint up to the current column ending with the i‑th valid state.\n\n- Precompute valid transitions between states to avoid repeated checks.",
        "complexity":   "Time Complexity: O(n * S^2) where S is the number of valid states (at most 3 * 2^(m-1)).\n\nSpace Complexity: O(S^2 + S) for storing state transition information and DP arrays.",
        "solution":     "We solve the problem using a dynamic programming (DP) approach on columns:\n\n- Generate all valid states for a column of height m ensuring vertical adjacent cells are painted with different colors.\n\n- Precompute a 2D boolean matrix capturing whether state i is compatible with state j for adjacent columns (i.e., for every row, the color in state i is different from the color in state j).\n\n- Initialize the DP array with 1 for each valid state as the first column can be painted in any valid configuration.\n\n- For each subsequent column, update the DP counts using the precomputed transitions.\n\n- Finally, return the sum of the DP array values modulo 10^9 + 7.\n\nBelow are solutions in Python, JavaScript, C++, and Java with detailed comments.",
    },
    "frog-jump": {
        "key_insights": "- Use dynamic programming to track possible jump sizes that can land the frog on each stone.\n\n- Leverage a hash-map (or dictionary) to map each stone's position to a set of possible jump sizes that can arrive there.\n\n- The valid next jump sizes from a stone reached using jump k are (k-1), k, and (k+1), ensuring the jump is >0.\n\n- Early termination is possible as soon as the last stone is reached.",
        "complexity":   "Time Complexity: O(n^2) in the worst-case scenario, where n is the number of stones, because for each stone we may explore up to three possible jump sizes.\nSpace Complexity: O(n^2) in the worst-case scenario, due to the storage of potential jump sizes for each stone in the hash-map.",
        "solution":     "The solution uses a dynamic programming approach by keeping track of all possible jump lengths that can reach every stone using a hash-map. Starting from the first stone with a known jump size (0 initially, to allow the first jump of 1 unit), for each stone, the algorithm computes the next possible stones that can be reached by jumps of size k-1, k, and k+1. If a jump results in landing on a valid stone, that jump size is stored for that stone. The process continues until the last stone is reached, in which case the function returns true. If the entire process finishes without reaching the last stone, then the answer is false.",
    },
    "optimal-account-balancing": {
        "key_insights": "- Compute the net balance for each person.\n\n- Only consider persons with non-zero balance, since persons with a zero net balance do not affect the result.\n\n- Use backtracking (DFS) to try settling debts between pairs of people.\n\n- Prune redundant operations by skipping similar debt values in subsequent recursive calls.\n\n- The problem size is small (max 12 persons and 8 transactions) so a DFS/backtracking solution is practical.",
        "complexity":   "Time Complexity: O(2^n) in the worst-case scenario, where n is the number of non-zero balances.\nSpace Complexity: O(n) due to recursion and the balance array.",
        "solution":     "We first compute each person's net balance. Then, we use a depth-first search (DFS) approach to try and settle each outstanding balance with minimal transactions. In the DFS, we attempt to settle the debt of one person with another by transferring money and recursively exploring the outcome. If a debt is completely settled, we move on to the next balance. To optimize, we skip over redundant operations when we have already tried to settle with a similar debt value. We employ backtracking to restore the original state after trying each settlement possibility. This recursive strategy allows us to explore all combinations while keeping track of the minimum number of transactions required.",
    },
    "minimum-difficulty-of-a-job-schedule": {
        "key_insights": "- Use dynamic programming to break the problem into subproblems by scheduling the first i jobs over a specific number of days.\n\n- Define a DP state dp[i][day] representing the minimum total difficulty for scheduling the first i jobs in \"day\" days.\n\n- Transition by iterating backwards to form the current day’s block, updating the maximum difficulty as you include more jobs.\n\n- The answer is dp[n][d], where n is the total number of jobs.\n\n- Edge case: if the number of jobs is less than d, it's impossible to schedule, return -1.",
        "complexity":   "Time Complexity: O(n^2 * d) – Iterating over days and for each, iterating over all possible partitions.\nSpace Complexity: O(n * d) – For maintaining the DP table.",
        "solution":     "We utilize a 2D DP array to store our intermediate results. For every day (from 1 to d) and every job index, we update the DP table by considering all valid partitions where the current day receives a block of consecutive jobs. As we backtrack, we maintain the maximum difficulty encountered for the current day, and combine it with the previous optimal schedule stored in the DP table. This ensures that each partition minimizes the total difficulty across all days.",
    },
    "minimum-cost-to-cut-a-stick": {
        "key_insights": "- Sort the list of cuts and include the two boundaries (0 and n) to enable easier segment computation.\n\n- Use dynamic programming where dp[i][j] represents the minimum cost to cut the stick between positions[i] and positions[j].\n\n- The cost to perform a cut is the length of the current segment (positions[j] - positions[i]).\n\n- For each segment, try each cut between the boundaries to determine the optimal cost using the recurrence:\n\n  dp[i][j] = min(dp[i][k] + dp[k][j] + (positions[j] - positions[i])) for every k such that i &#x3C; k &#x3C; j.\n\n- The problem exhibits optimal substructure, making it suitable for a bottom-up DP approach.",
        "complexity":   "Time Complexity: O(m^3), where m is the number of cut positions plus two (boundaries). Given that m is at most 102, this cubic time is acceptable.\nSpace Complexity: O(m^2) to store the dynamic programming table.",
        "solution":     "We approach this problem using a bottom-up dynamic programming algorithm. First, sort the cuts array and add the boundaries 0 and n. This results in an array positions where positions[0] = 0 and positions[m-1] = n. Then, define dp[i][j] as the minimum cost to cut the segment from positions[i] to positions[j]. For each interval, try every possible intermediate cut position k (where i &#x3C; k &#x3C; j) to determine the cost of cutting that segment. The recurrence relation is:\n\n  dp[i][j] = min(dp[i][k] + dp[k][j] + (positions[j] - positions[i])), for every valid k\n\nInitialize dp[i][j] = 0 for segments that have no cuts between them (i.e. j = i+1). Build the solution from smaller segments up to the entire stick. Finally, the answer is dp[0][m-1].",
    },
    "minimum-deletions-to-make-string-balanced": {
        "key_insights": "- A balanced string means every 'a' must come before every 'b'.\n\n- When scanning the string, conflicts occur when an 'a' appears after a 'b'.\n\n- At each 'a', we face a choice: delete the current 'a' or delete all preceding 'b's.\n\n- By maintaining a counter for 'b's and tracking the minimum deletions required so far, we can decide optimally at each step.\n\n- This approach leverages a greedy/dynamic programming method with a single pass.",
        "complexity":   "Time Complexity: O(n) where n is the length of the string.\nSpace Complexity: O(1) since only a few variables are used.",
        "solution":     "The solution iterates through the string while keeping track of two variables: countB (the number of 'b's encountered so far) and deletionCount (the minimum deletions required up to the current point). For each character:\n\n- If the character is 'b', increment countB.\n\n- If the character is 'a', update deletionCount to be the minimum of either deleting this 'a' (deletionCount + 1) or deleting all previous 'b's (which is countB).\nThis strategy ensures that the final deletionCount reflects the minimum number of deletions needed to achieve a balanced string.",
    },
    "word-break-ii": {
        "key_insights": "- Use backtracking (DFS) to try splitting the string at every possible index.\n\n- Utilize memoization to store the list of valid sentences starting from a given index to avoid redundant computations.\n\n- The problem is naturally solved through recursion with memoization (a top-down dynamic programming approach).",
        "complexity":   "Time Complexity: O(n^2 * m) in the worst case, where n is the length of the string and m is the average number of words found per recursion; memoization helps to reduce repeated work.\nSpace Complexity: O(n * m) for the recursion stack and memoization storage, where m represents the average length of the sentence list from each index.",
        "solution":     "We use a depth-first search (DFS) that tries every possible break point in the string. At each index, if the current substring is in the dictionary, we recursively solve for the remainder of the string. Memoization is used to cache results for each index to avoid redundant computations. The solution builds the final sentences by concatenating the current word with the sentence fragments returned by the recursive call.",
    },
    "wildcard-matching": {
        "key_insights": "- Use dynamic programming to track matching status between prefixes of s and p.\n\n- When p[j] is a regular character or '?', check for character match (or any character match for '?').\n\n- When p[j] is '*', it can match an empty sequence (dp[i][j-1]) or extend a previous match (dp[i-1][j]).\n\n- A greedy two-pointer approach can also be considered, but the DP solution is clearer for handling worst-case scenarios.",
        "complexity":   "Time Complexity: O(n * m), where n is the length of s and m is the length of p.\nSpace Complexity: O(n * m), due to the DP table used to store matching results.",
        "solution":     "We solve the problem using a dynamic programming approach by constructing a boolean table dp, where dp[i][j] indicates whether the first i characters of s match the first j characters of p. The recurrence is as follows:\n\n- If p[j-1] is a normal character or '?', then dp[i][j] = dp[i-1][j-1] if the current characters match (or p[j-1] is '?').\n\n- If p[j-1] is '', then dp[i][j] is true if either the star matches no characters (dp[i][j-1]) or if it matches one more character (dp[i-1][j]).\nA careful initialization of the dp table is required, especially for patterns that start with '' which can match empty strings.",
    },
    "strange-printer": {
        "key_insights": "- Use dynamic programming where dp[i][j] represents the minimum turns needed to print the substring s[i...j].\n\n- Base case: dp[i][i] = 1, since a single character takes one turn.\n\n- If s[i] and s[j] are the same, the last print can cover both, potentially reducing the turn count.\n\n- For every substring, try all possible partitions and combine results, taking advantage of matching characters to merge operations.",
        "complexity":   "Time Complexity: O(n^3) in the worst case because of three nested loops to fill in the dp table.\nSpace Complexity: O(n^2) due to the dp table storing solutions for each substring.",
        "solution":     "We use a 2D dynamic programming table dp where dp[i][j] indicates the minimum turns needed to print substring s[i...j]. Initialize dp[i][i] = 1 for all i. For each substring of length >=2, set dp[i][j] = dp[i][j-1] + 1 by default (printing s[j] separately). Then, iterate through every index k from i to j - 1; if s[k] equals s[j], merge the operation by calculating dp[i][j] = min(dp[i][j], dp[i][k] + dp[k+1][j-1]) while handling edge cases. This approach systematically breaks down the problem into smaller overlapping subproblems.",
    },
    "shortest-common-supersequence": {
        "key_insights": "- The problem can be solved by leveraging the concept of the Longest Common Subsequence (LCS).\n\n- By first finding the LCS of str1 and str2, we can merge the two strings while ensuring that common parts (LCS) are not repeated unnecessarily.\n\n- Dynamic Programming is used to compute the LCS.\n\n- Once the LCS is available, the final result is built by interleaving characters from str1 and str2 while aligning with the LCS.",
        "complexity":   "Time Complexity: O(m * n), where m and n are the lengths of str1 and str2 respectively.\nSpace Complexity: O(m * n) for the DP table to store LCS lengths, with additional space for the output string.",
        "solution":     "The solution is divided into two main steps:\n\n- Compute the LCS of the two strings using a Dynamic Programming table. This table dp[i][j] represents the length of the LCS of str1[0...i-1] and str2[0...j-1].\n\n- Reconstruct the shortest common supersequence by traversing the two strings in parallel and using the LCS as a guide. For every character in the LCS, append all non-LCS characters from str1 and str2 until that common character is reached, then add the LCS character. Append any remaining characters after processing the LCS.\n\nThe key observation is that by including the non-matching parts between matched LCS characters, we ensure both strings are subsequences of the constructed result, and the result is the shortest possible.",
    },
    "house-robber-iii": {
        "key_insights": "- The problem is modeled on a binary tree where each node represents a house.\n\n- If a node is robbed, its immediate children cannot be robbed.\n\n- Using DFS (Depth First Search) allows us to explore the tree and decide for each node whether to rob it or not.\n\n- At each node, compute two values: one when the node is robbed and one when it is not.\n\n- The optimal solution at each node is derived by considering the maximum money collected from its subtrees, taking the constraints into account.",
        "complexity":   "Time Complexity: O(n) where n is the number of nodes, as each node is visited once.\nSpace Complexity: O(n) due to the recursion call stack in the worst-case skewed tree.",
        "solution":     "We use a DFS-based recursive strategy to solve the problem. For each node, compute two scenarios:\n\n- Robbing the current node: In this case, we cannot rob its children. So, the value is node.val plus the sum of values from not robbing its left and right children.\n\n- Not robbing the current node: In this case, we can rob or not rob its children based on which option yields more money from each subtree.\nBy returning these two values (rob, notRob) for each node and combining them from the bottom-up, the solution picks the optimal strategy at the root. Key data structures include recursion (function call stack) and tuples/pairs to maintain the two states per node.",
    },
    "unique-binary-search-trees-ii": {
        "key_insights": "- Utilize a recursive (divide and conquer) approach where each number in the range can be chosen as the root.\n\n- For a chosen root, all numbers less than the root become part of the left subtree, and all numbers greater become part of the right subtree.\n\n- Recursively generate all valid left and right subtrees then combine them to form unique BSTs.\n\n- Base case: When the starting index exceeds the ending index, return a list containing None (or null in other languages) to represent an empty tree.\n\n- The problem essentially generates trees corresponding to Catalan numbers.",
        "complexity":   "Time Complexity: O(n * Catalan(n))\n\nSpace Complexity: O(n * Catalan(n)) (space used to store all generated trees and recursion stack)",
        "solution":     "We use a recursive function that accepts a range (start, end) and returns all possible BSTs constructed from numbers within that range. For every possible root value within the range, we recursively generate all possible left subtrees (from start to value-1) and right subtrees (from value+1 to end). We then iterate over every combination of left and right subtree and create a new tree node with the selected root value. This tree is appended to our list of unique BSTs. Key details include handling the base case where the start index exceeds the end index (which represents an empty subtree) and combining results appropriately.",
    },
    "number-of-ways-to-arrive-at-destination": {
        "key_insights": "- The problem requires computing the shortest distance from the source (0) to the destination (n-1) in a weighted graph.\n\n- Dijkstra’s algorithm is a natural choice due to non-negative edge weights.\n\n- While computing the shortest distances, maintain a count of how many ways each node can be reached using the minimum time.\n\n- Update the count when a new shorter path is found or when another path leading to the same shortest distance is discovered.\n\n- Always perform modulus operations to handle large numbers.",
        "complexity":   "Time Complexity: O(E log V), where E is the number of roads and V is the number of intersections.\nSpace Complexity: O(V + E), which accounts for the distance array, ways array, and graph representation.",
        "solution":     "We modify Dijkstra's algorithm to not only compute the shortest path but also track the number of ways to reach each intersection using shortest possible travel times. We use:\n\n- A graph represented as an adjacency list.\n\n- A distance array (or vector) initialized with infinity for all nodes except the starting node (set to 0).\n\n- A ways array to count the number of ways to achieve the recorded shortest distance.\n\n- A priority queue (or min-heap) to efficiently fetch the next node with the smallest temporary distance.\nDuring the relaxation step for each neighbor, if a shorter path is found, update both the distance and the ways count; if an equally short path is found, add the number of new ways to the count modulo 10^9 + 7.",
    },
    "binary-tree-cameras": {
        "key_insights": "- Use a DFS (depth-first search) to traverse the tree in postorder.\n\n- For each node, classify it with one of three states:\n\n- State 0: Node has a camera.\n\n- State 1: Node is covered by a camera (child or parent) but has no camera.\n\n- State 2: Node is not covered and needs a camera.\n\n- Place a camera at the current node if any child is in state 2.\n\n- If at least one child has a camera, mark the current node as covered.\n\n- For leaf nodes (or null nodes), consider them as covered for the sake of simplifying the DFS recurrence.",
        "complexity":   "Time Complexity: O(N) where N is the number of nodes in the tree since each node is visited once.\nSpace Complexity: O(H) due to the recursion stack, where H is the height of the tree.",
        "solution":     "We solve the problem using a DFS based on postorder traversal. At every node, inspect the state of its children. Based on that, decide if the current node should have a camera, be considered covered, or require a camera from its parent. The main challenge is to correctly propagate the state information upward to minimize the number of cameras while ensuring every node is covered. This strategy uses a greedy approach on the tree structure with three states.",
    },
    "sum-of-distances-in-tree": {
        "key_insights": "- Use tree dynamic programming with two DFS traversals.\n\n- The first DFS computes, for each node, the sum of distances from that node’s subtree and the count of nodes in that subtree.\n\n- The second DFS propagates the computed distances from the root to all children using the relation that adjusts the distances based on subtree sizes.\n\n- This method avoids recomputation, leading to an efficient O(n) solution.",
        "complexity":   "Time Complexity: O(n)\nSpace Complexity: O(n) (due to tree adjacency list and recursion stack)",
        "solution":     "We use a two-step DFS approach:\n\n- In the first DFS (postorder), for each node we compute:\n\n- count[node]: number of nodes in the subtree (including itself).\n\n- res[node]: sum of distances from node to all nodes in its subtree.\n\n- In the second DFS (preorder), we use the parent’s result to update each child’s result:\n\n- When moving from parent to child, update res[child] = res[parent] - count[child] + (n - count[child]).\n\n- This transformation works because moving to a child subtracts the distance for all nodes in the child’s subtree (as they get closer by 1) and adds the distance for the rest (as they get farther by 1).\n\nThe main data structures used are an adjacency list for the tree and two arrays (or vectors) to store the count and results. The algorithm leverages recursion to do DFS traversals.",
    },
    "campus-bikes-ii": {
        "key_insights": "- The problem is a variant of the assignment problem which can be solved using Dynamic Programming (DP) with bitmasking due to the small constraints (n, m ≤ 10).\n\n- A bitmask can represent which bikes have already been assigned. For example, if m = 3, then mask = 101 means bikes 0 and 2 have been taken.\n\n- The state of the DP can be represented using two parameters: the current worker index and the bitmask representing the used bikes.\n\n- Recursively assign bikes to workers by iterating over available bikes and take the minimum cumulative Manhattan distance.\n\n- Memoization is key to avoid duplicate computations in overlapping subproblems.",
        "complexity":   "Time Complexity: O(m * 2^m) in the worst case, since for each of the 2^m states, we try assigning up to m bikes.\nSpace Complexity: O(2^m), for storing the DP results (memoization cache).",
        "solution":     "The solution uses recursion with memoization (DP with bitmasking). We define a helper function dp(worker, mask) where worker is the index of the current worker to assign a bike and mask is an integer representing the set of bikes already taken. For each worker, we iterate through all bikes and if a bike is not already assigned (checked using the bitmask), we calculate the Manhattan distance from the current worker to this bike and recursively assign the remaining workers. The result for each state is memoized to avoid recomputation. The base case occurs when all workers have been assigned bikes.",
    },
    "partition-to-k-equal-sum-subsets": {
        "key_insights": "- The total sum of the array must be divisible by k; otherwise, it’s impossible to partition it into subsets of equal sum.\n\n- The target sum for each subset is the total sum divided by k.\n\n- Sorting the array in descending order helps in pruning branches early in the backtracking process.\n\n- A backtracking (DFS) approach, optionally enhanced with memoization or bitmasking, can be used to explore possible subset partitions.\n\n- Handling duplicate numbers carefully reduces redundant work during recursion.",
        "complexity":   "Time Complexity: O(k * 2^n) in the worst-case scenario (where n is the number of elements) due to exploring subset combinations via backtracking.\nSpace Complexity: O(n) for recursion stack and additional data structures (or O(2^n) if using memoization with bitmasking).",
        "solution":     "We first check if the total sum of the array is divisible by k. Then determine the target sum for each subset. By sorting the array in descending order, we ensure that the larger numbers are placed first so that invalid configurations are pruned quickly. We use a backtracking (DFS) approach to try and assign numbers to each subset while ensuring the running sum does not exceed the target. When a subset reaches the target sum, we recursively try to fill the next subset. Care is taken to skip duplicates in order to avoid redundant recursion. This approach efficiently determines if a valid k-partition exists.",
    },
    "minimum-number-of-work-sessions-to-finish-the-tasks": {
        "key_insights": "- Use bitmasking to represent the state of completed tasks since n ≤ 14.\n\n- Apply recursion with memoization (DP) to explore task assignments.\n\n- Each DP state is represented by a bitmask (indicating tasks done) and the current session’s used time.\n\n- When a task does not fit in the current session, start a new session and update the count.\n\n- The problem can be solved with backtracking combining DP over subsets of tasks.",
        "complexity":   "Time Complexity: O(2^n * n), where n is the number of tasks.\nSpace Complexity: O(2^n) due to memoization storage.",
        "solution":     "We use a dynamic programming approach with bitmasking to represent the subsets of tasks completed. The recursive function dp(mask, timeUsed) returns the number of extra sessions required (beyond an initial session) for a given state. For every state, we try assigning each incomplete task: if it fits into the current session (timeUsed + task[i] ≤ sessionTime), we continue; otherwise, we start a new session and add 1 to our count. By caching results of each state (mask, timeUsed), we prevent redundant calculations, which makes the approach feasible given the constraints.",
    },
    "fair-distribution-of-cookies": {
        "key_insights": "- Use a backtracking approach to try every possible distribution since the number of cookie bags (n) is small (n ≤ 8).\n\n- Maintain an array representing the total cookies each child has received.\n\n- Prune paths early if the current maximum exceeds the current best solution.\n\n- The goal is to minimize the maximum sum among children across all distributions.",
        "complexity":   "Time Complexity: O(k^n) in the worst case, since we assign n bags to k children.\nSpace Complexity: O(n) due to recursion depth and auxiliary data structures.",
        "solution":     "We can solve the problem using a recursive backtracking algorithm. At each recursive call, we consider giving the current cookie bag to each child and update their totals accordingly. After processing all bags, we compute the unfairness (maximum among all children's totals) and update the global minimum if the current distribution is better. The small input size allows an exponential approach. Key structures include an array (or vector) to store each child's accumulated cookies and a recursion mechanism to explore all assignments.",
    },
    "shortest-path-visiting-all-nodes": {
        "key_insights": "- The problem is a variant of the Traveling Salesman Problem, but the small constraint (n ≤ 12) permits a solution using bitmasking.\n\n- Each state can be represented as a combination of the current node and a bitmask indicating which nodes have been visited.\n\n- Breadth-first search (BFS) helps guarantee that the first time we encounter a state with all nodes visited, it is via the shortest path.\n\n- Using multi-source BFS from each node as a starting point simplifies the logic because we can start from any node.",
        "complexity":   "Time Complexity: O(n * 2^n) where n is the number of nodes since we potentially visit each state (node, visited-mask) once.\nSpace Complexity: O(n * 2^n) for storing the states in the visited set and the BFS queue.",
        "solution":     "The solution uses a BFS that expands states of the form (current node, visited mask). Initially, every node is added to the queue with its corresponding bitmask. At each BFS step, we iterate over all adjacent nodes and update the visited mask by setting the bit corresponding to the neighbor. When the visited mask equals (1 &#x3C;&#x3C; n) - 1 (all nodes visited), we return the number of steps taken. This BFS expansion ensures that we always find the shortest path.",
    },
    "count-numbers-with-unique-digits": {
        "key_insights": "- Use combinatorial counting to determine the number of valid numbers of a given digit length.\n\n- For numbers with more than one digit, the first digit cannot be zero.\n\n- For each additional digit, the choices decrease because digits must remain unique.\n\n- The solution adds up valid counts for each possible digit length from 1 to n, plus the number 0.",
        "complexity":   "Time Complexity: O(n) per computation since we iterate at most 10 times (n &#x3C;= 8).\nSpace Complexity: O(1) as only a few variables are used.",
        "solution":     "The solution uses a combinatorial approach. For n = 0, the answer is 1 (only number 0). For n >= 1, the solution counts valid numbers for each digit length k (1 &#x3C;= k &#x3C;= n):\n\n- For 1-digit numbers, there are 9 possibilities (1-9).\n\n- For k-digit numbers (k > 1), the first digit has 9 possibilities (1-9), and each subsequent digit has a decreasing number of available digits (from 10 minus the number already used). Specifically, for the second digit there are 9 choices, for the third digit there are 8, and so forth.\n\n- Sum the counts for each valid digit length and add 1 for the number 0.\nThis approach avoids generating all numbers and leverages basic combinatorial principles for an efficient count.",
    },
    "number-of-digit-one": {
        "key_insights": "- Analyze each digit position in n (ones, tens, hundreds, etc.) independently.\n\n- For each digit position, decompose n into three parts: high (digits left of the current position), current (the digit at the current position), and low (digits right of the current position).\n\n- Depending on the value of the current digit:\n\n- If it is 0, its contribution is solely defined by the higher digits.\n\n- If it is 1, count both the higher digits and the remainder (low) plus one.\n\n- If it is greater than 1, the contribution is fully determined by the higher digits incremented by one.\n\n- This mathematical method avoids inefficient iterations and leverages place value analysis.",
        "complexity":   "Time Complexity: O(log n) because the number of digits in n determines the loop iterations.\nSpace Complexity: O(1) as only a few integer variables are used.",
        "solution":     "We use a mathematical approach by iterating over each digit position. At each position, calculate:\n\n- high: n divided by the current digit place multiplied by 10.\n\n- current: the digit at the current position from n.\n\n- low: the remainder when n is divided by the current digit place.\nBased on the value of the current digit, determine its contribution to the total count of 1's using specific formulae:\n\n- If current == 0: add high * i.\n\n- If current == 1: add high * i + (low + 1).\n\n- Else: add (high + 1) * i.\nThis algorithm efficiently counts the digit 1 occurrences without iterating through every number from 0 to n.",
    },
    "numbers-at-most-n-given-digit-set": {
        "key_insights": "- Count numbers with a digit-length strictly less than the length of n; every combination is valid.\n\n- Handle numbers with the same digit-length as n digit by digit.\n\n- For each digit in the same-length number, consider:\n\n- The count of available digits less than the corresponding digit in n multiplied by the number of completions with any digits thereafter.\n\n- If the digit equal to the current digit in n exists, continue to the next digit.\n\n- If a chosen digit does not equal the current digit in n and is not less than it, break the loop.\n\n- This approach is similar to digit dynamic programming but done greedily with careful counting.",
        "complexity":   "Time Complexity: O(m * k) where m is the number of digits in n and k is the number of given digits.\nSpace Complexity: O(1) or O(m) if considering the string representation conversion space.",
        "solution":     "The solution counts valid numbers in two parts:\n\n- Count numbers with digit-length strictly smaller than that of n. For each d-digit number where d &#x3C; len(n), the count is (number of given digits)^d.\n\n- Count numbers with the same digit-length as n. Process each digit from left to right; for the current digit, count digits in the given array that are less than the corresponding digit in n and multiply by the number of completions possible for the remaining positions. If a matching digit is found, continue; if not, break. Finally, if we completed all digits matching, include the number n itself.\n\nKey data structures include simple variables for counting and arithmetic operations; no elaborate structure is required. The algorithm leverages greedy decisions at each digit position.",
    },
    "knight-probability-in-chessboard": {
        "key_insights": "- Use Dynamic Programming since the state (i.e., cell and number of moves remaining) overlaps.\n\n- A recursive approach with memoization or iterative bottom-up DP can be applied.\n\n- Each knight move occurs with a probability of 1/8.\n\n- Base cases include k=0 (the knight is on the board) and moves leading off the board contribute 0 probability.\n\n- Transition: For each valid cell at a certain move, accumulate the probability from all 8 possible moves from that cell.",
        "complexity":   "Time Complexity: O(k * n^2) as we iterate over k moves and each cell of the board.\nSpace Complexity: O(n^2) if using iterative DP with two 2D arrays (or O(k * n^2) for a memoization approach).",
        "solution":     "We solve the problem using a bottom-up dynamic programming approach. We maintain a 2D DP array that represents the probability of the knight being at each cell after a given number of moves. We start by initializing the starting position with a probability of 1. For each move, we compute a new probability distribution over the board by iterating through each cell and distributing its probability over the 8 knight moves (only if the move remains within bounds). After k moves, the sum of the probabilities in the DP table gives the final answer. This iterative updating avoids repeated computations and elegantly manages the probability propagation.",
    },
    "soup-servings": {
        "key_insights": "- The problem is inherently probabilistic and can be solved using recursion with memoization (DP).\n\n- Transform the problem by scaling down the soup amounts (dividing n by 25 and rounding up), converting the operations to unit decrements.\n\n- Use a recursive function dp(a, b) to compute the probability starting with a units of soup A and b units of soup B.\n\n- Base cases:\n\n- Both soups empty (a &#x3C;= 0 and b &#x3C;= 0): return 0.5.\n\n- Only soup A empty (a &#x3C;= 0): return 1.\n\n- Only soup B empty (b &#x3C;= 0): return 0.\n\n- For large n (n > 4800), the answer approaches 1, so return it early for performance reasons.",
        "complexity":   "Time Complexity: O((n/25)²) in the worst-case because the recursive state space is roughly bounded by the transformed soup amount.\nSpace Complexity: O((n/25)²) due to the memoization cache storing each state.",
        "solution":     "The solution employs recursion with memoization to avoid redundant calculations. By scaling down the amount of soup (dividing by 25) the problem works in a smaller state space. The recursive function handles:\n\n- Base conditions where one or both soups are empty.\n\n- A summation of the outcomes from the four possible operations.\nTo improve efficiency for very large soup amounts, an early return is added when n exceeds 4800, as the probability converges to 1.",
    },
    "new-21-game": {
        "key_insights": "- If k is 0, Alice never draws a card, so the answer is 1.\n\n- If n is large enough (n >= k - 1 + maxPts), Alice will always win.\n\n- Use dynamic programming to compute the probability of reaching every score from 0 to n.\n\n- Use a sliding window sum to efficiently compute the probability recurrence relation.\n\n- The state transition relates dp[x] to the sum of probabilities dp[x - 1] ... dp[x - maxPts] divided by maxPts.",
        "complexity":   "Time Complexity: O(n), because we calculate each dp state at most once.\nSpace Complexity: O(n), for storing the dp array.",
        "solution":     "We solve this problem using dynamic programming with a sliding window approach:\n\n- Create an array dp where dp[i] represents the probability of reaching exactly i points.\n\n- Base case: dp[0] = 1.0 (starting with 0 points).\n\n- Use a sliding window sum to maintain the sum of dp values for states from i - maxPts to i - 1.\n\n- For each score i from 1 to n:\n\n- The probability dp[i] is the average of the probabilities in the window.\n\n- If i is less than k, add dp[i] to the window sum.\n\n- If i - maxPts is within range, subtract dp[i - maxPts] from the window sum.\n\n- The final answer is the sum of dp[i] for i from k to n, as these are the terminal states where Alice stops.",
    },
    "best-time-to-buy-and-sell-stock-with-transaction-fee": {
        "key_insights": "- Use dynamic programming to track profits when holding and not holding the stock.\n\n- Define two states:\n\n- cash: maximum profit when you do not hold any stock.\n\n- hold: maximum profit when you hold a stock.\n\n- When buying, you update the hold state by subtracting the current price from cash.\n\n- When selling, add the current price to hold and subtract the fee.\n\n- Update both states iteratively as you traverse the price list.",
        "complexity":   "Time Complexity: O(n), where n is the number of days (prices.length).\n\nSpace Complexity: O(1), as only two variables (states) are maintained at each iteration.",
        "solution":     "We use a dynamic programming approach using two variables: cash and hold.\n\n- cash represents the maximum profit when no stock is held.\n\n- hold represents the maximum profit when a stock is held.\n\nInitialize cash = 0 and hold = -prices[0]. Then, for each day, update:\n\n- cash as the maximum of the previous cash or the profit from selling the stock held (hold + current price - fee).\n\n- hold as the maximum of the previous hold or the profit from buying the stock (cash - current price).\n\nAfter processing all days, the maximum profit will be in cash because you must have sold any held stock to realize the profit.",
    },
    "best-time-to-buy-and-sell-stock-iv": {
        "key_insights": "- For a high value of k (k >= n/2 where n is the number of days), the problem reduces to the unlimited transactions case.\n\n- When k is small, a dynamic programming approach is necessary to track the optimal profit at each transaction and day.\n\n- Use a DP recurrence: dp[t][i] = max(dp[t][i-1], prices[i] + maxDiff), where maxDiff is updated as max(maxDiff, dp[t-1][i] - prices[i]).\n\n- The idea is to iteratively build the solution for each transaction, using previously computed results efficiently.",
        "complexity":   "Time Complexity: O(k * n) where n is the number of days.\nSpace Complexity: O(k * n) for the DP table (can be optimized to O(n) per transaction if needed).",
        "solution":     "We use a dynamic programming solution. First, check if k is large enough to allow unlimited transactions. If yes, simply sum up all positive differences between consecutive days. Otherwise, create a 2D DP table where dp[t][i] represents the maximum profit using at most t transactions until day i. For each transaction from 1 to k and for each day we maintain a variable maxDiff representing the maximum value of (dp[t-1][j] - prices[j]) for j from 0 to the current day. This helps to update the dp state for a transaction with a sell on the current day using the optimal previous transaction state.",
    },
    "excel-sheet-column-title": {
        "key_insights": "- This problem is analogous to converting a number to a base-26 numeral system.\n\n- The column title is like a number in base 26 but without a zero digit; letters A-Z correspond to values 1-26.\n\n- A common trick is to subtract 1 from columnNumber before modulo operations to properly map the \"digit\" to a character.\n\n- Repeated division and modulo will generate letters from the least significant one to the most significant, thus they need to be reversed at the end.",
        "complexity":   "Time Complexity: O(log26(n)) — The number of iterations is proportional to the number of letters in the resulting string.\nSpace Complexity: O(1) — The extra space used does not depend on the size of columnNumber.",
        "solution":     "The solution uses a greedy algorithm approach:\n\n- While columnNumber > 0, subtract 1 from it to adjust for the 1-indexed letter mapping.\n\n- Compute the remainder (columnNumber % 26) to find the corresponding letter.\n\n- Convert the remainder to a character by adding it to the ASCII value of 'A'.\n\n- Append or prepend the letter to a result string.\n\n- Divide columnNumber by 26 and continue until it becomes zero.\nThe resulting string, after reversing (if built in reverse order) or by inserting at the front, gives the final Excel sheet column title.",
    },
    "greatest-common-divisor-of-strings": {
        "key_insights": "- The necessary condition for a common divisor string is that str1 + str2 must equal str2 + str1.\n\n- The length of the candidate divisor string must be a divisor of both string lengths.\n\n- Using the greatest common divisor (gcd) of the lengths of str1 and str2 gives the length of the largest possible candidate.\n\n- Once a candidate is found, it can be verified by checking if repeating it forms the original strings.",
        "complexity":   "Time Complexity: O(n + m), where n and m are the lengths of str1 and str2 respectively.\nSpace Complexity: O(n + m), primarily for storage of the input strings and the resulting substring.",
        "solution":     "The solution first checks if str1 + str2 equals str2 + str1; if not, no common divisor exists and an empty string is returned. Otherwise, the algorithm computes the gcd of the lengths of str1 and str2. The substring of str1 up to this gcd length is the common divisor string since repeating it forms both str1 and str2.",
    },
    "ugly-number": {
        "key_insights": "- If n is not positive, it cannot be an ugly number.\n\n- Continuously divide n by 2, 3, and 5 to factor out valid prime factors.\n\n- If after removing all factors 2, 3, and 5, n becomes 1, then n is an ugly number.\n\n- If n is not 1 after removal, then n contains other prime factors hence is not ugly.",
        "complexity":   "Time Complexity: O(log n) since the number is divided by constant factors.\nSpace Complexity: O(1) constant extra space.",
        "solution":     "The solution involves an iterative division process. We repeatedly divide the number by 2, 3, and 5 if it is divisible by any of them. The idea is that if n is composed solely of these factors, then after continuous division, n should reduce to 1. If n is reduced to any number other than 1, it means there are other prime factors involved, and the number is not ugly. This approach uses only basic arithmetic operations and conditional checks, which makes it efficient in terms of both time and space.",
    },
    "ugly-number-ii": {
        "key_insights": "- Ugly numbers are generated by multiplying smaller ugly numbers by 2, 3, or 5.\n\n- Use dynamic programming to build a sequence of ugly numbers starting from 1.\n\n- Maintain three pointers (for factors 2, 3, and 5) that determine the next candidate ugly number.\n\n- Avoid duplicate entries by checking and advancing all pointers that match the current minimum candidate.\n\n- The approach runs in O(n) time and uses O(n) space.",
        "complexity":   "Time Complexity: O(n)\n\nSpace Complexity: O(n)",
        "solution":     "The solution uses a dynamic programming approach. We maintain an array (or list) that stores the ugly numbers in ascending order, starting with 1. Three pointers are used—each corresponding to the multipliers 2, 3, and 5. For each iteration:\n\n- Calculate the next possible ugly numbers by multiplying the number pointed to by each pointer with 2, 3, and 5.\n\n- Select the minimum of these candidates as the next ugly number.\n\n- Increment the corresponding pointer(s) to avoid duplicate calculations.\n\n- Continue until the nth ugly number is generated.\nThis process ensures that each ugly number is generated in order and without duplicates.",
    },
    "count-good-numbers": {
        "key_insights": "- The string has n positions, where positions are 0-indexed.\n\n- Even indices must contain one of 5 even digits {0, 2, 4, 6, 8}.\n\n- Odd indices must contain one of 4 prime digits {2, 3, 5, 7}.\n\n- Count the number of even index positions as ceil(n / 2) and odd index positions as floor(n / 2).\n\n- The total good numbers equals 5^(ceil(n / 2)) * 4^(floor(n / 2)) modulo 10^9 + 7.\n\n- Use fast modular exponentiation due to the large constraint on n (up to 10^15).",
        "complexity":   "Time Complexity: O(log n) due to fast modular exponentiation.\nSpace Complexity: O(1) as only a fixed amount of memory is used.",
        "solution":     "We determine the count of good digit strings by dividing the positions into even and odd index positions. Even indexes must be one of the 5 even digits, so they contribute 5 possibilities each; odd indexes must be one of the 4 prime digits, and they contribute 4 possibilities each. If n is the length, then:\n\n- Number of even indexed positions = ceil(n / 2)\n\n- Number of odd indexed positions = floor(n / 2)\n\nThus, the total number of good strings is:\n5^(ceil(n / 2)) * 4^(floor(n / 2)) modulo (10^9 + 7).\n\nTo compute these large exponents efficiently, we use fast modular exponentiation (also known as modular power). This ensures that even for n as large as 10^15, the exponents are calculated quickly.\n\nThe provided code solutions in Python, JavaScript, C++, and Java include detailed inline comments to clarify each step.",
    },
    "count-primes": {
        "key_insights": "- The problem can be efficiently solved using the Sieve of Eratosthenes.\n\n- Only numbers less than n are considered.\n\n- Iterating only up to the square root of n will mark all non-prime numbers effectively.\n\n- An auxiliary boolean array can represent whether each number is prime.",
        "complexity":   "Time Complexity: O(n log log n)\n\nSpace Complexity: O(n)",
        "solution":     "The Sieve of Eratosthenes algorithm is used to solve this problem. We create a boolean array of size n where each index initially assumes the number is prime. We then mark 0 and 1 as non-prime. For each number p starting from 2 up to the square root of n, if p is still marked as prime, we mark all multiples of p (starting at p*p) as non-prime. Finally, the count of true values in the boolean array gives the number of primes less than n.",
    },
    "factorial-trailing-zeroes": {
        "key_insights": "- Trailing zeroes are produced by factors of 10 (2 * 5) in the factorial.\n\n- There are more 2s than 5s in n! so counting the number of 5 factors will suffice.\n\n- Count how many multiples of 5, 25, 125, etc., since each contributes additional factors of 5.\n\n- This approach runs in logarithmic time relative to n.",
        "complexity":   "Time Complexity: O(log n)\n\nSpace Complexity: O(1)",
        "solution":     "We count the factors of 5 in n! by repeatedly dividing n by 5. The intuition is that every 5th number contributes at least one factor of 5, every 25th contributes an extra factor because 25 = 5*5, and so on. We continue this division until n becomes 0. This avoids computing the factorial directly, providing a very efficient solution. The algorithm uses a simple loop and integer division, resulting in logarithmic time complexity and constant space usage.",
    },
    "valid-triangle-number": {
        "key_insights": "- Sorting the array simplifies meeting the triangle inequality condition.\n\n- Once sorted, if nums[left] + nums[right] > nums[k] for some triplet, then all elements between left and right with the current right will also satisfy the condition.\n\n- Using a two-pointer approach inside a loop iterating the largest side allows us to efficiently count valid pairs.\n\n- The innermost loop operates in O(n) time leading to a quadratic overall time complexity.",
        "complexity":   "Time Complexity: O(n^2) due to the two nested loops iterating through the array.\nSpace Complexity: O(1) or O(log n) if considering the sorting space overhead.",
        "solution":     "The solution begins by sorting the array. For each potential largest side (considered in reverse order), use two pointers (one starting at the beginning and another just before the largest side index) to find pairs of sides whose sum is greater than the largest side. When a valid pair is found, all numbers between the two pointers with the current right pointer also form valid triangles because the array is sorted. Increment the count accordingly and adjust the pointers until all possibilities are exhausted. This greedy two-pointer approach reduces the need for a three-nested loop, ensuring an efficient solution.",
    },
    "valid-square": {
        "key_insights": "- Calculate all six pairwise squared distances between the four points.\n\n- For a valid square, there should be exactly two unique distances: the smaller one representing the sides (appearing exactly 4 times) and the larger one representing the diagonals (appearing exactly 2 times).\n\n- Duplicate points or a zero distance immediately disqualify the formation of a square.",
        "complexity":   "Time Complexity: O(1) (since the number of points is fixed)\nSpace Complexity: O(1) (constant extra space used for computations)",
        "solution":     "We start by calculating the squared Euclidean distance between every pair of points to avoid the complications of floating point precision. We then count the frequency of these distances. A valid square must show exactly two distinct distances: one (side length squared) should appear 4 times and the other (diagonal length squared) should appear 2 times. This provides a robust check for both equal sides and the correct geometric property (diagonals in a square are longer than sides and appear exactly twice). The solution uses simple iteration over the fixed set of point pairs to tally frequencies and then verifies the required counts.",
    },
    "minimum-area-rectangle": {
        "key_insights": "- Use a hash set (or equivalent) to store points for O(1) lookup.\n\n- Iterate over all pairs of points that could form opposite corners of a rectangle (ensuring they have different x and y values).\n\n- For each such pair, check if the other two required points exist.\n\n- Keep track of the smallest area found; if no rectangle is detected, return 0.",
        "complexity":   "Time Complexity: O(n^2) where n is the number of points (each pair is inspected).\nSpace Complexity: O(n) for storing the points in a hash set.",
        "solution":     "The solution involves iterating through every pair of points. For each pair with distinct x and y coordinates (potential diagonal corners of a rectangle), check if the other two corners (with swapped y and x values) exist in the set. If they do, calculate the area and update the minimum area if it is smaller than the current minimum. If no valid rectangle is found after checking all pairs, return 0.",
    },
    "minimum-area-rectangle-ii": {
        "key_insights": "- A rectangle, even when rotated, has the property that its diagonals have the same midpoint and equal lengths.\n\n- By iterating over every pair of points (which can be potential diagonal endpoints), we can group pairs by their midpoint (represented in a way to avoid floating point issues) and their squared distance.\n\n- For each group of pairs sharing the same center and squared distance, any two pairs can potentially form a rectangle.\n\n- Given two candidate diagonal pairs (p, q) and (r, s) with the same midpoint, the rectangle’s area can be computed by taking one vertex p and calculating:\n• side1 = distance from p to r\n\n• side2 = distance from p to s\n\n• area = side1 * side2, provided that the vectors (r - p) and (s - p) are perpendicular (i.e. their dot product is zero).\n\n- Iterate through all valid pair combinations and keep track of the minimum area found.",
        "complexity":   "Time Complexity: O(n² * k²) where n is the number of points and k is the number of pairs in each grouping (in worst-case, overall it is O(n⁴), but since n ≤ 50, it is acceptable).\nSpace Complexity: O(n²) for storing all pairs in the dictionary.",
        "solution":     "The solution involves:\n\n- Iterating over all pairs of points and computing a key based on the pair’s midpoint (using the sum of coordinates to avoid floating point precision issues) and their squared distance.\n\n- Storing these pairs in a hash map (dictionary) keyed by (center, squared distance).\n\n- For each list in the dictionary with more than one pair, checking every combination of two pairs:\n\n- Choose one endpoint from the first pair and check with both endpoints of the second pair to form two vectors.\n\n- Verify that these two vectors are perpendicular (dot product equals 0). This confirms the points form a rectangle.\n\n- Compute the area of the rectangle as the product of the lengths of the two vectors.\n\n- Return the minimum area among all valid rectangles, or 0 if none exist.\n\nBelow are code solutions in multiple languages with detailed line-by-line comments.",
    },
    "max-points-on-a-line": {
        "key_insights": "- Use a nested loop to treat each point as an anchor and calculate slopes to all other points.\n\n- Represent slopes as a reduced fraction (dy/dx) using the greatest common divisor (gcd) to avoid floating-point imprecision.\n\n- Specially handle vertical lines (when dx is 0) by using a unique identifier.\n\n- The problem naturally leads to an O(n²) solution by processing each pair of points.\n\n- Use a hash table (or dictionary) to count how many points form the same slope with the anchor point.",
        "complexity":   "Time Complexity: O(n²) where n is the number of points, since every pair of points is compared.\nSpace Complexity: O(n) for the hash table used for storing slope counts for each anchor point.",
        "solution":     "The solution iterates through each point and treats it as an anchor. For every other point, we calculate the slope between the anchor and that point. The slope is computed as a pair (dy, dx) reduced to its simplest form via gcd. For vertical lines (dx == 0), we use a special key. A hash table is used to count occurrences of each slope. The maximum number of points with the same slope (plus the anchor) is recorded. The final result is the maximum count from all anchors.",
    },
    "repeated-substring-pattern": {
        "key_insights": "- The repeated substring must be a divisor of the entire string's length.\n\n- A trick uses string manipulation by checking if the string exists in (s+s) with its first and last characters removed.\n\n- Alternatively, iterate through all possible substring lengths (that are divisors of the total length) and check if repeating the substring yields the original string.",
        "complexity":   "Time Complexity: O(n^2) in the worst case if checking each possible candidate manually, but the trick method runs in O(n).\n\nSpace Complexity: O(n) due to the extra temporary string created in the trick method.",
        "solution":     "We can solve the problem using one of two approaches:\n\n- Brute-force: Try each possible substring whose length divides the original string length. For each candidate, repeat it and compare with the original string.\n\n- Trick approach: Form a new string by concatenating the original string with itself and remove the first and last characters. Then check if the original string exists in this modified string. If it does, the original string is composed of repeated substrings.\n\nThe trick approach is more elegant and efficient. It leverages the fact that a repeated pattern will appear at least twice in the overlapped string.",
    },
    "repeated-string-match": {
        "key_insights": "- The key observation is that b will be found within the repeated string a after a sufficient number of repeats, possibly spanning the junction between two copies of a.\n\n- The initial number of repeats can be approximated by ceiling(|b| / |a|). To handle potential overlaps at the boundaries, one additional repetition might be necessary.\n\n- A simple check using built-in substring methods is sufficient to verify if b is contained in the repeated string.",
        "complexity":   "Time Complexity: O(n * k), where n is the length of a and k is the minimum number of repeats (usually a small constant in practice).\nSpace Complexity: O(n * k), which is the space required to construct the repeated string.",
        "solution":     "The solution calculates an initial repeat count using the ceiling of the division of b's length by a's length. It then constructs a repeated version of a and checks if b is a substring. Because b could span across the boundary of two adjacent a's, the solution appends one more repetition of a if necessary. If b is still not found, return -1.",
    },
    "number-of-distinct-substrings-in-a-string": {
        "key_insights": "- All possible substrings can be generated by considering each possible start and end index.\n\n- Utilizing a set (or hash set) ensures that duplicates are automatically filtered.\n\n- Even though generating all substrings has a nested loop, the maximum length of s (500) keeps the solution efficient for the problem constraints.\n\n- Advanced methods (like suffix trees, suffix arrays, or rolling hash) can optimize the process closer to O(n) time but are more complex to implement.",
        "complexity":   "Time Complexity: O(n^2) in the context of iterating over all substring intervals (n is the length of s).\n\nSpace Complexity: O(n^2) in the worst case for storing all distinct substrings.",
        "solution":     "The solution involves iterating through every possible substring of s by using two nested loops: one for the start index and another for the end index. Each generated substring is added to a set which handles duplicate removals automatically. After the loops, the size of the set gives the number of distinct substrings.\n\nKey Data Structures and Techniques:\n\n- Set/Hash Set: to store distinct substrings.\n\n- Nested Loop: to generate all possible substrings.\n\n- (Optional) Advanced techniques like rolling hash and suffix arrays can optimize the solution, but the simple approach is sufficient given the constraints.",
    },
    "longest-happy-prefix": {
        "key_insights": "- The problem can be solved efficiently by leveraging techniques used in string matching algorithms.\n\n- A key method is to compute the prefix function (also known as pi array) from the KMP (Knuth-Morris-Pratt) algorithm.\n\n- The value at the last index of the pi array corresponds to the length of the longest prefix that is also a suffix (excluding the whole string).\n\n- An alternative approach is to use a rolling hash technique, but the KMP prefix function is more straightforward and has a linear time complexity.",
        "complexity":   "Time Complexity: O(n), where n is the length of s\n\nSpace Complexity: O(n), for storing the prefix function",
        "solution":     "We use the KMP prefix function to solve this problem:\n\n- Initialize an array pi of size n (length of s) with zeros.\n\n- Iterate through the string from the second character, and update the pi array which keeps track of the longest proper prefix which is also a suffix for each substring ending at that index.\n\n- The value at pi[n-1] gives the length of the longest happy prefix.\n\n- Return the substring from the beginning of s up to that length.\nThe algorithm ensures that we only traverse the string once with extra space proportional to n.",
    },
    "shortest-palindrome": {
        "key_insights": "- The main idea is to identify the longest prefix of s that is already a palindrome.\n\n- Once this prefix is identified, the remaining suffix can be reversed and prepended to s to form a complete palindrome.\n\n- A robust way to find the longest palindromic prefix is to use the KMP (Knuth-Morris-Pratt) algorithm on a constructed string made by concatenating s, a separator (such as '#'), and the reverse of s.\n\n- The prefix table computed from KMP helps determine the maximum matching prefix which is also a palindrome.",
        "complexity":   "Time Complexity: O(n), where n is the length of s, because building and processing the combined string takes linear time.\nSpace Complexity: O(n), needed for the combined string and the prefix (LPS) table.",
        "solution":     "The solution builds a new string combined = s + '#' + reverse(s). The KMP prefix function is then computed for this string. The last value of the prefix table indicates the length of the longest prefix of s which is also a palindrome. The characters not included in this palindrome (the suffix) are then reversed and prepended to s to form the shortest palindrome. This approach leverages string matching techniques to efficiently determine the required palindrome structure.",
    },
    "longest-duplicate-substring": {
        "key_insights": "- Use binary search on the possible lengths of duplicated substrings.\n\n- For each candidate length, use a rolling hash (polynomial rolling hash) to efficiently hash every substring of that length.\n\n- Use a hash set to check whether a substring hash has been seen before.\n\n- Adjust the binary search range based on whether a duplicate was found, ultimately retrieving the longest duplicate substring.",
        "complexity":   "Time Complexity: O(n log n)\n\nSpace Complexity: O(n)",
        "solution":     "The solution leverages binary search to determine the maximum length L for which a duplicate substring exists. For each candidate L, we use a rolling hash technique to compute hash values for all substrings of length L in O(n) time. A hash set tracks previously seen substrings by their hash values; if a duplicate exists, we update our answer and attempt a larger L. This approach is efficient for the problem's constraints and handles overlapping duplicates. Key details include careful management of the modulo arithmetic in the rolling hash to avoid collisions and overflow, and possibly using large mod values or double hashing in competitive environments.",
    },
    "range-sum-query-2d-mutable": {
        "key_insights": "- This is a 2D range sum query problem with updates.\n\n- A brute-force approach may be too slow due to frequent updates.\n\n- The Binary Indexed Tree (Fenwick Tree) or 2D Segment Tree can support both update and sumRegion queries efficiently.\n\n- Use an auxiliary BIT structure to maintain cumulative information to quickly answer sum queries.\n\n- Maintain a separate copy of the original matrix to compute differences during updates.",
        "complexity":   "Time Complexity:\n\n- update: O(M*log(M)*log(N)) in worst-case per update, where M and N are the dimensions of the matrix.\n\n- sumRegion: O(log(M)*log(N)) per query.\n\nSpace Complexity:\n\n- O(M*N) for storing the BIT and the matrix.",
        "solution":     "We implement a 2D Binary Indexed Tree. When performing an update, compute the difference between the new value and the current value of the cell, and update the BIT accordingly. For sumRegion queries, use the inclusion-exclusion principle on prefix sums from the BIT. This allows both updates and sum queries to be done in logarithmic time relative to the dimensions of the matrix. The BIT is a 2D array where each cell BIT[i][j] contributes to the cumulative sum over a specific range determined by the least significant bit. By iterating properly within the BIT update and query functions, we can ensure that all necessary BIT cells are updated or summed.",
    },
    "falling-squares": {
        "key_insights": "- Each square’s final height is determined by checking for overlap with all previously placed squares.\n\n- Two squares overlap if the interval [left, left + sideLength) of the current square and [prevLeft, prevLeft + prevSideLength) of a previous square intersect.\n\n- The height at which a new square lands is equal to its side length plus the maximum height of any square that overlaps its horizontal interval.\n\n- While a brute-force O(n^2) solution is feasible for up to 1000 positions, more efficient approaches can be implemented using segment trees or ordered sets when scaling to larger inputs.\n\n- Coordinate compression might be used along with advanced data structures if deciding for performance optimizations.",
        "complexity":   "Time Complexity: O(n^2) in the brute-force approach since for each square we scan all previously dropped squares\n\nSpace Complexity: O(n) for storing the square information and the answer array",
        "solution":     "The idea is to iterate over each square in the order provided. For the current square, determine its horizontal range using its left coordinate and side length. Check all previous squares to see if their horizontal range overlaps with the current square. If there is an overlap, the current square will land on top of that square, and its height becomes the height of that square plus its own side length. The final height for the current drop is the maximum height determined by all overlapping squares, or simply its side length if there is no overlap. Update the answer list with the maximum height seen so far.\n\nThis approach uses an array (or list) to store the details (left coordinate, right coordinate, and height) of each dropped square. The brute force check of overlapping intervals is intuitive and sufficient given the problem constraints.",
    },
    "block-placement-queries": {
        "key_insights": "- Placing obstacles on the number line splits [0, x] into free intervals.\n\n- A block of length sz can be placed if at least one free interval (or a truncated free interval at the boundary x) has length at least sz.\n\n- Using a segment tree that “remembers” the longest contiguous free segment is a great way to support both dynamic updates (when an obstacle is added) and range queries (to check for a large enough free interval).\n\n- In the segment tree, each node will store:\n• prefix length: the contiguous free segment starting at the left end,\n• suffix length: the contiguous free segment ending at the right end,\n• best: the maximum free contiguous interval within the segment,\n• total: the length (number of positions) of the segment.\n\n- For an update (type 1 query), we mark the position as blocked (i.e. “0”) and update the tree.\n\n- For a query (type 2), we query the segment tree for the range [0, x] and check if the “best” free length is at least sz.",
        "complexity":   "Time Complexity:\n\n- Update (placing an obstacle): O(log M) per update, where M is the maximum possible position (here, M ≤ 50000).\n\n- Query (checking free interval): O(log M) per query.\n\nSpace Complexity: O(M) to store the segment tree.",
        "solution":     "We solve the problem by modeling the positions [0, M] (with M equal to the maximum x encountered in the queries) in a segment tree. Initially, all positions are free. Each leaf node of the segment tree represents a single position, and its values (prefix, suffix, best) are initialized to 1. When an obstacle is placed at a position x, we update that leaf to 0 (blocked). The segment tree nodes merge their children’s values so that each node accurately reflects:\n• prefix: if the entire left child is free, then the free length extends into the right child’s prefix,\n• suffix: similarly using the right child,\n• best: the larger of the left child’s best, the right child’s best, or a combination of the left child’s suffix and the right child’s prefix.\nFor a query, we fetch the node information for the interval [0, x] and check if the best free contiguous segment length is at least sz.",
    },
    "number-of-pairs-satisfying-inequality": {
        "key_insights": "- Transform the problem by defining a new array value: diff_arr[i] = nums1[i] - nums2[i].\n\n- The inequality becomes: diff_arr[i] &#x3C;= diff_arr[j] + diff for i &#x3C; j.\n\n- For every index j, count how many earlier indices i have a diff_arr value &#x3C;= diff_arr[j] + diff.\n\n- Use data structures such as Binary Indexed Tree (Fenwick Tree) or balanced BST to count the number of valid indices efficiently.\n\n- Discretize the values since nums1 and nums2 lie in a moderate range but the computed differences can be shifted by diff.",
        "complexity":   "Time Complexity: O(n log n) due to sorting for discretization and BIT operations for each element.\nSpace Complexity: O(n) for the BIT and the discretized mapping.",
        "solution":     "The idea is to pre-compute an array diff_arr where each element is computed as nums1[i] - nums2[i]. The problem then reduces to counting, for each index j, the number of indices i &#x3C; j that satisfy:\n  diff_arr[i] &#x3C;= diff_arr[j] + diff.\nA Binary Indexed Tree (BIT) is used to maintain counts of previously seen diff_arr values (after discretization). We discretize the possible values (both diff_arr and diff_arr[j] + diff) to map them onto indices in the BIT. As we iterate through diff_arr from left to right, we query the BIT for how many indices have values less than or equal to diff_arr[j] + diff. Then update the BIT with the current diff_arr[j]. This counts all valid pairs in an efficient manner.",
    },
    "rectangle-area-ii": {
        "key_insights": "- Use a line sweep algorithm along the x-axis to break the problem into vertical slices.\n\n- For each vertical slice, compute the union of the active y-intervals.\n\n- Apply coordinate compression on the y-values since they can be very large.\n\n- Utilize a segment tree (or an equivalent data structure) to efficiently track and update the length of y-intervals that are currently active.\n\n- Process two event types per rectangle: one when entering (adding a y-interval) and one when leaving (removing a y-interval).",
        "complexity":   "Time Complexity: O(n log n) – sorting events and processing them using the segment tree.\nSpace Complexity: O(n) – for storing events, the list of unique y-coordinates, and the segment tree structure.",
        "solution":     "The solution involves sweeping a vertical line across the x-axis and handling events where a rectangle starts or ends. For each rectangle, generate two events: one for adding the corresponding y-interval when the sweep line hits the left edge, and one for removing the interval when hitting the right edge. The y-coordinates are compressed to allow efficient segment tree operations. The segment tree keeps track of the coverage count and the total covered length on the y-axis. For each segment between consecutive x-values, multiply the width of the segment (delta in x) by the current union length of y-intervals to accumulate the area. Finally, return the accumulated area modulo 10^9+7.",
    },
    "perfect-rectangle": {
        "key_insights": "- Compute the overall bounding rectangle by taking the minimum x, minimum y, maximum a, and maximum b from all rectangles.\n\n- Sum the areas of all given rectangles; this should equal the area of the overall bounding rectangle.\n\n- Use a set to track all four corners of each rectangle. For each rectangle, add its four corners if they are seen for the first time and remove them if they appear again.\n\n- At the end, the set should contain exactly the four corners of the overall bounding rectangle. Any extra points indicate either overlapping or gaps.",
        "complexity":   "Time Complexity: O(n), where n is the number of rectangles, because we iterate through each rectangle exactly once.\nSpace Complexity: O(n) in the worst-case scenario for storing corner points in the set.",
        "solution":     "The solution is based on two main observations. First, the total area of all small rectangles must equal the area of the overall bounding rectangle defined by the extreme coordinates. Second, by using a set to add and remove corner points of every rectangle, all internal points (which occur more than once) cancel out, leaving only the four corner points of the bounding rectangle. If the set does not exactly match these four corners, then the rectangles do not form a perfect cover.",
    },
    "pascals-triangle": {
        "key_insights": "- Each row begins and ends with the number 1.\n\n- Every inner element is the sum of the two elements from the previous row.\n\n- Only two nested loops are required: one for iterating through the rows and one for computing the inner elements.\n\n- The algorithm steadily builds the triangle row by row.",
        "complexity":   "Time Complexity: O(n^2) – We construct each row by iterating through all previous rows.\nSpace Complexity: O(n^2) – We store all the numbers in the triangle.",
        "solution":     "This problem is solved by iterative construction of each row. We start with a triangle containing only the first row. For each new row, we set the first and last element to 1. For the inner elements, sum the two adjacent numbers from the previous row. The primary data structure is a list (or array) of lists (or arrays) that holds the triangle. This algorithm uses nested loops: the outer loop to iterate through each row, and the inner loop to compute the inner cells of each row.",
    },
}
